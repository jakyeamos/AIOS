---
phase: 01-project-workflow-and-prompt-routing
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - schema.sql
  - services/aios_cli.py
  - tests/test_aios_cli.py
  - tests/test_orchestration_runtime.py
autonomous: true
requirements:
  - ROUT-01
  - ROUT-02
  - ROUT-03
  - ROUT-04
must_haves:
  truths:
    - "The main AIOS serious-work entrypoint can derive routing from a vague objective instead of depending on manually supplied workflow/backend defaults."
    - "Route metadata is persisted durably enough for later packet compilation, run inspection, and operator drill-down."
    - "Ambiguous or unsupported routes fail safely before packet creation continues."
  artifacts:
    - path: "services/aios_cli.py"
      provides: "CLI integration between route resolution and start-work packet/run creation"
    - path: "schema.sql"
      provides: "durable storage shape for route metadata or linked route-result fields"
    - path: "tests/test_aios_cli.py"
      provides: "CLI regression coverage for route-driven start-work behavior"
    - path: "tests/test_orchestration_runtime.py"
      provides: "control-plane regression coverage for persisted routing metadata where runtime linkage matters"
  key_links:
    - from: "services/task_routing.py"
      to: "services/aios_cli.py"
      via: "start-work route resolution before packet/run creation"
      pattern: "route|start-work|packet"
    - from: "services/aios_cli.py"
      to: "schema.sql"
      via: "persisted route metadata linked to runs/packets/invocations"
      pattern: "route_metadata|rationale|packet_id|workflow_key"
    - from: "services/aios_cli.py"
      to: "briefing_packets/orchestration_runs"
      via: "packet creation uses routed project/workflow/backend rather than manual-only defaults"
      pattern: "orchestration_runs|briefing_packets|orchestration_invocations"
---

<objective>
Wire the new Phase 1 route layer into the actual AIOS serious-work entrypoint and persist its outputs.

Purpose: make routing real in the product path that creates run and packet records, not only in isolated helpers.
Output: `start-work` uses route resolution, stores route metadata durably, blocks unsafe ambiguity, and proves the behavior with focused CLI/runtime tests.
</objective>

<execution_context>
@/Users/jakyeamos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakyeamos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/TIER_ONE_ACCEPTANCE_CHECKLIST.md
@.planning/phases/01-project-workflow-and-prompt-routing/01-CONTEXT.md
@.planning/phases/01-project-workflow-and-prompt-routing/01-RESEARCH.md
@services/aios_cli.py
@schema.sql
@tests/test_aios_cli.py
@tests/test_orchestration_runtime.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate route resolution into the start-work entrypoint</name>
  <files>services/aios_cli.py, tests/test_aios_cli.py</files>
  <action>Refactor the `start-work` flow so vague objectives can be routed through the new route layer before packet creation, per D-02, D-03, D-14, and D-17. Preserve explicit override behavior where useful, but make routed project/workflow/prompt/backend selection the default serious-work path. Unsafe ambiguity or unsupported objectives must stop packet creation with an explicit error/result rather than silently picking defaults.</action>
  <verify>uv run pytest tests/test_aios_cli.py -q</verify>
  <done>`aios start-work` can derive serious-work routing from an objective, preserves inspectable rationale, and blocks unsafe ambiguity before creating the packet.</done>
</task>

<task type="auto">
  <name>Task 2: Persist route metadata on the orchestration spine</name>
  <files>schema.sql, services/aios_cli.py, tests/test_aios_cli.py, tests/test_orchestration_runtime.py</files>
  <action>Persist the route-result fields needed by Phase 2 and later operator surfaces: selected project outcome, workflow, prompt/handoff family, backend recommendation, candidate/fallback/rationale metadata, and route identity. Use the existing orchestration spine where it stays coherent; if a dedicated linked record is necessary, keep it tightly coupled to runs and packets. Add or update tests so persisted route metadata survives packet/run creation and remains retrievable without re-running routing logic.</action>
  <verify>uv run pytest tests/test_aios_cli.py tests/test_orchestration_runtime.py -q</verify>
  <done>Route metadata is durably stored alongside the serious-work run/packet flow and can be inspected later without recomputing routing decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the routed CLI behavior with end-to-end Phase 1 coverage</name>
  <files>tests/test_aios_cli.py, tests/test_orchestration_runtime.py</files>
  <action>Add end-to-end tests proving that routed start-work creates persisted route-aware packet/run records for valid objectives and stops safely on ambiguous or unsupported objectives. The tests must check route rationale or candidate metadata, not just final status values, so ROUT-03 remains enforced.</action>
  <verify>uv run pytest tests/test_aios_cli.py tests/test_orchestration_runtime.py -q</verify>
  <done>Phase 1 routing is visible in the main CLI path and protected by end-to-end regression tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route result -> packet/run persistence | persisted metadata becomes authoritative for downstream execution and inspection |
| CLI overrides -> routed defaults | explicit user overrides can conflict with inferred route decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | T | start-work persistence | mitigate | persist route identity, rationale, and fallback state atomically with run/packet creation |
| T-01-08 | R | ambiguity handling | mitigate | return explicit blocked/unsupported outcomes so operators can trace why no packet was created |
| T-01-09 | I | route metadata visibility | mitigate | store machine-readable route fields that later packet/query/UI surfaces can inspect without re-routing |
</threat_model>

<verification>
Run `uv run pytest tests/test_aios_cli.py tests/test_orchestration_runtime.py -q`.
Confirm routed objectives create persisted route-aware run/packet records and ambiguous objectives fail safely before packet creation.
</verification>

<success_criteria>
- `start-work` uses routed serious-work defaults instead of manual-only workflow/backend selection.
- Route metadata is persisted durably enough for later packet and operator use.
- Ambiguous and unsupported routes fail safely and observably.
- ROUT-01 through ROUT-04 are covered by executable CLI/runtime regression tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-workflow-and-prompt-routing/01-03-SUMMARY.md`
</output>
