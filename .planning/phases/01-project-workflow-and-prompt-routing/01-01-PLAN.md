---
phase: 01-project-workflow-and-prompt-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/workflows/registry.json
  - prompts/registry.json
  - services/workflow_orchestration.py
  - services/execution_strategy.py
  - tests/test_workflow_orchestration.py
  - tests/test_execution_strategy.py
autonomous: true
requirements:
  - ROUT-02
  - ROUT-03
  - ROUT-04
must_haves:
  truths:
    - "AIOS can derive routeable workflow candidates and backend recommendations from explicit registries rather than ad hoc strings."
    - "Workflow selection exposes enough metadata for later route rationale instead of only returning a final workflow key."
    - "Prompt recommendation can operate at a routeable family level, not only one-off template heuristics."
  artifacts:
    - path: "config/workflows/registry.json"
      provides: "workflow family and route-hint metadata for default serious-work workflows"
    - path: "prompts/registry.json"
      provides: "prompt-family metadata that can be recommended during routing"
    - path: "services/workflow_orchestration.py"
      provides: "helpers that produce workflow candidates and prompt-family recommendations with rationale fields"
    - path: "services/execution_strategy.py"
      provides: "surface-aware backend recommendation inputs for route-time selection"
  key_links:
    - from: "config/workflows/registry.json"
      to: "services/workflow_orchestration.py"
      via: "typed workflow loading and route candidate helpers"
      pattern: "load_workflow_registry|route|candidate"
    - from: "prompts/registry.json"
      to: "services/workflow_orchestration.py"
      via: "prompt family selection and fallback metadata"
      pattern: "prompt_family|template_id|normalized_prompt"
    - from: "services/execution_strategy.py"
      to: "services/workflow_orchestration.py"
      via: "task-family and surface recommendation linkage"
      pattern: "compile_execution_strategy|surface"
---

<objective>
Establish the route-selection primitives that Phase 1 needs before project resolution or CLI integration can be trusted.

Purpose: turn the current workflow, prompt, and backend metadata into explicit routeable inputs with inspectable rationale.
Output: updated registries and Python helpers that can produce workflow candidates, prompt-family recommendations, and backend-selection inputs for serious-work routing.
</objective>

<execution_context>
@/Users/jakyeamos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakyeamos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/WORKFLOW_MATRIX.md
@.planning/phases/01-project-workflow-and-prompt-routing/01-CONTEXT.md
@.planning/phases/01-project-workflow-and-prompt-routing/01-RESEARCH.md
@services/workflow_orchestration.py
@services/execution_strategy.py
@services/invocation_backends.py
@config/workflows/registry.json
@prompts/registry.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend workflow and prompt metadata for route-time selection</name>
  <files>config/workflows/registry.json, prompts/registry.json</files>
  <action>Add explicit route-time metadata for the early serious-work families per D-08, D-11, and D-12. Preserve existing workflow/template ids, but extend the workflow registry with route hints or workflow-family fields and extend the prompt registry with prompt-family information, fallback state, and applicability tags that a route layer can consume without inventing a second prompt catalog. Do not add speculative families unrelated to Phase 1.</action>
  <verify>uv run pytest tests/test_workflow_orchestration.py tests/test_execution_strategy.py -q</verify>
  <done>Workflow and prompt registries can express default serious-work families and route-time prompt-family selection inputs without breaking existing loaders.</done>
</task>

<task type="auto">
  <name>Task 2: Expose workflow candidate and prompt-family selection helpers</name>
  <files>services/workflow_orchestration.py, services/execution_strategy.py</files>
  <action>Refactor the current workflow-selection internals so they can produce explicit workflow candidates, selected workflow rationale, prompt-family recommendation, and execution-surface recommendation inputs per D-07, D-09, D-10, D-14, and D-15. Keep the implementation grounded in existing typed registries and strategy bundles rather than creating a disconnected route stack. Ensure the helper API is reusable by the later route module and by CLI integration.</action>
  <verify>uv run pytest tests/test_workflow_orchestration.py tests/test_execution_strategy.py -q</verify>
  <done>Python helpers can return routeable workflow/prompt/backend recommendation data with inspectable rationale fields instead of only side-effecting a workflow execution report.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused regression coverage for route primitives</name>
  <files>tests/test_workflow_orchestration.py, tests/test_execution_strategy.py</files>
  <action>Add focused tests that prove the default serious-work workflows are candidate-selectable, that prompt-family recommendation is explicit for serious-work routes, and that backend recommendation respects surface-aware strategy selection. Cover at least one codex serious-work route and at least one fallback case where prompt metadata is partial but still yields explicit route output.</action>
  <verify>uv run pytest tests/test_workflow_orchestration.py tests/test_execution_strategy.py -q</verify>
  <done>Focused tests fail if workflow/prompt/backend route primitives regress or become implicit again.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| objective text -> route selection | untrusted user/task wording influences workflow and prompt selection |
| registry metadata -> route helpers | incorrect registry metadata can silently misroute serious work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | workflow/prompt registries | mitigate | keep typed validation and add tests that fail on missing route metadata or invalid family mappings |
| T-01-02 | I | route rationale helpers | mitigate | require machine-readable rationale fields and explicit fallback states instead of opaque strings |
| T-01-03 | E | backend recommendation | mitigate | anchor backend selection to validated strategy bundles and backend contracts rather than free-form recommendation text |
</threat_model>

<verification>
Run `uv run pytest tests/test_workflow_orchestration.py tests/test_execution_strategy.py -q`.
Confirm the new helper surface still preserves existing workflow execution behavior while adding route-time selection outputs.
</verification>

<success_criteria>
- Default serious-work workflow families are represented in registry-driven route primitives.
- Prompt-family recommendation is explicit and test-covered for serious-work routes.
- Backend recommendation inputs are surface-aware and inspectable.
- The changes satisfy D-07 through D-15 without introducing a separate routing stack.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-workflow-and-prompt-routing/01-01-SUMMARY.md`
</output>
