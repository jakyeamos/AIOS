---
phase: 01-project-workflow-and-prompt-routing
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - services/project_inventory.py
  - services/invocation_backends.py
  - services/task_routing.py
  - tests/test_project_inventory.py
  - tests/test_task_routing.py
autonomous: true
requirements:
  - ROUT-01
  - ROUT-02
  - ROUT-03
  - ROUT-04
must_haves:
  truths:
    - "A vague goal resolves to a route result with project outcome, task family, workflow, prompt family, and backend recommendation."
    - "Multiple plausible project candidates produce an explicit ambiguity block instead of a silent guess."
    - "The route result explains why the chosen workflow beat nearby alternatives."
  artifacts:
    - path: "services/task_routing.py"
      provides: "route result contract, project candidate resolution, task-family classification, workflow selection, prompt-family selection, and backend recommendation"
    - path: "services/project_inventory.py"
      provides: "project candidate inputs or helpers usable by route resolution"
    - path: "tests/test_task_routing.py"
      provides: "phase-specific regression coverage for exact, ambiguous, and unsupported routing outcomes"
  key_links:
    - from: "services/project_inventory.py"
      to: "services/task_routing.py"
      via: "inventory-backed project candidate generation"
      pattern: "project|candidate|inventory"
    - from: "services/task_routing.py"
      to: "services/workflow_orchestration.py"
      via: "workflow candidate selection and prompt-family recommendation"
      pattern: "workflow_key|prompt_family|rationale"
    - from: "services/task_routing.py"
      to: "services/invocation_backends.py"
      via: "recommended backend contract"
      pattern: "backend_key|surface|strict_handshake"
---

<objective>
Implement the first-class route-result layer that turns a vague objective into explicit project/workflow/prompt/backend routing with inspectable reasoning.

Purpose: satisfy the real Phase 1 product behavior instead of leaving routing spread across registries and CLI defaults.
Output: a route module and tests that cover exact-match, ambiguous, unsupported, and serious-work workflow-selection outcomes.
</objective>

<execution_context>
@/Users/jakyeamos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakyeamos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/PHASE_01_SUBROADMAP.md
@.planning/phases/01-project-workflow-and-prompt-routing/01-CONTEXT.md
@.planning/phases/01-project-workflow-and-prompt-routing/01-RESEARCH.md
@services/project_inventory.py
@services/workflow_orchestration.py
@services/execution_strategy.py
@services/invocation_backends.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the route-result contract and project candidate resolution</name>
  <files>services/task_routing.py, services/project_inventory.py, tests/test_project_inventory.py, tests/test_task_routing.py</files>
  <action>Create a dedicated routing module per D-01 through D-06 that defines the route-result contract, explicit project outcomes (exact, likely, ambiguous, unsupported), and inventory-backed candidate generation. Reuse stable project ids and existing inventory semantics where possible, but add the route-facing candidate ranking and ambiguity behavior outside the raw sync logic. Keep the route result machine-readable and durable-ready.</action>
  <verify>uv run pytest tests/test_project_inventory.py tests/test_task_routing.py -q</verify>
  <done>Route resolution can distinguish exact, likely, ambiguous, and unsupported project outcomes and returns candidate reasoning that later phases can inspect.</done>
</task>

<task type="auto">
  <name>Task 2: Compose workflow, prompt-family, and backend recommendation into the route layer</name>
  <files>services/task_routing.py, services/invocation_backends.py, tests/test_task_routing.py</files>
  <action>Use the primitives from Plan 01 to make `services/task_routing.py` derive task family, selected workflow, nearby alternatives, prompt-family recommendation, and backend recommendation per D-07 through D-18. Preserve ambiguity blocking, include fallback state when prompt-family coverage is partial, and ensure backend selection respects managed-runtime preference rather than defaulting to legacy execution. Only extend `services/invocation_backends.py` if the existing contract needs explicit route-facing metadata.</action>
  <verify>uv run pytest tests/test_task_routing.py tests/test_workflow_orchestration.py tests/test_execution_strategy.py -q</verify>
  <done>The route layer produces one explicit, explainable route result for serious-work objectives, including why alternatives lost and what fallback states still apply.</done>
</task>

<task type="auto">
  <name>Task 3: Add route-level regression fixtures and edge-case coverage</name>
  <files>tests/test_task_routing.py</files>
  <action>Add route-level tests for at least: exact current-project match, multi-project ambiguity block, unsupported/no-match objective, implementation-delivery route, failure-recovery route, audit-style route, and prompt-family fallback behavior. Ensure tests verify rationale content and do not only assert final workflow keys.</action>
  <verify>uv run pytest tests/test_task_routing.py -q</verify>
  <done>Phase 1 route behavior is locked down by focused tests that would catch silent guessing or opaque route outputs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| objective text -> project candidate ranking | arbitrary user phrasing can bias project resolution |
| route result -> later execution phases | incorrect route metadata can misdirect packets, runtimes, and writebacks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | S | project resolution | mitigate | require explicit ambiguity outcomes and candidate reasoning instead of inferring certainty from weak matches |
| T-01-05 | T | route result contract | mitigate | keep typed route results and route-level tests for fallback and alternative rationale |
| T-01-06 | E | backend recommendation | mitigate | avoid implicit legacy fallback; require explicit managed-runtime preference logic and fallback state |
</threat_model>

<verification>
Run `uv run pytest tests/test_project_inventory.py tests/test_task_routing.py tests/test_workflow_orchestration.py tests/test_execution_strategy.py -q`.
Inspect route-result fixtures to confirm rationale includes candidate outcomes and nearby alternatives.
</verification>

<success_criteria>
- Route results satisfy ROUT-01 through ROUT-04 directly.
- Project ambiguity is explicit and test-covered.
- Workflow, prompt-family, and backend selections are returned together with rationale and fallback state.
- The route module is reusable by CLI packet creation instead of remaining a detached helper.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-workflow-and-prompt-routing/01-02-SUMMARY.md`
</output>
