---
phase: 09-continuous-learning-and-conservative-optimization
plan: "05"
type: execute
wave: 3
autonomous: true
depends_on:
  - "09-01"
  - "09-02"
  - "09-03"
  - "09-04"
files_modified:
  - services/aios_cli.py
  - bin/hook-stop.py
  - aios-ui/lib/types.ts
  - aios-ui/server/aios/learning.ts
  - aios-ui/server/routers/learning.ts
  - aios-ui/server/routers/_app.ts
  - tests/test_aios_cli.py
  - tests/test_hook_stop.py
requirements:
  - LEARN-01
  - LEARN-04
must_haves:
  truths:
    - "services/aios_cli.py exposes three new subcommands: aios learning-analyze (wraps detect_recurring_patterns; supports --since 30d|ISO, --project, --json, --dry-run), aios learning-propose (wraps detect_recurring_patterns + propose_from_all_patterns; supports --since, --project, --json, --dry-run), aios learning-impact (wraps build_per_run_impact or build_rollup depending on --run-id vs --workflow/--prompt/--skill; supports --since, --json)."
    - "services/aios_cli.py _workflow_learning_payload return shape gains four NEW top-level summary keys: recurring_pattern_count, conservative_proposal_count, signal_kind_counts (a dict mapping LearningSignalKind → integer); a contract block addition (signal_kinds = LEARNING_SIGNAL_KINDS, pattern_source = 'services/learning_analysis.detect_recurring_patterns', conservatism_policy_source = 'config/learning/conservatism-policy.json'); plus recurring_patterns (up to 50 rows) and conservative_proposals (up to 50 rows) arrays alongside the existing persisted_events/inferred_evidence/no_learning_runs/proposals arrays. Existing keys remain backward-compatible."
    - "services/aios_cli.py contracts-audit gains a LearningSignal contract row with status='implemented' once the new modules + column are present; source_of_truth references services/learning_taxonomy.py + services/learning_analysis.py + services/conservative_optimizer.py + services/learning_impact.py + config/learning/conservatism-policy.json + workflow_learning_events."
    - "bin/hook-stop.py emits signal_kind values into workflow_learning_events at terminal-run closeout via the existing _record_workflow_learning_event seam (Plan 01 added the kwarg). The closeout heuristic derives signal_kind from already-computed evidence: follow_up_required=1 OR major_repair_required=1 → 'repeated_failure' candidate; workflow_execution_reports.status='failed' → 'weak_workflow' candidate; success_criteria_findings.level='blocker' AND resolution_status='open' → 'ignored_rule' candidate; otherwise NULL (per A12 — closeout heuristic vs cross-run detection can disagree; cross-run is authoritative)."
    - "aios-ui/server/aios/learning.ts gains four new exports: getLearningImpactForRun, getLearningImpactRollup, listRecurringPatterns, listConservativeProposals. Each returns the typed payload defined in aios-ui/lib/types.ts. proposeRunWritebacks remains unchanged (the per-run legacy emitter)."
    - "aios-ui/server/routers/learning.ts (NEW) exposes four tRPC procedures (getRunImpact, getRollup, listPatterns, listProposals) validated with zod and bound to the new learning.ts server functions; aios-ui/server/routers/_app.ts wires the new router under appRouter.learning."
    - "aios-ui/lib/types.ts gains LearningSignalKind, RecurringPattern, LearningImpactPerRun, LearningImpactRollup, ConservativeProposalRow type definitions that mirror the Python dataclasses; the Python and TypeScript surfaces stay aligned in field names."
  artifacts:
    - path: "services/aios_cli.py"
      provides: "cmd_learning_analyze, cmd_learning_propose, cmd_learning_impact functions + argparse wiring; _workflow_learning_payload extended with recurring_patterns + conservative_proposals + signal_kind_counts; contracts-audit extended with LearningSignal row"
    - path: "bin/hook-stop.py"
      provides: "signal_kind heuristic during closeout; passes signal_kind kwarg to _record_workflow_learning_event for terminal runs that meet one of the four heuristic conditions"
    - path: "aios-ui/lib/types.ts"
      provides: "LearningSignalKind union; RecurringPattern, LearningImpactPerRun, LearningImpactRollup, ConservativeProposalRow types"
    - path: "aios-ui/server/aios/learning.ts"
      provides: "getLearningImpactForRun, getLearningImpactRollup, listRecurringPatterns, listConservativeProposals server-side query functions using better-sqlite3"
    - path: "aios-ui/server/routers/learning.ts"
      provides: "NEW tRPC router with four procedures (getRunImpact, getRollup, listPatterns, listProposals); zod validation on inputs"
    - path: "aios-ui/server/routers/_app.ts"
      provides: "appRouter.learning = learningRouter wired under the root router"
    - path: "tests/test_aios_cli.py"
      provides: "CLI subcommand tests + extended workflow-learning-audit payload tests + contracts-audit row test"
    - path: "tests/test_hook_stop.py"
      provides: "signal_kind heuristic tests at closeout"
  key_links:
    - from: "services/aios_cli.py:cmd_learning_propose"
      to: "services/learning_analysis.detect_recurring_patterns + services/conservative_optimizer.propose_from_all_patterns"
      via: "imports both modules; dry-run mode runs detect + classify-skip without insert; live mode runs propose_from_all_patterns and returns the proposed/skipped tuple"
      pattern: "detect_recurring_patterns|propose_from_all_patterns"
    - from: "services/aios_cli.py:_workflow_learning_payload"
      to: "services/learning_analysis.detect_recurring_patterns + improvement_writebacks WHERE proposed_change_json LIKE %learning_analysis%"
      via: "bounded recent-window read (default 30d); top-level summary keys recurring_pattern_count + conservative_proposal_count + signal_kind_counts; backward-compatible additive payload"
      pattern: "recurring_pattern_count|conservative_proposal_count"
    - from: "bin/hook-stop.py:closeout"
      to: "services.aios_cli._record_workflow_learning_event (signal_kind kwarg)"
      via: "import via existing aios_cli surface or via a small closure that mirrors the helper; the heuristic computes a single signal_kind value per terminal run"
      pattern: "signal_kind"
    - from: "aios-ui/server/routers/learning.ts"
      to: "aios-ui/server/aios/learning.ts (new exports)"
      via: "tRPC procedures call the four new server functions with zod-validated args"
      pattern: "getLearningImpactForRun|getLearningImpactRollup|listRecurringPatterns|listConservativeProposals"
---

<objective>
Surface Plan 02-04 capabilities to the operator: three new aios CLI subcommands (learning-analyze, learning-propose, learning-impact); an extended _workflow_learning_payload that adds recurring_patterns + conservative_proposals + signal_kind_counts; an extended contracts-audit with a LearningSignal row; signal_kind emission at hook-stop closeout via the heuristic (closing out LEARN-01's per-run path); and a UI surface (lib types + server functions + new tRPC router) that exposes per-run impact, rollups, recurring patterns, and conservative proposals.

Purpose: LEARN-01 (per-run signal_kind emission) and LEARN-04 (operator-visible compounding) both require operator-reachable surfaces. Plan 05 stitches the Plan 01-04 backends together into CLI + UI without changing recommender runtime behavior (Phase 1 task_routing.py and Phase 2 packet compilation remain untouched — A4 invariant).

Output: services/aios_cli.py extensions; bin/hook-stop.py extension; aios-ui/lib/types.ts extension; aios-ui/server/aios/learning.ts extension; aios-ui/server/routers/learning.ts new file; aios-ui/server/routers/_app.ts wiring; two test file extensions (one new — tests/test_hook_stop.py if absent).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-continuous-learning-and-conservative-optimization/09-RESEARCH.md
@.planning/phases/09-continuous-learning-and-conservative-optimization/09-01-PLAN.md
@.planning/phases/09-continuous-learning-and-conservative-optimization/09-02-PLAN.md
@.planning/phases/09-continuous-learning-and-conservative-optimization/09-03-PLAN.md
@.planning/phases/09-continuous-learning-and-conservative-optimization/09-04-PLAN.md

<interfaces>
<!-- Surfaces this plan composes against. -->

From services/aios_cli.py (verified during planning):
- argparse-based subparsers — find existing subcommands via `grep -n "subparsers.add_parser" services/aios_cli.py | head -20`.
- `_workflow_learning_payload(conn)` at lines 2208-2377 — top-level returns `summary`, `contract`, `classification_counts`, `persisted_events`, `inferred_evidence`, `no_learning_runs`, `proposals`. Plan 05 adds four new top-level keys and three new contract keys without breaking existing consumers.
- `_contracts_audit_payload()` near line 2700+ — find via `grep -n "_contracts_audit_payload\|def cmd_contracts_audit" services/aios_cli.py | head -10`. Plan 05 appends a LearningSignal row.
- `_record_workflow_learning_event` with signal_kind kwarg (Plan 01).
- `WORKFLOW_LEARNING_EVIDENCE_TYPES` at line 147.

From services/learning_analysis.py (Plan 02):
- `detect_recurring_patterns(conn, *, since, project_id, signal_kinds=None) -> list[RecurringPattern]`.
- `RecurringPattern` dataclass.

From services/conservative_optimizer.py (Plan 04):
- `load_conservatism_policy(path=None) -> ConservatismPolicy`.
- `propose_from_all_patterns(conn, patterns, policy, *, actor) -> dict[str, Any]`.

From services/learning_impact.py (Plan 03):
- `build_per_run_impact(conn, *, run_id) -> LearningImpactPerRun | None`.
- `build_rollup(conn, *, scope, key, since, project_id=None, min_sample_size_for_trend=10) -> LearningImpactRollup`.

From bin/hook-stop.py (verified during planning):
- Closeout block emits writebacks + workflow_learning_events; find via `grep -n "_record_workflow_learning_event\|workflow_learning_writeback_expected" bin/hook-stop.py | head -15`.
- Closeout reads agentize_evaluations.follow_up_required and major_repair_required and workflow_execution_reports.status.

From aios-ui/server/aios/learning.ts (verified during planning, 170 lines):
- `proposeRunWritebacks(db, runId)` is the existing per-run emitter. Plan 05 KEEPS this as-is and adds sibling exports.

From aios-ui/server/routers/_app.ts:
- Existing routers wired include divergent, experiments, control-plane, patterns, query, sessions, projects, etc. Plan 05 adds `learning: learningRouter`.

From aios-ui/lib/types.ts:
- Existing types defined for control-plane / divergent / etc. Plan 05 appends the five new Learning* / RecurringPattern types without modifying existing.

NOTE: services/aios_cli.py CAN import from other services/ modules (services → services is allowed); the LEARN modules created in Plans 01-04 are imported here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add aios learning-analyze, learning-propose, learning-impact CLI subcommands; extend _workflow_learning_payload with recurring_patterns + conservative_proposals + signal_kind_counts + extended contract block; extend contracts-audit with LearningSignal row</name>
  <files>services/aios_cli.py, tests/test_aios_cli.py</files>
  <read_first>
    - services/aios_cli.py — read the existing subparser registration block (find via `grep -n "subparsers.add_parser\|workflow-learning-audit" services/aios_cli.py | head -20`). Read one full existing `cmd_*` function (e.g., the existing cmd for workflow-learning-audit) as a paste-of-record for argparse + JSON output convention.
    - services/aios_cli.py — read _workflow_learning_payload (lines 2208-2377) verbatim; identify the exact dict shape so the new keys are added in the right place without breaking the existing key set.
    - services/aios_cli.py — read _contracts_audit_payload (find via `grep -n "_contracts_audit_payload\|return.*contracts.*\[" services/aios_cli.py | head -10`); identify where existing contract rows are appended; the new LearningSignal row follows the same shape.
    - services/learning_analysis.py + services/conservative_optimizer.py + services/learning_impact.py — confirm import signatures match the function calls Plan 05 makes.
    - tests/test_aios_cli.py — find the existing workflow-learning-audit tests (`grep -n "def test_workflow_learning" tests/test_aios_cli.py | head -10`); confirm fixture style.
  </read_first>
  <behavior>
    - Test 1 `test_learning_analyze_cli_dry_run`: invoke `aios learning-analyze --since 30d --json --dry-run` on a seeded sqlite db (in-process via the CLI's main entrypoint); returns JSON with keys `recurring_patterns` (list), `total_patterns`, `since`, `policy_version`, `dry_run: true`. No improvement_writebacks rows inserted.
    - Test 2 `test_learning_analyze_cli_per_project`: same invocation with `--project p1`; only patterns matching project p1 returned.
    - Test 3 `test_learning_propose_cli_writes_writebacks`: invoke `aios learning-propose --since 30d --json` on a seeded db with three above-threshold patterns; returns JSON with `proposed_count`, `skipped_count`, `writebacks` list, `skipped` list; SELECT COUNT(*) FROM improvement_writebacks shows three new rows with proposed_change_json containing `"source": "learning_analysis"`.
    - Test 4 `test_learning_propose_cli_dry_run`: `aios learning-propose --since 30d --json --dry-run` returns the same shape as live mode (with proposed_count > 0 if patterns pass thresholds) but DOES NOT INSERT to improvement_writebacks (assert COUNT unchanged).
    - Test 5 `test_learning_impact_cli_per_run`: `aios learning-impact --run-id r1 --json` returns JSON keys matching LearningImpactPerRun fields (run_id, workflow_key, signals_emitted, assets_evidenced, proposals_created, learning_events_persisted, no_learning_reason); shape matches build_per_run_impact output.
    - Test 6 `test_learning_impact_cli_workflow_rollup`: `aios learning-impact --workflow implementation-delivery --since 30d --json` returns JSON keys matching LearningImpactRollup fields.
    - Test 7 `test_learning_impact_cli_prompt_rollup`: `aios learning-impact --prompt research --since 30d --json` returns rollup with scope="prompt".
    - Test 8 `test_workflow_learning_payload_includes_signal_kind_counts`: seed five workflow_learning_events with signal_kind values across three distinct kinds; _workflow_learning_payload(conn) returns a payload whose `summary["signal_kind_counts"]` is a dict mapping signal_kind → count matching the seed.
    - Test 9 `test_workflow_learning_payload_includes_recurring_patterns_and_proposals`: seed evidence that yields at least one recurring pattern and one Plan-04 writeback (manually insert with source=learning_analysis metadata); the payload contains `recurring_patterns` array (length ≥ 1) and `conservative_proposals` array (length ≥ 1); existing keys (persisted_events, proposals, no_learning_runs) remain present (backward-compatible).
    - Test 10 `test_workflow_learning_payload_contract_block_extended`: the payload's `contract` dict contains the new keys `signal_kinds`, `pattern_source`, `conservatism_policy_source` in addition to the existing keys.
    - Test 11 `test_contracts_audit_includes_learning_signal`: `aios contracts-audit --json` returns a payload whose contracts list contains an entry with name="LearningSignal", status="implemented", source_of_truth referencing services/learning_taxonomy.py and config/learning/conservatism-policy.json.
  </behavior>
  <action>
    1. **Extend `create_parser` (or equivalent argparse setup in services/aios_cli.py)** to register three new subcommands:
       - `learning-analyze`: `--since` (string; default "30d"), `--project` (string optional), `--signal-kind` (repeatable, optional — restricts to specific signal_kinds), `--json` (flag default true), `--dry-run` (flag — informational; learning-analyze never writes regardless, but the flag is exposed so output messaging can reflect the intent).
       - `learning-propose`: `--since` (default "30d"), `--project` (optional), `--actor` (default "operator-cli"), `--json` (default true), `--dry-run` (flag — when set, runs detect + classify-skip without invoking propose_from_all_patterns; reports the patterns that WOULD be proposed).
       - `learning-impact`: mutually-exclusive group on `--run-id` vs `--workflow` vs `--prompt` vs `--skill`. Also `--since` (default "30d"), `--project` (optional), `--json` (default true).

    2. **Add `cmd_learning_analyze(args) -> dict[str, Any]`:**
       - Resolves the since argument (reuse the existing `_resolve_since_argument` helper if Plan 08 introduced one; otherwise add a small local helper for "Nd" → ISO timestamp).
       - Calls `services.learning_analysis.detect_recurring_patterns(conn, since=resolved_since, project_id=args.project, signal_kinds=tuple(args.signal_kind) if args.signal_kind else None)`.
       - Returns `{"recurring_patterns": [dataclasses.asdict(p) for p in patterns], "total_patterns": len(patterns), "since": resolved_since, "project_id": args.project, "dry_run": bool(args.dry_run), "policy_version": "from-config"}`.

    3. **Add `cmd_learning_propose(args) -> dict[str, Any]`:**
       - Calls detect_recurring_patterns as above.
       - Calls `services.conservative_optimizer.load_conservatism_policy()`.
       - If args.dry_run: iterate patterns through `services.conservative_optimizer._classify_skip_reason` (or build a sibling public helper) and return `{"would_propose": [...], "would_skip": [...], "dry_run": True}` without writing.
       - Else: call `propose_from_all_patterns(conn, patterns, policy, actor=args.actor)` and return the dict it returns plus `"dry_run": False`.

    4. **Add `cmd_learning_impact(args) -> dict[str, Any]`:**
       - If args.run_id: call `build_per_run_impact(conn, run_id=args.run_id)`; return dataclasses.asdict() of the result (handle None → return `{"run_id": args.run_id, "found": false}`).
       - Else if args.workflow / prompt / skill: call `build_rollup(conn, scope=<scope>, key=<key>, since=resolved_since, project_id=args.project)`; return dataclasses.asdict().
       - Else: argparse mutually-exclusive group should prevent reaching here, but defensively return `{"error": "one of --run-id, --workflow, --prompt, --skill is required"}`.

    5. **Extend `_workflow_learning_payload(conn)` to add four new top-level summary keys and three contract keys:**
       - Read `from services.learning_analysis import detect_recurring_patterns, LEARNING_SIGNAL_KINDS` (or move LEARNING_SIGNAL_KINDS to services/learning_taxonomy.py if not already imported there — Plan 01 already places it in taxonomy).
       - Compute `signal_kind_counts`: SELECT signal_kind, COUNT(*) FROM workflow_learning_events WHERE signal_kind IS NOT NULL GROUP BY signal_kind. Build a dict keyed by signal_kind values.
       - Compute `recurring_patterns`: call `detect_recurring_patterns(conn, since=<30d default>, project_id=None)`; take the first 50; convert to dicts.
       - Compute `conservative_proposals`: SELECT id, layer_type, layer_key, impact_scope, status, requires_approval, proposed_change_json, created_at FROM improvement_writebacks WHERE proposed_change_json LIKE '%"source": "learning_analysis"%' ORDER BY created_at DESC LIMIT 50.
       - Add `summary["recurring_pattern_count"]`, `summary["conservative_proposal_count"]`, `summary["signal_kind_counts"]`.
       - Add `contract["signal_kinds"]` = list(LEARNING_SIGNAL_KINDS).
       - Add `contract["pattern_source"]` = "services/learning_analysis.detect_recurring_patterns".
       - Add `contract["conservatism_policy_source"]` = "config/learning/conservatism-policy.json".
       - Add top-level keys `recurring_patterns` (list) and `conservative_proposals` (list).
       - All EXISTING keys remain unchanged (backward-compatibility — confirm by reading the existing return shape at lines 2354-2377 and adding new keys, never renaming or removing).

    6. **Extend `_contracts_audit_payload()` (or wherever contracts list is constructed)** with one new row:
       ```
       {
         "name": "LearningSignal",
         "status": "implemented" if all([
             _table_has_column(conn, "workflow_learning_events", "signal_kind"),
             Path("services/learning_taxonomy.py").exists(),
             Path("services/learning_analysis.py").exists(),
             Path("services/conservative_optimizer.py").exists(),
             Path("services/learning_impact.py").exists(),
             Path("config/learning/conservatism-policy.json").exists(),
         ]) else "partial",
         "source_of_truth": [
           "services/learning_taxonomy.py",
           "services/learning_analysis.py",
           "services/conservative_optimizer.py",
           "services/learning_impact.py",
           "config/learning/conservatism-policy.json",
           "workflow_learning_events.signal_kind",
         ],
         "notes": "Two-axis classification (evidence_type + signal_kind); cross-run pattern detection; conservative optimizer with requires_approval=True invariant; impact projections (Phase 9)."
       }
       ```
       (Replace `_table_has_column` with the actual helper name in services/aios_cli.py — find via `grep -n "table_info\|has_column" services/aios_cli.py | head -5`.)

    7. **Wire the three new subcommands into the main dispatcher** following the existing pattern.

    8. **Add eleven behavior tests** to tests/test_aios_cli.py.

    9. **Quality ladder per touched file:** `uv run ruff check services/aios_cli.py tests/test_aios_cli.py && uv run ruff format --check services/aios_cli.py tests/test_aios_cli.py && uv run basedpyright services/aios_cli.py`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_aios_cli.py -x -q -k "learning_analyze or learning_propose or learning_impact or workflow_learning_payload_includes_signal_kind_counts or workflow_learning_payload_includes_recurring_patterns or workflow_learning_payload_contract_block_extended or contracts_audit_includes_learning_signal"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "learning-analyze\|learning-propose\|learning-impact" services/aios_cli.py | wc -l | tr -d ' '` returns at least 6 (parser registration + dispatcher branches for the three subcommands)
    - `grep -n "def cmd_learning_analyze\|def cmd_learning_propose\|def cmd_learning_impact" services/aios_cli.py | wc -l | tr -d ' '` returns at least 3
    - `grep -n "recurring_pattern_count\|conservative_proposal_count\|signal_kind_counts" services/aios_cli.py | wc -l | tr -d ' '` returns at least 3
    - `grep -n "from services.learning_analysis import\|from services.conservative_optimizer import\|from services.learning_impact import\|from services.learning_taxonomy import" services/aios_cli.py | wc -l | tr -d ' '` returns at least 3
    - `grep -n "LearningSignal" services/aios_cli.py | wc -l | tr -d ' '` returns at least 1 (contract row)
    - `grep -c "def test_learning_analyze_cli_dry_run\|def test_learning_analyze_cli_per_project\|def test_learning_propose_cli_writes_writebacks\|def test_learning_propose_cli_dry_run\|def test_learning_impact_cli_per_run\|def test_learning_impact_cli_workflow_rollup\|def test_learning_impact_cli_prompt_rollup\|def test_workflow_learning_payload_includes_signal_kind_counts\|def test_workflow_learning_payload_includes_recurring_patterns_and_proposals\|def test_workflow_learning_payload_contract_block_extended\|def test_contracts_audit_includes_learning_signal" tests/test_aios_cli.py | grep -v '^#'` returns at least 11
    - `uv run pytest tests/test_aios_cli.py -x -q` exits 0
    - `uv run pytest tests/test_architecture_enforcement.py -x -q` exits 0 (no services -> bin import introduced)
    - `uv run ruff check services/aios_cli.py tests/test_aios_cli.py` exits 0
    - `uv run ruff format --check services/aios_cli.py tests/test_aios_cli.py` exits 0
    - `uv run basedpyright services/aios_cli.py` exits 0
  </acceptance_criteria>
  <done>
    Three new aios CLI subcommands wired and tested; _workflow_learning_payload extended with recurring_patterns, conservative_proposals, signal_kind_counts (backward-compatible); contracts-audit lists LearningSignal; eleven behavior tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add signal_kind emission heuristic in bin/hook-stop.py closeout; pass signal_kind kwarg to _record_workflow_learning_event for terminal runs that meet any of the four heuristic conditions</name>
  <files>bin/hook-stop.py, tests/test_hook_stop.py</files>
  <read_first>
    - bin/hook-stop.py — read the closeout block; find via `grep -n "_record_workflow_learning_event\|workflow_learning\|follow_up_required\|major_repair_required\|workflow_execution_reports" bin/hook-stop.py | head -20`. Identify where the workflow_learning_event row is currently written and where the heuristic data (follow_up_required, major_repair_required, status, blocker findings) is already in scope.
    - tests/test_hook_stop.py — find via `ls tests/test_hook_stop.py 2>/dev/null && head -100 tests/test_hook_stop.py`. If the file does not exist, create it with a minimal in-memory sqlite + hook-stop function invocation pattern (mirror the fixture style from tests/test_aios_cli.py).
    - services/aios_cli.py — confirm _record_workflow_learning_event signal_kind kwarg (Plan 01).
    - .planning/phases/09-continuous-learning-and-conservative-optimization/09-RESEARCH.md — A12 (closeout heuristic vs cross-run detection); Pattern 5 (Idempotent Schema Migration); LEARN-01 closeout emission section.
  </read_first>
  <behavior>
    - Test 1 `test_closeout_signal_kind_repeated_failure_when_follow_up_required`: simulate hook-stop closeout for a terminal run with agentize_evaluations.follow_up_required=1; assert the workflow_learning_events row inserted has signal_kind="repeated_failure".
    - Test 2 `test_closeout_signal_kind_repeated_failure_when_major_repair_required`: agentize_evaluations.major_repair_required=1 (and follow_up_required=0); signal_kind="repeated_failure".
    - Test 3 `test_closeout_signal_kind_weak_workflow_when_status_failed`: workflow_execution_reports.status="failed" (and no follow_up/repair flags); signal_kind="weak_workflow".
    - Test 4 `test_closeout_signal_kind_ignored_rule_when_open_blocker_finding`: success_criteria_findings row for the run with level="blocker" and resolution_status="open" (and status="completed" — i.e., the run completed despite an unresolved blocker); signal_kind="ignored_rule".
    - Test 5 `test_closeout_signal_kind_null_when_clean_run`: completed run with no follow_up, no major_repair, no failed status, no open blockers; signal_kind=NULL.
    - Test 6 `test_closeout_signal_kind_priority_order`: a run that triggers BOTH follow_up_required AND status="failed" gets signal_kind="repeated_failure" (higher-priority heuristic wins). Document the priority order in a module comment: 1) follow_up_required or major_repair_required → repeated_failure; 2) workflow_execution_reports.status="failed" → weak_workflow; 3) open blocker finding → ignored_rule; 4) otherwise → NULL.
    - Test 7 `test_closeout_signal_kind_only_for_terminal_runs`: a non-terminal run (status="active") does NOT emit a signal_kind (the closeout block doesn't run); assert COUNT(*) FROM workflow_learning_events WHERE run_id=... AND signal_kind IS NOT NULL is 0.
  </behavior>
  <action>
    1. **Locate the closeout block in bin/hook-stop.py** where the workflow_learning_event is currently written. The current call passes (run_id, evidence_type, proposal_target, confidence, approval_state, rationale, source); Plan 01 already added the optional signal_kind kwarg.

    2. **Add a `_classify_closeout_signal_kind(*, follow_up_required, major_repair_required, run_status, has_open_blocker) -> str | None`** helper INSIDE bin/hook-stop.py (NOT in services — closeout is a bin-side concern):
       ```
       if follow_up_required or major_repair_required:
           return "repeated_failure"
       if run_status == "failed":
           return "weak_workflow"
       if has_open_blocker:
           return "ignored_rule"
       return None
       ```
       Document the priority order in a comment block above the helper (Test 6).

    3. **Before the call to `_record_workflow_learning_event` in the closeout block**, compute the four inputs:
       - follow_up_required, major_repair_required from agentize_evaluations (latest row for the run_id) — likely already in scope; if not, SELECT it.
       - run_status from workflow_execution_reports.status for the run (most recent).
       - has_open_blocker from `SELECT 1 FROM success_criteria_findings WHERE run_id = ? AND level='blocker' AND resolution_status='open' LIMIT 1` — bool.
       - Call `signal_kind = _classify_closeout_signal_kind(...)`.

    4. **Pass `signal_kind=signal_kind`** to `_record_workflow_learning_event(...)`. When signal_kind is None, the call still works (Plan 01 made it optional).

    5. **Create tests/test_hook_stop.py** (or extend if exists) with the seven behavior tests above. Use the existing hook-stop test pattern: in-memory sqlite + seed runtime schema + invoke the closeout entrypoint (function name to be confirmed via read_first — likely something like `_emit_workflow_learning_event_for_run` or the higher-level closeout function).

    6. **Quality ladder per touched file:** `uv run ruff check bin/hook-stop.py tests/test_hook_stop.py && uv run ruff format --check bin/hook-stop.py tests/test_hook_stop.py && uv run basedpyright bin/hook-stop.py`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_hook_stop.py -x -q -k "closeout_signal_kind"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def _classify_closeout_signal_kind\|signal_kind=" bin/hook-stop.py | wc -l | tr -d ' '` returns at least 2 (helper + call site)
    - `grep -n "repeated_failure\|weak_workflow\|ignored_rule" bin/hook-stop.py | wc -l | tr -d ' '` returns at least 3 (the three heuristic-emitted signal_kinds)
    - `grep -c "def test_closeout_signal_kind_repeated_failure_when_follow_up_required\|def test_closeout_signal_kind_repeated_failure_when_major_repair_required\|def test_closeout_signal_kind_weak_workflow_when_status_failed\|def test_closeout_signal_kind_ignored_rule_when_open_blocker_finding\|def test_closeout_signal_kind_null_when_clean_run\|def test_closeout_signal_kind_priority_order\|def test_closeout_signal_kind_only_for_terminal_runs" tests/test_hook_stop.py | grep -v '^#'` returns at least 7
    - `uv run pytest tests/test_hook_stop.py -x -q` exits 0
    - `uv run pytest tests/test_aios_cli.py -x -q` exits 0 (regression on Plan 01 + Task 1)
    - `uv run pytest tests/test_architecture_enforcement.py -x -q` exits 0
    - `uv run ruff check bin/hook-stop.py tests/test_hook_stop.py` exits 0
    - `uv run ruff format --check bin/hook-stop.py tests/test_hook_stop.py` exits 0
    - `uv run basedpyright bin/hook-stop.py` exits 0
  </acceptance_criteria>
  <done>
    bin/hook-stop.py emits signal_kind values at terminal-run closeout via the four-step heuristic; seven behavior tests pass; LEARN-01 closeout emission complete.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add aios-ui surfaces — extend lib/types.ts; add four exports to server/aios/learning.ts; create server/routers/learning.ts; wire under appRouter.learning</name>
  <files>aios-ui/lib/types.ts, aios-ui/server/aios/learning.ts, aios-ui/server/routers/learning.ts, aios-ui/server/routers/_app.ts</files>
  <read_first>
    - aios-ui/lib/types.ts — full file; identify the existing type-export convention (`export type Foo = { ... }` vs `export interface Foo {}`); identify where to append new types so they don't merge-conflict with concurrent UI work.
    - aios-ui/server/aios/learning.ts — full file (170 lines); read proposeRunWritebacks and ensureControlPlaneSchema; identify the better-sqlite3 Database type import and the prepared-statement pattern.
    - aios-ui/server/routers/_app.ts — full file; identify the existing `router({...})` block and where to add the new `learning: learningRouter` entry.
    - aios-ui/server/routers/control-plane.ts — full file; identify the tRPC procedure conventions (publicProcedure vs operatorProcedure if any), zod input/output validation patterns, and how errors are thrown.
    - aios-ui/server/routers/divergent.ts — paste-of-record for a router that proxies to a server/aios/*.ts file with prepared statements.
    - schema.sql — improvement_writebacks columns (the listConservativeProposals query reads them); workflow_learning_events columns including signal_kind (Plan 01).
    - aios-ui/package.json — confirm better-sqlite3 + zod + tRPC versions (no install needed if already present).
    - .planning/phases/09-continuous-learning-and-conservative-optimization/09-RESEARCH.md — A7 (new router file preferred over extending control-plane.ts); Pitfall 8 (UI labels must show proposal_status vs current_lifecycle_state).
  </read_first>
  <behavior>
    - Test 1 (type-level, no runtime test framework wired): `cd aios-ui && pnpm tsc --noEmit` exits 0 after the type additions; this is the gate for Task 3 type changes since pnpm test isn't configured for aios-ui.
    - Test 2 (lint): `cd aios-ui && pnpm lint` exits 0.
    - Test 3 (router wiring): after the changes, the appRouter exports a `learning` namespace; verified by reading aios-ui/server/routers/_app.ts and confirming the import + assignment lines are present.
    - Test 4 (smoke check): a minimal script that imports the four new server/aios/learning.ts exports compiles without error:
      `node -e "require('./aios-ui/dist/server/aios/learning.js')"` — only after build; if Next.js build isn't part of Phase 9 verification, skip the runtime smoke and rely on tsc.
    - Test 5 (Pitfall 8): the ConservativeProposalRow type includes both proposal_status (mapped from improvement_writebacks.status) AND current_lifecycle_state (mapped from promotion_lifecycle_items.status if a row exists for the same scope_key, else null). The type declaration in lib/types.ts MUST include both fields.
  </behavior>
  <action>
    1. **Extend aios-ui/lib/types.ts** with:
       ```typescript
       export type LearningSignalKind =
         | "repeated_failure"
         | "ignored_rule"
         | "bloated_packet"
         | "weak_prompt"
         | "weak_workflow"
         | "route_misroute"
         | "standards_regression"
         | "writeback_adopted"
         | "writeback_rejected"
         | "compounding_gain";

       export interface RecurringPattern {
         pattern_id: string;
         signal_kind: LearningSignalKind;
         scope_kind: string;
         scope_key: string;
         project_id: string | null;
         sample_size: number;
         recurrence_count: number;
         confidence: number;
         since: string;
         summary: string;
         evidence_run_ids: readonly string[];
         suggested_remediation_class: string;
         metadata: Record<string, unknown>;
       }

       export interface AssetEvidenceDelta {
         asset_kind: "prompt" | "skill" | "workflow" | "standard" | "route";
         asset_key: string;
         delta_sample_size: number;
         delta_success_count: number;
         delta_blocker_count: number;
       }

       export interface ProposalCreated {
         writeback_id: string;
         signal_kind: LearningSignalKind | null;
         requires_approval: boolean;
         status: string;
       }

       export interface LearningImpactPerRun {
         run_id: string;
         workflow_key: string | null;
         signals_emitted: readonly LearningSignalKind[];
         assets_evidenced: readonly AssetEvidenceDelta[];
         proposals_created: readonly ProposalCreated[];
         learning_events_persisted: number;
         no_learning_reason: string | null;
       }

       export interface LearningImpactRollup {
         scope: "workflow" | "prompt" | "skill";
         key: string;
         since: string;
         sample_size: number;
         rework_rate_30d: number | null;
         rework_rate_90d: number | null;
         success_rate_30d: number | null;
         blocker_rate_30d: number | null;
         trend: "improving" | "flat" | "regressing" | "insufficient_data";
         rationale: string;
         project_id: string | null;
       }

       export interface ConservativeProposalRow {
         writeback_id: string;
         layer_type: string;
         layer_key: string;
         impact_scope: string;
         signal_kind: LearningSignalKind | null;
         pattern_id: string | null;
         proposal_status: string;          // improvement_writebacks.status (e.g., "pending_approval")
         current_lifecycle_state: string | null;  // joined from promotion_lifecycle_items when present (Pitfall 8)
         requires_approval: boolean;
         created_at: string;
         sample_size: number | null;
         recurrence_count: number | null;
         confidence: number | null;
       }
       ```

    2. **Extend aios-ui/server/aios/learning.ts** with four new exports:
       - `getLearningImpactForRun(db: Database.Database, runId: string): LearningImpactPerRun | null` — mirrors build_per_run_impact's reads. Use prepared statements via better-sqlite3.
       - `getLearningImpactRollup(db: Database.Database, scope: "workflow"|"prompt"|"skill", key: string, since: string, projectId?: string | null): LearningImpactRollup` — mirrors build_rollup.
       - `listRecurringPatterns(db: Database.Database, since: string, projectId?: string | null, limit?: number): RecurringPattern[]` — reads workflow_learning_events grouped by signal_kind + scope_key + workflow_key over the window AND also runs an OPTIONAL subset of the seven detectors when the heavy patterns are cached; for v1, list patterns from workflow_learning_events.signal_kind history (lighter than full detection). Document the simplification: the heavy cross-run detection runs server-side via the Python CLI; the UI surfaces what's already persisted.
       - `listConservativeProposals(db: Database.Database, status?: string, limit?: number): ConservativeProposalRow[]` — reads improvement_writebacks WHERE proposed_change_json LIKE '%"source": "learning_analysis"%' (use the prepared-statement equivalent for better-sqlite3 — bind the LIKE pattern as a parameter). For each row, parse proposed_change_json to extract pattern_id + signal_kind + sample_size + recurrence_count + confidence. LEFT JOIN promotion_lifecycle_items ON item_kind matching layer_type AND item_key=layer_key to populate current_lifecycle_state (Pitfall 8).

    3. **Create aios-ui/server/routers/learning.ts** as a NEW file with the tRPC router:
       ```typescript
       import { z } from "zod";
       import { router, publicProcedure } from "../trpc";  // or whatever the project's tRPC bootstrap export is — confirm via grep on existing routers
       import {
         getLearningImpactForRun,
         getLearningImpactRollup,
         listRecurringPatterns,
         listConservativeProposals,
       } from "../aios/learning";

       export const learningRouter = router({
         getRunImpact: publicProcedure
           .input(z.object({ runId: z.string() }))
           .query(({ ctx, input }) => getLearningImpactForRun(ctx.db, input.runId)),
         getRollup: publicProcedure
           .input(z.object({
             scope: z.enum(["workflow", "prompt", "skill"]),
             key: z.string(),
             since: z.string(),
             projectId: z.string().nullable().optional(),
           }))
           .query(({ ctx, input }) => getLearningImpactRollup(ctx.db, input.scope, input.key, input.since, input.projectId ?? null)),
         listPatterns: publicProcedure
           .input(z.object({
             since: z.string(),
             projectId: z.string().nullable().optional(),
             limit: z.number().int().min(1).max(200).optional(),
           }))
           .query(({ ctx, input }) => listRecurringPatterns(ctx.db, input.since, input.projectId ?? null, input.limit ?? 50)),
         listProposals: publicProcedure
           .input(z.object({
             status: z.string().optional(),
             limit: z.number().int().min(1).max(200).optional(),
           }))
           .query(({ ctx, input }) => listConservativeProposals(ctx.db, input.status, input.limit ?? 50)),
       });
       ```
       Confirm the exact tRPC bootstrap import path (`../trpc` vs `../../trpc`) by reading an existing router file during read_first.

    4. **Wire the new router into aios-ui/server/routers/_app.ts:**
       - Add `import { learningRouter } from "./learning";` to the imports block.
       - Add `learning: learningRouter,` to the appRouter object.

    5. **Quality ladder per touched file:** `cd aios-ui && pnpm lint && pnpm tsc --noEmit`. No Python tests for this task (UI does not have pnpm test wired per CLAUDE.md).
  </action>
  <verify>
    <automated>cd aios-ui && pnpm lint && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "LearningSignalKind\|RecurringPattern\|LearningImpactPerRun\|LearningImpactRollup\|ConservativeProposalRow\|AssetEvidenceDelta\|ProposalCreated" aios-ui/lib/types.ts | wc -l | tr -d ' '` returns at least 7
    - `grep -n "current_lifecycle_state" aios-ui/lib/types.ts | wc -l | tr -d ' '` returns at least 1 (Pitfall 8)
    - `grep -n "getLearningImpactForRun\|getLearningImpactRollup\|listRecurringPatterns\|listConservativeProposals" aios-ui/server/aios/learning.ts | wc -l | tr -d ' '` returns at least 4
    - `grep -n "proposeRunWritebacks" aios-ui/server/aios/learning.ts | wc -l | tr -d ' '` returns at least 1 (existing per-run emitter preserved)
    - `test -f aios-ui/server/routers/learning.ts && echo OK` prints "OK"
    - `grep -n "learningRouter\|learning: learningRouter\|import { learningRouter }" aios-ui/server/routers/_app.ts | wc -l | tr -d ' '` returns at least 2
    - `cd aios-ui && pnpm lint` exits 0
    - `cd aios-ui && pnpm tsc --noEmit` exits 0
    - `cd aios-ui && pnpm tsc --noEmit aios-ui/server/routers/learning.ts 2>&1 | grep -v "^$" | head -5` does not show a "Cannot find module" error referencing trpc imports
  </acceptance_criteria>
  <done>
    aios-ui/lib/types.ts defines all five learning-related types including the Pitfall-8-compliant ConservativeProposalRow with both proposal_status and current_lifecycle_state; aios-ui/server/aios/learning.ts exposes four new server-side query functions while preserving proposeRunWritebacks; aios-ui/server/routers/learning.ts NEW with four tRPC procedures; appRouter.learning wired; lint + typecheck green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aios CLI -> services/learning_*.py + services/conservative_optimizer.py | Operator-controlled entry; argparse validates inputs; --dry-run mode prevents accidental writes during analysis. |
| bin/hook-stop.py closeout -> _record_workflow_learning_event signal_kind kwarg | The heuristic is computed from already-in-scope closeout evidence; the kwarg lands in workflow_learning_events.signal_kind. |
| aios-ui server -> better-sqlite3 prepared statements | Read-only queries; no UI write path; Pitfall 8 enforced by joining promotion_lifecycle_items.status into the proposal row payload. |
| aios-ui router -> zod input validation | All four procedures validate inputs; unknown signal_kind values get rejected at the type-system boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-24 | Information Disclosure | UI shows proposal as "active" when only proposal_status is "approved" but current_lifecycle_state is still "candidate" (Pitfall 8) | mitigate | Task 3 Test 5 + ConservativeProposalRow type includes BOTH fields; the UI labeling layer (Phase 10) consumes both. |
| T-09-25 | Tampering | `aios learning-propose` writes proposals while operator intended only to inspect (no --dry-run flag) | mitigate | Task 1 Test 4 enforces --dry-run skips writes; the live mode emits a console banner that says "wrote N proposals to improvement_writebacks (pending_approval)" so the operator sees the write happened. |
| T-09-26 | Repudiation | hook-stop emits the wrong signal_kind due to ambiguous heuristic priority | mitigate | Task 2 Test 6 + documented priority order in a code comment; A12 acknowledges closeout heuristic and cross-run detection may disagree (cross-run is authoritative). |
| T-09-27 | Information Disclosure | _workflow_learning_payload signal_kind_counts leak signal_kind values outside the LearningSignalKind literal (corrupted DB rows) | accept | Plan 01 dedupe key extension validates writes; the SELECT in Task 1 includes only non-NULL values; corrupted rows surface as unknown keys (operator-visible). |
| T-09-28 | Denial of Service | _workflow_learning_payload's new patterns/proposals SELECTs slow down the existing audit command | mitigate | Both new SELECTs LIMIT 50; detect_recurring_patterns is bounded by the 30-day window. Profile via Test 9 timing if regressions emerge. |
</threat_model>

<verification>
- `uv run pytest tests/test_aios_cli.py tests/test_hook_stop.py tests/test_architecture_enforcement.py -x -q` exits 0
- `uv run ruff check services/aios_cli.py bin/hook-stop.py tests/test_aios_cli.py tests/test_hook_stop.py` exits 0
- `uv run ruff format --check services/aios_cli.py bin/hook-stop.py tests/test_aios_cli.py tests/test_hook_stop.py` exits 0
- `uv run basedpyright services/aios_cli.py bin/hook-stop.py` exits 0
- `cd aios-ui && pnpm lint` exits 0
- `cd aios-ui && pnpm tsc --noEmit` exits 0
- `python -c "from services.aios_cli import cmd_learning_analyze, cmd_learning_propose, cmd_learning_impact"` exits 0 (or — if the cmd_* are not at module top level — confirm via the argparse dispatch table that the three subcommand names route correctly: `python -c "import services.aios_cli as m; p=m.create_parser(); a=p.parse_args(['learning-analyze','--help'])"` exits 0; same for the other two)
- Quality ladder per CLAUDE.md: hard gates green.
</verification>

<success_criteria>
- LEARN-01 (closeout emission complete): bin/hook-stop.py classifies and emits signal_kind on every terminal-run learning event via the four-step heuristic.
- LEARN-04 (operator-visible compounding): three CLI subcommands + four new tRPC procedures + extended _workflow_learning_payload + LearningSignal contract row deliver the operator surface for "what did this run improve?" and "is this asset getting better?"
- Pitfall 8 closed: ConservativeProposalRow type and the listConservativeProposals JOIN preserve both proposal_status and current_lifecycle_state.
- _workflow_learning_payload remains backward-compatible (no key renamed or removed; six new additive keys).
- No services → bin imports introduced.
- Phase 1 (task_routing.py) and Phase 2 (packet compilation) remain untouched at runtime (A4 invariant preserved).
</success_criteria>

<output>
After completion, create `.planning/phases/09-continuous-learning-and-conservative-optimization/09-05-SUMMARY.md` per the template, including:
- The three new CLI subcommand argparse blocks (paste-of-record for shape)
- The exact extended _workflow_learning_payload JSON shape (with the six new keys flagged)
- The closeout signal_kind priority order (the four-step heuristic) and the rationale for that order
- The four new UI server-side functions and the four tRPC procedures
- The ConservativeProposalRow type definition with both proposal_status + current_lifecycle_state fields (Pitfall 8 closed)
- The LearningSignal contract-audit row shape
- Note that Plan 06 will wire divergent_strategy.py and workflow_experiments.py through Phase 8 propose_asset_promotion to close the governed-promotion loop (Pattern 6 from RESEARCH).
- Note that the actual aios-ui page implementation (the rendered React surface that consumes the new tRPC procedures) is OUT OF SCOPE for Phase 9 and lands in Phase 10 (Pitfall 8 sustained mitigation will need the page to actually render both fields).
</output>
</content>
</invoke>