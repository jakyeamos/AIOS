---
phase: 09-continuous-learning-and-conservative-optimization
plan: "03"
type: execute
wave: 2
autonomous: true
depends_on:
  - "09-01"
files_modified:
  - services/learning_impact.py
  - tests/test_learning_impact.py
requirements:
  - LEARN-04
must_haves:
  truths:
    - "services/learning_impact.py exposes two frozen dataclasses (LearningImpactPerRun and LearningImpactRollup) plus two pure read functions (build_per_run_impact and build_rollup) that project already-persisted Phase 1-8 evidence into the compounding-visibility surface; the module has no write side-effects."
    - "build_per_run_impact(conn, *, run_id) returns LearningImpactPerRun{run_id, workflow_key, signals_emitted (tuple of LearningSignalKind from workflow_learning_events.signal_kind for this run), assets_evidenced (tuple of AssetEvidenceDelta — one entry per prompt/skill/workflow/standard/route the run touched, with delta_sample_size, delta_success_count, delta_blocker_count), proposals_created (tuple of ProposalCreated rows linked via improvement_writebacks.run_id), learning_events_persisted (count from workflow_learning_events), no_learning_reason (str | None from the existing _no_learning_reason taxonomy)}."
    - "build_rollup(conn, *, scope, key, since, project_id) returns LearningImpactRollup{scope (Literal['workflow','prompt','skill']), key, since, sample_size, rework_rate_30d (float | None when sample_size < threshold), rework_rate_90d (float | None), success_rate_30d (float | None), blocker_rate_30d (float | None), trend (Literal['improving','flat','regressing','insufficient_data']), rationale (str — cites sample_size always per Pitfall 9)}."
    - "trend is 'insufficient_data' when sample_size < ConservatismPolicy.min_sample_size_for_trend (default 10 per A11); rationale text explicitly includes the sample_size figure (Pitfall 9)."
    - "trend is 'improving' only when rework_rate_30d < rework_rate_90d AND sample_size >= threshold; 'regressing' only when rework_rate_30d > rework_rate_90d AND sample_size >= threshold; 'flat' otherwise (with sample_size sufficient)."
    - "AssetEvidenceDelta records use already-persisted Phase 1-8 surfaces only: prompts_used (for prompt), agentize_evaluations.selected_skills_json (for skill), workflow_execution_reports + success_criteria_findings (for workflow), standards_delta_items (for standard), orchestration_runs.route_result_json (for route)."
  artifacts:
    - path: "services/learning_impact.py"
      provides: "LearningImpactPerRun, LearningImpactRollup, AssetEvidenceDelta, ProposalCreated frozen dataclasses; build_per_run_impact + build_rollup pure read functions; _trend_from_rates helper; _load_signals_for_run + _load_assets_evidenced_for_run private helpers"
      contains: "LearningImpactPerRun|LearningImpactRollup|build_per_run_impact|build_rollup"
    - path: "tests/test_learning_impact.py"
      provides: "per-run payload completeness; rollup trend math (insufficient_data / improving / flat / regressing); rationale always cites sample_size; no_learning_reason passthrough; pure-read assertion"
  key_links:
    - from: "services/learning_impact.py:build_per_run_impact"
      to: "workflow_learning_events.signal_kind (Plan 01) + agentize_evaluations + improvement_writebacks"
      via: "three SELECTs per run_id, joined in Python"
      pattern: "build_per_run_impact"
    - from: "services/learning_impact.py:build_rollup"
      to: "workflow_execution_reports + success_criteria_findings + prompts_used"
      via: "two windowed SELECTs (30d + 90d); rework_rate computed from completed/failed counts"
      pattern: "rework_rate"
    - from: "services/learning_impact.py:_trend_from_rates"
      to: "ConservatismPolicy.min_sample_size_for_trend"
      via: "import from services.learning_taxonomy; default 10 when no policy passed"
      pattern: "min_sample_size_for_trend"
---

<objective>
Ship services/learning_impact.py — the compounding-visibility projection layer that turns already-persisted per-run and per-asset evidence into LearningImpactPerRun (per-run "what did THIS run improve?") and LearningImpactRollup (per-asset "is this getting better?"). Pure reads, no writes, no new persistence.

Purpose: LEARN-04 requires AIOS to make compounding visible by showing what each meaningful run improved. Plan 03 is the backend projection module that Plan 05's CLI subcommands and UI router consume.

Output: services/learning_impact.py (new), tests/test_learning_impact.py (new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-continuous-learning-and-conservative-optimization/09-RESEARCH.md
@.planning/phases/09-continuous-learning-and-conservative-optimization/09-01-PLAN.md

<interfaces>
<!-- Symbols and data sources this plan composes against. -->

From services/learning_taxonomy.py (Plan 01):
- `LearningSignalKind = Literal[...]` — typed-set for signals_emitted tuple.
- `ConservatismPolicy` frozen dataclass with `min_sample_size_for_trend: int` field — Plan 03 imports for the trend threshold (default 10 per A11). Plan 03 does NOT load the policy from JSON (Plan 04 owns that); Plan 03 accepts the threshold as a function argument or uses the dataclass default.

From schema.sql (verified during planning):
- `orchestration_runs(id, status, project_id, workflow_key, route_result_json, briefing_packet_id, created_at, updated_at, ...)` — Plan 03 reads workflow_key + project_id.
- `workflow_learning_events(id, run_id, evidence_type, proposal_target, confidence, approval_state, rationale, source_json, signal_kind, created_at)` — Plan 03 reads run-specific signal_kind values.
- `agentize_evaluations(id, run_id, outcome_quality, tests_passed, follow_up_required, major_repair_required, selected_skills_json, selected_standards_json, recommendation_source, ...)` — Plan 03 reads outcome_quality + selected_*_json to attribute asset evidence per run.
- `improvement_writebacks(id, run_id, layer_type, layer_key, status, impact_scope, requires_approval, created_at, ...)` — Plan 03 reads writebacks linked to the run for proposals_created tuple. Plan 03 ALSO reads metadata_json to detect Plan-04 source="learning_analysis" rows when present (but does not require Plan 04 to be shipped — defensive read).
- `prompts_used(id, run_id, template_id, outcome_score, ...)` — Plan 03 reads to attribute prompt-level evidence per run.
- `workflow_execution_reports(id, run_id, workflow_key, status, report_json, created_at)` — Plan 03 reads status for rollup rework_rate.
- `success_criteria_findings(id, run_id, level, ...)` — Plan 03 reads blocker counts for rollup.

From services/aios_cli.py (verified during planning):
- `_no_learning_reason(run, workflow_payload)` near line 2196 — returns one of `canceled_without_signal`, `failed_before_artifact`, `missing_closeout_summary`, `insufficient_evidence`, `one_off_task`, or None. Plan 03 reuses this signal via `from services.aios_cli import _no_learning_reason`.
  - IMPORTANT: this import is allowed (services → services). If basedpyright complains about the `_`-prefix import, treat it as the existing pattern in the codebase per `grep -n "from services.aios_cli import" services/` — confirm convention via read_first; if not present, duplicate the small reason-classifier as a public `no_learning_reason_for_run` helper in services/aios_cli.py (Plan 05 will surface it; Plan 03 only imports).

Plan 03 has NO write paths; this is a pure projection module.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create services/learning_impact.py with LearningImpactPerRun + build_per_run_impact (covers the "what did THIS run improve?" projection)</name>
  <files>services/learning_impact.py, tests/test_learning_impact.py</files>
  <read_first>
    - .planning/phases/09-continuous-learning-and-conservative-optimization/09-RESEARCH.md — Pattern 4 (Compounding Visibility) lines 563-619; LEARN-04 row in Phase Requirements table.
    - services/learning_taxonomy.py — LearningSignalKind literal + ConservatismPolicy dataclass.
    - services/aios_cli.py — _no_learning_reason near line 2196 (confirm exact signature and return literal set).
    - schema.sql — read columns for workflow_learning_events (post-Plan-01 with signal_kind), agentize_evaluations, improvement_writebacks, orchestration_runs.
    - services/capability_truth.py + services/standards_health.py — paste-of-record for frozen-dataclass + windowed-SELECT style (find via `grep -n "@dataclass(frozen=True)" services/*.py | head -10`).
    - tests/test_standards_health.py and tests/test_capability_truth.py — paste-of-record for in-memory sqlite seed style.
  </read_first>
  <behavior>
    - Test 1 `test_per_run_impact_returns_dataclass`: seed minimal orchestration_runs + workflow_learning_events + agentize_evaluations rows for run_id="r1"; call build_per_run_impact(conn, run_id="r1"); assert returned instance is LearningImpactPerRun, is frozen, has workflow_key matching the seeded value.
    - Test 2 `test_per_run_impact_signals_emitted_from_workflow_learning_events`: seed three workflow_learning_events rows for run_id="r1" with signal_kind values "weak_prompt", "weak_workflow", and NULL (Plan-01 dedupe key extension); assert signals_emitted == ("weak_prompt", "weak_workflow"); NULL signal_kind values are excluded from the tuple.
    - Test 3 `test_per_run_impact_assets_evidenced_includes_workflow`: seed orchestration_runs (workflow_key="implementation-delivery") + one agentize_evaluations row (outcome_quality=4, tests_passed=1, follow_up_required=0) + zero success_criteria_findings; assets_evidenced contains exactly one AssetEvidenceDelta with asset_kind="workflow", asset_key="implementation-delivery", delta_sample_size=1, delta_success_count=1, delta_blocker_count=0.
    - Test 4 `test_per_run_impact_assets_evidenced_includes_prompts`: seed two prompts_used rows for run_id="r1" with template_id="research" and template_id="coding_debug"; both prompts appear in assets_evidenced with asset_kind="prompt".
    - Test 5 `test_per_run_impact_assets_evidenced_includes_skills`: seed agentize_evaluations.selected_skills_json='[{"key":"agentize_intent_compiler"}]' for run_id="r1"; assets_evidenced contains one AssetEvidenceDelta with asset_kind="skill", asset_key="agentize_intent_compiler".
    - Test 6 `test_per_run_impact_proposals_created_links_to_improvement_writebacks`: seed two improvement_writebacks rows linked to run_id="r1" (one status="pending_approval" with requires_approval=1, one status="proposed" with requires_approval=0); proposals_created contains two ProposalCreated entries with the correct writeback_id, signal_kind extracted from metadata (or "compounding_gain" fallback when metadata absent), requires_approval bools matching the rows, status matching.
    - Test 7 `test_per_run_impact_learning_events_persisted_count`: seed five workflow_learning_events rows; learning_events_persisted equals 5.
    - Test 8 `test_per_run_impact_no_learning_reason_passthrough`: seed an orchestration_runs row with status="canceled" and no workflow_learning_events; no_learning_reason returns one of the existing literal values (e.g., "canceled_without_signal"); assert it is a string (not None).
    - Test 9 `test_per_run_impact_run_not_found_returns_none_or_empty_payload`: call build_per_run_impact(conn, run_id="nonexistent"); assert returns either None OR a LearningImpactPerRun with workflow_key=None and empty tuples. Pick one and document.
    - Test 10 `test_per_run_impact_is_pure_read`: snapshot all DB tables before calling build_per_run_impact; call the function; re-snapshot; assert no row counts changed and no new rows appeared anywhere.
  </behavior>
  <action>
    1. **Create services/learning_impact.py** with module docstring, `from __future__ import annotations`, and imports:
       - stdlib: `import json, sqlite3`; `from dataclasses import dataclass, field`; `from datetime import datetime, timedelta, timezone`; `from typing import Any, Literal`.
       - project: `from services.learning_taxonomy import LearningSignalKind, ConservatismPolicy`.

    2. **Add frozen dataclasses** in this order:
       - `AssetEvidenceDelta` with fields `asset_kind: Literal["prompt","skill","workflow","standard","route"]`, `asset_key: str`, `delta_sample_size: int`, `delta_success_count: int`, `delta_blocker_count: int`.
       - `ProposalCreated` with fields `writeback_id: str`, `signal_kind: LearningSignalKind | None`, `requires_approval: bool`, `status: str`.
       - `LearningImpactPerRun` with fields `run_id: str`, `workflow_key: str | None`, `signals_emitted: tuple[LearningSignalKind, ...]`, `assets_evidenced: tuple[AssetEvidenceDelta, ...]`, `proposals_created: tuple[ProposalCreated, ...]`, `learning_events_persisted: int`, `no_learning_reason: str | None`.

    3. **Add `build_per_run_impact(conn, *, run_id) -> LearningImpactPerRun | None`:**
       - Query 1: `SELECT id, workflow_key, status, project_id FROM orchestration_runs WHERE id = ?` — if no row, return None per Test 9.
       - Query 2: `SELECT signal_kind FROM workflow_learning_events WHERE run_id = ? AND signal_kind IS NOT NULL` — collect into tuple, deduplicate while preserving order, validate each against LearningSignalKind by checking membership in the LEARNING_SIGNAL_KINDS tuple (import from services.learning_taxonomy); drop unknown values with a warning.
       - Query 3: `SELECT COUNT(*) FROM workflow_learning_events WHERE run_id = ?` — learning_events_persisted.
       - Query 4: build assets_evidenced via `_load_assets_evidenced_for_run(conn, run_id)` helper that:
         - Loads prompts_used rows for run_id → one AssetEvidenceDelta per template_id with delta_sample_size=1, delta_success_count from outcome_score >= 0.5, delta_blocker_count=0.
         - Loads agentize_evaluations row for run_id (most recent if multiple): for each entry in selected_skills_json → one AssetEvidenceDelta(asset_kind="skill"); the workflow itself contributes one AssetEvidenceDelta(asset_kind="workflow", asset_key=orchestration_runs.workflow_key) with success/blocker counts derived from agentize_evaluations + success_criteria_findings.
         - Loads selected_standards_json → one AssetEvidenceDelta per standard_id.
         - Loads orchestration_runs.route_result_json → one AssetEvidenceDelta(asset_kind="route", asset_key=workflow_key) if route_result_json present.
         - Returns the tuple (order: workflow first, then prompts, skills, standards, route).
       - Query 5: build proposals_created via `_load_proposals_for_run(conn, run_id)`:
         - SELECT id, status, requires_approval, proposed_change_json FROM improvement_writebacks WHERE run_id = ?
         - For each row, parse proposed_change_json (or metadata_json — confirm exact column from Plan 01 read_first) to extract signal_kind. If absent or unparseable, signal_kind=None.
         - Return tuple of ProposalCreated.
       - no_learning_reason: import `_no_learning_reason` from services.aios_cli (or the public alias if the underscore-prefix import is rejected). Build the small `workflow_payload` dict it expects (per services/aios_cli.py:2196 read_first) from the run row and signals_emitted; return what the helper returns.
       - Construct and return LearningImpactPerRun.

    4. **Create tests/test_learning_impact.py** with the ten behavior tests above. Use sqlite3.connect(":memory:") and seed each table inline. For Test 10 (pure-read), use `SELECT name FROM sqlite_master WHERE type='table'` + `SELECT COUNT(*) FROM each_table` before and after to snapshot.

    5. **Quality ladder per touched file:** `uv run ruff check services/learning_impact.py tests/test_learning_impact.py && uv run ruff format --check services/learning_impact.py tests/test_learning_impact.py && uv run basedpyright services/learning_impact.py`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_learning_impact.py -x -q -k "per_run_impact"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class LearningImpactPerRun\|class AssetEvidenceDelta\|class ProposalCreated\|def build_per_run_impact" services/learning_impact.py | wc -l | tr -d ' '` returns at least 4
    - `grep -n "from services.learning_taxonomy import" services/learning_impact.py | wc -l | tr -d ' '` returns at least 1
    - `grep -n "FROM orchestration_runs\|FROM workflow_learning_events\|FROM agentize_evaluations\|FROM improvement_writebacks\|FROM prompts_used" services/learning_impact.py | wc -l | tr -d ' '` returns at least 5
    - `grep -n "INSERT\|UPDATE\|DELETE" services/learning_impact.py | grep -v "FROM\|--\|#" | wc -l | tr -d ' '` returns 0 (pure read module — no writes)
    - `grep -c "def test_per_run_impact_returns_dataclass\|def test_per_run_impact_signals_emitted_from_workflow_learning_events\|def test_per_run_impact_assets_evidenced_includes_workflow\|def test_per_run_impact_assets_evidenced_includes_prompts\|def test_per_run_impact_assets_evidenced_includes_skills\|def test_per_run_impact_proposals_created_links_to_improvement_writebacks\|def test_per_run_impact_learning_events_persisted_count\|def test_per_run_impact_no_learning_reason_passthrough\|def test_per_run_impact_run_not_found_returns_none_or_empty_payload\|def test_per_run_impact_is_pure_read" tests/test_learning_impact.py | grep -v '^#'` returns at least 10
    - `uv run pytest tests/test_learning_impact.py -x -q -k per_run_impact` exits 0
    - `uv run pytest tests/test_architecture_enforcement.py -x -q` exits 0
    - `uv run ruff check services/learning_impact.py tests/test_learning_impact.py` exits 0
    - `uv run ruff format --check services/learning_impact.py tests/test_learning_impact.py` exits 0
    - `uv run basedpyright services/learning_impact.py` exits 0
  </acceptance_criteria>
  <done>
    services/learning_impact.py exposes LearningImpactPerRun + AssetEvidenceDelta + ProposalCreated dataclasses and build_per_run_impact pure read function; ten behavior tests pass; module is verified pure-read by snapshot diff test.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add LearningImpactRollup + build_rollup + _trend_from_rates (covers the "is this asset getting better?" projection)</name>
  <files>services/learning_impact.py, tests/test_learning_impact.py</files>
  <read_first>
    - services/learning_impact.py (post-Task-1) — confirm module imports, dataclass style, helper conventions.
    - services/learning_taxonomy.py — ConservatismPolicy.min_sample_size_for_trend field.
    - .planning/phases/09-continuous-learning-and-conservative-optimization/09-RESEARCH.md — Pattern 4 (LearningImpactRollup lines 596-619); Pitfall 9 ("Improving" trend on tiny sample_size); A11 (default threshold 10).
    - services/standards_health.py — find via `grep -n "def _trend\|trend = " services/standards_health.py | head -10` for the project convention for trend computation (may exist; reuse style if so).
  </read_first>
  <behavior>
    - Test 1 `test_rollup_returns_dataclass`: seed workflow_execution_reports + success_criteria_findings for workflow_key="implementation-delivery"; call build_rollup(conn, scope="workflow", key="implementation-delivery", since="2026-04-21T00:00:00Z", project_id=None); returned instance is LearningImpactRollup and is frozen.
    - Test 2 `test_rollup_trend_insufficient_data` (Pitfall 9): seed only 3 workflow_execution_reports rows (sample_size < threshold 10); build_rollup with min_sample_size_for_trend=10 returns trend="insufficient_data"; rationale text contains "sample_size=3" (literal numeric).
    - Test 3 `test_rollup_trend_improving_requires_sample_floor`: seed 6 reports in the older 30-90d window (4 failed, 2 completed, rework_rate ≈ 0.67) and 6 reports in the recent 30d window (1 failed, 5 completed, rework_rate ≈ 0.17); both windows have sample_size=6 < 10 threshold → trend="insufficient_data". Repeat with 15 reports each window → trend="improving"; rationale mentions "rework_rate dropped" with both rates and sample_size.
    - Test 4 `test_rollup_trend_regressing`: seed 12 reports older window (rework_rate 0.1) and 12 reports recent window (rework_rate 0.5); trend="regressing"; rationale includes both rates and the sample_size.
    - Test 5 `test_rollup_trend_flat`: seed 15 reports each window with the same rework_rate (e.g., 0.3 both); trend="flat".
    - Test 6 `test_rollup_rationale_always_cites_sample_size` (Pitfall 9): for any trend value, the rationale string contains the substring `f"sample_size={sample_size}"` (or equivalent — confirm exact format and assert).
    - Test 7 `test_rollup_scope_prompt`: seed prompts_used + agentize_evaluations rows; call build_rollup(scope="prompt", key="research", since=...); rollup populates from prompt-evaluation evidence (mean outcome_score → success_rate_30d; absence of explicit blocker_rate for prompts → blocker_rate_30d is None or 0.0; document and assert).
    - Test 8 `test_rollup_scope_skill`: call build_rollup(scope="skill", key="agentize_intent_compiler", since=...); rollup populates from agentize_evaluations.selected_skills_json + outcome_quality.
    - Test 9 `test_rollup_per_project_filter`: seed 10 reports for project p1 and 10 for project p2 of the same workflow; build_rollup with project_id="p1" returns sample_size=10 from p1 only.
    - Test 10 `test_rollup_is_pure_read`: same snapshot-diff approach as Task 1 Test 10.
    - Test 11 `test_trend_from_rates_helper_unit`: _trend_from_rates(recent_rate=0.2, prior_rate=0.5, sample_size=12, min_sample_size_for_trend=10) returns "improving"; with sample_size=8 returns "insufficient_data"; with recent=0.5 prior=0.2 returns "regressing"; with recent=0.3 prior=0.3 returns "flat".
  </behavior>
  <action>
    1. **Add `LearningImpactRollup` frozen dataclass** with fields:
       - `scope: Literal["workflow","prompt","skill"]`
       - `key: str`
       - `since: str`
       - `sample_size: int`
       - `rework_rate_30d: float | None`
       - `rework_rate_90d: float | None`
       - `success_rate_30d: float | None`
       - `blocker_rate_30d: float | None`
       - `trend: Literal["improving","flat","regressing","insufficient_data"]`
       - `rationale: str`
       - `project_id: str | None`

    2. **Add `_trend_from_rates(*, recent_rate, prior_rate, sample_size, min_sample_size_for_trend=10) -> tuple[Literal[...], str]`:**
       - If sample_size < min_sample_size_for_trend → returns ("insufficient_data", f"sample_size={sample_size} below trend threshold {min_sample_size_for_trend}; rate {recent_rate:.2f} not reliable.").
       - Else if recent_rate < prior_rate (with a small epsilon, e.g., 0.05) → returns ("improving", f"rework_rate dropped {prior_rate:.2f} → {recent_rate:.2f} over sample_size={sample_size}").
       - Else if recent_rate > prior_rate + epsilon → returns ("regressing", f"rework_rate rose {prior_rate:.2f} → {recent_rate:.2f} over sample_size={sample_size}").
       - Else → returns ("flat", f"rework_rate stable at {recent_rate:.2f} over sample_size={sample_size}").
       - Module constant: `_TREND_EPSILON = 0.05`.

    3. **Add `build_rollup(conn, *, scope, key, since, project_id=None, min_sample_size_for_trend=10) -> LearningImpactRollup`:**
       - Compute the recent-30d cutoff and prior-30-to-90d cutoff from `since` (use datetime.fromisoformat parsing; if since is in "Nd" relative form, reuse the helper or parse inline).
       - Dispatch by scope:
         - scope="workflow": SELECT status FROM workflow_execution_reports WHERE workflow_key=? AND created_at >= ? AND (project filter via JOIN orchestration_runs); compute rework_rate = failed/total for each window.
         - scope="prompt": SELECT outcome_score FROM prompts_used WHERE template_id=? AND created_at >= ? AND (project filter via JOIN); compute success_rate = mean outcome_score; rework_rate proxy = 1.0 - success_rate.
         - scope="skill": SELECT outcome_quality, selected_skills_json FROM agentize_evaluations WHERE created_at >= ? AND (project filter); filter rows where selected_skills_json contains the skill key (JSON LIKE or in-Python filter); rework_rate proxy = (count outcome_quality < 3) / total. Document the proxy semantics in the docstring.
       - Compute sample_size from the recent-30d window only.
       - Call _trend_from_rates with the two rates and sample_size.
       - blocker_rate_30d: for scope="workflow" only, JOIN success_criteria_findings and count level='blocker'; for prompt/skill, return None or 0.0 per Test 7.
       - Return the dataclass; rationale is the trend rationale string from _trend_from_rates.

    4. **Add the eleven behavior tests** for Task 2.

    5. **Quality ladder per touched file:** `uv run ruff check services/learning_impact.py tests/test_learning_impact.py && uv run ruff format --check services/learning_impact.py tests/test_learning_impact.py && uv run basedpyright services/learning_impact.py`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_learning_impact.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class LearningImpactRollup\|def build_rollup\|def _trend_from_rates" services/learning_impact.py | wc -l | tr -d ' '` returns at least 3
    - `grep -n "_TREND_EPSILON" services/learning_impact.py | wc -l | tr -d ' '` returns at least 1
    - `grep -n "insufficient_data" services/learning_impact.py | wc -l | tr -d ' '` returns at least 1 (Pitfall 9 trend value present)
    - `grep -n "sample_size=" services/learning_impact.py | wc -l | tr -d ' '` returns at least 3 (rationale always cites sample_size per Pitfall 9 — appears in each trend branch)
    - `grep -n "INSERT\|UPDATE\|DELETE" services/learning_impact.py | grep -v "FROM\|--\|#" | wc -l | tr -d ' '` returns 0 (still pure read after Task 2)
    - `grep -c "def test_rollup_returns_dataclass\|def test_rollup_trend_insufficient_data\|def test_rollup_trend_improving_requires_sample_floor\|def test_rollup_trend_regressing\|def test_rollup_trend_flat\|def test_rollup_rationale_always_cites_sample_size\|def test_rollup_scope_prompt\|def test_rollup_scope_skill\|def test_rollup_per_project_filter\|def test_rollup_is_pure_read\|def test_trend_from_rates_helper_unit" tests/test_learning_impact.py | grep -v '^#'` returns at least 11
    - `uv run pytest tests/test_learning_impact.py -x -q` exits 0
    - `uv run pytest tests/test_architecture_enforcement.py -x -q` exits 0
    - `uv run ruff check services/learning_impact.py tests/test_learning_impact.py` exits 0
    - `uv run ruff format --check services/learning_impact.py tests/test_learning_impact.py` exits 0
    - `uv run basedpyright services/learning_impact.py` exits 0
    - `python -c "from services.learning_impact import LearningImpactPerRun, LearningImpactRollup, AssetEvidenceDelta, ProposalCreated, build_per_run_impact, build_rollup, _trend_from_rates"` exits 0
  </acceptance_criteria>
  <done>
    services/learning_impact.py also exposes LearningImpactRollup + build_rollup + _trend_from_rates; trend values are insufficient_data | improving | regressing | flat; rationale always cites sample_size (Pitfall 9); workflow / prompt / skill scopes each have a dedicated dispatch; eleven tests added on top of Task 1's ten; module still pure read.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| services/learning_impact.py -> per-run + per-asset evidence tables | Read-only; same Pattern-Detection-Poisoning concern as Plan 02; mitigated by reuse of managed-hook-written evidence only. |
| LearningImpactRollup.trend label -> operator UI | A misleading "improving" trend on a tiny sample misinforms the operator (Pitfall 9). Mitigated by insufficient_data threshold + rationale cites sample_size always. |
| LearningImpactPerRun.proposals_created -> improvement_writebacks join | The join uses improvement_writebacks.run_id; pre-Plan-04 there are no source="learning_analysis" rows. Plan 03 must defensively handle the absence — the proposals_created tuple is allowed to be empty. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Information Disclosure | LearningImpactRollup trend="improving" on tiny sample misleads the operator (Pitfall 9) | mitigate | Task 2 Tests 2, 6 + the _trend_from_rates insufficient_data branch + rationale always cites sample_size. |
| T-09-14 | Tampering | LearningImpactPerRun silently writes rows during projection | mitigate | Task 1 Test 10 + Task 2 Test 10 (snapshot-diff) + grep acceptance: zero INSERT/UPDATE/DELETE statements. |
| T-09-15 | Repudiation | Proposals_created tuple cites the wrong writeback row (wrong run_id) | mitigate | The SELECT filter is `WHERE run_id = ?` explicitly; Test 6 in Task 1 verifies the linkage with two seeded rows. |
| T-09-16 | Information Disclosure | signal_kind values not in LearningSignalKind literal slip through into signals_emitted tuple | mitigate | Task 1 Test 2 filters by membership in LEARNING_SIGNAL_KINDS; unknown values dropped. |
</threat_model>

<verification>
- `uv run pytest tests/test_learning_impact.py tests/test_architecture_enforcement.py -x -q` exits 0
- `uv run ruff check services/learning_impact.py tests/test_learning_impact.py` exits 0
- `uv run ruff format --check services/learning_impact.py tests/test_learning_impact.py` exits 0
- `uv run basedpyright services/learning_impact.py` exits 0
- `python -c "from services.learning_impact import LearningImpactPerRun, LearningImpactRollup, AssetEvidenceDelta, ProposalCreated, build_per_run_impact, build_rollup, _trend_from_rates"` exits 0
- Quality ladder per CLAUDE.md: hard gates green.
</verification>

<success_criteria>
- LEARN-04 (backend): services/learning_impact.py exposes build_per_run_impact and build_rollup; both pure-read. Plan 05 will surface them via CLI and UI router.
- Pitfall 9 closed: trend="insufficient_data" when sample_size < threshold; rationale always cites sample_size.
- Module is provably pure-read (zero INSERT/UPDATE/DELETE statements, snapshot-diff tests).
- Reuses Phase 1-8 evidence surfaces only; does not introduce new persistence.
</success_criteria>

<output>
After completion, create `.planning/phases/09-continuous-learning-and-conservative-optimization/09-03-SUMMARY.md` per the template, including:
- The four frozen dataclasses (LearningImpactPerRun, LearningImpactRollup, AssetEvidenceDelta, ProposalCreated) with field sets
- The exact SQL each scope-branch of build_rollup runs (workflow vs prompt vs skill)
- The _trend_from_rates decision tree (insufficient_data → improving → regressing → flat)
- The proof that the module is pure read (zero INSERT/UPDATE/DELETE; snapshot-diff test)
- Note that Plan 05 will surface build_per_run_impact + build_rollup via aios CLI subcommands (learning-impact --run-id / --workflow / --prompt / --skill) and via aios-ui/server/aios/learning.ts new exports
- Note that Plan 04 will populate improvement_writebacks rows with metadata source="learning_analysis", which then flow back into the proposals_created tuple naturally
</output>
</content>
</invoke>