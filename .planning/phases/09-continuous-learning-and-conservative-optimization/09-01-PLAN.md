---
phase: 09-continuous-learning-and-conservative-optimization
plan: "01"
type: execute
wave: 1
autonomous: true
depends_on: []
files_modified:
  - services/learning_taxonomy.py
  - bin/aios_orchestration_runtime.py
  - services/aios_cli.py
  - tests/test_learning_taxonomy.py
  - tests/test_orchestration_runtime.py
  - tests/test_aios_cli.py
requirements:
  - LEARN-01
must_haves:
  truths:
    - "services/learning_taxonomy.py exports LearningSignalKind = Literal[\"repeated_failure\",\"ignored_rule\",\"bloated_packet\",\"weak_prompt\",\"weak_workflow\",\"route_misroute\",\"standards_regression\",\"writeback_adopted\",\"writeback_rejected\",\"compounding_gain\"] plus SIGNAL_TO_IMPACT_SCOPE map plus IS_ACTIONABLE_SIGNAL set plus ConservatismPolicy frozen dataclass; this module is the single source of truth imported by services/learning_analysis.py, services/conservative_optimizer.py, and services/learning_impact.py in later plans."
    - "workflow_learning_events table carries a new TEXT nullable signal_kind column ensured by an idempotent _ensure_workflow_learning_signal_kind_column helper in bin/aios_orchestration_runtime.py; pre-migration rows remain visible with NULL signal_kind; the helper runs from ensure_runtime_schema and is re-callable safely (idempotent)."
    - "bin/aios_orchestration_runtime.py writeback_approval_policy recognizes two new impact_scope values route-default and packet-default and returns requires_approval=True with policy_class='route-default_change' / 'packet-default_change' for each, matching the existing prompt-default/skill-default/workflow-default discipline (Pitfall 7 closed)."
    - "services/aios_cli.py _record_workflow_learning_event accepts an optional signal_kind keyword argument (LearningSignalKind | None), persists it into the new column, and dedupes on (run_id, evidence_type, proposal_target, signal_kind) per A9; existing call sites continue to pass no signal_kind and still write rows."
    - "_ensure_workflow_learning_schema in services/aios_cli.py also ensures the signal_kind column so CLI-only invocations that bootstrap the table do not diverge from the bin/ runtime bootstrap."
  artifacts:
    - path: "services/learning_taxonomy.py"
      provides: "LearningSignalKind literal; SIGNAL_TO_IMPACT_SCOPE dict[LearningSignalKind, str]; IS_ACTIONABLE_SIGNAL frozenset[LearningSignalKind]; ConservatismPolicy frozen dataclass; impact_scope_for_signal(kind) helper"
      contains: "LearningSignalKind"
    - path: "bin/aios_orchestration_runtime.py"
      provides: "writeback_approval_policy extended high_impact_scopes set to include 'route-default' and 'packet-default'; _ensure_workflow_learning_signal_kind_column(conn) idempotent helper; ensure_runtime_schema invokes the helper"
    - path: "services/aios_cli.py"
      provides: "_record_workflow_learning_event extended with signal_kind kwarg; INSERT column list includes signal_kind; _workflow_learning_event_exists dedupe includes signal_kind per A9; _ensure_workflow_learning_schema ensures signal_kind column"
    - path: "tests/test_learning_taxonomy.py"
      provides: "literal completeness, impact_scope mapping coverage, IS_ACTIONABLE_SIGNAL correctness, ConservatismPolicy dataclass construction"
    - path: "tests/test_orchestration_runtime.py"
      provides: "test_signal_kind_column_added_idempotently; test_writeback_policy_route_default_requires_approval; test_writeback_policy_packet_default_requires_approval"
    - path: "tests/test_aios_cli.py"
      provides: "test_record_learning_event_persists_signal_kind; test_workflow_learning_event_dedupe_respects_signal_kind; test_workflow_learning_payload_surfaces_signal_kind"
  key_links:
    - from: "services/learning_taxonomy.py:SIGNAL_TO_IMPACT_SCOPE"
      to: "bin/aios_orchestration_runtime.py:writeback_approval_policy"
      via: "downstream Plan 04 will call writeback_approval_policy with the impact_scope values from this map; the two route-default + packet-default scopes added here are the load-bearing additions"
      pattern: "route-default|packet-default"
    - from: "services/aios_cli.py:_record_workflow_learning_event"
      to: "workflow_learning_events.signal_kind column"
      via: "INSERT statement augmented with new column; optional kwarg drives column value (NULL when omitted)"
      pattern: "signal_kind"
    - from: "bin/aios_orchestration_runtime.py:_ensure_workflow_learning_signal_kind_column"
      to: "ensure_runtime_schema"
      via: "called from ensure_runtime_schema for every connection so existing databases pick up the column on first runtime touch"
      pattern: "_ensure_workflow_learning_signal_kind_column"
---

<objective>
Land the Phase 9 foundation: the LearningSignalKind taxonomy and ConservatismPolicy dataclass in services/learning_taxonomy.py; the idempotent signal_kind column migration on workflow_learning_events; the two new high-impact scopes (route-default, packet-default) on writeback_approval_policy; and the _record_workflow_learning_event signal_kind kwarg + dedupe-key extension. Every other Phase 9 plan imports from these surfaces, so this plan ships first and alone in Wave 1.

Purpose: LEARN-01 requires capturing run evidence in a form that can inform future improvements; Phase 9 RESEARCH layer (a) is the signal taxonomy. Without the taxonomy literal and the column, downstream detectors (Plan 02), optimizer (Plan 04), and impact projections (Plan 03) have nothing to share. Pitfall 7 (new impact scopes missing from Phase 5 policy) must be closed here so Plan 04 cannot accidentally emit non-gated route or packet proposals.

Output: services/learning_taxonomy.py (new), bin/aios_orchestration_runtime.py extension (column migration + scope additions), services/aios_cli.py extension (signal_kind kwarg + ensure-column path), three test files (one new, two extended).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-continuous-learning-and-conservative-optimization/09-RESEARCH.md
@.planning/phases/05-governed-writeback-and-approval-control/05-CONTEXT.md

<interfaces>
<!-- Phase 5 surfaces this plan extends. -->

From bin/aios_orchestration_runtime.py (verified during planning):
- `writeback_approval_policy(*, layer_type, impact_scope, proposed_change, requires_approval=False, approval_reason=None) -> dict[str, Any]` at line 816.
  - Current high_impact_scopes set (line 828): `{"global", "project-truth", "workflow-default", "prompt-default", "skill-default"}` — Plan 01 ADDS `"route-default"` and `"packet-default"`.
  - Current high_impact_layers set (line 827): `{"truth", "standard", "standards", "prompt", "skill", "workflow", "packet"}` — Plan 01 ADDS `"route"`.
  - Returns `{policy_class, requires_approval, reason}`.
- `insert_writeback(conn, ...)` at line 854 — Plan 04 will reuse; Plan 01 does not modify.
- `workflow_learning_events` CREATE TABLE at lines 189-209 — current columns: `id, run_id, evidence_type, proposal_target, confidence, approval_state, rationale, source_json, created_at`; Plan 01 ADDS `signal_kind TEXT` via idempotent ALTER (NOT in CREATE TABLE — keep CREATE TABLE unchanged so first-time bootstrap stays in sync, and add the column via _ensure_workflow_learning_signal_kind_column called from ensure_runtime_schema for back-compat).
- `ensure_runtime_schema(conn)` is the runtime bootstrap hook called by hooks and CLI; Plan 01 hooks the new column ensure here.

From services/aios_cli.py (verified during planning):
- `WORKFLOW_LEARNING_EVIDENCE_TYPES = ("workflow_evidence", "prompt_template_evidence", "standards_health_evidence", "bug_quality_evidence", "no_learning_signal")` near line 147.
- `_ensure_workflow_learning_schema(conn)` at lines 2111-2132 — re-ensures the table for CLI-only flows; Plan 01 calls the new bin helper from here (or duplicates the same idempotent ALTER inline) so CLI-bootstrapped DBs gain the column too.
- `_workflow_learning_event_exists(conn, *, run_id, evidence_type, proposal_target) -> bool` near line 2148 — Plan 01 extends to include `signal_kind` so different signal_kinds for the same (run_id, evidence_type, proposal_target) are persisted as distinct rows per A9.
- `_record_workflow_learning_event(conn, *, run_id, evidence_type, proposal_target, confidence, approval_state, rationale, source) -> None` at lines 2156-2193 — Plan 01 adds optional `signal_kind: str | None = None` kwarg; INSERT column list grows to include signal_kind; dedupe key updated.
- `_workflow_learning_payload(conn) -> dict[str, Any]` at lines 2208-2377 — Plan 01 does NOT modify the payload shape; Plan 05 will extend with recurring_patterns and conservative_proposals.

From schema.sql (verified during planning):
- `workflow_learning_events` lines 499-510: nine columns enumerated above. The ALTER TABLE in Plan 01 is additive only; old rows keep NULL signal_kind permanently (no backfill).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create services/learning_taxonomy.py with LearningSignalKind, SIGNAL_TO_IMPACT_SCOPE, IS_ACTIONABLE_SIGNAL, ConservatismPolicy</name>
  <files>services/learning_taxonomy.py, tests/test_learning_taxonomy.py</files>
  <read_first>
    - .planning/phases/09-continuous-learning-and-conservative-optimization/09-RESEARCH.md — Pattern 1 (Learning Signal Taxonomy) lines 338-386 and Pitfall 4 (Pattern Proposals Bypass requires_approval). These are the paste-of-record for the literal members, the SIGNAL_TO_IMPACT_SCOPE map, and the informational-vs-actionable split.
    - services/agent_rules.py — find via `grep -n "Literal\|frozenset\|@dataclass" services/agent_rules.py | head -20` and read the small module to confirm the existing convention for Literal + frozenset + frozen dataclass declarations in services/ (paste-of-record for style).
    - services/capability_truth.py — find via `grep -n "Provenance = Literal\|@dataclass(frozen=True)" services/capability_truth.py | head -10` to confirm the project convention for Literal + frozen dataclass.
    - tests/test_capability_truth.py — find via `head -60 tests/test_capability_truth.py` to confirm the existing test style for literal completeness.
  </read_first>
  <behavior>
    - Test 1 `test_learning_signal_kind_literal_members`: import LEARNING_SIGNAL_KINDS (a module-level tuple mirroring the Literal values), assert it equals exactly the ten values: "repeated_failure", "ignored_rule", "bloated_packet", "weak_prompt", "weak_workflow", "route_misroute", "standards_regression", "writeback_adopted", "writeback_rejected", "compounding_gain".
    - Test 2 `test_signal_to_impact_scope_covers_every_kind`: assert SIGNAL_TO_IMPACT_SCOPE has a key for every member of LEARNING_SIGNAL_KINDS; assert SIGNAL_TO_IMPACT_SCOPE["repeated_failure"] == "workflow-default", ["ignored_rule"] == "standards-default", ["bloated_packet"] == "packet-default", ["weak_prompt"] == "prompt-default", ["weak_workflow"] == "workflow-default", ["route_misroute"] == "route-default", ["standards_regression"] == "standards-default", and ["writeback_adopted"], ["writeback_rejected"], ["compounding_gain"] each == "scoped".
    - Test 3 `test_is_actionable_signal_excludes_informational`: assert IS_ACTIONABLE_SIGNAL is a frozenset; assert "repeated_failure" in IS_ACTIONABLE_SIGNAL, "ignored_rule" in IS_ACTIONABLE_SIGNAL, "bloated_packet" in IS_ACTIONABLE_SIGNAL, "weak_prompt" in IS_ACTIONABLE_SIGNAL, "weak_workflow" in IS_ACTIONABLE_SIGNAL, "route_misroute" in IS_ACTIONABLE_SIGNAL, "standards_regression" in IS_ACTIONABLE_SIGNAL; assert "writeback_adopted" NOT in IS_ACTIONABLE_SIGNAL, "writeback_rejected" NOT in IS_ACTIONABLE_SIGNAL, "compounding_gain" NOT in IS_ACTIONABLE_SIGNAL (Pitfall 4).
    - Test 4 `test_impact_scope_for_signal_helper`: impact_scope_for_signal("repeated_failure") returns "workflow-default"; impact_scope_for_signal("route_misroute") returns "route-default"; impact_scope_for_signal with an unknown string raises KeyError or ValueError (decide one and document in docstring; tests assert that decision).
    - Test 5 `test_conservatism_policy_dataclass_is_frozen`: instantiate ConservatismPolicy(min_sample_size=5, min_recurrence_count=3, min_confidence=0.6, cooling_period_days=14, min_sample_size_for_trend=10, per_signal_overrides={}); assert dataclasses.is_dataclass(instance) and the instance is frozen (attempting to set an attribute raises FrozenInstanceError or dataclasses.FrozenInstanceError).
    - Test 6 `test_conservatism_policy_per_signal_overrides_typed`: instance.per_signal_overrides is a dict; instance.per_signal_overrides.get("weak_prompt", {}).get("min_sample_size", instance.min_sample_size) returns the override value if present or the policy default otherwise (the helper logic that Plan 04 will rely on is exercised at the dataclass-construction level).
  </behavior>
  <action>
    1. **Create services/learning_taxonomy.py** with:
       - Module docstring noting this is the Phase 9 single source of truth for the LearningSignalKind axis and conservatism policy.
       - `from __future__ import annotations` at top.
       - `from dataclasses import dataclass, field` and `from typing import Literal` imports.
       - `LearningSignalKind = Literal["repeated_failure","ignored_rule","bloated_packet","weak_prompt","weak_workflow","route_misroute","standards_regression","writeback_adopted","writeback_rejected","compounding_gain"]`.
       - `LEARNING_SIGNAL_KINDS: tuple[LearningSignalKind, ...]` populated with the same ten values in the same order.
       - `SIGNAL_TO_IMPACT_SCOPE: dict[LearningSignalKind, str]` per RESEARCH Pattern 1 lines 364-376.
       - `IS_ACTIONABLE_SIGNAL: frozenset[LearningSignalKind]` containing only the seven actionable signals (Pitfall 4).
       - `def impact_scope_for_signal(kind: LearningSignalKind) -> str` returning `SIGNAL_TO_IMPACT_SCOPE[kind]`; raises KeyError on unknown.
       - `@dataclass(frozen=True)` `ConservatismPolicy` with fields: `min_sample_size: int`, `min_recurrence_count: int`, `min_confidence: float`, `cooling_period_days: int`, `min_sample_size_for_trend: int`, `per_signal_overrides: dict[str, dict[str, float | int]] = field(default_factory=dict)`.
       - Do not import from `bin/` or any module outside `services/` and stdlib — this is the foundation module.

    2. **Create tests/test_learning_taxonomy.py** with the six behavior tests above. Use the in-tree project style (per `tests/test_capability_truth.py` paste-of-record from read_first).

    3. **Quality ladder per touched file:** `uv run ruff check services/learning_taxonomy.py tests/test_learning_taxonomy.py && uv run ruff format --check services/learning_taxonomy.py tests/test_learning_taxonomy.py && uv run basedpyright services/learning_taxonomy.py`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_learning_taxonomy.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^LearningSignalKind = Literal\|^LEARNING_SIGNAL_KINDS\|^SIGNAL_TO_IMPACT_SCOPE\|^IS_ACTIONABLE_SIGNAL\|^def impact_scope_for_signal\|^class ConservatismPolicy" services/learning_taxonomy.py | wc -l | tr -d ' '` returns at least 6
    - `python -c "from services.learning_taxonomy import LEARNING_SIGNAL_KINDS; assert len(LEARNING_SIGNAL_KINDS) == 10"` exits 0
    - `python -c "from services.learning_taxonomy import SIGNAL_TO_IMPACT_SCOPE, LEARNING_SIGNAL_KINDS; assert set(SIGNAL_TO_IMPACT_SCOPE.keys()) == set(LEARNING_SIGNAL_KINDS)"` exits 0
    - `python -c "from services.learning_taxonomy import IS_ACTIONABLE_SIGNAL; assert 'writeback_adopted' not in IS_ACTIONABLE_SIGNAL and 'repeated_failure' in IS_ACTIONABLE_SIGNAL"` exits 0
    - `grep -c "def test_learning_signal_kind_literal_members\|def test_signal_to_impact_scope_covers_every_kind\|def test_is_actionable_signal_excludes_informational\|def test_impact_scope_for_signal_helper\|def test_conservatism_policy_dataclass_is_frozen\|def test_conservatism_policy_per_signal_overrides_typed" tests/test_learning_taxonomy.py | grep -v '^#'` returns at least 6
    - `uv run pytest tests/test_learning_taxonomy.py -x -q` exits 0
    - `uv run ruff check services/learning_taxonomy.py tests/test_learning_taxonomy.py` exits 0
    - `uv run ruff format --check services/learning_taxonomy.py tests/test_learning_taxonomy.py` exits 0
    - `uv run basedpyright services/learning_taxonomy.py` exits 0
    - `uv run pytest tests/test_architecture_enforcement.py -x -q` exits 0 (no services -> bin import introduced)
  </acceptance_criteria>
  <done>
    services/learning_taxonomy.py exists with LearningSignalKind literal, LEARNING_SIGNAL_KINDS tuple, SIGNAL_TO_IMPACT_SCOPE, IS_ACTIONABLE_SIGNAL, impact_scope_for_signal helper, and ConservatismPolicy frozen dataclass. Six behavior tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend bin/aios_orchestration_runtime.py with signal_kind column migration and route-default + packet-default impact scopes</name>
  <files>bin/aios_orchestration_runtime.py, tests/test_orchestration_runtime.py</files>
  <read_first>
    - bin/aios_orchestration_runtime.py — read writeback_approval_policy at lines 816-851 and the workflow_learning_events CREATE TABLE at lines 189-209 (verified during planning). Read ensure_runtime_schema (find via `grep -n "def ensure_runtime_schema\|ensure_column" bin/aios_orchestration_runtime.py | head -20`) to confirm where to call the new column-ensure helper and to mirror the existing ensure_column idempotent style.
    - tests/test_orchestration_runtime.py — find via `head -80 tests/test_orchestration_runtime.py` to confirm the in-memory sqlite test fixture pattern used for runtime schema tests. If the file does not exist, create it; otherwise extend.
    - .planning/phases/09-continuous-learning-and-conservative-optimization/09-RESEARCH.md — Pattern 5 (Idempotent Schema Migration), Pitfall 2 (signal_kind migration on existing rows), Pitfall 7 (new impact scopes missing from Phase 5 policy).
    - schema.sql — find via `grep -n "workflow_learning_events" schema.sql` and read the columns block (lines 499-510 per planning verification).
  </read_first>
  <behavior>
    - Test 1 `test_signal_kind_column_added_idempotently`: open an in-memory sqlite; CREATE TABLE workflow_learning_events with the legacy nine columns (no signal_kind); call `_ensure_workflow_learning_signal_kind_column(conn)`; PRAGMA table_info shows signal_kind present; call the helper again; no error; PRAGMA shows signal_kind still present exactly once. Confirm an idx_workflow_learning_events_signal index also exists.
    - Test 2 `test_signal_kind_column_left_alone_when_present`: pre-create the table WITH signal_kind already; call `_ensure_workflow_learning_signal_kind_column(conn)`; assert no ALTER ran (use sqlite_master to assert no error and column count unchanged).
    - Test 3 `test_ensure_runtime_schema_invokes_signal_kind_helper`: call ensure_runtime_schema(conn) on a fresh in-memory db; PRAGMA table_info(workflow_learning_events) shows signal_kind column present (this proves the wiring from ensure_runtime_schema).
    - Test 4 `test_writeback_policy_route_default_requires_approval`: writeback_approval_policy(layer_type="route", impact_scope="route-default", proposed_change=None) returns `{"policy_class": "route-default_change", "requires_approval": True, "reason": "route-default changes require approval before promotion."}`.
    - Test 5 `test_writeback_policy_packet_default_requires_approval`: writeback_approval_policy(layer_type="packet", impact_scope="packet-default", proposed_change=None) returns `{"policy_class": "packet-default_change", "requires_approval": True, "reason": "packet-default changes require approval before promotion."}`.
    - Test 6 `test_writeback_policy_existing_scopes_unchanged`: writeback_approval_policy for impact_scope in ("global","project-truth","workflow-default","prompt-default","skill-default","standards-default") still returns requires_approval=True with their canonical policy_class values; impact_scope="scoped" still returns requires_approval=False. Confirms additive change.
    - Test 7 `test_pre_migration_rows_remain_visible_in_select` (Pitfall 2): seed a workflow_learning_events row with explicit NULL signal_kind via direct INSERT; SELECT * FROM workflow_learning_events returns the row; signal_kind comes back as None. Confirms additive nullable column does not drop legacy rows.
  </behavior>
  <action>
    1. **Add `_ensure_workflow_learning_signal_kind_column(conn: sqlite3.Connection) -> None`** to bin/aios_orchestration_runtime.py near the other table-ensure helpers (search for "ensure_column" to find the canonical home). The helper:
       - Reads `PRAGMA table_info(workflow_learning_events)`; if signal_kind not in columns, runs `ALTER TABLE workflow_learning_events ADD COLUMN signal_kind TEXT`.
       - Runs `CREATE INDEX IF NOT EXISTS idx_workflow_learning_events_signal ON workflow_learning_events(signal_kind, created_at DESC)`.
       - Wraps both in a try/except sqlite3.OperationalError block for safety on missing-table cases (the table-ensure path in ensure_runtime_schema must run first, but defensive try/except matches the existing ensure_column pattern).

    2. **Call `_ensure_workflow_learning_signal_kind_column(conn)` from `ensure_runtime_schema(conn)`** after the existing workflow_learning_events table-ensure block. Use the same calling convention the surrounding ensure_* helpers use.

    3. **Extend the `high_impact_scopes` set in writeback_approval_policy (line 828)** to include `"route-default"` and `"packet-default"`. Extend the `high_impact_layers` set (line 827) to include `"route"`. These are the only changes inside writeback_approval_policy.

    4. **Extend tests/test_orchestration_runtime.py** (or create it if absent) with the seven behavior tests above. Use `sqlite3.connect(":memory:")` directly; do not import anything from services/.

    5. **Quality ladder per touched file:** `uv run ruff check bin/aios_orchestration_runtime.py tests/test_orchestration_runtime.py && uv run ruff format --check bin/aios_orchestration_runtime.py tests/test_orchestration_runtime.py && uv run basedpyright bin/aios_orchestration_runtime.py`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_orchestration_runtime.py -x -q -k "signal_kind or writeback_policy_route_default or writeback_policy_packet_default or writeback_policy_existing_scopes or pre_migration_rows"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def _ensure_workflow_learning_signal_kind_column" bin/aios_orchestration_runtime.py | wc -l | tr -d ' '` returns 1
    - `grep -n "idx_workflow_learning_events_signal" bin/aios_orchestration_runtime.py | wc -l | tr -d ' '` returns at least 1
    - `grep -n "route-default\|packet-default" bin/aios_orchestration_runtime.py | wc -l | tr -d ' '` returns at least 2
    - `grep -n "_ensure_workflow_learning_signal_kind_column" bin/aios_orchestration_runtime.py | wc -l | tr -d ' '` returns at least 2 (definition + call site in ensure_runtime_schema)
    - `grep -c "def test_signal_kind_column_added_idempotently\|def test_signal_kind_column_left_alone_when_present\|def test_ensure_runtime_schema_invokes_signal_kind_helper\|def test_writeback_policy_route_default_requires_approval\|def test_writeback_policy_packet_default_requires_approval\|def test_writeback_policy_existing_scopes_unchanged\|def test_pre_migration_rows_remain_visible_in_select" tests/test_orchestration_runtime.py | grep -v '^#'` returns at least 7
    - `uv run pytest tests/test_orchestration_runtime.py -x -q` exits 0
    - `uv run ruff check bin/aios_orchestration_runtime.py tests/test_orchestration_runtime.py` exits 0
    - `uv run ruff format --check bin/aios_orchestration_runtime.py tests/test_orchestration_runtime.py` exits 0
    - `uv run basedpyright bin/aios_orchestration_runtime.py` exits 0
    - `uv run pytest tests/test_architecture_enforcement.py -x -q` exits 0
  </acceptance_criteria>
  <done>
    workflow_learning_events table carries a nullable signal_kind column via an idempotent migration callable from ensure_runtime_schema; writeback_approval_policy recognizes route-default and packet-default scopes with requires_approval=True; seven behavior tests pass; Pitfall 7 closed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend services/aios_cli.py _record_workflow_learning_event with signal_kind kwarg; extend _workflow_learning_event_exists dedupe key; ensure CLI bootstrap path also creates signal_kind column</name>
  <files>services/aios_cli.py, tests/test_aios_cli.py</files>
  <read_first>
    - services/aios_cli.py — read the exact bodies of `_ensure_workflow_learning_schema` (lines 2111-2132), `_workflow_learning_event_exists` (near line 2148; find via `grep -n "def _workflow_learning_event_exists" services/aios_cli.py`), and `_record_workflow_learning_event` (lines 2156-2193) verbatim before editing. Also read the WORKFLOW_LEARNING_EVIDENCE_TYPES literal (line 147) to confirm the existing literal lives alongside the new signal_kind kwarg, NOT replacing it.
    - bin/aios_orchestration_runtime.py — confirm _ensure_workflow_learning_signal_kind_column signature from Task 2 (the CLI ensure path can reuse the same idempotent ALTER inline OR call the bin helper IF services/ → bin import is permitted; it is NOT permitted per tests/test_architecture_enforcement.py, so duplicate the idempotent ALTER inline inside _ensure_workflow_learning_schema following the existing pattern that file already uses for ensure-schema duplication).
    - tests/test_aios_cli.py — find via `grep -n "def test_workflow_learning\|_workflow_learning_payload\|_record_workflow_learning_event" tests/test_aios_cli.py | head -20` to confirm existing test fixture style and to locate the cluster of workflow-learning tests where new tests should be appended.
    - .planning/phases/09-continuous-learning-and-conservative-optimization/09-RESEARCH.md — Pattern 5 (Idempotent Schema Migration) and A9 (dedupe key extension).
    - tests/test_architecture_enforcement.py — verify the services-cannot-import-bin rule lines 1-100.
  </read_first>
  <behavior>
    - Test 1 `test_record_learning_event_persists_signal_kind`: open in-memory sqlite; call _ensure_workflow_learning_schema(conn); call _record_workflow_learning_event(conn, run_id="r1", evidence_type="workflow_evidence", proposal_target="propose-x", confidence=0.7, approval_state="pending", rationale="seed", source={"k":"v"}, signal_kind="weak_workflow"); SELECT signal_kind FROM workflow_learning_events WHERE run_id="r1" returns "weak_workflow".
    - Test 2 `test_record_learning_event_signal_kind_optional`: _record_workflow_learning_event without signal_kind kwarg writes a row with signal_kind IS NULL (legacy behavior preserved).
    - Test 3 `test_workflow_learning_event_dedupe_respects_signal_kind` (A9): call _record_workflow_learning_event twice with identical (run_id, evidence_type, proposal_target) but different signal_kind values ("weak_workflow" then "ignored_rule"); SELECT COUNT(*) returns 2. Calling a third time with the same (run_id, evidence_type, proposal_target, signal_kind) as the first call returns COUNT(*) still equal to 2 (the third call no-ops).
    - Test 4 `test_workflow_learning_event_dedupe_legacy_path_unchanged`: calling _record_workflow_learning_event twice with identical (run_id, evidence_type, proposal_target) and signal_kind=None both times results in COUNT(*) = 1 (legacy single-row dedupe preserved when signal_kind absent).
    - Test 5 `test_ensure_workflow_learning_schema_creates_signal_kind_column`: open a fresh in-memory db, call _ensure_workflow_learning_schema(conn), PRAGMA table_info(workflow_learning_events) shows signal_kind column present.
    - Test 6 `test_workflow_learning_payload_surfaces_signal_kind`: seed two workflow_learning_events rows, one with signal_kind="weak_workflow" and one with NULL, plus a matching orchestration_runs row with terminal status; call _workflow_learning_payload(conn); assert the rows appear in `persisted_events` and that each row dict carries a "signal_kind" key (value is "weak_workflow" or null/None accordingly). Plan 05 will extend the payload top-level summary; Plan 01 just ensures the per-row passthrough.
  </behavior>
  <action>
    1. **Extend `_ensure_workflow_learning_schema` in services/aios_cli.py** to ALSO run `ALTER TABLE workflow_learning_events ADD COLUMN signal_kind TEXT` idempotently after the CREATE TABLE block. Use the same idempotent pattern (PRAGMA table_info check) used elsewhere in the file. Also create the `idx_workflow_learning_events_signal` index. Do NOT import from bin/.

    2. **Extend `_workflow_learning_event_exists`** to accept and use a new keyword argument `signal_kind: str | None = None`. The SELECT becomes:
       - When signal_kind is None: existing logic (`WHERE run_id=? AND evidence_type=? AND (proposal_target IS ? OR proposal_target = ?)`)
       - When signal_kind is not None: extended to also require `signal_kind = ?` (so two distinct signal_kinds for the same (run_id, evidence_type, proposal_target) are NOT collapsed).
       - The dedupe semantics MUST match Test 3 (different signal_kind → distinct rows) and Test 4 (legacy NULL path → single row).

    3. **Extend `_record_workflow_learning_event`** signature to add `signal_kind: str | None = None` as a final keyword-only argument. Pass signal_kind into `_workflow_learning_event_exists`. Extend the INSERT column list and VALUES tuple to include signal_kind. Do NOT modify the WORKFLOW_LEARNING_EVIDENCE_TYPES literal or alter existing call sites — they pass no signal_kind and continue to write NULL.

    4. **Extend `_workflow_learning_payload`'s `persisted_events` list-construction** so each row dict carries a `signal_kind` key. Find the SELECT that builds persisted_events (near line 2333), add `signal_kind` to the column list, and add `"signal_kind": row["signal_kind"]` to the per-row dict construction. Do NOT add new top-level summary keys (Plan 05 owns that).

    5. **Extend tests/test_aios_cli.py** with the six behavior tests above. Use the existing in-memory sqlite fixture style; no subprocess required.

    6. **Quality ladder per touched file:** `uv run ruff check services/aios_cli.py tests/test_aios_cli.py && uv run ruff format --check services/aios_cli.py tests/test_aios_cli.py && uv run basedpyright services/aios_cli.py`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_aios_cli.py -x -q -k "record_learning_event_persists_signal_kind or record_learning_event_signal_kind_optional or workflow_learning_event_dedupe_respects_signal_kind or workflow_learning_event_dedupe_legacy_path_unchanged or ensure_workflow_learning_schema_creates_signal_kind_column or workflow_learning_payload_surfaces_signal_kind"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "signal_kind" services/aios_cli.py | wc -l | tr -d ' '` returns at least 5 (ensure-schema ALTER + helper signature + dedupe SELECT + INSERT column + payload row)
    - `grep -n "ADD COLUMN signal_kind" services/aios_cli.py | wc -l | tr -d ' '` returns at least 1
    - `grep -n "idx_workflow_learning_events_signal" services/aios_cli.py | wc -l | tr -d ' '` returns at least 1
    - `grep -c "def test_record_learning_event_persists_signal_kind\|def test_record_learning_event_signal_kind_optional\|def test_workflow_learning_event_dedupe_respects_signal_kind\|def test_workflow_learning_event_dedupe_legacy_path_unchanged\|def test_ensure_workflow_learning_schema_creates_signal_kind_column\|def test_workflow_learning_payload_surfaces_signal_kind" tests/test_aios_cli.py | grep -v '^#'` returns at least 6
    - `uv run pytest tests/test_aios_cli.py -x -q` exits 0
    - `uv run pytest tests/test_architecture_enforcement.py -x -q` exits 0 (no new services → bin import introduced)
    - `uv run ruff check services/aios_cli.py tests/test_aios_cli.py` exits 0
    - `uv run ruff format --check services/aios_cli.py tests/test_aios_cli.py` exits 0
    - `uv run basedpyright services/aios_cli.py` exits 0
    - `python -c "import services.aios_cli as m; import inspect; sig=inspect.signature(m._record_workflow_learning_event); assert 'signal_kind' in sig.parameters"` exits 0
  </acceptance_criteria>
  <done>
    services/aios_cli.py _record_workflow_learning_event accepts and persists signal_kind; dedupe key includes signal_kind so distinct signal kinds coexist; _ensure_workflow_learning_schema creates the column; _workflow_learning_payload surfaces signal_kind in per-row payloads; six behavior tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| services/learning_taxonomy.py -> downstream Plan 02/03/04/05 modules | Single source of truth for the LearningSignalKind literal and ConservatismPolicy shape; drift in this file silently breaks downstream callers. Mitigated by literal completeness tests. |
| bin/aios_orchestration_runtime.py:writeback_approval_policy -> Plan 04 conservative_optimizer | The two new scopes (route-default, packet-default) MUST return requires_approval=True; if the policy regresses, Plan 04 emits non-gated proposals (Pitfall 7). |
| workflow_learning_events.signal_kind column migration -> existing data | Pre-migration rows must remain visible (Pitfall 2). Additive nullable column ensures no data loss. |
| services/ → bin/ import boundary | Plan 01 must NOT introduce a services → bin import. The signal_kind ALTER is duplicated in services/aios_cli.py._ensure_workflow_learning_schema rather than imported from bin/. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | writeback_approval_policy regresses such that route-default returns requires_approval=False | mitigate | Task 2 Test 4 + Test 5 assert requires_approval=True for both new scopes; Test 6 asserts existing scopes unchanged. |
| T-09-02 | Tampering | signal_kind migration drops or rewrites pre-migration rows (Pitfall 2) | mitigate | Task 2 Test 7 seeds a NULL signal_kind row before the migration and SELECTs it back afterwards. |
| T-09-03 | Integrity | LearningSignalKind literal drifts between services/learning_taxonomy.py and SIGNAL_TO_IMPACT_SCOPE / IS_ACTIONABLE_SIGNAL | mitigate | Task 1 Test 2 + Test 3 assert key-set equality across the literal and the maps. |
| T-09-04 | Repudiation | _record_workflow_learning_event silently collapses two distinct signal_kinds into one row (A9 lost) | mitigate | Task 3 Test 3 + Test 4 enforce the new dedupe key explicitly. |
| T-09-05 | Information Disclosure | _workflow_learning_payload drops the new signal_kind column from per-row dicts | mitigate | Task 3 Test 6 asserts the key passes through. |
| T-09-06 | Elevation of Privilege | A future caller of writeback_approval_policy with an unknown scope is silently treated as requires_approval=False | accept | Existing Phase 5 fall-through (line 844) returns requires_approval=False for unrecognized scopes; this is the existing project behavior. Phase 9 only adds two known scopes and is not in scope to redesign the fall-through. Note for Phase 10 follow-up. |
</threat_model>

<verification>
- `uv run pytest tests/test_learning_taxonomy.py tests/test_orchestration_runtime.py tests/test_aios_cli.py tests/test_architecture_enforcement.py -x -q` exits 0
- `uv run ruff check services/learning_taxonomy.py bin/aios_orchestration_runtime.py services/aios_cli.py tests/test_learning_taxonomy.py tests/test_orchestration_runtime.py tests/test_aios_cli.py` exits 0
- `uv run ruff format --check services/learning_taxonomy.py bin/aios_orchestration_runtime.py services/aios_cli.py tests/test_learning_taxonomy.py tests/test_orchestration_runtime.py tests/test_aios_cli.py` exits 0
- `uv run basedpyright services/learning_taxonomy.py bin/aios_orchestration_runtime.py services/aios_cli.py` exits 0
- `python -c "from services.learning_taxonomy import LearningSignalKind, SIGNAL_TO_IMPACT_SCOPE, IS_ACTIONABLE_SIGNAL, ConservatismPolicy, impact_scope_for_signal"` exits 0
- Quality ladder per CLAUDE.md: hard gates green.
</verification>

<success_criteria>
- LEARN-01 (partial — taxonomy axis + persistence path): the LearningSignalKind literal exists, the workflow_learning_events.signal_kind column exists, and _record_workflow_learning_event persists signal_kind values. Plan 05 will complete LEARN-01 by emitting signal_kind from bin/hook-stop closeout heuristics.
- Pitfall 7 closed: writeback_approval_policy recognizes route-default and packet-default with requires_approval=True.
- Pitfall 2 closed: pre-migration workflow_learning_events rows remain visible with NULL signal_kind.
- All foundation surfaces (taxonomy module + column + scopes + CLI helper extension) ready for Plans 02-05 to import / extend.
</success_criteria>

<output>
After completion, create `.planning/phases/09-continuous-learning-and-conservative-optimization/09-01-SUMMARY.md` per the template, including:
- Exact field set and impact-scope mappings shipped in services/learning_taxonomy.py
- The idempotent ALTER + index creation in bin/aios_orchestration_runtime.py and the same ALTER duplicated in services/aios_cli.py._ensure_workflow_learning_schema (architecture-boundary rationale)
- The two new high_impact_scopes (route-default, packet-default) and the new high_impact_layers entry (route)
- The extended dedupe key on _workflow_learning_event_exists per A9 with worked examples
- Note that Plan 02 will import LearningSignalKind from services/learning_taxonomy.py for the detector dispatch table, and Plan 04 will import SIGNAL_TO_IMPACT_SCOPE + IS_ACTIONABLE_SIGNAL + ConservatismPolicy
- Note that Plan 05 will emit signal_kind values at hook-stop closeout (currently all writes pass NULL)
</output>
</content>
</invoke>