---
phase: 09-continuous-learning-and-conservative-optimization
plan: "04"
type: execute
wave: 3
autonomous: true
depends_on:
  - "09-01"
  - "09-02"
files_modified:
  - services/conservative_optimizer.py
  - config/learning/conservatism-policy.json
  - tests/test_conservative_optimizer.py
requirements:
  - LEARN-03
must_haves:
  truths:
    - "config/learning/conservatism-policy.json (NEW) exists and is git-tracked with declarative thresholds: min_sample_size, min_recurrence_count, min_confidence, cooling_period_days, min_sample_size_for_trend, version, and an optional per_signal_overrides map keyed by LearningSignalKind."
    - "services/conservative_optimizer.py exposes load_conservatism_policy() (reads the JSON file and returns ConservatismPolicy), propose_from_pattern(conn, pattern, policy, *, actor) (emits one improvement_writebacks row or returns None when thresholds fail), and propose_from_all_patterns(conn, patterns, policy, *, actor) (batch helper)."
    - "propose_from_pattern ALWAYS sets requires_approval=True on emitted writebacks regardless of writeback_approval_policy return value (LEARN-03 invariant + Pitfall 4)."
    - "propose_from_pattern honors a cooling-period dedupe: when an improvement_writebacks row already exists with the same metadata.pattern_id whose decision is approved, rejected, or pending AND whose created_at is within cooling_period_days, the new proposal is skipped (Pitfall 3)."
    - "Informational signals (IS_ACTIONABLE_SIGNAL membership check) are silently skipped — they NEVER emit improvement_writebacks rows (Pitfall 4)."
    - "Every emitted improvement_writebacks row carries metadata_json (or proposed_change_json — Plan 04 confirms the canonical column during read_first) with source='learning_analysis', pattern_id=pattern.pattern_id, evidence_run_ids=list(pattern.evidence_run_ids), sample_size, recurrence_count, confidence."
    - "The conservative_optimizer never imports from bin/; it duplicates the writeback INSERT against improvement_writebacks via a private helper that mirrors the Phase 5 INSERT column list (same pattern Plan 08-01 established for asset_lifecycle.writeback_approval_policy_shim; if Phase 8 already published the shim under services/, reuse it; otherwise duplicate inline)."
  artifacts:
    - path: "services/conservative_optimizer.py"
      provides: "load_conservatism_policy(); propose_from_pattern; propose_from_all_patterns; _writeback_within_cooling_period(conn, pattern_id, days); _insert_conservative_writeback (private INSERT helper) or import of the Phase 8 shim if present"
      contains: "load_conservatism_policy|propose_from_pattern|propose_from_all_patterns"
    - path: "config/learning/conservatism-policy.json"
      provides: "version='2026-05-22'; min_sample_size=5; min_recurrence_count=3; min_confidence=0.6; cooling_period_days=14; min_sample_size_for_trend=10; per_signal_overrides={}"
    - path: "tests/test_conservative_optimizer.py"
      provides: "threshold gate tests (sample, recurrence, confidence); cooling-period dedupe tests; requires_approval=True invariant; informational-signal skip; metadata source tag; per-signal override; reuse of Phase 8 shim when available; never-mutate-registry pure-write-to-improvement_writebacks assertion"
  key_links:
    - from: "services/conservative_optimizer.py:propose_from_pattern"
      to: "improvement_writebacks (Phase 5)"
      via: "INSERT through duplicated Phase 5 INSERT column list OR through Phase 8 services-side shim if available; metadata column tagged source='learning_analysis'"
      pattern: "improvement_writebacks"
    - from: "services/conservative_optimizer.py:_writeback_within_cooling_period"
      to: "improvement_writebacks.metadata_json.pattern_id"
      via: "SELECT WHERE json_extract(metadata_json,'$.pattern_id')=? AND created_at >= ?"
      pattern: "pattern_id"
    - from: "config/learning/conservatism-policy.json"
      to: "services/conservative_optimizer.py:load_conservatism_policy"
      via: "JSON file read at module-level OR per-call (per-call preferred so operators can edit without reloading the Python process)"
      pattern: "load_conservatism_policy"
---

<objective>
Ship services/conservative_optimizer.py + config/learning/conservatism-policy.json — the LEARN-03 layer that converts RecurringPattern records from Plan 02 into improvement_writebacks rows with strict approval gating, declarative thresholds, and a cooling-period dedupe. Every emitted proposal is reviewable; nothing auto-promotes.

Purpose: LEARN-03 requires conservative improvement proposals grounded in reviewed outcomes. Plan 04 is the proposal-synthesis layer; thresholds live in JSON so operators can tune without touching code; cooling-period prevents the optimizer from looping on its own proposals (Pitfall 3); requires_approval=True is a hard invariant (Pitfall 4).

Output: services/conservative_optimizer.py (new), config/learning/conservatism-policy.json (new), tests/test_conservative_optimizer.py (new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-continuous-learning-and-conservative-optimization/09-RESEARCH.md
@.planning/phases/09-continuous-learning-and-conservative-optimization/09-01-PLAN.md
@.planning/phases/09-continuous-learning-and-conservative-optimization/09-02-PLAN.md

<interfaces>
<!-- Symbols and surfaces this plan composes against. -->

From services/learning_taxonomy.py (Plan 01):
- `LearningSignalKind`, `SIGNAL_TO_IMPACT_SCOPE`, `IS_ACTIONABLE_SIGNAL`, `ConservatismPolicy`, `impact_scope_for_signal`.

From services/learning_analysis.py (Plan 02):
- `RecurringPattern` frozen dataclass.

From bin/aios_orchestration_runtime.py (Phase 5, verified during planning):
- `writeback_approval_policy(*, layer_type, impact_scope, proposed_change, requires_approval=False, approval_reason=None) -> dict` at line 816 — Plan 01 extended high_impact_scopes to include route-default and packet-default.
- `insert_writeback(conn, ...)` at line 854 — the canonical INSERT helper. The architecture rule forbids services/ → bin/ imports, so Plan 04 EITHER reuses a services-side shim (if Phase 8 shipped services/asset_lifecycle.py:writeback_approval_policy_shim and a sibling insert helper) OR duplicates the INSERT column list inline.

From schema.sql:
- improvement_writebacks columns (verified): id, run_id, project_id, layer_type, layer_key, title, summary, evidence_json, proposed_change_json, impact_scope, status, requires_approval, approval_reason, token_regressive, created_at, updated_at. Plan 5 may also have ensured metadata_json — confirm during read_first; if metadata_json is not present, Plan 04 embeds source/pattern_id inside proposed_change_json instead.
- improvement_writeback_events columns: (id, writeback_id, event_type, actor, payload_json, created_at). Plan 04 emits one row of event_type='proposed' per writeback.

Plan 04 reads-only against pattern data; writes-only against improvement_writebacks + improvement_writeback_events.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create config/learning/conservatism-policy.json and load_conservatism_policy loader; assert schema validity</name>
  <files>config/learning/conservatism-policy.json, services/conservative_optimizer.py, tests/test_conservative_optimizer.py</files>
  <read_first>
    - .planning/phases/09-continuous-learning-and-conservative-optimization/09-RESEARCH.md — Pattern 3 (Conservative Proposal Synthesis) lines 460-561; Pitfall 1 (Pattern Detection Threshold Tuning Drift); Pitfall 4 (Auto-Promotion Bypass); A3 (config file location).
    - services/learning_taxonomy.py — ConservatismPolicy field set.
    - config/ directory — find via `ls config/ 2>/dev/null` to confirm convention for new config subdirectories. If `config/learning/` doesn't exist, create it; the policy file is the first occupant.
    - config/workflows/registry.json or config/success-criteria/registry.json — paste-of-record for JSON file shape with `version` field.
    - services/aios_cli.py — find via `grep -n "json.load\|_load_json" services/aios_cli.py | head -10` for the project's JSON loader convention.
  </read_first>
  <behavior>
    - Test 1 `test_conservatism_policy_json_is_well_formed`: open config/learning/conservatism-policy.json and json.load it; the resulting dict contains keys version, min_sample_size, min_recurrence_count, min_confidence, cooling_period_days, min_sample_size_for_trend, per_signal_overrides.
    - Test 2 `test_load_conservatism_policy_returns_dataclass`: load_conservatism_policy() reads the file and returns a ConservatismPolicy instance with field values matching the JSON; per_signal_overrides is a dict.
    - Test 3 `test_load_conservatism_policy_accepts_path_override`: load_conservatism_policy(path=tmp_path_with_custom_json) loads the custom file rather than the canonical one (operator override path).
    - Test 4 `test_load_conservatism_policy_rejects_unknown_keys`: a JSON file with an unknown top-level key (e.g., "min_confidence" → "min_confidance" typo) raises ValueError with the offending key name (use a strict-load helper that validates against the ConservatismPolicy field set; document the validation policy in the docstring).
    - Test 5 `test_load_conservatism_policy_defaults_when_per_signal_overrides_absent`: a JSON file without per_signal_overrides loads cleanly; per_signal_overrides comes back as {}.
    - Test 6 `test_conservatism_policy_thresholds_are_conservative`: the canonical config/learning/conservatism-policy.json has min_sample_size>=5, min_recurrence_count>=3, min_confidence>=0.6, cooling_period_days>=7 (sanity-check defaults are not loosened by accident).
  </behavior>
  <action>
    1. **Create config/learning/conservatism-policy.json** with:
       ```
       {
         "version": "2026-05-22",
         "min_sample_size": 5,
         "min_recurrence_count": 3,
         "min_confidence": 0.6,
         "cooling_period_days": 14,
         "min_sample_size_for_trend": 10,
         "per_signal_overrides": {}
       }
       ```
       Use Write tool (never heredoc per system rules).

    2. **Create services/conservative_optimizer.py** with module docstring, `from __future__ import annotations`, and imports:
       - stdlib: `import json, sqlite3, uuid`; `from datetime import datetime, timedelta, timezone`; `from pathlib import Path`; `from typing import Any`.
       - project: `from services.learning_taxonomy import ConservatismPolicy, LearningSignalKind, SIGNAL_TO_IMPACT_SCOPE, IS_ACTIONABLE_SIGNAL, impact_scope_for_signal`.
       - project: `from services.learning_analysis import RecurringPattern`.

    3. **Add `_DEFAULT_POLICY_PATH = Path(__file__).resolve().parents[1] / "config" / "learning" / "conservatism-policy.json"`** as a module-level constant.

    4. **Add `load_conservatism_policy(path: Path | str | None = None) -> ConservatismPolicy`:**
       - Resolves path to _DEFAULT_POLICY_PATH when None.
       - json.load the file.
       - Validate the key set: known keys are exactly {version, min_sample_size, min_recurrence_count, min_confidence, cooling_period_days, min_sample_size_for_trend, per_signal_overrides}. Unknown keys → raise ValueError(f"Unknown key {key!r} in conservatism-policy.json; allowed: {sorted(allowed)}").
       - Validate types loosely (int/float/dict — basedpyright will catch most issues).
       - Construct and return ConservatismPolicy(...). Note: version field is NOT in the dataclass; load_conservatism_policy stores it internally OR drops it after validation (document the choice — recommend drop it after a version check; if version < a known floor, log a warning).

    5. **Add tests for Tests 1-6** in tests/test_conservative_optimizer.py. Use tmp_path fixture for Test 3.

    6. **Quality ladder per touched file:** `uv run ruff check services/conservative_optimizer.py tests/test_conservative_optimizer.py && uv run ruff format --check services/conservative_optimizer.py tests/test_conservative_optimizer.py && uv run basedpyright services/conservative_optimizer.py`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_conservative_optimizer.py -x -q -k "conservatism_policy_json or load_conservatism_policy"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import json; p=json.load(open('config/learning/conservatism-policy.json')); assert {'version','min_sample_size','min_recurrence_count','min_confidence','cooling_period_days','min_sample_size_for_trend','per_signal_overrides'} <= set(p.keys())"` exits 0
    - `python -c "import json; p=json.load(open('config/learning/conservatism-policy.json')); assert p['min_sample_size']>=5 and p['min_recurrence_count']>=3 and p['min_confidence']>=0.6 and p['cooling_period_days']>=7"` exits 0
    - `grep -n "def load_conservatism_policy\|_DEFAULT_POLICY_PATH" services/conservative_optimizer.py | wc -l | tr -d ' '` returns at least 2
    - `grep -n "from services.learning_taxonomy import\|from services.learning_analysis import" services/conservative_optimizer.py | wc -l | tr -d ' '` returns at least 2
    - `grep -c "def test_conservatism_policy_json_is_well_formed\|def test_load_conservatism_policy_returns_dataclass\|def test_load_conservatism_policy_accepts_path_override\|def test_load_conservatism_policy_rejects_unknown_keys\|def test_load_conservatism_policy_defaults_when_per_signal_overrides_absent\|def test_conservatism_policy_thresholds_are_conservative" tests/test_conservative_optimizer.py | grep -v '^#'` returns at least 6
    - `uv run pytest tests/test_conservative_optimizer.py -x -q -k "conservatism_policy_json or load_conservatism_policy"` exits 0
    - `uv run pytest tests/test_architecture_enforcement.py -x -q` exits 0
    - `uv run ruff check services/conservative_optimizer.py tests/test_conservative_optimizer.py` exits 0
    - `uv run ruff format --check services/conservative_optimizer.py tests/test_conservative_optimizer.py` exits 0
    - `uv run basedpyright services/conservative_optimizer.py` exits 0
  </acceptance_criteria>
  <done>
    config/learning/conservatism-policy.json exists with conservative defaults; services/conservative_optimizer.py exposes load_conservatism_policy with strict-key validation; six behavior tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement propose_from_pattern with threshold gates, requires_approval=True invariant, informational-signal skip, and improvement_writebacks INSERT</name>
  <files>services/conservative_optimizer.py, tests/test_conservative_optimizer.py</files>
  <read_first>
    - services/conservative_optimizer.py (post-Task-1) — module imports, _DEFAULT_POLICY_PATH, load_conservatism_policy.
    - bin/aios_orchestration_runtime.py lines 854-940 (insert_writeback) — confirm the canonical INSERT column list for improvement_writebacks; Plan 04 duplicates this column list because services/ cannot import bin/.
    - services/asset_lifecycle.py — check via `ls services/asset_lifecycle.py 2>/dev/null` whether Phase 8 has already shipped a services-side writeback shim. If YES, import writeback_approval_policy_shim + a writeback-insert helper from there. If NO (Phase 8 still pending), duplicate the policy class and INSERT column list inline (document this clearly in the module docstring with a TODO referencing Phase 8 follow-up).
    - schema.sql — improvement_writebacks columns (verified) and improvement_writeback_events columns.
    - services/divergent_strategy.py — find via `grep -n "insert_writeback\|INSERT INTO improvement_writebacks" services/divergent_strategy.py | head -10` to confirm whether services/ has an existing pattern of duplicating the INSERT (it does, in workflow_synthesis.py — verify and reuse the convention).
    - .planning/phases/09-continuous-learning-and-conservative-optimization/09-RESEARCH.md — Pitfall 4 (requires_approval=True invariant) + Pattern 3 paste-of-record for the proposal flow.
  </read_first>
  <behavior>
    - Test 1 `test_propose_from_pattern_rejects_low_sample_size`: build a RecurringPattern with sample_size=2; policy.min_sample_size=5; propose_from_pattern returns None; no INSERT happens (assert COUNT(*) FROM improvement_writebacks == 0).
    - Test 2 `test_propose_from_pattern_rejects_low_recurrence`: pattern.recurrence_count=1; policy.min_recurrence_count=3; returns None; no INSERT.
    - Test 3 `test_propose_from_pattern_rejects_low_confidence`: pattern.confidence=0.3; policy.min_confidence=0.6; returns None; no INSERT.
    - Test 4 `test_propose_from_pattern_emits_writeback_when_all_thresholds_met`: pattern with sample_size=10, recurrence_count=5, confidence=0.8; propose_from_pattern returns dict with writeback_id, pattern_id, requires_approval=True; COUNT(*) FROM improvement_writebacks == 1; the row has layer_type matching the signal_kind's layer (e.g., "workflow" for weak_workflow → "workflow"; signature: layer_type derived from scope_kind), impact_scope matching SIGNAL_TO_IMPACT_SCOPE[signal_kind], status="pending_approval", requires_approval=1.
    - Test 5 `test_propose_from_pattern_always_requires_approval` (Pitfall 4 hard invariant): even when calling writeback_approval_policy returns requires_approval=False (impossible for the new scopes Plan 01 added, but assert the invariant by pattern.signal_kind="weak_prompt" → impact_scope="prompt-default" → policy returns requires_approval=True; assert the emitted row has requires_approval=1; then via a monkey-patched policy returning requires_approval=False, assert the emitted row STILL has requires_approval=1 because the optimizer hard-overrides).
    - Test 6 `test_propose_from_pattern_skips_informational_signal`: pattern.signal_kind="writeback_adopted" (not in IS_ACTIONABLE_SIGNAL); propose_from_pattern returns None; no INSERT (Pitfall 4).
    - Test 7 `test_propose_from_pattern_metadata_includes_source_and_pattern_id`: the emitted improvement_writebacks row's proposed_change_json (or metadata_json — confirm during read_first) contains source="learning_analysis", pattern_id=pattern.pattern_id, evidence_run_ids=list(pattern.evidence_run_ids), sample_size, recurrence_count, confidence (each present and matching the pattern).
    - Test 8 `test_propose_from_pattern_emits_writeback_event`: after a successful proposal, an improvement_writeback_events row exists with writeback_id matching, event_type="proposed", actor="learning_analysis" (or the actor argument passed in).
    - Test 9 `test_propose_from_pattern_per_signal_override`: policy.per_signal_overrides={"weak_prompt":{"min_sample_size":20}}; a pattern with signal_kind="weak_prompt" and sample_size=10 (above global min 5, below override 20) returns None; the same pattern with sample_size=25 emits a writeback.
    - Test 10 `test_propose_from_pattern_only_writes_to_improvement_writebacks_tables` (never-mutate-registry assertion): snapshot all DB tables before propose_from_pattern; call it with a passing pattern; assert ONLY improvement_writebacks and improvement_writeback_events tables grew (config/workflows/registry.json, prompts/registry.json, promotion_lifecycle_items remain untouched). Use sqlite_master + per-table COUNT.
  </behavior>
  <action>
    1. **Add `_layer_type_for_signal(kind: LearningSignalKind) -> str`** helper:
       - Maps signal_kind → improvement_writebacks.layer_type value:
         - "repeated_failure" / "weak_workflow" → "workflow"
         - "ignored_rule" / "standards_regression" → "standard"
         - "bloated_packet" → "packet"
         - "weak_prompt" → "prompt"
         - "route_misroute" → "route"
         - informational signals → raise ValueError (should not reach this helper)

    2. **Add `_insert_conservative_writeback(conn, *, run_id, project_id, layer_type, layer_key, title, summary, evidence_run_ids, proposed_change, impact_scope, actor) -> str`** as a private helper that:
       - If `services/asset_lifecycle.py` exists with a published shim (check via `from services.asset_lifecycle import writeback_approval_policy_shim` in a try/ImportError block), use that shim + its insert helper.
       - Otherwise (Phase 8 not yet shipped), duplicate the Phase 5 INSERT INTO improvement_writebacks column list inline. Generate writeback_id as `f"writeback-{uuid.uuid4()}"`. Set status="pending_approval", requires_approval=1 (HARD-CODED — never derived from policy.requires_approval in this helper; Pitfall 4 invariant lives here). evidence_json=json.dumps(list(evidence_run_ids)). proposed_change_json=json.dumps(proposed_change_dict_including_metadata). Return writeback_id.
       - After INSERT, also INSERT one improvement_writeback_events row with event_type="proposed", actor=actor, payload_json=json.dumps({"source":"learning_analysis"}).

    3. **Add `propose_from_pattern(conn, pattern: RecurringPattern, policy: ConservatismPolicy, *, actor: str = "learning_analysis") -> dict[str, Any] | None`:**
       - If pattern.signal_kind NOT IN IS_ACTIONABLE_SIGNAL → return None (Pitfall 4).
       - Compute the effective thresholds (apply per_signal_overrides over policy defaults):
         ```
         overrides = policy.per_signal_overrides.get(pattern.signal_kind, {})
         min_sample = int(overrides.get("min_sample_size", policy.min_sample_size))
         min_recur = int(overrides.get("min_recurrence_count", policy.min_recurrence_count))
         min_conf = float(overrides.get("min_confidence", policy.min_confidence))
         ```
       - If pattern.sample_size < min_sample → return None.
       - If pattern.recurrence_count < min_recur → return None.
       - If pattern.confidence < min_conf → return None.
       - Compute impact_scope = impact_scope_for_signal(pattern.signal_kind).
       - Compute layer_type = _layer_type_for_signal(pattern.signal_kind).
       - Build proposed_change dict:
         ```
         {
           "signal_kind": pattern.signal_kind,
           "scope_kind": pattern.scope_kind,
           "scope_key": pattern.scope_key,
           "suggested_remediation_class": pattern.suggested_remediation_class,
           "rationale": pattern.summary,
           "metadata": {
             "source": "learning_analysis",
             "pattern_id": pattern.pattern_id,
             "evidence_run_ids": list(pattern.evidence_run_ids),
             "sample_size": pattern.sample_size,
             "recurrence_count": pattern.recurrence_count,
             "confidence": pattern.confidence,
           },
         }
         ```
       - title = f"Conservative improvement: {pattern.signal_kind} on {pattern.scope_kind} {pattern.scope_key}".
       - summary = pattern.summary.
       - Call `_insert_conservative_writeback(...)`.
       - Return `{"writeback_id": ..., "pattern_id": pattern.pattern_id, "requires_approval": True}`.

    4. **Add tests for Tests 1-10** in tests/test_conservative_optimizer.py. Use sqlite3.connect(":memory:") and create the minimal improvement_writebacks + improvement_writeback_events tables inline. For Test 10, also create dummy "registry-like" tables (e.g., promotion_lifecycle_items, prompts_registry as a stub) and assert their counts remain 0.

    5. **Quality ladder per touched file:** `uv run ruff check services/conservative_optimizer.py tests/test_conservative_optimizer.py && uv run ruff format --check services/conservative_optimizer.py tests/test_conservative_optimizer.py && uv run basedpyright services/conservative_optimizer.py`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_conservative_optimizer.py -x -q -k "propose_from_pattern"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def propose_from_pattern\|def _insert_conservative_writeback\|def _layer_type_for_signal" services/conservative_optimizer.py | wc -l | tr -d ' '` returns at least 3
    - `grep -n "requires_approval=1\|requires_approval = 1\|requires_approval=True" services/conservative_optimizer.py | wc -l | tr -d ' '` returns at least 1 (hard-coded invariant)
    - `grep -n "IS_ACTIONABLE_SIGNAL" services/conservative_optimizer.py | wc -l | tr -d ' '` returns at least 1
    - `grep -n "source.*learning_analysis\|\"source\": \"learning_analysis\"" services/conservative_optimizer.py | wc -l | tr -d ' '` returns at least 1
    - `grep -n "INSERT INTO improvement_writebacks\|INSERT INTO improvement_writeback_events" services/conservative_optimizer.py | wc -l | tr -d ' '` returns at least 2 (or one + import of Phase 8 shim if available)
    - `grep -c "def test_propose_from_pattern_rejects_low_sample_size\|def test_propose_from_pattern_rejects_low_recurrence\|def test_propose_from_pattern_rejects_low_confidence\|def test_propose_from_pattern_emits_writeback_when_all_thresholds_met\|def test_propose_from_pattern_always_requires_approval\|def test_propose_from_pattern_skips_informational_signal\|def test_propose_from_pattern_metadata_includes_source_and_pattern_id\|def test_propose_from_pattern_emits_writeback_event\|def test_propose_from_pattern_per_signal_override\|def test_propose_from_pattern_only_writes_to_improvement_writebacks_tables" tests/test_conservative_optimizer.py | grep -v '^#'` returns at least 10
    - `uv run pytest tests/test_conservative_optimizer.py -x -q -k "propose_from_pattern"` exits 0
    - `uv run pytest tests/test_architecture_enforcement.py -x -q` exits 0 (no services -> bin import)
    - `uv run ruff check services/conservative_optimizer.py tests/test_conservative_optimizer.py` exits 0
    - `uv run ruff format --check services/conservative_optimizer.py tests/test_conservative_optimizer.py` exits 0
    - `uv run basedpyright services/conservative_optimizer.py` exits 0
  </acceptance_criteria>
  <done>
    propose_from_pattern gates on sample/recurrence/confidence thresholds with per-signal override support; skips informational signals; ALWAYS emits requires_approval=True (Pitfall 4 invariant); writes only to improvement_writebacks + improvement_writeback_events; ten behavior tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add cooling-period dedupe (_writeback_within_cooling_period) and propose_from_all_patterns batch helper; never re-propose the same pattern_id within cooling_period_days</name>
  <files>services/conservative_optimizer.py, tests/test_conservative_optimizer.py</files>
  <read_first>
    - services/conservative_optimizer.py (post-Task-2) — propose_from_pattern signature, _insert_conservative_writeback helper.
    - .planning/phases/09-continuous-learning-and-conservative-optimization/09-RESEARCH.md — Pitfall 3 (Conservative Optimizer Loops On Its Own Proposals); A10 (cooling period applies regardless of decision); STRIDE entry "Cooling-period bypass".
    - schema.sql — improvement_writebacks.proposed_change_json or metadata_json column used to store pattern_id (Plan 04 chose proposed_change_json based on Task 2 read_first).
    - sqlite docs — `json_extract` availability (SQLite 3.38+ has json1; if older, use LIKE matching on the JSON string).
  </read_first>
  <behavior>
    - Test 1 `test_writeback_within_cooling_period_finds_recent_proposed`: seed an improvement_writebacks row with proposed_change_json containing pattern_id="pattern-abc" created 3 days ago; cooling_period_days=14; _writeback_within_cooling_period(conn, "pattern-abc", 14) returns True.
    - Test 2 `test_writeback_within_cooling_period_ignores_old`: same pattern_id but created 30 days ago; cooling_period_days=14; returns False.
    - Test 3 `test_writeback_within_cooling_period_finds_approved_and_rejected_decisions` (A10): seed two rows with the same pattern_id — one status="approved", one status="rejected", both within window; _writeback_within_cooling_period returns True (BOTH decision states count).
    - Test 4 `test_propose_from_pattern_honors_cooling_period`: call propose_from_pattern with a passing pattern; one writeback inserted. Call it again immediately with the same pattern; returns None; no second writeback inserted.
    - Test 5 `test_propose_from_pattern_re_proposes_after_cooling_period`: call propose_from_pattern, then manually backdate the first writeback's created_at to 30 days ago, then call propose_from_pattern again with the same pattern; second writeback IS inserted (cooling period elapsed).
    - Test 6 `test_propose_from_all_patterns_batch`: call propose_from_all_patterns(conn, [p1, p2, p3], policy, actor="...") where p1 and p2 pass thresholds and p3 fails (low sample_size); returns dict with at least keys "proposed_count": 2, "skipped_count": 1, "writebacks": list of dicts; COUNT(*) FROM improvement_writebacks == 2.
    - Test 7 `test_propose_from_all_patterns_dedupes_across_batch`: passing [p1, p1, p1] (same pattern_id three times); first call proposes, subsequent calls skip via cooling-period dedupe; only one writeback inserted.
    - Test 8 `test_dispatcher_skip_reasons_are_returned`: the return shape from propose_from_all_patterns includes a per-pattern "skipped" entry with reason ∈ {"below_sample","below_recurrence","below_confidence","informational","within_cooling_period"}; verify the four reason values are emitted across a varied input set.
  </behavior>
  <action>
    1. **Add `_writeback_within_cooling_period(conn, pattern_id: str, cooling_period_days: int) -> bool`:**
       - Compute cutoff = datetime.now(timezone.utc) - timedelta(days=cooling_period_days).
       - SELECT id FROM improvement_writebacks WHERE proposed_change_json LIKE ? AND created_at >= ? LIMIT 1; bind `f'%"pattern_id": "{pattern_id}"%'` and cutoff ISO string. (Use LIKE for portability — json_extract requires SQLite 3.38+. Document the choice.)
       - Per A10: the query does NOT filter by status. Approved, rejected, and pending all count.
       - Return True if any row found, else False.

    2. **Extend `propose_from_pattern`** to call `_writeback_within_cooling_period(conn, pattern.pattern_id, policy.cooling_period_days)` AFTER the threshold gates and BEFORE the INSERT. If True, return None (the function silently dedupes; the caller's batch helper will tag the skip reason).

    3. **Add `propose_from_all_patterns(conn, patterns: list[RecurringPattern] | tuple[RecurringPattern, ...], policy: ConservatismPolicy, *, actor: str = "learning_analysis") -> dict[str, Any]`:**
       - Iterate over patterns. For each, call propose_from_pattern.
       - When propose_from_pattern returns None, classify the skip reason by re-checking the gates inline (sample/recurrence/confidence/informational/cooling) to produce a structured skip entry. Avoid duplicating the threshold logic by exposing a sibling private helper `_classify_skip_reason(conn, pattern, policy) -> str | None` that returns the reason string or None when the pattern would actually pass.
       - Accumulate writebacks (list of dicts) and skipped (list of {pattern_id, signal_kind, reason} dicts).
       - Return `{"proposed_count": len(writebacks), "skipped_count": len(skipped), "writebacks": writebacks, "skipped": skipped, "policy_version": policy_version_or_none}`.

    4. **Add tests for Tests 1-8** in tests/test_conservative_optimizer.py. For Test 5, backdate via direct UPDATE statement on improvement_writebacks.created_at.

    5. **Quality ladder per touched file:** `uv run ruff check services/conservative_optimizer.py tests/test_conservative_optimizer.py && uv run ruff format --check services/conservative_optimizer.py tests/test_conservative_optimizer.py && uv run basedpyright services/conservative_optimizer.py`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_conservative_optimizer.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def _writeback_within_cooling_period\|def propose_from_all_patterns\|def _classify_skip_reason" services/conservative_optimizer.py | wc -l | tr -d ' '` returns at least 3
    - `grep -n "cooling_period_days\|cooling_period" services/conservative_optimizer.py | wc -l | tr -d ' '` returns at least 3
    - `grep -n "within_cooling_period\|below_sample\|below_recurrence\|below_confidence\|informational" services/conservative_optimizer.py | wc -l | tr -d ' '` returns at least 5 (the five skip reasons present in the classifier)
    - `grep -c "def test_writeback_within_cooling_period_finds_recent_proposed\|def test_writeback_within_cooling_period_ignores_old\|def test_writeback_within_cooling_period_finds_approved_and_rejected_decisions\|def test_propose_from_pattern_honors_cooling_period\|def test_propose_from_pattern_re_proposes_after_cooling_period\|def test_propose_from_all_patterns_batch\|def test_propose_from_all_patterns_dedupes_across_batch\|def test_dispatcher_skip_reasons_are_returned" tests/test_conservative_optimizer.py | grep -v '^#'` returns at least 8
    - `uv run pytest tests/test_conservative_optimizer.py -x -q` exits 0
    - `uv run pytest tests/test_architecture_enforcement.py -x -q` exits 0
    - `uv run ruff check services/conservative_optimizer.py tests/test_conservative_optimizer.py` exits 0
    - `uv run ruff format --check services/conservative_optimizer.py tests/test_conservative_optimizer.py` exits 0
    - `uv run basedpyright services/conservative_optimizer.py` exits 0
    - `python -c "from services.conservative_optimizer import load_conservatism_policy, propose_from_pattern, propose_from_all_patterns, _writeback_within_cooling_period"` exits 0
  </acceptance_criteria>
  <done>
    Cooling-period dedupe added (Pitfall 3 closed) — counts approved, rejected, and pending rows alike per A10; propose_from_all_patterns batch helper returns structured proposed/skipped counts with reason taxonomy; eight new tests + previous Task 1-2 tests all pass; LEARN-03 fully implemented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| services/conservative_optimizer.py -> improvement_writebacks | Write-side; the optimizer is the only Phase 9 writer to improvement_writebacks. The hard requires_approval=True invariant lives in the _insert_conservative_writeback helper. |
| Conservatism policy JSON -> optimizer thresholds | Operator-tunable thresholds; reviewable via git. Strict-key validation rejects typos at load time. |
| Cooling-period dedupe -> Pitfall 3 | The query uses LIKE on proposed_change_json; tests assert the dedupe covers pending/approved/rejected per A10. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-17 | Elevation of Privilege | propose_from_pattern emits a writeback with requires_approval=0 (Pitfall 4) | mitigate | Task 2 Test 5 + the hard-coded `requires_approval=1` in _insert_conservative_writeback; grep acceptance enforces. |
| T-09-18 | Repudiation | An informational signal (writeback_adopted, compounding_gain) silently emits a writeback bypassing the actionable-signal filter (Pitfall 4) | mitigate | Task 2 Test 6 + IS_ACTIONABLE_SIGNAL membership check at the top of propose_from_pattern. |
| T-09-19 | Denial of Service | The optimizer re-proposes the same pattern every analysis run, flooding the approver (Pitfall 3) | mitigate | Task 3 Tests 4, 5, 7 + cooling-period dedupe covers approved/rejected/pending per A10. |
| T-09-20 | Tampering | Conservatism policy JSON loosened to bypass thresholds | accept | Policy file is git-tracked; commits are reviewable; sanity-check test (Task 1 Test 6) asserts conservative floors. Document tuning guidance in the SUMMARY. |
| T-09-21 | Integrity | propose_from_pattern silently mutates a registry table (workflow registry, prompt registry, promotion_lifecycle_items) | mitigate | Task 2 Test 10 (snapshot-diff) asserts ONLY improvement_writebacks + improvement_writeback_events grow. |
| T-09-22 | Tampering | The proposed_change_json field is missing source/pattern_id, breaking the audit trail | mitigate | Task 2 Test 7 asserts metadata fields present; grep acceptance enforces "source.*learning_analysis" appears in the module. |
| T-09-23 | Information Disclosure | The cooling-period LIKE query matches pattern_id substrings that aren't real (false positive dedupe) | accept | pattern_ids are hashes prefixed `pattern-` followed by 16 hex chars; the LIKE bind uses the full quoted form `"pattern_id": "pattern-..."`; collision risk is negligible. If sqlite version >= 3.38, the implementation can switch to `json_extract` in a follow-up. |
</threat_model>

<verification>
- `uv run pytest tests/test_conservative_optimizer.py tests/test_architecture_enforcement.py -x -q` exits 0
- `uv run ruff check services/conservative_optimizer.py tests/test_conservative_optimizer.py config/learning/conservatism-policy.json` exits 0 (JSON files do not fail ruff)
- `uv run ruff format --check services/conservative_optimizer.py tests/test_conservative_optimizer.py` exits 0
- `uv run basedpyright services/conservative_optimizer.py` exits 0
- `python -c "import json; json.load(open('config/learning/conservatism-policy.json'))"` exits 0
- `python -c "from services.conservative_optimizer import load_conservatism_policy; p=load_conservatism_policy(); assert p.min_sample_size>=5"` exits 0
- Quality ladder per CLAUDE.md: hard gates green.
</verification>

<success_criteria>
- LEARN-03: conservative_optimizer emits improvement_writebacks rows gated by sample/recurrence/confidence thresholds + cooling period; all rows have requires_approval=True.
- Pitfall 1 closed: thresholds live in config/learning/conservatism-policy.json with sanity-checked conservative defaults.
- Pitfall 3 closed: cooling-period dedupe applies to approved/rejected/pending (A10).
- Pitfall 4 closed: requires_approval=True hard invariant; informational signals never emit writebacks; never-mutate-registry assertion proven.
- No services → bin imports introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/09-continuous-learning-and-conservative-optimization/09-04-SUMMARY.md` per the template, including:
- The conservatism-policy.json shape and the rationale for each conservative default
- The propose_from_pattern decision flow (informational skip → threshold gates → cooling dedupe → INSERT)
- The hard-coded requires_approval=True invariant location (in _insert_conservative_writeback) — paste of the line
- The cooling-period LIKE query SQL and the rationale for choosing LIKE over json_extract (SQLite portability)
- The propose_from_all_patterns batch return shape including the skipped-reason taxonomy
- Note that Plan 05 will add the `aios learning-propose` CLI subcommand that wraps load_conservatism_policy + detect_recurring_patterns + propose_from_all_patterns
- Note that operator tuning guidance lives in PROJECT.md or a follow-up doc (Pitfall 1 sustained mitigation)
- Confirmation that no registry tables (workflow / prompt / promotion_lifecycle) were mutated — only improvement_writebacks + improvement_writeback_events
</output>
</content>
</invoke>