---
phase: 09-continuous-learning-and-conservative-optimization
plan: "02"
type: execute
wave: 2
autonomous: true
depends_on:
  - "09-01"
files_modified:
  - services/learning_analysis.py
  - tests/test_learning_analysis.py
requirements:
  - LEARN-02
must_haves:
  truths:
    - "services/learning_analysis.py exposes a RecurringPattern frozen dataclass and a detect_recurring_patterns(conn, *, since, project_id, signal_kinds) entrypoint that dispatches to seven private detectors keyed by LearningSignalKind: _detect_repeated_failures, _detect_ignored_rules, _detect_bloated_packets, _detect_weak_prompts, _detect_weak_workflows, _detect_route_misroutes, _detect_standards_regression."
    - "Each detector accepts (conn, *, since, project_id) and returns list[RecurringPattern]; each enforces a bounded SELECT with `created_at >= ?` (no unbounded history scans per Pitfall 5); each returns an empty list when sources are empty rather than raising."
    - "Every RecurringPattern carries pattern_id (stable hash over signal_kind + scope_kind + scope_key + since_window_bucket), signal_kind, scope_kind (workflow|prompt|skill|route|packet|standard|criterion), scope_key, project_id (or None for cross-project), sample_size, recurrence_count, confidence (recurrence_count / sample_size for frequency detectors, mean-based for outcome detectors), since, summary (bucketed text per Pitfall 6 — no raw packet content), evidence_run_ids tuple, suggested_remediation_class, metadata dict."
    - "_detect_route_misroutes joins orchestration_runs.route_result_json with success_criteria_findings.level='blocker' to find route_result.workflow_key values that consistently lead to immediate blocker findings; the detector returns one pattern per (workflow_key, project_id) above threshold."
    - "_detect_bloated_packets reads briefing_packets.selection_trace_json length and selected_context_packets count, computes per-workflow median + stdev, flags packets > 2 stdev above median; per Pitfall 6 the summary uses bucketed size descriptors (e.g., 'median 23 sections, this packet 47 sections') not raw content."
    - "_detect_weak_prompts groups prompts_used.outcome_score by template_id (or the equivalent prompt id column) over the window, joins agentize_evaluations.outcome_quality, returns one pattern per template with mean below threshold AND sample_size >= floor (Pitfall 9 guard)."
    - "_detect_standards_regression reads standards_delta_items.priority_bucket='regressed' (or the equivalent regression bucket id) history, returns one pattern per standard_id with multiple regression instances in the window."
    - "Per-project default with cross-project fallback when per-project sample_size < threshold (A6 + Anti-Patterns); each pattern's rationale text explicitly states the scope used."
    - "pattern_id is stable: calling detect_recurring_patterns twice on the same seeded fixture in the same window returns the same pattern_id for the same logical pattern (regression test prevents Cooling-Period Bypass per Pattern Detection Poisoning STRIDE entry)."
  artifacts:
    - path: "services/learning_analysis.py"
      provides: "RecurringPattern dataclass; detect_recurring_patterns dispatch; seven private detector functions; _stable_pattern_id helper; _bucket_window helper; private SQL helpers for each evidence source"
      contains: "RecurringPattern|detect_recurring_patterns"
    - path: "tests/test_learning_analysis.py"
      provides: "one test per detector + dispatch + since-bound + pattern-id stability + project-scope fallback (~12 tests total)"
  key_links:
    - from: "services/learning_analysis.py:detect_recurring_patterns"
      to: "services/learning_taxonomy.LearningSignalKind + IS_ACTIONABLE_SIGNAL"
      via: "import from services.learning_taxonomy; the dispatch table is keyed by literal members; informational signals (writeback_adopted, writeback_rejected, compounding_gain) get NO detector — they are emitted only at impact-projection time in Plan 03"
      pattern: "from services.learning_taxonomy import"
    - from: "services/learning_analysis.py:_detect_route_misroutes"
      to: "orchestration_runs.route_result_json + success_criteria_findings"
      via: "SQL JOIN on run_id; route_result_json parsed with json.loads to extract workflow_key + project_id"
      pattern: "route_result_json"
    - from: "services/learning_analysis.py:_detect_bloated_packets"
      to: "briefing_packets + workflow_execution_reports"
      via: "JOIN on run_id; size computed from selected_context_packets count + selection_trace_json length"
      pattern: "briefing_packets"
    - from: "services/learning_analysis.py:_stable_pattern_id"
      to: "hashlib.sha256 over (signal_kind, scope_kind, scope_key, since_window_bucket)"
      via: "deterministic id used as cooling-period dedupe key in Plan 04"
      pattern: "_stable_pattern_id|hashlib"
---

<objective>
Ship services/learning_analysis.py with the RecurringPattern dataclass and the seven detector dispatch table that converts already-persisted Phase 1-8 evidence into typed pattern records. Every detector is bounded, every pattern is explainable, every pattern_id is stable across runs.

Purpose: LEARN-02 requires recurring-failure / ignored-rule / bloated-packet / weak-workflow detection. Plan 02 is the analysis layer; it has no write side-effects (other tests prove this); Plan 04 turns RecurringPattern records into governed proposals.

Output: services/learning_analysis.py (new), tests/test_learning_analysis.py (new) with detector-level and integration-level tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-continuous-learning-and-conservative-optimization/09-RESEARCH.md
@.planning/phases/09-continuous-learning-and-conservative-optimization/09-01-PLAN.md
@.planning/phases/06-standards-resolution-and-evidence-based-evaluation/06-RESEARCH.md
@.planning/phases/07-delta-scoring-and-health-backfill/07-RESEARCH.md

<interfaces>
<!-- Symbols this plan composes against. -->

From services/learning_taxonomy.py (Plan 01):
- `LearningSignalKind = Literal[...]` — used as the dispatch key.
- `LEARNING_SIGNAL_KINDS: tuple[LearningSignalKind, ...]` — used to validate signal_kinds parameter input.
- `IS_ACTIONABLE_SIGNAL: frozenset[LearningSignalKind]` — informational signals SKIPPED in the detector dispatch (they only flow through learning_impact in Plan 03).

From bin/aios_orchestration_runtime.py and schema.sql (verified during planning):
- `orchestration_runs(id, status, project_id, workflow_key, route_result_json, briefing_packet_id, ...)` — route_result_json carries workflow_key + project_id + task_family.
- `briefing_packets(id, run_id, selection_trace_json, selected_context_packets_json, ...)` — Phase 2 packet identity carrier.
- `agentize_evaluations(id, run_id, outcome_quality, tests_passed, follow_up_required, major_repair_required, selected_skills_json, selected_standards_json, recommendation_source, ...)` — Phase 1-8 per-run evaluation.
- `success_criteria_findings(id, run_id, criterion_id, workflow_key, level, summary, evidence_json, resolution_status, created_at, ...)` — Phase 6 findings with level ∈ {info, warning, blocker} and resolution_status ∈ {open, accepted, resolved, waived, stale}.
- `success_criteria_stage_findings(id, run_id, stage_key, criterion_id, level, ...)` — Phase 6 stage-level findings.
- `workflow_execution_reports(id, run_id, invocation_id, workflow_key, status, report_json, created_at, ...)` — Phase 3 closeout; report_json post-Phase-8 carries stage_evaluations[].
- `prompts_used(id, run_id, template_id, classification, outcome_score, reusable_candidate, retrieval_fired, retrieval_source, created_at, ...)` — Phase 1/8 prompt usage.
- `standards_delta_items(id, snapshot_id, standard_id, priority_score, priority_bucket, created_at, ...)` — Phase 7 deltas; priority_bucket includes "regressed" per Phase 7 research.
- `standards_health_snapshots(id, project_id, created_at, ...)` — Phase 7 snapshot anchor.
- `workflow_learning_events(id, run_id, evidence_type, proposal_target, confidence, approval_state, rationale, source_json, signal_kind, created_at)` — Plan 01 added signal_kind.

Phase 9 detector inputs are read-only; no writes from this plan.

NOTE: Plan 02 does NOT consume Phase 8 outputs that may not be shipped yet (e.g., promotion_lifecycle_items standardized status, workflow_execution_reports.report_json.stage_evaluations[]). If a detector needs Phase 8 surfaces, it MUST defensively check column / key existence (try/except sqlite3.OperationalError; dict.get with default empty list) so the detector returns an empty list rather than crashing on a pre-Phase-8 schema.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create services/learning_analysis.py with RecurringPattern dataclass, _stable_pattern_id, _bucket_window, detect_recurring_patterns dispatch entrypoint, and stub detector dispatch table</name>
  <files>services/learning_analysis.py, tests/test_learning_analysis.py</files>
  <read_first>
    - .planning/phases/09-continuous-learning-and-conservative-optimization/09-RESEARCH.md — Pattern 2 (Cross-Run Recurring-Pattern Detection) lines 388-458; Pitfall 5 (Detector Performance on Large Run Histories); Pitfall 6 (Divergent strategy wiring) is NOT in scope here.
    - services/learning_taxonomy.py — Plan 01 output; confirm LEARNING_SIGNAL_KINDS tuple and IS_ACTIONABLE_SIGNAL frozenset.
    - services/standards_health.py — find via `grep -n "@dataclass(frozen=True)\|class DeltaExplanation\|class TrustedSignal" services/standards_health.py | head -10` for the project's frozen-dataclass convention as paste-of-record.
    - services/capability_truth.py — find via `grep -n "Provenance = Literal\|@dataclass" services/capability_truth.py | head -10` for the project's Literal + dataclass convention.
    - tests/test_standards_health.py — find via `head -100 tests/test_standards_health.py` to confirm in-memory sqlite + seed-runtime fixture pattern (paste-of-record for Plan 02 tests).
    - schema.sql — read all column definitions for orchestration_runs, briefing_packets, agentize_evaluations, success_criteria_findings, workflow_execution_reports, prompts_used, standards_delta_items, standards_health_snapshots. The detectors must use real column names; do not guess.
  </read_first>
  <behavior>
    - Test 1 `test_recurring_pattern_is_frozen_dataclass`: instantiate RecurringPattern with all required fields; assert dataclasses.is_dataclass(instance) and instance is frozen (attempting to set attribute raises FrozenInstanceError).
    - Test 2 `test_detect_recurring_patterns_with_empty_db_returns_empty_list`: open an in-memory sqlite, run ensure_runtime_schema (or whatever bootstrap is required), call detect_recurring_patterns(conn, since="2026-01-01T00:00:00Z", project_id=None); assert returns []. No exceptions.
    - Test 3 `test_detect_recurring_patterns_skips_informational_signals`: call detect_recurring_patterns with signal_kinds=("writeback_adopted","compounding_gain"); assert returns [] (no detectors run because none of the informational signals are in IS_ACTIONABLE_SIGNAL).
    - Test 4 `test_detect_recurring_patterns_runs_all_actionable_when_signal_kinds_omitted`: with signal_kinds=None, the dispatch iterates exactly the seven actionable kinds (use a monkey-patched detector dispatch dict to count invocations).
    - Test 5 `test_stable_pattern_id_is_deterministic`: _stable_pattern_id(signal_kind="weak_workflow", scope_kind="workflow", scope_key="implementation-delivery", since_window_bucket="2026-W18") called twice returns the same string. _stable_pattern_id called with a different window bucket returns a different string.
    - Test 6 `test_bucket_window_handles_iso_and_day_offset_format`: _bucket_window("2026-05-21T00:00:00Z") returns a stable bucket id (e.g., year-week or year-month-day — choose one and document); _bucket_window("30d") returns the bucket for now - 30 days; both are strings.
    - Test 7 `test_detect_recurring_patterns_honors_since_bound` (Pitfall 5): seed two success_criteria_findings rows, one inside the window (`created_at` >= since) and one outside; the detector sees only the in-window row. Use a synthetic _detect_repeated_failures stub via monkeypatch to confirm the since parameter actually reaches the SQL.
  </behavior>
  <action>
    1. **Create services/learning_analysis.py** with module docstring, `from __future__ import annotations`, and imports:
       - stdlib: `import hashlib, json, sqlite3, statistics`; `from dataclasses import dataclass, field`; `from datetime import datetime, timedelta, timezone`; `from typing import Any, Callable`.
       - project: `from services.learning_taxonomy import LearningSignalKind, LEARNING_SIGNAL_KINDS, IS_ACTIONABLE_SIGNAL`.

    2. **Add `@dataclass(frozen=True)` `RecurringPattern`** with fields:
       - `pattern_id: str`
       - `signal_kind: LearningSignalKind`
       - `scope_kind: str` (one of "workflow","prompt","skill","route","packet","standard","criterion")
       - `scope_key: str`
       - `project_id: str | None`
       - `sample_size: int`
       - `recurrence_count: int`
       - `confidence: float`
       - `since: str`
       - `summary: str`
       - `evidence_run_ids: tuple[str, ...]`
       - `suggested_remediation_class: str`
       - `metadata: dict[str, Any] = field(default_factory=dict)`

    3. **Add `_stable_pattern_id(*, signal_kind: str, scope_kind: str, scope_key: str, since_window_bucket: str) -> str`**: returns `"pattern-" + hashlib.sha256(...).hexdigest()[:16]`. The hash input is the four arguments joined by "|".

    4. **Add `_bucket_window(since: str) -> str`**: accepts either an ISO timestamp string (`2026-05-21T00:00:00Z`) or a relative bucket like `"30d"` / `"90d"`. Returns a year-week stamp (e.g., `"2026-W21"`) when given an ISO string, and the equivalent year-week for now() - N days when given a relative bucket. Document the choice clearly in the docstring.

    5. **Add `_resolve_since(since: str | None) -> str`**: converts None → ISO timestamp 30 days before now; accepts ISO strings as-is; converts `"Nd"` → ISO timestamp N days before now. The detectors call this helper at the top.

    6. **Add the dispatch table:**
       ```
       DETECTORS: dict[LearningSignalKind, Callable[..., list[RecurringPattern]]] = {
         "repeated_failure":     _detect_repeated_failures,
         "ignored_rule":         _detect_ignored_rules,
         "bloated_packet":       _detect_bloated_packets,
         "weak_prompt":          _detect_weak_prompts,
         "weak_workflow":        _detect_weak_workflows,
         "route_misroute":       _detect_route_misroutes,
         "standards_regression": _detect_standards_regression,
       }
       ```
       Each detector is defined as a function returning [] in Task 1 (real bodies land in Task 2 and Task 3 — keep this plan executable in three iterations). Document this explicitly in a TODO comment inside each detector pointing at the Task in this plan that fills it in.

    7. **Add `detect_recurring_patterns(conn, *, since, project_id, signal_kinds=None) -> list[RecurringPattern]`**:
       - Resolves `since` via `_resolve_since`.
       - Filters `signal_kinds` to the intersection with `IS_ACTIONABLE_SIGNAL` (informational signals get no detector).
       - For each remaining signal_kind, calls `DETECTORS[kind](conn, since=since, project_id=project_id)` and extends the result list.
       - Returns the combined list.
       - Wraps each detector call in try/except sqlite3.OperationalError that logs the table that was missing and continues (so a partial schema does not crash the dispatcher; per Pitfall 5 + Phase 5 evidence read style).

    8. **Add stub bodies for the seven detector functions** that return `[]` and contain a `# TODO(Plan 09-02 Task 2/3): implement` comment indicating which subsequent task fills them in. Type-annotated signatures and `pass`-equivalent bodies.

    9. **Create tests/test_learning_analysis.py** with the seven behavior tests above. Use `sqlite3.connect(":memory:")` and seed only the schema columns each test needs. No bin/ imports.

    10. **Quality ladder per touched file:** `uv run ruff check services/learning_analysis.py tests/test_learning_analysis.py && uv run ruff format --check services/learning_analysis.py tests/test_learning_analysis.py && uv run basedpyright services/learning_analysis.py`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_learning_analysis.py -x -q -k "recurring_pattern_is_frozen_dataclass or detect_recurring_patterns_with_empty_db_returns_empty_list or detect_recurring_patterns_skips_informational_signals or detect_recurring_patterns_runs_all_actionable_when_signal_kinds_omitted or stable_pattern_id_is_deterministic or bucket_window_handles or detect_recurring_patterns_honors_since_bound"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class RecurringPattern\|def detect_recurring_patterns\|def _stable_pattern_id\|def _bucket_window\|def _resolve_since\|^DETECTORS\b" services/learning_analysis.py | wc -l | tr -d ' '` returns at least 6
    - `grep -n "def _detect_repeated_failures\|def _detect_ignored_rules\|def _detect_bloated_packets\|def _detect_weak_prompts\|def _detect_weak_workflows\|def _detect_route_misroutes\|def _detect_standards_regression" services/learning_analysis.py | wc -l | tr -d ' '` returns at least 7
    - `grep -n "from services.learning_taxonomy import" services/learning_analysis.py | wc -l | tr -d ' '` returns at least 1
    - `python -c "from services.learning_analysis import detect_recurring_patterns, RecurringPattern, DETECTORS; assert len(DETECTORS) == 7"` exits 0
    - `grep -c "def test_recurring_pattern_is_frozen_dataclass\|def test_detect_recurring_patterns_with_empty_db_returns_empty_list\|def test_detect_recurring_patterns_skips_informational_signals\|def test_detect_recurring_patterns_runs_all_actionable_when_signal_kinds_omitted\|def test_stable_pattern_id_is_deterministic\|def test_bucket_window_handles_iso_and_day_offset_format\|def test_detect_recurring_patterns_honors_since_bound" tests/test_learning_analysis.py | grep -v '^#'` returns at least 7
    - `uv run pytest tests/test_learning_analysis.py -x -q` exits 0
    - `uv run pytest tests/test_architecture_enforcement.py -x -q` exits 0 (no services -> bin imports)
    - `uv run ruff check services/learning_analysis.py tests/test_learning_analysis.py` exits 0
    - `uv run ruff format --check services/learning_analysis.py tests/test_learning_analysis.py` exits 0
    - `uv run basedpyright services/learning_analysis.py` exits 0
  </acceptance_criteria>
  <done>
    services/learning_analysis.py exposes RecurringPattern + detect_recurring_patterns + DETECTORS dispatch + stable_pattern_id + bucket_window helpers; the seven detector function stubs exist and return []; the dispatcher routes only actionable signals; seven scaffolding tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement _detect_repeated_failures, _detect_ignored_rules, _detect_weak_prompts, _detect_weak_workflows detectors with bounded SQL and per-project default with cross-project fallback</name>
  <files>services/learning_analysis.py, tests/test_learning_analysis.py</files>
  <read_first>
    - services/learning_analysis.py (post-Task-1) — RecurringPattern, dispatch table, _stable_pattern_id, _resolve_since.
    - schema.sql — success_criteria_findings columns (run_id, criterion_id, workflow_key, level, resolution_status, created_at); workflow_execution_reports columns (run_id, workflow_key, status, report_json, created_at); prompts_used columns (run_id, template_id, classification, outcome_score, created_at); agentize_evaluations columns (run_id, outcome_quality, follow_up_required, major_repair_required, recommendation_source, created_at); orchestration_runs (id, project_id, workflow_key, status).
    - services/standards_health.py — paste-of-record for windowed SELECT + aggregation style (find via `grep -n "WHERE created_at >= ?\|GROUP BY" services/standards_health.py | head -10`).
    - services/success_criteria.py — paste-of-record for finding-reads (find via `grep -n "FROM success_criteria_findings\|level\|resolution_status" services/success_criteria.py | head -15`).
    - .planning/phases/09-continuous-learning-and-conservative-optimization/09-RESEARCH.md — Pattern 2 (Detector dispatch examples) + Anti-Patterns (Aggregating evidence across project boundaries by default) + Pitfall 9 (insufficient_data trend).
  </read_first>
  <behavior>
    - Test 1 `test_detect_repeated_failures_groups_by_criterion_and_workflow`: seed orchestration_runs (3 rows project_id="p1") + success_criteria_findings (criterion_id="C-arch-cohesion", level="blocker", workflow_key="implementation-delivery", 3 distinct run_ids); call _detect_repeated_failures(conn, since=<recent>, project_id="p1") with a recurrence threshold of 3; returns exactly one RecurringPattern with signal_kind="repeated_failure", scope_kind="criterion", scope_key="C-arch-cohesion", recurrence_count=3, sample_size=3, evidence_run_ids contains all three.
    - Test 2 `test_detect_repeated_failures_below_threshold_returns_empty`: same seed but only 1 finding; returns [].
    - Test 3 `test_detect_repeated_failures_per_project_default`: seed 4 findings for project p1 and 4 findings for project p2 (different criterion ids); calling project_id="p1" returns only p1 patterns. calling project_id=None returns both (cross-project explicit opt-in).
    - Test 4 `test_detect_ignored_rules_requires_multi_run_recurrence`: seed success_criteria_findings rows with resolution_status="open" and level="blocker" across two runs same workflow_key, same criterion_id; threshold=2; detector returns a pattern with signal_kind="ignored_rule"; sample_size=2; suggested_remediation_class="raise_blocker_priority" (or similar — define the constant in the module).
    - Test 5 `test_detect_ignored_rules_resolved_findings_excluded`: same seed but resolution_status="resolved"; detector returns [].
    - Test 6 `test_detect_weak_prompts_mean_below_threshold`: seed prompts_used rows for template_id="research" with outcome_score values [0.2, 0.3, 0.25, 0.2, 0.3] (mean ≈ 0.25); threshold mean 0.5; sample floor 5; detector returns one pattern with signal_kind="weak_prompt", scope_kind="prompt", scope_key="research", sample_size=5, confidence reflects 1.0 - mean.
    - Test 7 `test_detect_weak_prompts_requires_sample_floor`: seed only 2 prompts_used rows (mean 0.25); sample_floor 5; detector returns [] (insufficient sample).
    - Test 8 `test_detect_weak_workflows_high_failure_rate`: seed workflow_execution_reports for workflow_key="implementation-delivery" with 6 rows status="failed" and 4 rows status="completed" (sample 10, failure rate 0.6); threshold 0.5; detector returns one pattern signal_kind="weak_workflow", scope_key="implementation-delivery", recurrence_count=6, sample_size=10.
    - Test 9 `test_detect_weak_workflows_below_sample_floor_returns_empty`: only 2 reports; below floor of 5; returns [].
    - Test 10 `test_all_four_detectors_emit_stable_pattern_id`: call each detector twice on identical seeded data; the same RecurringPattern.pattern_id appears in both invocations (regression test for cooling-period dedupe in Plan 04).
  </behavior>
  <action>
    1. **Implement `_detect_repeated_failures(conn, *, since, project_id) -> list[RecurringPattern]`:**
       - SELECT `criterion_id, workflow_key, COUNT(*) AS recurrence, GROUP_CONCAT(run_id) AS run_ids, COUNT(DISTINCT run_id) AS sample_size` FROM success_criteria_findings WHERE level='blocker' AND created_at >= ? AND (project_id IS NULL OR project_id = ?)` — but success_criteria_findings doesn't have project_id directly. Use JOIN against orchestration_runs ON success_criteria_findings.run_id = orchestration_runs.id and filter `orchestration_runs.project_id = ?` when project_id is not None.
       - GROUP BY criterion_id, workflow_key HAVING COUNT(*) >= 3 (recurrence threshold — store as `_REPEATED_FAILURE_RECURRENCE_THRESHOLD = 3` module constant).
       - For each group, build a RecurringPattern with pattern_id=_stable_pattern_id(signal_kind="repeated_failure", scope_kind="criterion", scope_key=criterion_id, since_window_bucket=_bucket_window(since)), scope_kind="criterion", scope_key=criterion_id, project_id=project_id, sample_size=sample_size, recurrence_count=recurrence, confidence=recurrence/sample_size (clamped to 1.0), summary=f"Criterion {criterion_id!r} blocked {recurrence} runs of workflow {workflow_key!r} in window {since}", suggested_remediation_class="raise_blocker_priority", metadata={"workflow_key": workflow_key}.
       - Implement the per-project default with cross-project fallback: if project_id is not None AND sample_size below threshold, re-run with project_id=None and tag metadata["scope_fallback"]="cross-project".

    2. **Implement `_detect_ignored_rules(conn, *, since, project_id) -> list[RecurringPattern]`:**
       - SELECT criterion_id, COUNT(DISTINCT run_id) AS sample_size, GROUP_CONCAT(run_id) AS run_ids FROM success_criteria_findings WHERE level='blocker' AND resolution_status='open' AND created_at >= ? AND (project filter) GROUP BY criterion_id HAVING COUNT(DISTINCT run_id) >= 2.
       - Build RecurringPattern with signal_kind="ignored_rule", scope_kind="criterion", scope_key=criterion_id, suggested_remediation_class="raise_blocker_priority" or "review_criterion_definition" (pick one and use as a module constant).

    3. **Implement `_detect_weak_prompts(conn, *, since, project_id) -> list[RecurringPattern]`:**
       - SELECT template_id, AVG(outcome_score) AS mean_score, COUNT(*) AS sample_size, GROUP_CONCAT(run_id) AS run_ids FROM prompts_used WHERE outcome_score IS NOT NULL AND created_at >= ? AND (project filter via JOIN orchestration_runs) GROUP BY template_id HAVING COUNT(*) >= 5 AND AVG(outcome_score) < 0.5.
       - Module constants: `_WEAK_PROMPT_SAMPLE_FLOOR = 5`, `_WEAK_PROMPT_MEAN_THRESHOLD = 0.5`.
       - Build RecurringPattern with signal_kind="weak_prompt", scope_kind="prompt", scope_key=template_id, sample_size=count, recurrence_count=count (every sample contributes), confidence=(1.0 - mean_score) clamped, suggested_remediation_class="deprecate_prompt_or_revise".

    4. **Implement `_detect_weak_workflows(conn, *, since, project_id) -> list[RecurringPattern]`:**
       - SELECT workflow_key, SUM(CASE WHEN status='failed' THEN 1 ELSE 0 END) AS failed, COUNT(*) AS sample_size, GROUP_CONCAT(run_id) AS run_ids FROM workflow_execution_reports WHERE created_at >= ? AND (project filter via JOIN orchestration_runs) GROUP BY workflow_key HAVING COUNT(*) >= 5 AND CAST(SUM(CASE WHEN status='failed' THEN 1 ELSE 0 END) AS FLOAT) / COUNT(*) > 0.4.
       - Module constants: `_WEAK_WORKFLOW_SAMPLE_FLOOR = 5`, `_WEAK_WORKFLOW_FAILURE_RATE_THRESHOLD = 0.4`.
       - Build RecurringPattern with signal_kind="weak_workflow", scope_kind="workflow", scope_key=workflow_key, sample_size=total, recurrence_count=failed, confidence=failed/total, suggested_remediation_class="tighten_validations_or_deprecate".

    5. **Cross-project fallback wrapper:** add a small helper `_apply_project_scope_fallback(conn, detector_fn, *, since, project_id, project_threshold=3)` that calls detector_fn(project_id=project_id); if the union of returned patterns has total sample_size < project_threshold AND project_id is not None, re-call with project_id=None and tag each resulting pattern's metadata with `"scope_fallback": "cross-project"`. Use this wrapper from each of the four detectors above.

    6. **Add tests** for Tests 1-10 in tests/test_learning_analysis.py. Use a `_seed_minimal_schema(conn)` helper at the top of the test file that creates the minimum subset of orchestration_runs, success_criteria_findings, prompts_used, workflow_execution_reports columns the detectors read.

    7. **Quality ladder per touched file:** `uv run ruff check services/learning_analysis.py tests/test_learning_analysis.py && uv run ruff format --check services/learning_analysis.py tests/test_learning_analysis.py && uv run basedpyright services/learning_analysis.py`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_learning_analysis.py -x -q -k "detect_repeated_failures or detect_ignored_rules or detect_weak_prompts or detect_weak_workflows or all_four_detectors_emit_stable_pattern_id"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "FROM success_criteria_findings\|FROM prompts_used\|FROM workflow_execution_reports" services/learning_analysis.py | wc -l | tr -d ' '` returns at least 3
    - `grep -n "WHERE.*created_at >= ?\|AND created_at >= ?" services/learning_analysis.py | wc -l | tr -d ' '` returns at least 4 (one per implemented detector — bounded windows)
    - `grep -n "_REPEATED_FAILURE_RECURRENCE_THRESHOLD\|_WEAK_PROMPT_SAMPLE_FLOOR\|_WEAK_WORKFLOW_SAMPLE_FLOOR" services/learning_analysis.py | wc -l | tr -d ' '` returns at least 3 (module-level constants for thresholds; Plan 04 may move these into ConservatismPolicy overrides later)
    - `grep -n "scope_fallback" services/learning_analysis.py | wc -l | tr -d ' '` returns at least 1 (per-project default + cross-project fallback tag)
    - `grep -c "def test_detect_repeated_failures_groups_by_criterion_and_workflow\|def test_detect_repeated_failures_below_threshold_returns_empty\|def test_detect_repeated_failures_per_project_default\|def test_detect_ignored_rules_requires_multi_run_recurrence\|def test_detect_ignored_rules_resolved_findings_excluded\|def test_detect_weak_prompts_mean_below_threshold\|def test_detect_weak_prompts_requires_sample_floor\|def test_detect_weak_workflows_high_failure_rate\|def test_detect_weak_workflows_below_sample_floor_returns_empty\|def test_all_four_detectors_emit_stable_pattern_id" tests/test_learning_analysis.py | grep -v '^#'` returns at least 10
    - `uv run pytest tests/test_learning_analysis.py -x -q` exits 0
    - `uv run pytest tests/test_architecture_enforcement.py -x -q` exits 0
    - `uv run ruff check services/learning_analysis.py tests/test_learning_analysis.py` exits 0
    - `uv run ruff format --check services/learning_analysis.py tests/test_learning_analysis.py` exits 0
    - `uv run basedpyright services/learning_analysis.py` exits 0
  </acceptance_criteria>
  <done>
    Four detectors (repeated_failures, ignored_rules, weak_prompts, weak_workflows) implemented with bounded SQL, per-project scope with cross-project fallback, stable pattern_id; ten behavior tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement _detect_bloated_packets, _detect_route_misroutes, _detect_standards_regression detectors and a cross-cutting since-bound test for the dispatcher</name>
  <files>services/learning_analysis.py, tests/test_learning_analysis.py</files>
  <read_first>
    - services/learning_analysis.py (post-Task-2) — pattern of detector implementation, _apply_project_scope_fallback helper, module-level threshold constants.
    - schema.sql — briefing_packets columns (run_id, selection_trace_json, selected_context_packets_json, created_at); orchestration_runs.route_result_json shape (from services/task_routing.py).
    - services/task_routing.py — find via `grep -n "route_result_json\|workflow_key\|class RouteResult" services/task_routing.py | head -15` to confirm the JSON shape of route_result_json (likely keys workflow_key, project_id, task_family, recommended_surface, rationale).
    - services/standards_health.py — find via `grep -n "priority_bucket\|regressed" services/standards_health.py | head -15` to confirm the exact bucket id for regressions (likely "regressed" but verify from the source).
    - schema.sql — standards_delta_items columns (id, snapshot_id, standard_id, priority_bucket, priority_score, created_at) and standards_health_snapshots (id, project_id, created_at).
    - .planning/phases/09-continuous-learning-and-conservative-optimization/09-RESEARCH.md — Pitfall 5 (Detector Performance on Large Run Histories), Pitfall 6 (route_misroute proposal pitfall — link to specific runs), Pitfall 9 (insufficient_data — already covered by sample floors).
  </read_first>
  <behavior>
    - Test 1 `test_detect_bloated_packets_uses_per_workflow_median`: seed briefing_packets for workflow_key="implementation-delivery" with selected_context_packets counts [10, 12, 11, 9, 50, 11, 10] across 7 runs; median is ~11, stdev ~14; the row with count=50 exceeds median + 2σ; detector returns one pattern with signal_kind="bloated_packet", scope_kind="packet", scope_key=run_id of the bloated packet, summary mentions "median 11 sections, this packet 50 sections" (Pitfall 6 — bucketed descriptors, no raw content).
    - Test 2 `test_detect_bloated_packets_summary_excludes_raw_content` (Pitfall 6): inspect the returned RecurringPattern.summary; assert it does NOT contain the full selection_trace_json text; assert it DOES contain a numeric bucket descriptor (e.g., "median" and the integer count).
    - Test 3 `test_detect_route_misroutes_uses_route_result_json`: seed orchestration_runs with route_result_json={"workflow_key":"audit-only","project_id":"p1"} on 4 runs, all of which have at least one success_criteria_findings row with level="blocker" within 24h of the run.created_at; detector returns one pattern signal_kind="route_misroute", scope_kind="route", scope_key="audit-only", evidence_run_ids contains all four runs.
    - Test 4 `test_detect_route_misroutes_clean_runs_excluded`: seed 4 runs with route_result.workflow_key="implementation-delivery" and NO blocker findings; detector returns [].
    - Test 5 `test_detect_route_misroutes_evidence_run_ids_cite_actual_runs` (Pitfall 6): the returned pattern's evidence_run_ids tuple matches exactly the run_ids of the failing runs (each can be cross-referenced back to orchestration_runs).
    - Test 6 `test_detect_standards_regression_reads_priority_bucket`: seed standards_health_snapshots (3 snapshots project_id="p1") and standards_delta_items with priority_bucket="regressed" on standard_id="STND-coverage" in 3 distinct snapshots; detector returns one pattern signal_kind="standards_regression", scope_kind="standard", scope_key="STND-coverage", sample_size=3, recurrence_count=3.
    - Test 7 `test_detect_standards_regression_single_instance_excluded`: only 1 regressed delta; detector returns [] (sample floor 2).
    - Test 8 `test_dispatcher_since_bound_applies_to_all_detectors`: seed evidence across all seven detector sources, half older than 90d and half within 30d; call detect_recurring_patterns(conn, since="30d", project_id=None); confirm no returned pattern includes an evidence_run_id from the older half (use a helper to fetch the run.created_at for each returned id and assert all are within 30d).
    - Test 9 `test_dispatcher_returns_patterns_from_all_seven_kinds_when_seeded`: seed minimum-viable evidence for each actionable signal_kind; call detect_recurring_patterns; assert the returned list contains exactly one pattern of each of the seven actionable signal_kinds.
  </behavior>
  <action>
    1. **Implement `_detect_bloated_packets(conn, *, since, project_id) -> list[RecurringPattern]`:**
       - SELECT briefing_packets.run_id, orchestration_runs.workflow_key, json_array_length(selected_context_packets_json) AS section_count FROM briefing_packets JOIN orchestration_runs ON briefing_packets.run_id = orchestration_runs.id WHERE briefing_packets.created_at >= ? AND (project filter) AND selected_context_packets_json IS NOT NULL.
       - In Python, group rows by workflow_key; for each group with >= 5 rows compute median and stdev (use `statistics.median` and `statistics.pstdev`); flag rows with section_count > median + 2*stdev; emit one RecurringPattern per flagged row with scope_kind="packet", scope_key=run_id, summary=f"Median packet for workflow {workflow_key!r} has {median:.0f} sections; this packet has {section_count} (> median + 2σ).", suggested_remediation_class="trim_packet_section".
       - Module constants: `_BLOATED_PACKET_SAMPLE_FLOOR = 5`, `_BLOATED_PACKET_STDEV_MULTIPLIER = 2.0`.
       - The summary string MUST NOT contain the raw selection_trace_json text (Pitfall 6).

    2. **Implement `_detect_route_misroutes(conn, *, since, project_id) -> list[RecurringPattern]`:**
       - SELECT orchestration_runs.id AS run_id, orchestration_runs.route_result_json, orchestration_runs.created_at FROM orchestration_runs WHERE created_at >= ? AND (project filter) AND route_result_json IS NOT NULL.
       - In Python, parse each route_result_json, extract workflow_key. For each run, check if there is a success_criteria_findings row with run_id=that.run_id AND level='blocker' AND created_at within 24h of run.created_at (separate SELECT or LEFT JOIN inline). Build a count per workflow_key.
       - GROUP BY workflow_key in Python after the join; for groups where blocker_run_count >= 3 AND blocker_run_count / total_runs_for_workflow > 0.4, emit one RecurringPattern with signal_kind="route_misroute", scope_kind="route", scope_key=workflow_key, sample_size=total, recurrence_count=blocker_run_count, evidence_run_ids=tuple of the failing run_ids (Pitfall 6 — cite specific runs), suggested_remediation_class="tighten_route_hint_or_lower_priority".
       - Module constants: `_ROUTE_MISROUTE_RECURRENCE_FLOOR = 3`, `_ROUTE_MISROUTE_RATE_THRESHOLD = 0.4`.

    3. **Implement `_detect_standards_regression(conn, *, since, project_id) -> list[RecurringPattern]`:**
       - SELECT standard_id, COUNT(DISTINCT snapshot_id) AS recurrence FROM standards_delta_items JOIN standards_health_snapshots ON standards_delta_items.snapshot_id = standards_health_snapshots.id WHERE standards_delta_items.priority_bucket='regressed' AND standards_delta_items.created_at >= ? AND (standards_health_snapshots.project_id = ? OR ? IS NULL — pass project_id twice).
       - GROUP BY standard_id HAVING COUNT(DISTINCT snapshot_id) >= 2 (module constant `_STANDARDS_REGRESSION_RECURRENCE_FLOOR = 2`).
       - Emit RecurringPattern with signal_kind="standards_regression", scope_kind="standard", scope_key=standard_id, sample_size=recurrence, recurrence_count=recurrence, suggested_remediation_class="prioritize_backfill", evidence_run_ids=tuple of source_run_ids if present in metadata or () if snapshot does not preserve run_id linkage (fall back gracefully).

    4. **Replace the dispatcher try/except** so a detector raising sqlite3.OperationalError logs the missing-table message (use stderr via the project's existing logging convention — find via `grep -n "print(.*stderr\|logger" services/standards_health.py | head -5`), tags the dispatcher result with a `partial: True` metadata key by APPENDING (not raising) a single sentinel-shaped RecurringPattern OR by returning a (patterns, partial) tuple via a sibling helper `detect_recurring_patterns_partial` — choose the simplest of the two (the tuple-helper) and document the choice in the docstring.

    5. **Add tests** for Tests 1-9 in tests/test_learning_analysis.py. Use the `_seed_minimal_schema` helper from Task 2; extend it to also create briefing_packets, standards_delta_items, standards_health_snapshots columns as needed.

    6. **Quality ladder per touched file:** `uv run ruff check services/learning_analysis.py tests/test_learning_analysis.py && uv run ruff format --check services/learning_analysis.py tests/test_learning_analysis.py && uv run basedpyright services/learning_analysis.py`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_learning_analysis.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "FROM briefing_packets\|FROM standards_delta_items\|FROM orchestration_runs" services/learning_analysis.py | wc -l | tr -d ' '` returns at least 3 (each detector reads its source)
    - `grep -n "statistics.median\|statistics.pstdev" services/learning_analysis.py | wc -l | tr -d ' '` returns at least 1 (bloated_packets uses stdlib statistics)
    - `grep -n "route_result_json" services/learning_analysis.py | wc -l | tr -d ' '` returns at least 1
    - `grep -n "priority_bucket" services/learning_analysis.py | wc -l | tr -d ' '` returns at least 1
    - `grep -n "_BLOATED_PACKET_SAMPLE_FLOOR\|_BLOATED_PACKET_STDEV_MULTIPLIER\|_ROUTE_MISROUTE_RECURRENCE_FLOOR\|_STANDARDS_REGRESSION_RECURRENCE_FLOOR" services/learning_analysis.py | wc -l | tr -d ' '` returns at least 4
    - `grep -c "def test_detect_bloated_packets_uses_per_workflow_median\|def test_detect_bloated_packets_summary_excludes_raw_content\|def test_detect_route_misroutes_uses_route_result_json\|def test_detect_route_misroutes_clean_runs_excluded\|def test_detect_route_misroutes_evidence_run_ids_cite_actual_runs\|def test_detect_standards_regression_reads_priority_bucket\|def test_detect_standards_regression_single_instance_excluded\|def test_dispatcher_since_bound_applies_to_all_detectors\|def test_dispatcher_returns_patterns_from_all_seven_kinds_when_seeded" tests/test_learning_analysis.py | grep -v '^#'` returns at least 9
    - `uv run pytest tests/test_learning_analysis.py -x -q` exits 0
    - `uv run pytest tests/test_architecture_enforcement.py -x -q` exits 0
    - `uv run ruff check services/learning_analysis.py tests/test_learning_analysis.py` exits 0
    - `uv run ruff format --check services/learning_analysis.py tests/test_learning_analysis.py` exits 0
    - `uv run basedpyright services/learning_analysis.py` exits 0
  </acceptance_criteria>
  <done>
    All seven detectors implemented; bloated_packets uses per-workflow median + stdev with bucketed summary text (Pitfall 6); route_misroutes joins route_result_json with success_criteria_findings; standards_regression reads priority_bucket='regressed'; dispatcher honors since bound across all seven; partial-result handling preserved; nine new tests + existing tests all green; LEARN-02 fully implemented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| services/learning_analysis.py reads -> per-run evidence tables | Read-only; no recommendation poisoning vector beyond what Phase 5/6/7/8 already accept (Pattern-Detection Poisoning STRIDE entry — evidence is written by managed hooks only). |
| Detector dispatch -> downstream Plan 04 conservative_optimizer | Plan 04 trusts the pattern_id stability invariant; Task 2 Test 10 + Task 3 Test 8 enforce it. |
| _detect_bloated_packets summary text -> operator UI | Summary uses bucketed descriptors; Task 3 Test 2 enforces no raw content leakage (Pitfall 6). |
| Per-project vs cross-project scope -> rationale field | The rationale text MUST cite which scope was used so reviewers know why a pattern was emitted; the metadata["scope_fallback"] tag is the audit trail. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Tampering | pattern_id drifts across runs because the hash includes a non-deterministic input | mitigate | Task 1 Test 5 + Task 2 Test 10 + Task 3 Test 8 — _stable_pattern_id input is fixed (signal_kind + scope_kind + scope_key + since_window_bucket). |
| T-09-08 | Information Disclosure | _detect_bloated_packets leaks raw selection_trace_json content into summary | mitigate | Task 3 Test 2 explicitly asserts no raw content; summary uses numeric buckets only (Pitfall 6). |
| T-09-09 | Tampering | Detector returns patterns spanning runs OUTSIDE the since window (Pitfall 5) | mitigate | Task 3 Test 8 cross-cuts all seven detectors against the since bound. |
| T-09-10 | Repudiation | route_misroute pattern cites the wrong runs as evidence (Pitfall 6) | mitigate | Task 3 Test 5 asserts evidence_run_ids matches exactly the run_ids that produced the blocker findings. |
| T-09-11 | Information Disclosure | Cross-project scope used silently for a per-project signal | mitigate | metadata["scope_fallback"]="cross-project" tag + summary text mentions the fallback when used (verified informally by Task 2 Test 3). |
| T-09-12 | Denial of Service | Unbounded SQL scan on long-running deployments (Pitfall 5) | mitigate | Every detector's SELECT has `WHERE ... AND created_at >= ?`; grep-counted in acceptance criteria. |
</threat_model>

<verification>
- `uv run pytest tests/test_learning_analysis.py tests/test_architecture_enforcement.py -x -q` exits 0
- `uv run ruff check services/learning_analysis.py tests/test_learning_analysis.py` exits 0
- `uv run ruff format --check services/learning_analysis.py tests/test_learning_analysis.py` exits 0
- `uv run basedpyright services/learning_analysis.py` exits 0
- `python -c "from services.learning_analysis import RecurringPattern, detect_recurring_patterns, DETECTORS, _stable_pattern_id, _bucket_window; assert len(DETECTORS) == 7"` exits 0
- `python -c "import sqlite3; from services.learning_analysis import detect_recurring_patterns; conn=sqlite3.connect(':memory:'); patterns=detect_recurring_patterns(conn, since='2026-01-01T00:00:00Z', project_id=None); assert isinstance(patterns, list)"` exits 0
- Quality ladder per CLAUDE.md: hard gates green.
</verification>

<success_criteria>
- LEARN-02: services/learning_analysis.py exposes RecurringPattern + detect_recurring_patterns; seven detectors map cleanly to repeated failures / ignored rules / bloated packets / weak prompts / weak workflows / route misroutes / standards regressions.
- Pitfall 5 closed: every detector uses bounded SELECT with created_at >= ?; cross-cutting test enforces.
- Pitfall 6 closed: bloated_packets summary uses bucketed descriptors; route_misroute evidence_run_ids cite specific runs.
- pattern_id stable across runs for cooling-period dedupe (Plan 04 prerequisite).
- Per-project default with cross-project fallback honors Anti-Patterns guidance.
</success_criteria>

<output>
After completion, create `.planning/phases/09-continuous-learning-and-conservative-optimization/09-02-SUMMARY.md` per the template, including:
- RecurringPattern field set + the seven detector signatures
- Module-level threshold constants (the seven `_*_THRESHOLD` / `_*_FLOOR` constants) and rationale for each value
- The pattern_id stability proof (paste of Task 2 Test 10 / Task 3 Test 8 assertions)
- The Pitfall 6 enforcement: how summary text is built without raw content
- The per-project / cross-project fallback pattern: metadata["scope_fallback"] tag + when it fires
- Note that Plan 04 will import RecurringPattern + detect_recurring_patterns and add a cooling-period dedupe layer
- Note that Plan 03 will reuse the IS_ACTIONABLE_SIGNAL filter pattern for the impact rollup's informational-signal handling
</output>
</content>
</invoke>