---
phase: 09-continuous-learning-and-conservative-optimization
plan: "06"
type: execute
wave: 4
autonomous: true
depends_on:
  - "09-01"
  - "09-04"
files_modified:
  - services/divergent_strategy.py
  - services/workflow_experiments.py
  - tests/test_divergent_strategy.py
  - tests/test_workflow_experiments.py
requirements:
  - LEARN-03
must_haves:
  truths:
    - "services/divergent_strategy.py replaces its direct transition_promotion_lifecycle call at line 784 (current head) with a call to services.workflow_promotion.propose_asset_promotion (Phase 8 surface); the divergent portfolio winner now enters as lifecycle state 'candidate' (NOT 'active' or freeform 'promoted'); the call emits one improvement_writebacks row with requires_approval=True and source='divergent_strategy' tagged in metadata."
    - "services/workflow_experiments.py post-experiment promotion (find via grep) is rewritten the same way: the experiment winner flows through propose_asset_promotion or propose_workflow_promotion (depending on whether the asset is a prompt/skill or a workflow) instead of direct lifecycle writes."
    - "Both wiring changes are defensive: if services/workflow_promotion.py is NOT yet present (Phase 8 partially shipped), the divergent and experiments paths fall back to their existing transition_promotion_lifecycle call AND emit a stderr warning explicitly noting the fallback (Pitfall 6 governance-tightening cannot regress the existing flow)."
    - "Both wiring changes update the corresponding test files: divergent_strategy test asserts the new propose-then-approve contract — divergent winner emits a writeback with requires_approval=True and the promotion_lifecycle_items row stays at status='proposed' or 'candidate' until approval — when services/workflow_promotion.py is present; otherwise the test asserts the fallback path with stderr warning."
    - "The wiring respects A4: the runtime path of route/packet recommendation does NOT change; only the post-experiment / post-divergent promotion semantics tighten."
  artifacts:
    - path: "services/divergent_strategy.py"
      provides: "extended portfolio-winner promotion path: prefer propose_asset_promotion via services.workflow_promotion; fallback to transition_promotion_lifecycle with stderr warning when Phase 8 surface absent"
    - path: "services/workflow_experiments.py"
      provides: "same propose-then-approve wiring for experiment winners; same fallback semantics"
    - path: "tests/test_divergent_strategy.py"
      provides: "test_divergent_winner_goes_through_propose_asset_promotion (when Phase 8 module present) + test_divergent_winner_fallback_emits_warning_when_phase8_absent"
    - path: "tests/test_workflow_experiments.py"
      provides: "test_experiment_winner_goes_through_propose_asset_promotion + test_experiment_winner_fallback_when_phase8_absent"
  key_links:
    - from: "services/divergent_strategy.py (line ~784)"
      to: "services.workflow_promotion.propose_asset_promotion"
      via: "try/ImportError detection of services.workflow_promotion; live path emits via propose_asset_promotion; fallback path emits stderr + transition_promotion_lifecycle"
      pattern: "propose_asset_promotion"
    - from: "services/workflow_experiments.py (post-scoring promotion call)"
      to: "services.workflow_promotion.propose_asset_promotion or propose_workflow_promotion"
      via: "branching on asset kind: skill/prompt → propose_asset_promotion; workflow → propose_workflow_promotion"
      pattern: "propose_asset_promotion|propose_workflow_promotion"
---

<objective>
Tighten the governance discipline for divergent-strategy and workflow-experiments winners. Both currently use freeform `transition_promotion_lifecycle` direct writes that predate Phase 5 governance and Phase 8 asset-lifecycle discipline. Plan 06 routes both through the Phase 8 propose_*_promotion entry points (when available) so portfolio winners emit reviewable writebacks instead of silently promoting. A defensive fallback preserves existing behavior when Phase 8 hasn't shipped, with an explicit stderr warning.

Purpose: LEARN-03 / Pattern 6 / Anti-Pattern "Treating divergent / experiment winners as automatic promotions" — divergent and experiment outcomes must flow through the same writeback approval contract every other Phase 9 proposal uses.

Output: services/divergent_strategy.py one-call-site rewrite + services/workflow_experiments.py same; two test-file updates with both live and fallback assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-continuous-learning-and-conservative-optimization/09-RESEARCH.md
@.planning/phases/08-prompt-skill-workflow-contracts-and-asset-lifecycle/08-04-PLAN.md
@.planning/phases/09-continuous-learning-and-conservative-optimization/09-01-PLAN.md
@.planning/phases/09-continuous-learning-and-conservative-optimization/09-04-PLAN.md

<interfaces>
<!-- Surfaces this plan composes against. -->

From services/workflow_promotion.py (Phase 8 Plan 08-04 deliverable — may not yet exist at execution time):
- `propose_asset_promotion(conn, *, asset_kind, asset_key, to_state, evidence, actor, rationale=None) -> dict[str, Any]` — emits one improvement_writebacks row + one promotion_lifecycle_items row with status="proposed"; returns {writeback_id, lifecycle_id, requires_approval=True}.
- `propose_workflow_promotion(conn, *, workflow_key, to_state, evidence, actor, rationale) -> dict[str, Any]` — same shape for workflows.
- `finalize_workflow_promotion` exists but is not called from Phase 9.

NOTE FOR EXECUTOR: If `services/workflow_promotion.py` is missing at execution time (Phase 8 didn't ship Plan 08-04 to completion), Plan 06 STILL ships but takes the fallback path documented in each task. The fallback uses the existing `transition_promotion_lifecycle` direct write with a stderr warning. Phase 10 cleans up the fallback once Phase 8 completes.

From services/divergent_strategy.py (verified during planning):
- `transition_promotion_lifecycle(conn, ...)` defined at line 578.
- Direct call at line 784: `transition_promotion_lifecycle(conn, item_id=..., to_status="promoted", actor="divergent_strategy", rationale=...)` — the call site Plan 06 rewrites.

From services/workflow_experiments.py (verified during planning, 779 lines):
- Post-experiment scoring emits a promotion via either a transition_promotion_lifecycle direct call or a workflow_skill_experiments status update — find the exact call site via `grep -n "transition_promotion_lifecycle\|promote\|lifecycle" services/workflow_experiments.py | head -10` during read_first.

From schema.sql:
- promotion_lifecycle_items columns (verified): id, item_kind, item_key, source_run_id, status, evidence_json, status_reason, created_at, updated_at, metadata_json.
- improvement_writebacks columns (Phase 5 / Plan 01).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite services/divergent_strategy.py portfolio-winner promotion path to use services.workflow_promotion.propose_asset_promotion with defensive fallback when Phase 8 surface absent</name>
  <files>services/divergent_strategy.py, tests/test_divergent_strategy.py</files>
  <read_first>
    - services/divergent_strategy.py — read the full block around line 784 (the existing direct `transition_promotion_lifecycle` call). Identify the surrounding context: which dataclass / dict holds the winning candidate, what `item_id`/`item_key` is passed, what the rationale text looks like, what the kind of asset is (prompt vs skill vs workflow). Read at least 50 lines above and below to understand inputs.
    - services/divergent_strategy.py — also read the `transition_promotion_lifecycle` definition near line 578 to confirm its current contract.
    - .planning/phases/08-prompt-skill-workflow-contracts-and-asset-lifecycle/08-04-PLAN.md — read the `propose_asset_promotion` and `propose_workflow_promotion` signatures and the expected return shape (writeback_id, lifecycle_id, requires_approval).
    - tests/test_divergent_strategy.py — find via `ls tests/test_divergent_strategy.py 2>/dev/null && head -120 tests/test_divergent_strategy.py`; identify the existing test that asserts the lifecycle status after a portfolio winner; this test will be UPDATED, not duplicated.
    - .planning/phases/09-continuous-learning-and-conservative-optimization/09-RESEARCH.md — Pattern 6 (Wiring Divergent / Experiment Winners Into Governed Promotion); Pitfall 6 (Divergent strategy wiring breaks existing behavior).
    - check `ls services/workflow_promotion.py 2>/dev/null` to determine whether Phase 8 surface exists at execution time and to choose the live vs fallback path tests will exercise.
  </read_first>
  <behavior>
    - Test 1 `test_divergent_winner_goes_through_propose_asset_promotion_when_phase8_present`: this test SKIPS itself unless services/workflow_promotion.py exists (use `pytest.importorskip` or an equivalent conditional). When present, simulate a divergent run with a winning candidate; invoke the relevant divergent function; assert:
       - one new improvement_writebacks row exists with requires_approval=1 and proposed_change_json containing source matching "divergent_strategy" in metadata
       - one new promotion_lifecycle_items row exists with status="proposed" (NOT "promoted") and item_kind matching the winning asset's kind
       - the direct `transition_promotion_lifecycle(..., to_status="promoted", ...)` was NOT called for the winning candidate (assert via a spy on the function or by checking the actual lifecycle row state)
    - Test 2 `test_divergent_winner_falls_back_when_phase8_missing`: this test runs only when services/workflow_promotion.py is ABSENT; if present, the test SKIPS or uses an explicit import path mock. Simulate the same divergent flow; assert:
       - a stderr warning was emitted containing the substring "Phase 8 workflow_promotion absent" (or similar — exact substring documented in the warning string)
       - the existing transition_promotion_lifecycle behavior fires (promotion_lifecycle_items row with status="promoted")
       - no improvement_writebacks row from Plan 06 is emitted (because the fallback uses the legacy path)
    - Test 3 `test_divergent_runtime_behavior_otherwise_unchanged`: aside from the one rewritten promotion call, the rest of the divergent strategy flow behaves identically (run-level / candidate-level / judgment-level rows are still written; the candidate selection logic is untouched). Use a sample fixture from existing tests as a regression baseline.
    - Test 4 `test_divergent_winner_asset_kind_routing`: when the winning candidate is a "skill", propose_asset_promotion is called with asset_kind="skill"; when "prompt", asset_kind="prompt"; when "workflow", propose_workflow_promotion is called instead. (Skip the workflow branch test if divergent never produces workflow-kind candidates today — confirm during read_first.)
  </behavior>
  <action>
    1. **At services/divergent_strategy.py around the existing transition_promotion_lifecycle call (line 784):** introduce a small helper or inline guarded import:
       ```python
       def _propose_via_workflow_promotion_if_available(
           conn, *, asset_kind, asset_key, to_state, evidence, actor, rationale
       ) -> dict | None:
           try:
               from services.workflow_promotion import (
                   propose_asset_promotion,
                   propose_workflow_promotion,
               )
           except ImportError:
               import sys
               print(
                   "[warn] Phase 8 workflow_promotion absent; falling back to "
                   "transition_promotion_lifecycle direct write. Promotion governance "
                   "is loose for this run; Phase 10 will tighten once Phase 8 ships.",
                   file=sys.stderr,
               )
               return None
           if asset_kind == "workflow":
               return propose_workflow_promotion(
                   conn,
                   workflow_key=asset_key,
                   to_state=to_state,
                   evidence=evidence,
                   actor=actor,
                   rationale=rationale,
               )
           return propose_asset_promotion(
               conn,
               asset_kind=asset_kind,
               asset_key=asset_key,
               to_state=to_state,
               evidence=evidence,
               actor=actor,
               rationale=rationale,
           )
       ```

    2. **Replace the direct `transition_promotion_lifecycle(conn, item_id=..., to_status="promoted", actor="divergent_strategy", rationale=...)` call (line 784)** with:
       ```python
       result = _propose_via_workflow_promotion_if_available(
           conn,
           asset_kind=<inferred from the winning candidate's kind>,
           asset_key=<winning candidate key>,
           to_state="candidate",  # winners enter as candidate, NOT active per Pitfall 6
           evidence={
             "source": "divergent_strategy",
             "divergent_run_id": divergent_run_id,
             "judge_scores": judge_scores,
             # any other already-in-scope evidence
           },
           actor="divergent_strategy",
           rationale="Portfolio winner with judge consensus",
       )
       if result is None:
           # Fallback path: legacy direct write (Pitfall 6 fallback)
           transition_promotion_lifecycle(
               conn,
               item_id=lifecycle_id,
               to_status="promoted",
               actor="divergent_strategy",
               rationale="Portfolio winner with judge consensus (Phase 8 unavailable; legacy promotion path)",
           )
       ```

    3. **Update tests/test_divergent_strategy.py** with the four behavior tests. The tests use `pytest.importorskip("services.workflow_promotion")` to switch between live and fallback. For the fallback test, use `monkeypatch.setattr` on the `_propose_via_workflow_promotion_if_available` helper to force the ImportError branch, then assert stderr contents via `capsys.readouterr().err`.

    4. **Quality ladder per touched file:** `uv run ruff check services/divergent_strategy.py tests/test_divergent_strategy.py && uv run ruff format --check services/divergent_strategy.py tests/test_divergent_strategy.py && uv run basedpyright services/divergent_strategy.py`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_divergent_strategy.py -x -q -k "divergent_winner or divergent_runtime_behavior_otherwise_unchanged"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_propose_via_workflow_promotion_if_available\|propose_asset_promotion\|propose_workflow_promotion" services/divergent_strategy.py | wc -l | tr -d ' '` returns at least 3 (helper + import + at least one call site)
    - `grep -n "Phase 8 workflow_promotion absent\|falling back to" services/divergent_strategy.py | wc -l | tr -d ' '` returns at least 1 (stderr fallback warning)
    - `grep -n "to_state=\"candidate\"\|to_state='candidate'" services/divergent_strategy.py | wc -l | tr -d ' '` returns at least 1 (winners enter as candidate per Pitfall 6)
    - `grep -c "def test_divergent_winner_goes_through_propose_asset_promotion_when_phase8_present\|def test_divergent_winner_falls_back_when_phase8_missing\|def test_divergent_runtime_behavior_otherwise_unchanged\|def test_divergent_winner_asset_kind_routing" tests/test_divergent_strategy.py | grep -v '^#'` returns at least 3 (asset_kind_routing optional if divergent only emits one kind today)
    - `uv run pytest tests/test_divergent_strategy.py -x -q` exits 0
    - `uv run pytest tests/test_architecture_enforcement.py -x -q` exits 0
    - `uv run ruff check services/divergent_strategy.py tests/test_divergent_strategy.py` exits 0
    - `uv run ruff format --check services/divergent_strategy.py tests/test_divergent_strategy.py` exits 0
    - `uv run basedpyright services/divergent_strategy.py` exits 0
  </acceptance_criteria>
  <done>
    services/divergent_strategy.py portfolio-winner promotion routes through services.workflow_promotion when Phase 8 is present; falls back to legacy path with stderr warning when absent; winners enter as "candidate" not "active"; three-to-four behavior tests pass; Pitfall 6 closed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply the same propose-then-approve wiring to services/workflow_experiments.py post-scoring promotion call; update tests/test_workflow_experiments.py accordingly</name>
  <files>services/workflow_experiments.py, tests/test_workflow_experiments.py</files>
  <read_first>
    - services/workflow_experiments.py — find via `grep -n "transition_promotion_lifecycle\|promotion\|promote\|to_status\|lifecycle" services/workflow_experiments.py | head -15` to locate the post-experiment promotion call site. If the file does NOT call transition_promotion_lifecycle directly (e.g., it uses workflow_skill_experiments.status updates instead), document that finding and skip Task 2 with an explicit note in the SUMMARY (the file still ships the test stub but the production wiring change collapses to a no-op).
    - services/divergent_strategy.py — confirm the helper from Task 1 if reuse is desired (it is private to divergent_strategy.py; Plan 06 duplicates the helper inline in workflow_experiments.py rather than introducing a new shared module).
    - tests/test_workflow_experiments.py — find via `ls tests/test_workflow_experiments.py 2>/dev/null && head -100 tests/test_workflow_experiments.py`.
    - .planning/phases/09-continuous-learning-and-conservative-optimization/09-RESEARCH.md — Pattern 6 (experiments are the second wiring target) + Pitfall 6.
    - .planning/phases/08-prompt-skill-workflow-contracts-and-asset-lifecycle/08-04-PLAN.md — propose_asset_promotion / propose_workflow_promotion signatures.
  </read_first>
  <behavior>
    - Test 1 `test_experiment_winner_goes_through_propose_asset_promotion_when_phase8_present`: parametrize over kind="prompt" and kind="skill"; simulate an experiment with a winning candidate of the given kind; invoke the post-scoring promotion path; assert:
       - one improvement_writebacks row exists with requires_approval=1 and metadata source matching "workflow_experiments"
       - one promotion_lifecycle_items row exists with status="proposed" (NOT promoted/active)
       - the legacy transition_promotion_lifecycle direct call was NOT executed
       - if Phase 8 surface absent (services/workflow_promotion.py missing), SKIP via pytest.importorskip
    - Test 2 `test_experiment_winner_falls_back_when_phase8_missing`: monkeypatch the helper to force the fallback; assert stderr warning emitted and legacy lifecycle write happens.
    - Test 3 `test_experiment_runtime_behavior_otherwise_unchanged`: aside from the promotion call site, the experiment flow (queue / run / score / candidate persistence) is unchanged. Use the existing test fixture as the regression baseline.
    - Test 4 (conditional — only if read_first finds that workflow_experiments.py does NOT currently call transition_promotion_lifecycle directly): `test_workflow_experiments_promotion_call_site_absent_documented`: assert that the SUMMARY documentation note exists (manual verification — this test becomes a docstring check or is omitted; in the latter case Plan 06 ships with a clearly documented "no-op for workflow_experiments — promotion already deferred to ad-hoc operator action" note in 09-06-SUMMARY.md).
  </behavior>
  <action>
    1. **Locate the post-experiment promotion call site in services/workflow_experiments.py** via the read_first grep. If found:
       - Duplicate the `_propose_via_workflow_promotion_if_available` helper inline inside services/workflow_experiments.py (or extract to a shared services-side module if the same helper is needed in 3+ places; Plan 06 prefers duplication of three similar lines per CLAUDE.md "three similar lines is better than a premature abstraction" rule).
       - Replace the direct `transition_promotion_lifecycle` call with the helper, branching on the experiment's asset kind (prompts → asset_kind="prompt"; skills → "skill").
       - Use `to_state="candidate"` (NOT active).
       - Tag evidence with `{"source": "workflow_experiments", "experiment_id": ..., "baseline_score": ..., "candidate_score": ...}`.

    2. **If the call site is NOT found** (workflow_experiments.py does not call transition_promotion_lifecycle directly): document this in the plan SUMMARY as a "no-op for workflow_experiments" note. The Task still completes by adding the four tests with the conditional Test 4 documenting the absence.

    3. **Update tests/test_workflow_experiments.py** with the four behavior tests. Use `pytest.importorskip("services.workflow_promotion")` for live-path tests and monkeypatch for fallback-path tests.

    4. **Quality ladder per touched file:** `uv run ruff check services/workflow_experiments.py tests/test_workflow_experiments.py && uv run ruff format --check services/workflow_experiments.py tests/test_workflow_experiments.py && uv run basedpyright services/workflow_experiments.py`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_workflow_experiments.py -x -q -k "experiment_winner or experiment_runtime_behavior_otherwise_unchanged or workflow_experiments_promotion_call_site_absent"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_propose_via_workflow_promotion_if_available\|propose_asset_promotion\|propose_workflow_promotion" services/workflow_experiments.py | wc -l | tr -d ' '` returns at least 1 (either the helper + call site OR explicit absence-documented comment block)
    - `grep -n "to_state=\"candidate\"\|to_state='candidate'" services/workflow_experiments.py | wc -l | tr -d ' '` returns at least 1 if the call site exists (else 0; the absence is documented)
    - `grep -c "def test_experiment_winner_goes_through_propose_asset_promotion_when_phase8_present\|def test_experiment_winner_falls_back_when_phase8_missing\|def test_experiment_runtime_behavior_otherwise_unchanged\|def test_workflow_experiments_promotion_call_site_absent_documented" tests/test_workflow_experiments.py | grep -v '^#'` returns at least 3 (Test 4 conditional)
    - `uv run pytest tests/test_workflow_experiments.py -x -q` exits 0
    - `uv run pytest tests/test_architecture_enforcement.py -x -q` exits 0
    - `uv run pytest tests/test_divergent_strategy.py -x -q` exits 0 (regression — Task 1 changes still pass)
    - `uv run ruff check services/workflow_experiments.py tests/test_workflow_experiments.py` exits 0
    - `uv run ruff format --check services/workflow_experiments.py tests/test_workflow_experiments.py` exits 0
    - `uv run basedpyright services/workflow_experiments.py` exits 0
  </acceptance_criteria>
  <done>
    services/workflow_experiments.py post-experiment promotion is either rewritten to use propose_asset_promotion / propose_workflow_promotion (live path) OR confirmed via read_first as having no direct lifecycle write (no-op documented); fallback path with stderr warning preserved; three-to-four behavior tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| services/divergent_strategy.py -> services/workflow_promotion.py | When Phase 8 surface present, divergent winners route through propose_asset_promotion → improvement_writebacks. When absent, fallback to legacy transition_promotion_lifecycle direct write with stderr warning. |
| services/workflow_experiments.py -> services/workflow_promotion.py | Same pattern as divergent. |
| Live vs fallback path -> tests | Conditional via pytest.importorskip; the tests cover BOTH paths so the rewrite ships safely regardless of Phase 8 status at execution time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-29 | Tampering | Divergent winner silently promoted to "active" without operator review (Pattern 6 + Pitfall 6) | mitigate | Task 1 Test 1 + the to_state="candidate" hardcoded value; the live path requires approval before active. |
| T-09-30 | Repudiation | Phase 8 partially shipped → divergent winners promoted via legacy path WITHOUT operator notice | mitigate | Stderr warning emitted in the fallback branch; Task 1 Test 2 asserts the warning text. Operators see "Phase 8 workflow_promotion absent" message. |
| T-09-31 | Integrity | Existing divergent run/candidate/judgment row writes regress due to the rewrite | mitigate | Task 1 Test 3 + Task 2 Test 3 (regression baselines) — only the promotion call site changes; all other writes are untouched. |
| T-09-32 | Tampering | Experiment winner promoted as "active" bypassing review | mitigate | Task 2 Test 1 + to_state="candidate" hardcoded; if workflow_experiments.py has no direct call site (the absence case), no behavior change is introduced. |
| T-09-33 | Information Disclosure | The stderr warning leaks Phase 8 implementation status to the operator | accept | The warning is intentional operator-visible signal — Phase 8 is a known dependency, and visibility into the fallback state aids triage. |
</threat_model>

<verification>
- `uv run pytest tests/test_divergent_strategy.py tests/test_workflow_experiments.py tests/test_architecture_enforcement.py -x -q` exits 0
- `uv run ruff check services/divergent_strategy.py services/workflow_experiments.py tests/test_divergent_strategy.py tests/test_workflow_experiments.py` exits 0
- `uv run ruff format --check services/divergent_strategy.py services/workflow_experiments.py tests/test_divergent_strategy.py tests/test_workflow_experiments.py` exits 0
- `uv run basedpyright services/divergent_strategy.py services/workflow_experiments.py` exits 0
- `python -c "import services.divergent_strategy as d; import services.workflow_experiments as e"` exits 0
- Quality ladder per CLAUDE.md: hard gates green.
</verification>

<success_criteria>
- LEARN-03 (governance-tightening complete): divergent and experiment winners flow through the same writeback approval contract every Phase 9 proposal uses (when Phase 8 ships) OR continue with legacy behavior plus explicit operator-visible warning (when Phase 8 partially shipped).
- Pitfall 6 closed: divergent / experiment winners enter as "candidate" not "active"; manual approval required.
- Defensive fallback preserves brownfield continuity: existing tests pass in both live and fallback path scenarios.
- A4 invariant preserved: route/packet recommender runtime is untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/09-continuous-learning-and-conservative-optimization/09-06-SUMMARY.md` per the template, including:
- The exact call-site rewrites in services/divergent_strategy.py (line ~784) and services/workflow_experiments.py
- The _propose_via_workflow_promotion_if_available helper shape and the rationale for duplicating it in both files (CLAUDE.md "three similar lines" rule)
- The fallback warning text and the conditions under which it fires
- Whether workflow_experiments.py needed a real rewrite or just a no-op note (decided during read_first)
- The conditional-skip approach for live vs fallback tests (pytest.importorskip)
- Note that once Phase 8 fully ships, the fallback can be removed; Phase 10 owns the cleanup
- Note that this wiring change tightens governance discipline AND breaks the prior assertion that divergent winner status="promoted" (Pitfall 6 — test updates explicitly assert the new contract)
</output>
</content>
</invoke>