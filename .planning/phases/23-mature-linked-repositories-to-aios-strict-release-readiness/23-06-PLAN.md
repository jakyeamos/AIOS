---
phase: 23-mature-linked-repositories-to-aios-strict-release-readiness
plan: "06"
type: execute
wave: 3
autonomous: false
depends_on:
  - "23-02"
files_modified:
  - config/quality-gates.json
  - config/quality-pipeline.json
  - config/architecture-enforcement/projects.json
requirements:
  - TBD
must_haves:
  truths:
    - "Content/vault/container repos use content or aggregate validation, not fake application gates."
    - "BBDSE's git diff --check floor is replaced by aggregate subproject validation or explicit delegated subproject gates."
  artifacts:
    - path: "docs/audits/linked-repo-adoption-readiness-audit.md"
      provides: "Updated content/vault/container evidence rows."
---

# Plan 23-06: Content Vault And Container Repo Maturation

## Objective

Mature content and container repos without pretending they are application repos.

## Repos

`Vaults`, `BBDSE`

## Scope

- Add vault/content validation for `Vaults`, such as link/frontmatter/path checks.
- Replace BBDSE's `git diff --check` floor with aggregate validation or delegated gates for source subprojects such as `LIS`.
- Add quality-pipeline evidence and documented CI/default-branch proof or non-remote exception.

## Verification

- `BBDSE` no longer has `floor_only`.
- `Vaults` no longer has `pre_cr_only`.
- Both repos have class-specific validation evidence.
