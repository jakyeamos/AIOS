---
phase: 21-macos-native-app-skill-pack
plan: "05"
type: execute
wave: 5
autonomous: true
depends_on:
  - "21-03"
  - "21-04"
files_modified:
  - skills/macos/settings-window/SKILL.md
  - skills/macos/sparkle-auto-update/SKILL.md
  - skills/macos/notch-overlay/SKILL.md
  - tests/skills/macos/test_settings_window.md
  - tests/skills/macos/test_sparkle_auto_update.md
  - tests/skills/macos/test_notch_overlay.md
requirements:
  - MACS-05
must_haves:
  truths:
    - "macos-settings-window includes SwiftUI/AppKit environment guard, existing lifecycle discovery, NSWindowController vs SwiftUI Window scene guidance, architecture preservation, minimal file policy, macOS 26 fallback, duplicate settings window checks, menu-bar activation behavior checks, and approval gate before replacing existing settings architecture."
    - "macos-sparkle-auto-update confirms non-Mac-App-Store distribution, detects existing Sparkle dependency, gates dependency addition, gates Info.plist edits, gates appcast creation, protects private signing keys, handles SUPublicEDKey placeholders, DEBUG guards, updater controller timing, and menu-bar activation policy."
    - "macos-notch-overlay detects notch availability safely, handles non-notch/external display fallback, NSPanel vs NSWindow decisions, window-level warnings, click-through behavior, multi-monitor behavior, frame vs visibleFrame decisions, reduced-motion, and manual verification."
    - "Each skill invokes macOS project detection and build verification where relevant and includes repair recommendations on validation failure."
  artifacts:
    - path: "skills/macos/settings-window/SKILL.md"
      provides: "Guarded settings/preferences window skill."
    - path: "skills/macos/sparkle-auto-update/SKILL.md"
      provides: "Guarded Sparkle auto-update skill."
    - path: "skills/macos/notch-overlay/SKILL.md"
      provides: "Guarded notch overlay skill."
  key_links:
    - from: "skills/macos/sparkle-auto-update/SKILL.md"
      to: "skills/macos/build-verify/SKILL.md"
      via: "Sparkle changes require build verification."
      pattern: "Sparkle"
---

# Plan 21-05: Settings, Sparkle, And Notch Skills

## Objective

Create three guarded macOS app implementation skills: settings windows, Sparkle auto-update, and notch overlays.

## Scope

Implement only task-shaped, safety-gated skills. Do not add broad macOS reference context.

## Verification

- Settings tests cover new settings, repair existing settings, menu-bar activation behavior, and macOS 26 unavailable fallback.
- Sparkle tests cover missing dependency, existing dependency, missing Info.plist keys, missing DEBUG guard, appcast URL mismatch, and private key leakage checks.
- Notch tests cover notch Mac, non-notch Mac, external monitor, click-through overlay, and incorrect visibleFrame usage.
