---
phase: 21-macos-native-app-skill-pack
plan: "08"
type: execute
wave: 8
autonomous: true
depends_on:
  - "21-07"
files_modified:
  - tests/skills/macos/
  - scripts/validate-skills.sh
requirements:
  - MACS-08
must_haves:
  truths:
    - "Behavioral tests cover build detection, native pattern routing, settings UI, Sparkle, notch overlay, and release safety scenarios from the source spec."
    - "Tests verify correct skill selection, unnecessary modules not loaded, permission gates before destructive commands, validation step inclusion, and repair recommendation generation on failure."
    - "Validation command checks unique skill IDs, TMCP pointers, referenced modules, source provenance entries, permission-gated actions, behavioral tests, private-key patterns, broad macOS triggers, and release publish gates."
    - "Validation integrates with existing repo validation if present; otherwise `scripts/validate-skills.sh` is created."
  artifacts:
    - path: "tests/skills/macos/"
      provides: "Behavioral test scenarios for macOS skill pack."
    - path: "scripts/validate-skills.sh"
      provides: "Skill-pack validation command if no existing validation command is available."
  key_links:
    - from: "scripts/validate-skills.sh"
      to: "config/skills/macos-manifest.json"
      via: "Validation checks manifest, provenance, gates, and route pointers."
      pattern: "validate-skills"
---

# Plan 21-08: Behavioral Tests And Validation Command

## Objective

Add test and validation coverage proving the macOS skill pack routes correctly, remains safety-gated, and does not load broad context by default.

## Scope

Create tests under the discovered convention or `tests/skills/macos/` if no convention exists.

## Required Scenario Families

- Build detection
- Native pattern routing
- Settings UI
- Sparkle
- Notch overlay
- Release

## Verification

- Behavioral tests pass.
- Validation command passes.
- Private-key patterns are not present in committed files.
- Release skill cannot publish without explicit approval.
