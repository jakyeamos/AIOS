---
phase: 21-macos-native-app-skill-pack
plan: "07"
type: execute
wave: 7
autonomous: true
depends_on:
  - "21-03"
  - "21-04"
  - "21-05"
  - "21-06"
files_modified:
  - config/tmcp/macos-skills-router.json
  - config/skills/macos-manifest.json
  - docs/skills/macos-dependency-map.md
requirements:
  - MACS-07
must_haves:
  truths:
    - "TMCP routing maps build/compile/check build to macos-project-detection then macos-build-verify; macOS architecture questions to macos-native-patterns-router; settings/preferences to project detection, settings-window, and build verification; Sparkle/appcast to project detection, sparkle-auto-update, and build verification; notch overlay to project detection, notch-overlay, and build verification; release/publish to project detection, release-pipeline, permission gate, dry-run, approval, then execution."
    - "Strict node IDs exist for @task:macos.project.detect, @task:macos.build.verify, @task:macos.patterns.route, @task:macos.settings.window, @task:macos.sparkle.auto_update, @task:macos.notch.overlay, @task:macos.release.pipeline and all required @module:macos.* modules unless existing conventions require a different prefix."
    - "Manifest entries include id, title, purpose, triggers, anti-triggers, dependencies, required tools, permission gates, validation commands, source provenance, freshness/version notes, behavioral tests, and related modules."
    - "Provenance includes source repository, source path, transformation type, notes on what changed, license note, and date audited."
  artifacts:
    - path: "config/tmcp/macos-skills-router.json"
      provides: "Task-first TMCP routes for macOS skills."
    - path: "config/skills/macos-manifest.json"
      provides: "Manifest/provenance for macOS skills/modules."
    - path: "docs/skills/macos-dependency-map.md"
      provides: "Dependency map and related-module pointers."
  key_links:
    - from: "config/tmcp/macos-skills-router.json"
      to: "config/skills/macos-manifest.json"
      via: "Router references manifest node IDs."
      pattern: "@task:macos"
---

# Plan 21-07: TMCP Routing, Manifests, Provenance, And Dependency Maps

## Objective

Make the macOS skill pack discoverable and auditable through TMCP routing, manifests, strict node IDs, provenance, and dependency maps.

## Scope

Use actual paths discovered in Plan 21-01. If no TMCP convention exists, create the smallest compatible routing/manifest files.

## Verification

- All skill IDs are unique.
- All TMCP pointers resolve.
- All referenced modules exist.
- All provenance entries exist.
- No broad "always load for any macOS app" triggers remain.
