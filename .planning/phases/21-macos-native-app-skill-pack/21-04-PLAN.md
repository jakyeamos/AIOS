---
phase: 21-macos-native-app-skill-pack
plan: "04"
type: execute
wave: 4
autonomous: true
depends_on:
  - "21-01"
  - "21-02"
files_modified:
  - skills/macos/native-patterns-router/SKILL.md
  - skills/macos/modules/
  - tests/skills/macos/test_native_pattern_routing.md
requirements:
  - MACS-04
must_haves:
  truths:
    - "macos-native-patterns-router routes to small semantic modules only when needed and does not load broad macOS knowledge by default."
    - "Semantic modules exist for menu bar, window/panel, screen geometry, keyboard shortcuts, file picker, pasteboard, drag/drop, navigation/inspector, launch/login, Quick Look/workspace, ScreenCaptureKit, and UserDefaults/AppStorage."
    - "Each module includes when to use, when not to use, common wrong web mental model, correct native macOS pattern, minimal code pattern, validation notes, related modules, and donor provenance pointer."
    - "Routing tests cover menu bar app, floating overlay, global keyboard shortcut, clipboard/pasteboard, and web mental models such as z-index or navigator.clipboard."
  artifacts:
    - path: "skills/macos/native-patterns-router/SKILL.md"
      provides: "Task-first router for native macOS pattern modules."
    - path: "skills/macos/modules/"
      provides: "Small semantic macOS pattern modules."
  key_links:
    - from: "skills/macos/native-patterns-router/SKILL.md"
      to: "skills/macos/modules/"
      via: "Router loads only relevant modules."
      pattern: "macos-native-patterns-router"
---

# Plan 21-04: Native macOS Pattern Router And Semantic Modules

## Objective

Split broad native macOS pattern knowledge into small task-routed modules so agents load only what the task requires.

## Scope

Create the router and semantic modules following discovered AIOS/TMCP skill conventions.

## Required Modules

- `macos-menu-bar-patterns`
- `macos-window-panel-patterns`
- `macos-screen-geometry-patterns`
- `macos-keyboard-shortcuts-patterns`
- `macos-file-picker-patterns`
- `macos-pasteboard-patterns`
- `macos-drag-drop-patterns`
- `macos-navigation-inspector-patterns`
- `macos-launch-login-patterns`
- `macos-quicklook-workspace-patterns`
- `macos-screencapturekit-patterns`
- `macos-userdefaults-appstorage-patterns`

## Verification

- Tests prove correct module selection.
- Tests prove unnecessary modules are not loaded.
- Tests cover web mental model corrections.
