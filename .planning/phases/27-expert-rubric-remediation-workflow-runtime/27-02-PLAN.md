---
phase: 27-expert-rubric-remediation-workflow-runtime
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - config/workflows/registry.json
  - config/workflows/skills.json
  - tests/test_workflow_orchestration.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "The expert rubric remediation workflow loads from the registry as an active audit-and-plan workflow."
    - "All required expert workflow skills load from the skill registry and match their allowed stage kinds."
    - "Workflow required validations cover TMCP packet, rubric dimensions, evidence-backed findings, and remediation verification."
  artifacts:
    - path: config/workflows/registry.json
      provides: "expert_rubric_remediation_v1 workflow contract"
    - path: config/workflows/skills.json
      provides: "expert review and validation skill specs"
    - path: tests/test_workflow_orchestration.py
      provides: "registry contract regression coverage"
  key_links:
    - from: config/workflows/registry.json
      to: config/workflows/skills.json
      via: "stage required_skills keys"
    - from: workflow.required_validations
      to: validate-stage skill keys
      via: "tmcp_packet_compiled, rubric_dimensions_present, findings_have_evidence, remediation_has_verification"
---

<objective>
Register expert rubric remediation as a first-class active workflow and skill set.

Purpose: Runtime dispatch from Plan 27-01 must be reachable through normal AIOS workflow registry loading and validation.
Output: Workflow and skill registry entries plus registry contract tests.
</objective>

<execution_context>
@/Users/jakyeamos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakyeamos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-expert-rubric-remediation-workflow-runtime/27-RESEARCH.md
@.planning/phases/27-expert-rubric-remediation-workflow-runtime/27-VALIDATION.md
@.planning/phases/27-expert-rubric-remediation-workflow-runtime/27-01-SUMMARY.md
@docs/superpowers/specs/2026-06-24-expert-rubric-remediation-design.md
@docs/superpowers/plans/2026-06-24-expert-rubric-remediation.md

<interfaces>
From `services/workflow_orchestration.py`:
- `load_workflow_registry(path) -> dict[str, WorkflowSpec]`
- `load_skill_registry(path) -> dict[str, SkillSpec]`
- `validate_workflow_bindings(workflows, skills) -> list[str]`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add registry contract test</name>
  <files>tests/test_workflow_orchestration.py</files>
  <read_first>
    - tests/test_workflow_orchestration.py
    - services/workflow_orchestration.py
    - docs/superpowers/specs/2026-06-24-expert-rubric-remediation-design.md
  </read_first>
  <behavior>
    - Registry loads `expert_rubric_remediation_v1`.
    - Workflow family is `audit_and_plan`.
    - Stage keys are exactly `expertise_compile`, `rubric_synthesize`, `evidence_audit`, `remediation_plan`, `implementation_handoff`, `artifact_validate`.
    - Required validations match the design spec.
  </behavior>
  <action>Add `test_expert_review_workflow_registry_contract` to `tests/test_workflow_orchestration.py`. The test must call `load_workflow_registry`, `load_skill_registry`, and `validate_workflow_bindings`, then assert the exact family, stage key list, and required validations from the approved implementation plan. Run this test before editing registries and confirm it fails because the workflow registry does not yet provide the final contract.</action>
  <verify>
    <automated>uv run pytest tests/test_workflow_orchestration.py::test_expert_review_workflow_registry_contract -q</automated>
  </verify>
  <done>The failing test exists and encodes the final workflow registry contract.</done>
  <acceptance_criteria>
    - Test asserts `validate_workflow_bindings(workflows, skills) == []`.
    - Test asserts the six exact stage keys in order.
    - Test asserts required validations are the four expert validation keys.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add workflow and skill registry entries</name>
  <files>config/workflows/registry.json, config/workflows/skills.json, tests/test_workflow_orchestration.py</files>
  <read_first>
    - config/workflows/registry.json
    - config/workflows/skills.json
    - services/workflow_orchestration.py
    - tests/test_workflow_orchestration.py
    - docs/superpowers/plans/2026-06-24-expert-rubric-remediation.md
  </read_first>
  <behavior>
    - `config/workflows/skills.json` contains all expert generation/finalize/validate skills with `lifecycle_state: active`.
    - `config/workflows/registry.json` contains `expert_rubric_remediation_v1` with the six stages and expected artifacts.
    - Registry validation passes and the Plan 27-01 runtime execution test passes.
  </behavior>
  <action>Append the expert skill entries and workflow object from approved implementation plan Task 4. Preserve valid JSON formatting and existing registry order. Ensure each skill's `allowed_stages` matches its workflow stage kind: `tmcp_expertise_compiler` uses `enrich_context`; rubric and audit skills use `generate`; remediation and handoff builders use `finalize`; validation skills use `validate`. Set workflow `implementation_bearing` to false and include the approval gate on `implementation_handoff` with `impact_scope: workflow-default` and `condition: always`.</action>
  <verify>
    <automated>uv run pytest tests/test_workflow_orchestration.py::test_expert_review_workflow_registry_contract tests/test_workflow_orchestration.py::test_expert_review_workflow_executes_with_artifacts -q</automated>
    <automated>uv run ruff check tests/test_workflow_orchestration.py</automated>
  </verify>
  <done>The registry contract and runtime execution tests pass together.</done>
  <acceptance_criteria>
    - `config/workflows/registry.json` includes workflow key `expert_rubric_remediation_v1`.
    - `config/workflows/skills.json` includes all nine expert skill/validation keys from the approved plan.
    - `validate_workflow_bindings(load_workflow_registry(...), load_skill_registry(...))` returns an empty list.
    - `test_expert_review_workflow_executes_with_artifacts` passes after registry entries exist.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registry JSON -> runtime workflow execution | Declarative workflow and skill data controls what runtime dispatch executes. |
| Workflow required validations -> stage status | Missing or mismatched validation keys can produce false success. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04 | Tampering | `config/workflows/registry.json` | mitigate | Add a registry contract test that validates exact stage keys and required validations. |
| T-27-05 | Elevation of Privilege | `implementation_handoff` stage | mitigate | Declare approval gate with `condition: always` and keep workflow `implementation_bearing: false`. |
| T-27-06 | Repudiation | validate stage | mitigate | Require explicit validation skill keys so `execute_workflow` records pass/fail validation dictionaries. |
</threat_model>

<verification>
Run:
- `uv run pytest tests/test_workflow_orchestration.py::test_expert_review_workflow_registry_contract tests/test_workflow_orchestration.py::test_expert_review_workflow_executes_with_artifacts -q`
- `uv run ruff check tests/test_workflow_orchestration.py`
</verification>

<success_criteria>
- Expert workflow and skills are active registry assets.
- Registry validation catches missing or invalid skill bindings.
- Runtime execution passes through the registered workflow contract.
- No CLI behavior is added in this phase plan.
</success_criteria>

<output>
After completion, create `.planning/phases/27-expert-rubric-remediation-workflow-runtime/27-02-SUMMARY.md`.
</output>
