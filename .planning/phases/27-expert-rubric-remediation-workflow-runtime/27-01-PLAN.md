---
phase: 27-expert-rubric-remediation-workflow-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/workflow_orchestration.py
  - tests/test_workflow_orchestration.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "The workflow runtime can execute expert review skills from a TMCP packet and evidence items."
    - "The execution report exposes expert rubric, audit, remediation, handoff, and artifact path outputs."
    - "Missing required expert workflow inputs fail explicitly instead of producing empty artifacts."
  artifacts:
    - path: services/workflow_orchestration.py
      provides: "WorkflowExecutionContext inputs and deterministic expert skill dispatch"
    - path: tests/test_workflow_orchestration.py
      provides: "Runtime execution regression for expert review artifacts"
  key_links:
    - from: WorkflowExecutionContext.tmcp_packet
      to: tmcp_expertise_compiler
      via: "run_state['tmcp_packet'] and state['expertise_packet']"
    - from: WorkflowExecutionContext.evidence_items
      to: expert_evidence_auditor
      via: "run_state['review_evidence_items']"
    - from: expert_implementation_handoff_builder
      to: report["artifacts"]["expert_review_artifact_paths"]
      via: "write_review_artifacts output path map"
---

<objective>
Wire Phase 26 artifact builders into the workflow runtime skill dispatcher.

Purpose: The registry in Plan 27-02 needs real runtime behavior for each expert review skill.
Output: Extended `WorkflowExecutionContext`, expert skill dispatch branches, expert artifacts in execution reports, and runtime tests.
</objective>

<execution_context>
@/Users/jakyeamos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakyeamos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-expert-rubric-remediation-workflow-runtime/27-RESEARCH.md
@.planning/phases/27-expert-rubric-remediation-workflow-runtime/27-VALIDATION.md
@.planning/phases/26-expert-rubric-remediation-core-artifacts/26-01-SUMMARY.md
@.planning/phases/26-expert-rubric-remediation-core-artifacts/26-02-SUMMARY.md
@docs/superpowers/specs/2026-06-24-expert-rubric-remediation-design.md
@docs/superpowers/plans/2026-06-24-expert-rubric-remediation.md

<interfaces>
From `services/workflow_orchestration.py`:
- `WorkflowExecutionContext` currently carries objective, workflow key, runtime metadata, and `tmcp_packet`.
- `_execute_skill(skill, stage, *, state, context) -> tuple[dict[str, Any], dict[str, Any] | None]`
- `execute_workflow(...)` builds `run_state`, stage reports, validations, and final `report["artifacts"]`.

From Phase 26 service:
- `synthesize_rubric`, `build_audit_report`, `build_remediation_plan`, `build_implementation_handoff`
- `validate_rubric`, `validate_audit_report`, `validate_remediation_plan`
- `write_review_artifacts`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add runtime execution test and context fields</name>
  <files>tests/test_workflow_orchestration.py, services/workflow_orchestration.py</files>
  <read_first>
    - tests/test_workflow_orchestration.py
    - services/workflow_orchestration.py
    - services/expert_rubric_remediation.py
  </read_first>
  <behavior>
    - `WorkflowExecutionContext` accepts `evidence_items: tuple[dict[str, Any], ...] = ()`.
    - `WorkflowExecutionContext` accepts `selected_slice_id: str | None = None`.
    - The new execution test can construct a context with TMCP packet, evidence item, and selected slice.
  </behavior>
  <action>Add `test_expert_review_workflow_executes_with_artifacts` to `tests/test_workflow_orchestration.py` using the approved implementation plan Task 3 shape. Extend `WorkflowExecutionContext` in `services/workflow_orchestration.py` with `evidence_items` and `selected_slice_id`, and seed `run_state` with `review_evidence_items` plus `selected_slice_id`. Run the new test before adding expert skill dispatch and confirm the failure is the next missing runtime/registry behavior, not a dataclass constructor failure.</action>
  <verify>
    <automated>uv run pytest tests/test_workflow_orchestration.py::test_expert_review_workflow_executes_with_artifacts -q</automated>
  </verify>
  <done>The context accepts expert review inputs and the execution test progresses beyond context construction.</done>
  <acceptance_criteria>
    - `WorkflowExecutionContext` has `evidence_items: tuple[dict[str, Any], ...] = ()`.
    - `WorkflowExecutionContext` has `selected_slice_id: str | None = None`.
    - `execute_workflow` run_state includes `review_evidence_items` and `selected_slice_id`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dispatch expert workflow skills and expose report artifacts</name>
  <files>services/workflow_orchestration.py, tests/test_workflow_orchestration.py</files>
  <read_first>
    - services/workflow_orchestration.py
    - services/expert_rubric_remediation.py
    - tests/test_workflow_orchestration.py
  </read_first>
  <behavior>
    - `tmcp_expertise_compiler` stores `expertise_packet` and returns validation key `tmcp_packet_compiled`.
    - Expert generate/finalize skills create rubric, audit report, remediation plan, implementation handoff, and artifact paths.
    - Expert validate skills call Phase 26 validators and contribute required validations.
  </behavior>
  <action>Import Phase 26 service functions into `services/workflow_orchestration.py`. Add `_execute_skill` branches for `tmcp_expertise_compiler`, `expert_rubric_synthesizer`, `expert_evidence_auditor`, `expert_remediation_planner`, `expert_implementation_handoff_builder`, `tmcp_packet_compiled`, `rubric_dimensions_present`, `findings_have_evidence`, and `remediation_has_verification`. Use `context.run_id or "expert-review-preview"` consistently. For artifact writes, use `Path(context.repo_path or ".") / ".aios" / "reviews" / run_id`. Add expert artifact keys to the final `report["artifacts"]` map. Do not create registry entries in this task.</action>
  <verify>
    <automated>uv run pytest tests/test_workflow_orchestration.py::test_expert_review_workflow_executes_with_artifacts -q</automated>
    <automated>uv run ruff check services/workflow_orchestration.py tests/test_workflow_orchestration.py</automated>
  </verify>
  <done>The expert runtime test reaches the expected registry failure before Plan 27-02, or passes after Plan 27-02 registry entries exist; dispatch code is deterministic and report artifacts are exposed.</done>
  <acceptance_criteria>
    - Final report artifacts include `expertise_packet`, `expert_rubric`, `expert_audit_report`, `expert_remediation_plan`, `expert_implementation_handoff`, and `expert_review_artifact_paths`.
    - Missing TMCP packet raises `ValueError("tmcp_expertise_compiler requires context.tmcp_packet")`.
    - Validate-stage skills return validation dictionaries with keys matching workflow `required_validations`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WorkflowExecutionContext -> run_state | Caller-provided packet/evidence enters runtime stage state. |
| run_state -> filesystem artifacts | Runtime writes review artifacts under the target repo path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Tampering | `tmcp_expertise_compiler` | mitigate | Raise `ValueError` when no TMCP packet exists instead of synthesizing ungrounded expertise. |
| T-27-02 | Information Disclosure | artifact output root | accept | Runtime writes only caller-provided packet/evidence-derived artifacts into local `.aios/reviews/{run_id}`; no repo scan occurs in this plan. |
| T-27-03 | Repudiation | report artifacts | mitigate | Expose artifact paths and validation results in the workflow report so generated outputs remain inspectable. |
</threat_model>

<verification>
Run:
- `uv run pytest tests/test_workflow_orchestration.py::test_expert_review_workflow_executes_with_artifacts -q`
- `uv run ruff check services/workflow_orchestration.py tests/test_workflow_orchestration.py`
</verification>

<success_criteria>
- Workflow runtime can execute expert review skill keys against Phase 26 service functions.
- Runtime report exposes all expert review artifacts and artifact paths.
- Missing required inputs fail explicitly.
- Registry and route behavior remain separate follow-up plans.
</success_criteria>

<output>
After completion, create `.planning/phases/27-expert-rubric-remediation-workflow-runtime/27-01-SUMMARY.md`.
</output>
