---
phase: 19-native-workflow-command-pack
plan: "04"
type: execute
wave: 4
autonomous: true
depends_on:
  - "19-02"
  - "19-03"
files_modified:
  - services/native_commands.py
  - services/aios_cli.py
  - tests/test_native_commands.py
  - tests/test_aios_cli.py
requirements:
  - CMDP-06
must_haves:
  truths:
    - "`aios audit security` is read-only by default and supports branch diff and selected file scopes."
    - "`aios audit security --mode strict` reports critical/high findings only."
    - "`aios audit security --mode practical` reports critical/high/medium findings relevant to the actual project, including personal projects, auth, privacy, data handling, secrets, local automation, and filesystem access."
    - "Security findings include severity, affected files, issue, why it matters, exploit or failure scenario, recommended fix, and confidence level."
    - "Output includes non-issues checked and suggested verification; it does not produce vague generic security advice."
  artifacts:
    - path: "services/native_commands.py"
      provides: "Security audit command implementation or dispatch helper."
    - path: "tests/test_native_commands.py"
      provides: "Coverage for strict/practical mode filtering and required finding fields."
  key_links:
    - from: "services/native_commands.py:security_audit"
      to: "config/commands/native-workflow-commands.json"
      via: "Command metadata defines mode schema and read-only safety class."
      pattern: "security"
---

# Plan 19-04: Targeted Security Audit Command

## Objective

Implement `aios audit security` as a contextual read-only security review command with strict and practical modes.

## Scope

Extend the native command implementation from Plan 19-03.

Prioritize:

- secrets
- auth/authz
- permissions
- filesystem access
- dependency risk
- data privacy
- injection
- unsafe shell execution
- destructive automation

## Verification

- Tests cover strict mode.
- Tests cover practical mode.
- Tests prove generic checklist-only output is not accepted by schema/required fields.
