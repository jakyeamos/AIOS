---
phase: 25-make-aios-planning-a-governed-first-class-workflow
plan: "02"
type: execute
wave: 2
depends_on:
  - "25-01"
files_modified:
  - config/workflows/registry.json
  - config/workflows/skills.json
  - services/workflow_orchestration.py
  - services/task_routing.py
  - tests/test_workflow_orchestration.py
  - tests/test_task_routing.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "AIOS start-work can select a governed planning workflow for GSD planning objectives instead of route-blocking."
    - "Weak or ambiguous routes still block before packet creation."
  artifacts:
    - path: "config/workflows/registry.json"
      provides: "Active governed planning workflow contract."
    - path: "config/workflows/skills.json"
      provides: "Planning workflow skill bindings if a new planning skill key is needed."
    - path: "services/workflow_orchestration.py"
      provides: "Route candidate ranking that consumes planning workflow evidence."
    - path: "services/task_routing.py"
      provides: "Ready/blocked routing result that preserves project and workflow safety gates."
  key_links:
    - from: "services/planning_workflow_detection.py"
      to: "services/workflow_orchestration.py"
      via: "planning detection consumed by route candidate ranking"
    - from: "config/workflows/registry.json"
      to: "services.workflow_orchestration.load_workflow_registry"
      via: "active planning workflow entry"
    - from: "services/workflow_orchestration.py"
      to: "services/task_routing.py"
      via: "recommend_route_primitives() result"
---

<objective>
Make governed workflow selection route planning objectives through an active planning workflow.

Purpose: Detection alone does not fix `start-work`; route selection must choose a governed workflow and keep existing route-blocking safeguards.
Output: Active planning workflow registry entry, route ranking integration, and regression tests.
</objective>

<execution_context>
@/Users/jakyeamos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakyeamos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/25-make-aios-planning-a-governed-first-class-workflow/25-RESEARCH.md
@config/workflows/registry.json
@config/workflows/skills.json
@services/workflow_orchestration.py
@services/task_routing.py
@services/planning_workflow_detection.py
@tests/test_workflow_orchestration.py
@tests/test_task_routing.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register a governed planning workflow</name>
  <read_first>
    <file>config/workflows/registry.json</file>
    <file>config/workflows/skills.json</file>
    <file>services/workflow_orchestration.py</file>
    <file>tests/test_workflow_orchestration.py</file>
  </read_first>
  <files>config/workflows/registry.json, config/workflows/skills.json, tests/test_workflow_orchestration.py</files>
  <behavior>
    - `load_workflow_registry()` includes an active planning workflow keyed `planning-governance`.
    - `validate_workflow_bindings()` returns no errors for the new workflow and existing workflows.
    - The workflow declares validation, expected artifacts, standards bindings, and verification behavior.
  </behavior>
  <action>Add a `planning-governance` workflow entry to `config/workflows/registry.json` with `workflow_family` set to `planning_governance`, lifecycle `active`, trigger hints covering `gsd planning`, `add phase`, `plan phase`, `roadmap phase`, `execution contract`, `verification plan`, and `blockers into phase`. Define stages for parse/plan/validate/finalize using existing skill keys where possible. When the existing skill registry has no clean planning-governance key, add a registry-only skill entry in `config/workflows/skills.json` with allowed planning/validate stages, no side effects beyond planning artifacts, and invariants for standards-before-execution and executable plan contracts.</action>
  <verify>
    <automated>uv run pytest -q tests/test_workflow_orchestration.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_workflow_orchestration.py` asserts `planning-governance` loads as active.
    - `validate_workflow_bindings()` has zero errors.
    - The workflow declares at least one validation binding and is not verification-exempt without reason.
  </acceptance_criteria>
  <done>AIOS has an active governed workflow contract for planning work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route planning evidence to the planning workflow</name>
  <read_first>
    <file>services/workflow_orchestration.py</file>
    <file>services/task_routing.py</file>
    <file>services/planning_workflow_detection.py</file>
    <file>tests/test_workflow_orchestration.py</file>
    <file>tests/test_task_routing.py</file>
  </read_first>
  <files>services/workflow_orchestration.py, services/task_routing.py, tests/test_workflow_orchestration.py, tests/test_task_routing.py</files>
  <behavior>
    - `recommend_route_primitives("Add a new GSD phase for planning governance")` selects `planning-governance`.
    - `route_objective()` returns `status="ready"` for the original phase-add style objective when project resolution succeeds.
    - Existing ambiguous-project and weak/tied workflow tests continue to block.
  </behavior>
  <action>Import and use `detect_planning_workflow()` in the route primitive ranking path. Give explicit configured GSD planning evidence enough score to beat implementation fallback while preserving the existing selected-score and runner-up tie checks. Ensure planning routes get a valid backend recommendation through `WORKFLOW_TASK_FAMILIES` or the existing backend-selection path, and make the packet/agent rationale explicitly planning-oriented in `task_routing.py` instead of defaulting silently to implementation semantics.</action>
  <verify>
    <automated>uv run pytest -q tests/test_workflow_orchestration.py tests/test_task_routing.py</automated>
  </verify>
  <acceptance_criteria>
    - The exact route-blocked phrase family from Phase 25 research selects `planning-governance`.
    - `workflow_candidates` includes planning detection rationale or matched planning signals.
    - Existing route-blocking tests for ambiguous project selection still pass.
  </acceptance_criteria>
  <done>Governed `start-work` routing selects planning workflow for planning objectives without weakening safety gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Workflow registry to routing | JSON workflow metadata changes governed route outcomes. |
| Objective text to route selection | Human text selects workflow, prompt, backend, and packet contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-25-03 | Elevation of privilege | `recommend_route_primitives()` | mitigate | Route planning only from structured planning evidence; keep weak/tied route blocking. |
| T-25-04 | Spoofing | `planning-governance` workflow triggers | mitigate | Use specific planning trigger hints and regression tests for non-planning objectives. |
</threat_model>

<verification>
Run `uv run pytest -q tests/test_workflow_orchestration.py tests/test_task_routing.py`.
</verification>

<success_criteria>
- `planning-governance` is active and registry-valid.
- The original planning objective routes successfully.
- Ambiguous and unsupported routes still block before packet creation.
</success_criteria>

<output>
After completion, create `.planning/phases/25-make-aios-planning-a-governed-first-class-workflow/25-02-SUMMARY.md`.
</output>
