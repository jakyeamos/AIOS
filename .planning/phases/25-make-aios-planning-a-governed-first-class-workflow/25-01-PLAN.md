---
phase: 25-make-aios-planning-a-governed-first-class-workflow
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - config/planning/gsd-workflow-phases.json
  - services/planning_workflow_detection.py
  - tests/test_planning_workflow_detection.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "AIOS detects GSD phase-add, phase-plan, blocker-to-phase, and roadmap-gap planning objectives as planning workflow intent before route selection."
    - "The original failing objective is covered by a regression test and no longer classifies as unknown planning intent."
  artifacts:
    - path: "config/planning/gsd-workflow-phases.json"
      provides: "Configurable planning aliases for GSD plan, add-phase, and blocker-to-phase objectives."
    - path: "services/planning_workflow_detection.py"
      provides: "Structured planning workflow detection for GSD planning operations."
    - path: "tests/test_planning_workflow_detection.py"
      provides: "Regression tests for planning workflow detection breadth."
  key_links:
    - from: "config/planning/gsd-workflow-phases.json"
      to: "services/planning_workflow_detection.py"
      via: "load_gsd_phase_registry() and detect_planning_workflow()"
    - from: "services/planning_workflow_detection.py"
      to: "tests/test_planning_workflow_detection.py"
      via: "detect_planning_workflow() assertions"
---

<objective>
Make planning intent detection strong enough to recognize GSD phase operations before governed workflow selection runs.

Purpose: The Phase 25 failure was not a literal `/gsd-plan-phase` command; it was natural-language phase-add work. This plan makes that intent explicit and test-covered.
Output: Updated planning alias/config and detection tests.
</objective>

<execution_context>
@/Users/jakyeamos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakyeamos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-make-aios-planning-a-governed-first-class-workflow/25-RESEARCH.md
@.planning/phases/25-make-aios-planning-a-governed-first-class-workflow/25-VALIDATION.md
@config/planning/gsd-workflow-phases.json
@services/planning_workflow_detection.py
@tests/test_planning_workflow_detection.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand GSD planning workflow aliases</name>
  <read_first>
    <file>config/planning/gsd-workflow-phases.json</file>
    <file>services/planning_workflow_detection.py</file>
    <file>tests/test_planning_workflow_detection.py</file>
  </read_first>
  <files>config/planning/gsd-workflow-phases.json, tests/test_planning_workflow_detection.py</files>
  <behavior>
    - `detect_planning_workflow("Add a new GSD phase for planning governance")` returns workflow `gsd`, phase `plan`, output format `gsd_ready_plan`.
    - `detect_planning_workflow("Use gsd-add-phase to make planning governance Phase 25")` returns workflow `gsd`, phase `plan`, and a matched alias from the registry.
    - Existing `/gsdplanphase`, `/gsd-execute-phase`, review, and validate alias tests still pass.
  </behavior>
  <action>Add configurable aliases to the `plan` phase in `config/planning/gsd-workflow-phases.json` for `gsd-add-phase`, `/gsd-add-phase`, `add phase`, `add a phase`, `create phase`, `new phase`, `make this into phase`, `blockers into a phase`, `roadmap phase`, and `phase planning`. Add tests that assert those aliases classify as `workflow="gsd"`, `phase="plan"`, `handoff_target="gsd"`, and `output_format="gsd_ready_plan"`.</action>
  <verify>
    <automated>uv run pytest -q tests/test_planning_workflow_detection.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_planning_workflow_detection.py` includes the original phase-add style objective and a blocker-to-phase objective.
    - `config/planning/gsd-workflow-phases.json` keeps slash commands as aliases, not a separate routing system.
    - Existing tests for slash overrides and configured aliases still pass.
  </acceptance_criteria>
  <done>Natural-language GSD phase creation/planning objectives are classified as GSD planning intent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve unknown and non-planning classification boundaries</name>
  <read_first>
    <file>services/planning_workflow_detection.py</file>
    <file>tests/test_planning_workflow_detection.py</file>
  </read_first>
  <files>services/planning_workflow_detection.py, tests/test_planning_workflow_detection.py</files>
  <behavior>
    - Non-planning text without planning terms still returns workflow `unknown`.
    - Audit-to-implementation and generated-implementation prompt detection still return their existing AIOS plan output formats.
    - Detection records `signals` that distinguish configured GSD aliases from natural-language planning.
  </behavior>
  <action>Adjust `services/planning_workflow_detection.py` only if registry aliases are insufficient to classify the failing phrase family. Keep the existing return values for audit-to-implementation, generated-implementation prompt, CLI-shaped planning, and unknown objectives. Add negative-boundary tests for an ordinary implementation objective that should not classify as GSD planning.</action>
  <verify>
    <automated>uv run pytest -q tests/test_planning_workflow_detection.py</automated>
  </verify>
  <acceptance_criteria>
    - Unknown objectives remain unknown when they contain no configured alias or planning signal.
    - GSD phase planning objectives have `signals=("configured_gsd_alias",)` when matched by registry alias.
    - No test broadens all implementation objectives into planning.
  </acceptance_criteria>
  <done>Planning detection becomes broader without becoming a generic catch-all.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Objective text to planning detector | Untrusted human text becomes structured workflow intent. |
| Planning alias registry to detector | JSON config changes route classification behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-25-01 | Tampering | `config/planning/gsd-workflow-phases.json` | mitigate | Keep aliases specific to planning and add negative tests for non-planning objectives. |
| T-25-02 | Repudiation | `detect_planning_workflow()` | mitigate | Preserve `matched_alias`, `signals`, and `rationale` in returned detection objects. |
</threat_model>

<verification>
Run `uv run pytest -q tests/test_planning_workflow_detection.py`.
</verification>

<success_criteria>
- The original phase-add style objective is detected as GSD planning intent.
- Existing planning detection tests continue to pass.
- Non-planning objectives do not route as GSD planning.
</success_criteria>

<output>
After completion, create `.planning/phases/25-make-aios-planning-a-governed-first-class-workflow/25-01-SUMMARY.md`.
</output>
