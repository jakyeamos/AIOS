---
phase: 25-make-aios-planning-a-governed-first-class-workflow
plan: "04"
type: execute
wave: 4
depends_on:
  - "25-03"
files_modified:
  - scripts/codex-aios-shadow.py
  - scripts/codex-aios-route.py
  - services/workflow_orchestration.py
  - services/task_routing.py
  - tests/test_codex_aios_shadow.py
  - tests/test_codex_aios_route.py
  - tests/test_workflow_orchestration.py
  - tests/test_task_routing.py
  - PROJECT.md
  - .planning/STATE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "The original Codex shadow planning route-blocked scenario is covered by an automated smoke or unit test."
    - "The Codex shadow command-invocation route-blocked scenario for `gsd-execute-phase 24` is covered by an automated smoke or unit test."
    - "Project truth records that AIOS planning and known invoked GSD commands are governed through start-work route selection and packet standards."
    - "Phase 25 closes with evidence that planning routes, invoked-command routes, packets, and route-blocking safeguards work together."
  artifacts:
    - path: "tests/test_codex_aios_shadow.py"
      provides: "Regression coverage for Codex shadow planning and invoked-command route behavior."
    - path: "tests/test_codex_aios_route.py"
      provides: "Regression coverage for route helper behavior."
    - path: "tests/test_task_routing.py"
      provides: "Regression coverage for governed routing of known GSD command invocations."
    - path: "tests/test_workflow_orchestration.py"
      provides: "Regression coverage for workflow selection of invoked command families."
    - path: "PROJECT.md"
      provides: "Durable project truth update."
    - path: ".planning/STATE.md"
      provides: "GSD planning status update."
  key_links:
    - from: "scripts/codex-aios-shadow.py"
      to: "services/task_routing.py"
      via: "route helper start-work call"
    - from: "tests/test_codex_aios_shadow.py"
      to: "scripts/codex-aios-shadow.py"
      via: "planning objective and invoked-command smoke/regression"
---

<objective>
Close Phase 25 with shadow-route proof, truth updates, and final validation.

Purpose: The product gap was found through Codex shadow routing, so the closeout must prove that same entry point no longer blocks planning work.
The follow-on execution failure shows the same class of bug can affect known invoked commands when command identity is flattened into weak prose such as `Execute GSD phase 24`.
Output: Shadow/route tests for planning and command invocation, truth updates, and final evidence.
</objective>

<execution_context>
@/Users/jakyeamos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakyeamos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-make-aios-planning-a-governed-first-class-workflow/25-RESEARCH.md
@scripts/codex-aios-shadow.py
@scripts/codex-aios-route.py
@services/workflow_orchestration.py
@services/task_routing.py
@tests/test_codex_aios_shadow.py
@tests/test_codex_aios_route.py
@tests/test_workflow_orchestration.py
@tests/test_task_routing.py
@PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove Codex shadow planning and command routes succeed</name>
  <read_first>
    <file>scripts/codex-aios-shadow.py</file>
    <file>scripts/codex-aios-route.py</file>
    <file>services/workflow_orchestration.py</file>
    <file>services/task_routing.py</file>
    <file>tests/test_codex_aios_shadow.py</file>
    <file>tests/test_codex_aios_route.py</file>
    <file>tests/test_workflow_orchestration.py</file>
    <file>tests/test_task_routing.py</file>
  </read_first>
  <files>tests/test_codex_aios_shadow.py, tests/test_codex_aios_route.py, tests/test_workflow_orchestration.py, tests/test_task_routing.py, scripts/codex-aios-shadow.py, scripts/codex-aios-route.py, services/workflow_orchestration.py, services/task_routing.py</files>
  <behavior>
    - A test covers a planning objective equivalent to `Add a new GSD phase to rectify all linked-repo adoption readiness blockers except agent-router`.
    - The route helper returns a selected planning workflow for the objective when project inference succeeds.
    - A test covers an invoked command objective equivalent to `gsd-execute-phase 24` / `Execute GSD phase 24`.
    - Known GSD command invocations route to a governed AIOS execution lane without depending on weak natural-language objective scoring.
    - Command identity, when available, is treated as first-class routing evidence before fallback prose matching.
    - Shadow creation can be tested with `--no-worktree` or mocked worktree creation to avoid mutating extra worktrees.
  </behavior>
  <action>Add or update tests so the Codex route helper and shadow helper exercise both a planning-governance objective and a known GSD execution command invocation. Prefer `--no-worktree` or mocks for `create_shadow_lane()` so tests do not create git worktrees. If script changes are needed, keep them limited to preserving command metadata, exposing selected route metadata, or improving JSON error details; do not bypass route-blocked failures generally. If routing changes are needed, add a registry-backed or command-family-aware path for known GSD command invocations rather than a broad fallback that routes any command-shaped text.</action>
  <verify>
    <automated>uv run pytest -q tests/test_workflow_orchestration.py tests/test_task_routing.py tests/test_codex_aios_route.py tests/test_codex_aios_shadow.py</automated>
  </verify>
  <acceptance_criteria>
    - The tests assert selected workflow `planning-governance` or the final configured planning workflow key.
    - The tests fail if route-blocked is returned for the original planning objective.
    - The tests fail if route-blocked is returned for `gsd-execute-phase 24` or objective text equivalent to `Execute GSD phase 24`.
    - Known GSD command invocations preserve command identity or canonical command-family evidence through the Codex route/shadow helper boundary.
    - Worktree creation is not required for automated tests.
  </acceptance_criteria>
  <done>Codex shadow routing has regression coverage for governed planning objectives and known GSD command invocations.</done>
</task>

<task type="auto">
  <name>Task 2: Record truth and final phase evidence</name>
  <read_first>
    <file>PROJECT.md</file>
    <file>.planning/STATE.md</file>
    <file>.planning/phases/25-make-aios-planning-a-governed-first-class-workflow/25-VALIDATION.md</file>
  </read_first>
  <files>PROJECT.md, .planning/STATE.md, .planning/phases/25-make-aios-planning-a-governed-first-class-workflow/25-VALIDATION.md</files>
  <action>Update `PROJECT.md` with a concise 2026-06-24 truth entry stating that AIOS planning routes and known GSD command invocations are governed through start-work, planning packets surface standards before execution, and the Codex shadow planning plus `gsd-execute-phase 24` command scenarios are covered by regression evidence. Update `.planning/STATE.md` only through the GSD state pattern used in prior phase closeouts. In `25-VALIDATION.md`, mark completed validation commands and keep any failed command visible rather than hiding it.</action>
  <verify>
    <automated>pnpm context:validate</automated>
  </verify>
  <acceptance_criteria>
    - `PROJECT.md` includes a factual entry for governed planning routes and known command-invocation routes, without claiming broader routing maturity than the tests prove.
    - `.planning/STATE.md` references Phase 25 execution evidence without deleting Phase 24 context.
    - `25-VALIDATION.md` records the final command set and results.
  </acceptance_criteria>
  <done>Durable truth and validation artifacts reflect the implemented planning governance route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Test DB to route helper | Tests simulate routing without corrupting live AIOS DB. |
| Invoked command metadata to route selector | User-invoked commands become structured routing evidence. |
| Truth file updates | Project truth changes can overstate capability if not evidence-bound. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-25-07 | Tampering | shadow route tests | mitigate | Use temp DBs/mocks and avoid creating worktrees in tests. |
| T-25-08 | Repudiation | `PROJECT.md` truth | mitigate | Cite exact tested behavior and keep limitations explicit. |
| T-25-09 | Elevation of privilege | invoked command routing | mitigate | Route only known command families through registry-backed or explicit command-family mappings; preserve route-blocked behavior for unknown commands. |
</threat_model>

<verification>
Run `uv run pytest -q tests/test_planning_workflow_detection.py tests/test_workflow_orchestration.py tests/test_task_routing.py tests/test_aios_cli.py tests/test_execution_symmetric_planner.py tests/test_codex_aios_route.py tests/test_codex_aios_shadow.py` and `pnpm context:validate`.
</verification>

<success_criteria>
- The original shadow planning objective has automated regression coverage.
- The `gsd-execute-phase 24` command-invocation route-blocked case has automated regression coverage.
- Project truth records only evidence-backed planning and command-invocation governance behavior.
- Phase 25 validation artifacts show all required checks and remaining risks.
</success_criteria>

<output>
After completion, create `.planning/phases/25-make-aios-planning-a-governed-first-class-workflow/25-04-SUMMARY.md`.
</output>
