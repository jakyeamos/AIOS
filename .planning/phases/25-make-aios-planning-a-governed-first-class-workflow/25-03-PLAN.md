---
phase: 25-make-aios-planning-a-governed-first-class-workflow
plan: "03"
type: execute
wave: 3
depends_on:
  - "25-02"
files_modified:
  - services/aios_cli.py
  - services/execution_symmetric_planner.py
  - services/planning_lenses.py
  - config/planning/planning-lenses.json
  - tests/test_aios_cli.py
  - tests/test_execution_symmetric_planner.py
  - tests/test_planning_lenses.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "A planning-governance start-work packet shows standards, planning lenses, acceptance criteria, evidence expectations, and verification handoff before implementation begins."
    - "Planning packets are visibly planning-oriented and do not instruct agents to start code execution as the first action."
  artifacts:
    - path: "services/aios_cli.py"
      provides: "Start-work packet sections and route metadata for planning governance."
    - path: "services/execution_symmetric_planner.py"
      provides: "Execution-symmetric planning sections enriched for governed planning routes."
    - path: "config/planning/planning-lenses.json"
      provides: "Standards-at-planning-time lens mapping for planning governance."
  key_links:
    - from: "planning-governance route result"
      to: "briefing packet markdown"
      via: "services/aios_cli.py packet section construction"
    - from: "config/planning/planning-lenses.json"
      to: "services/execution_symmetric_planner.py"
      via: "select_planning_lenses(workflow='gsd' or planning workflow context)"
---

<objective>
Make governed planning packets carry the quality contract before execution starts.

Purpose: The product gap is not only route selection; AIOS must surface standards early enough that planning produces better execution and verification.
Output: Packet and planning sections that expose standards, lenses, artifacts, gates, and closeout rules.
</objective>

<execution_context>
@/Users/jakyeamos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakyeamos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-make-aios-planning-a-governed-first-class-workflow/25-RESEARCH.md
@services/aios_cli.py
@services/execution_symmetric_planner.py
@services/planning_lenses.py
@config/planning/planning-lenses.json
@tests/test_aios_cli.py
@tests/test_execution_symmetric_planner.py
@tests/test_planning_lenses.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add planning-governance lenses and planner sections</name>
  <read_first>
    <file>config/planning/planning-lenses.json</file>
    <file>services/planning_lenses.py</file>
    <file>services/execution_symmetric_planner.py</file>
    <file>tests/test_planning_lenses.py</file>
    <file>tests/test_execution_symmetric_planner.py</file>
  </read_first>
  <files>config/planning/planning-lenses.json, services/execution_symmetric_planner.py, tests/test_planning_lenses.py, tests/test_execution_symmetric_planner.py</files>
  <behavior>
    - Planning-governance workflow context selects lenses for executor-readiness, constraint-preservation, artifact-definition, validation-strategy, standards-surfacing, and verification-handoff.
    - `generate_execution_symmetric_plan()` includes planning contract sections for acceptance criteria, evidence expectations, and closeout rules when workflow context is planning-oriented.
  </behavior>
  <action>Add a `planning-governance:plan` or equivalent workflow-phase mapping in `config/planning/planning-lenses.json`. If a new lens is needed, define it with a concrete purpose and standards refs for testing, maintainability, observability, and governance. Update `execution_symmetric_planner.py` so planning-governance routes expose `gsd_ready` or planning-governance-ready output with sections named `planning_quality_contract`, `acceptance_criteria_contract`, `evidence_contract`, and `verification_handoff`.</action>
  <verify>
    <automated>uv run pytest -q tests/test_planning_lenses.py tests/test_execution_symmetric_planner.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert the planning-governance workflow selects the expected lenses.
    - Tests assert generated planning sections include acceptance criteria and verification handoff.
    - Existing GSD-ready planning tests continue to pass.
  </acceptance_criteria>
  <done>Planning standards and verification expectations are machine-visible before execution planning begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add planning contract sections to start-work packets</name>
  <read_first>
    <file>services/aios_cli.py</file>
    <file>services/task_routing.py</file>
    <file>services/workflow_orchestration.py</file>
    <file>tests/test_aios_cli.py</file>
  </read_first>
  <files>services/aios_cli.py, tests/test_aios_cli.py</files>
  <behavior>
    - `aios start-work --json "Add a new GSD phase for planning governance" --project <AIOS project>` creates a packet whose markdown includes `Planning Quality Contract`.
    - The packet includes selected planning lenses or standards, required planning artifacts, evidence expectations, and verification handoff.
    - The packet does not frame the first next action as code implementation for planning-governance routes.
  </behavior>
  <action>Update the packet construction helpers in `services/aios_cli.py` to detect `planning-governance` route results and append planning-specific sections. Use concrete section titles: `Planning Quality Contract`, `Required Planning Artifacts`, `Standards Before Execution`, and `Verification Handoff`. Include route metadata from planning detection where available. Add `tests/test_aios_cli.py` coverage that seeds an AIOS project, runs `start-work`, inspects `packet_markdown`, and asserts the new section titles and planning-oriented next action are present.</action>
  <verify>
    <automated>uv run pytest -q tests/test_aios_cli.py tests/test_task_routing.py</automated>
  </verify>
  <acceptance_criteria>
    - Packet markdown includes the four planning section titles for planning-governance routes.
    - Packet route JSON preserves selected workflow `planning-governance`.
    - Existing implementation-delivery packet tests continue to pass.
  </acceptance_criteria>
  <done>AIOS governed planning packets surface standards and verification expectations before implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Route result to packet markdown | Structured route data becomes instructions consumed by agents. |
| Planning lenses to execution behavior | Standards and quality constraints influence downstream work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-25-05 | Repudiation | `briefing_packets.packet_markdown` | mitigate | Persist explicit planning contract sections and route metadata. |
| T-25-06 | Information disclosure | packet construction | mitigate | Include selected standards/lenses only, not broad unscoped context dumps. |
</threat_model>

<verification>
Run `uv run pytest -q tests/test_planning_lenses.py tests/test_execution_symmetric_planner.py tests/test_aios_cli.py tests/test_task_routing.py`.
</verification>

<success_criteria>
- Planning packets are visibly planning-specific.
- Planning packets include standards before execution.
- Start-work packet tests prove the planning route creates durable packet evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/25-make-aios-planning-a-governed-first-class-workflow/25-03-SUMMARY.md`.
</output>
