---
phase: 10-operator-surfaces-query-and-daily-flow-visibility
plan: "08"
type: execute
wave: 5
autonomous: true
depends_on:
  - "10-07"
files_modified:
  - scripts/quality-eval.sh
  - docs/backfill/agent-eval-backfill.md
  - package.json
requirements:
  - OPER-04
must_haves:
  truths:
    - "scripts/quality-eval.sh (NEW) is a safe, read-only shell script that runs quality hotspot detection across the AIOS repo. It checks: (1) Python files over 500 lines (find . -name '*.py' -not -path './.venv/*' | xargs wc -l | awk '$1 > 500' | sort -rn); (2) test files with no assertions (grep -rL 'assert\|assertEqual\|assertIn\|pytest.raises' tests/ --include='*.py'); (3) services/ files that import from bin/ (grep -rn 'from bin\.' services/ --include='*.py'); (4) TypeScript files over 400 lines in aios-ui/components (find aios-ui/components -name '*.tsx' -o -name '*.ts' | xargs wc -l | awk '$1 > 400' | sort -rn); (5) dead-code candidates in Python using vulture if available (command -v vulture && vulture . --min-confidence 80 || echo 'vulture not available — install with uv add --dev vulture'); (6) shell scripts with shellcheck violations (find bin -name '*.sh' | xargs shellcheck --severity=warning 2>/dev/null || echo 'shellcheck run complete'). The script is non-destructive — it only reads and reports. It exits 0 regardless of findings so CI is not blocked. It prints a summary section at the end listing each check and the count of findings."
    - "package.json (EXTENSION) gains a new script entry: 'quality:eval': 'bash scripts/quality-eval.sh'. The script is added alongside existing scripts (lint, typecheck, test, context:compile, context:validate, wiki:check) without modifying any existing script."
    - "docs/backfill/agent-eval-backfill.md (NEW) is the top-level backfill document based on actual repository inspection. It is organized into project areas: (1) Python Services (services/), (2) CLI / Bin Scripts (bin/), (3) Tests, (4) aios-ui (Next.js), (5) Config / Standards / Success Criteria, (6) Documentation / Eval Infrastructure. For each area it lists: Current Status, Quality Risks (based on actual file inspection — not invented), Complexity Hotspots, Test Gaps, Documentation Gaps, Architecture Concerns, Eval Instrumentation Gaps (what the Phase 11 eval automation will need), and Priority (P0 / P1 / P2). A 'Needs Deeper Audit' section identifies areas that require a dedicated follow-up pass — particularly any services exceeding 500 lines or test modules that only exercise happy paths. All findings are derived from actual filesystem inspection using the same checks as scripts/quality-eval.sh — no speculative or invented problems."
    - "scripts/quality-eval.sh is marked executable (chmod +x) and includes a shebang line (#!/usr/bin/env bash). It uses set -eo pipefail. Each check section prints a clear header (e.g., '=== Python files over 500 lines ==='). The script never modifies files, never commits, never runs tests, and never calls services."
  artifacts:
    - path: "scripts/quality-eval.sh"
      provides: "Non-destructive quality hotspot scan: large Python files, large TS components, assertion-free tests, services-importing-bin violations (architecture enforcement), dead code candidates via vulture, shellcheck summary. Prints per-check counts and a summary table. Exits 0 always."
    - path: "package.json"
      provides: "quality:eval script entry pointing to scripts/quality-eval.sh."
    - path: "docs/backfill/agent-eval-backfill.md"
      provides: "Per-project-area backfill inventory covering Python services, CLI scripts, tests, aios-ui, config, and docs/eval infrastructure. Each area lists status, risks, hotspots, gaps, and priority tier. Needs-deeper-audit section for areas too large to fully assess. All findings are observation-backed."
  key_links:
    - from: "package.json:quality:eval"
      to: "scripts/quality-eval.sh"
      via: "direct bash invocation"
      pattern: "quality:eval|quality-eval.sh"
    - from: "docs/backfill/agent-eval-backfill.md"
      to: "docs/evals/templates/backfill-hotspot.md"
      via: "each backfill item in this doc uses the same field structure as the backfill-hotspot.md template"
      pattern: "backfill-hotspot.md"
    - from: "docs/backfill/agent-eval-backfill.md"
      to: "scripts/quality-eval.sh"
      via: "findings in this doc are derived from the same checks the script runs; doc and script stay in sync"
      pattern: "quality-eval.sh"
---

<objective>
Create the quality hotspot detection script and top-level backfill inventory that complete the Agent Eval Foundation. The script gives agents a single `pnpm quality:eval` command to run after major work. The backfill doc gives the project a factual starting inventory of hotspots, derived from actual file inspection, that Phase 11 automation can use as regression baselines.

Purpose: The eval templates from Plan 10-07 define what to look for. This plan provides the tool to find it and the first concrete snapshot of what exists. Every future eval run can compare against this baseline. The backfill doc is not an accusation — it is evidence that the eval system produces real findings, not invented ones.

The script must be safe (read-only, exits 0, no side effects). The backfill doc must be honest (no invented problems; every item sourced from actual inspection). Together they close the Phase 1 Agent Eval Foundation.

Output: scripts/quality-eval.sh NEW; package.json extension (one new script); docs/backfill/agent-eval-backfill.md NEW.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/10-operator-surfaces-query-and-daily-flow-visibility/10-RESEARCH.md
@docs/evals/benchmark-eval-architecture.md
@docs/evals/templates/backfill-hotspot.md
@AGENTS.md
@package.json
</context>
