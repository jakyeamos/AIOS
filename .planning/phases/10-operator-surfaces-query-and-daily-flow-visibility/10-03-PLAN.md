---
phase: 10-operator-surfaces-query-and-daily-flow-visibility
plan: "03"
type: execute
wave: 2
autonomous: true
depends_on:
  - "10-02"
files_modified:
  - services/daily_flow.py
  - services/agentize.py
  - services/aios_cli.py
  - tests/test_daily_flow.py
  - tests/test_aios_cli.py
  - tests/test_architecture_enforcement.py
requirements:
  - OPER-04
  - OPER-03
must_haves:
  truths:
    - "services/daily_flow.py exports two pure functions: preview_from_objective(conn, *, objective, project_id=None) -> DailyFlowTrace runs the route + packet projection in DRY MODE (no orchestration_runs or briefing_packets rows inserted; verified by SAVEPOINT-then-ROLLBACK invariant and counted-rows post-condition); replay_from_run(conn, *, run_id) -> DailyFlowTrace joins existing rows for a real run and emits no mutations."
    - "DailyFlowTrace dataclass carries project_id (nullable), objective (str), is_preview (bool), and steps (tuple[DailyFlowStep, ...]). DailyFlowStep dataclass carries kind (DailyFlowStepKind Literal), summary (str), evidence_ref (dict with table+id keys), drill_down_path (str), provenance (Literal['confirmed','inferred','missing','contradictory']), freshness (ISO timestamp string), and metadata (dict)."
    - "DailyFlowStepKind Literal exactly covers eight values in this canonical order: goal, route, packet, run, evaluation, writeback, unresolved_delta, next_action. preview_from_objective and replay_from_run both emit steps in this order; missing source data yields a step with provenance='missing' rather than skipping the step (Pitfall 1 — graceful degradation, not silent omission)."
    - "preview_from_objective calls services.task_routing.recommend_route_primitives (Phase 1) and services.agentize.agentize_request (Phase 2) in dry mode; services/agentize.py gains a dry_run kwarg (default False) that wraps the route+packet generation in a sqlite3 SAVEPOINT-then-ROLLBACK so no rows persist; the dry kwarg propagates cleanly through any nested calls."
    - "replay_from_run reads orchestration_runs.route_result_json + briefing_packets + success_criteria_findings + improvement_writebacks + standards_delta_items (open) for the same project + next_action.get_next_actions(project_id) for the next_action step. The replay is a pure-read SELECT; tests assert no INSERT/UPDATE/DELETE on any of the seven step source tables during a replay (counted-rows post-condition + sqlite3 transaction trace)."
    - "Both functions tolerate missing upstream-phase tables: when Phase 6 success_criteria_findings is absent, the evaluation step renders provenance='missing' with a documented summary ('source data unavailable — Phase 6 not complete'); same for Phase 5/7/9 inputs. The is_preview flag and step ordering remain identical regardless of degradation."
    - "Every DailyFlowStep has a non-null drill_down_path constructed via services/next_action._drill_down_for and equivalent kind-aware path builders for goal (no drill — points to /search?query=<urlencoded-objective>), route (/runs/<run_id>?route=<route_id> in replay; /search for preview), packet (/control?packet=<packet_id> in replay; '/control' for preview), run (/runs/<run_id> in replay; '/' for preview), evaluation (/runs/<run_id>?finding=<finding_id> for first finding; /runs/<run_id> otherwise), writeback (/writebacks#<writeback_id>), unresolved_delta (/projects/<project_id>?delta=<delta_id>), next_action (carries the underlying NextAction.drill_down_path)."
    - "services/aios_cli.py exposes aios daily-flow subcommand with two mutually-exclusive entry modes: --objective <text> [--project X] [--dry-run] for preview, and --run-id <id> for replay; --json (default true). The CLI surfaces the dataclasses.asdict() of the DailyFlowTrace including each step's evidence_ref and drill_down_path; contracts-audit gains a DailyFlow row."
    - "services/daily_flow.py does NOT import from bin/; services/agentize.py dry_run extension also does not introduce new bin imports."
  artifacts:
    - path: "services/daily_flow.py"
      provides: "DailyFlowStepKind, DailyFlowStep, DailyFlowTrace dataclasses; preview_from_objective(); replay_from_run(); _step_for_<kind>() helpers; _drill_down_for_step() helper; _safe_table_exists() helper."
    - path: "services/agentize.py"
      provides: "dry_run=False kwarg on agentize_request; SAVEPOINT-then-ROLLBACK wrapper that ensures no rows persist when dry_run=True; otherwise behavior unchanged."
    - path: "services/aios_cli.py"
      provides: "cmd_daily_flow; argparse subparser with mutually-exclusive --objective vs --run-id group; dispatcher branch; contracts-audit row for DailyFlow (status=partial in this plan; flips to implemented in Plan 06)."
    - path: "tests/test_daily_flow.py"
      provides: "preview-is-dry assertion (zero new orchestration_runs / briefing_packets rows after 100-call loop); replay step ordering (canonical 8-step sequence); evidence_ref + drill_down_path + provenance non-null on every step; graceful degradation when Phase 6/7 tables absent; replay reads but does not mutate."
    - path: "tests/test_aios_cli.py"
      provides: "daily-flow CLI tests for both preview and replay paths; contracts-audit DailyFlow row test."
    - path: "tests/test_architecture_enforcement.py"
      provides: "assertion that services/daily_flow.py does not import from bin/."
  key_links:
    - from: "services/daily_flow.py:preview_from_objective"
      to: "services.task_routing.recommend_route_primitives + services.agentize.agentize_request(dry_run=True)"
      via: "Phase 1 routing + Phase 2 packet generation; dry_run kwarg engages a sqlite3 SAVEPOINT wrapper that always rolls back"
      pattern: "recommend_route_primitives|agentize_request.*dry_run"
    - from: "services/daily_flow.py:replay_from_run"
      to: "orchestration_runs + briefing_packets + success_criteria_findings + improvement_writebacks + standards_delta_items"
      via: "pure-read SELECTs joined by run_id and project_id; no INSERT/UPDATE/DELETE"
      pattern: "SELECT.*FROM orchestration_runs|SELECT.*FROM briefing_packets|SELECT.*FROM success_criteria_findings|SELECT.*FROM improvement_writebacks|SELECT.*FROM standards_delta_items"
    - from: "services/daily_flow.py:replay_from_run (next_action step)"
      to: "services.next_action.get_next_actions"
      via: "calls get_next_actions(conn, project_id=run.project_id, limit=1) and embeds the top action as the next_action step"
      pattern: "from services.next_action import|get_next_actions"
    - from: "services/agentize.py:agentize_request"
      to: "sqlite3 SAVEPOINT / ROLLBACK TO SAVEPOINT"
      via: "context-manager wrapper around the packet write path engaged when dry_run=True"
      pattern: "SAVEPOINT|ROLLBACK TO SAVEPOINT|dry_run"
---

<objective>
Ship the Python daily-flow trace projection that powers OPER-04: a canonical seven-(plus-one) step trace from goal through unresolved deltas, with two entry modes — preview (objective input, dry route+packet generation, no persistence) and replay (run_id input, pure-read join across existing tables). Produce a typed `DailyFlowTrace` shape with every step carrying `evidence_ref` + `drill_down_path` + `provenance` (OPER-03). Extend services/agentize.py with a dry_run kwarg that engages a SAVEPOINT-then-ROLLBACK wrapper (closes Pitfall 3: "Daily-Flow Preview Side Effects Leak Into Persistence"). Expose via `aios daily-flow` CLI.

Purpose: OPER-04 requires AIOS to surface the default-layer daily flow end-to-end. Every step is already persisted by Phases 1-9; what is missing is a projection that threads them together for a single input. This plan ships the backend; the UI projection (Plan 04), tRPC router (Plan 05), and rendered DailyFlowTrace component (Plan 06) consume it.

Output: services/daily_flow.py NEW (~400 lines); services/agentize.py extension (dry_run kwarg + SAVEPOINT wrapper, ~30 lines added); services/aios_cli.py extension; two test file additions; one architecture-enforcement assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-operator-surfaces-query-and-daily-flow-visibility/10-RESEARCH.md
@.planning/phases/10-operator-surfaces-query-and-daily-flow-visibility/10-02-PLAN.md
@.planning/phases/02-context-query-and-briefing-compilation/02-CONTEXT.md
@.planning/phases/01-project-workflow-and-prompt-routing/01-CONTEXT.md

<interfaces>
<!-- Symbols and contracts this plan composes against. Verify during planning via grep. -->

From services/task_routing.py (Phase 1):
- `recommend_route_primitives(objective: str, project_id: str | None = None) -> RouteResult` — find via `grep -n "def recommend_route_primitives" services/task_routing.py | head -5`.
- `RouteResult` dataclass with fields: route_id, objective, project_id, task_family, workflow_key, prompt_family, recommended_surface, candidates, rationale, ambiguity_block, timestamp — confirm via grep on the dataclass definition.

From services/agentize.py (Phase 2):
- `agentize_request(conn, *, objective, project_id, route_result=None, ...) -> dict` — find via `grep -n "def agentize_request" services/agentize.py | head -5`. The return shape includes packet_id, sections_loaded, sections_skipped, selection_trace.
- Current implementation: writes to briefing_packets, orchestration_runs, possibly orchestration_invocations. Plan 10-03 extends with `dry_run: bool = False`.

From orchestration_runs (existing — Phase 1):
- columns include id, objective, project_id, workflow_key, status, route_result_json, prompt_family, created_at, terminal_at.

From briefing_packets (Phase 2):
- columns include id, run_id, project_id, summary, selection_trace_json, omitted_context_json, route_id, created_at.

From success_criteria_findings (Phase 6):
- columns include id, run_id, criterion_id, level, message, resolution_status, evidence_json, created_at.

From improvement_writebacks (Phase 5):
- columns include id, run_id, project_id, layer_type, layer_key, title, summary, status, requires_approval, impact_scope, created_at.

From standards_delta_items (Phase 7):
- columns include id, project_id, domain, priority_bucket, summary, status, last_evaluated_at.

From services/next_action.py (Plan 10-02):
- `get_next_actions(conn, *, project_id=None, limit=10) -> list[NextAction]`.

From services/aios_cli.py:
- argparse subparser block; existing cmd_* shape; _contracts_audit_payload location; DB connection helper — verify via grep as in Plans 10-01/10-02.

From RESEARCH.md (this phase):
- Pattern 3 (Daily-Flow Trace) verbatim — code sketch for DailyFlowStepKind, DailyFlowStep, DailyFlowTrace, preview_from_objective, replay_from_run.
- Pitfall 3 (Daily-Flow Preview Side Effects Leak Into Persistence) — MUST add SAVEPOINT-then-ROLLBACK wrapper; test with 100-call loop asserting no row count increase.
- Pitfall 1 (Surfaces built on incomplete upstream phases render misleading data) — graceful degradation with provenance='missing'.
- Architectural Responsibility Map row for daily-flow trace — Primary Tier: Python (`services/daily_flow.py`); Secondary Tier: UI mirror.
- Don't Hand-Roll — recommend_route_primitives (Phase 1), agentize_request (Phase 2), get_next_actions (Plan 10-02), Provenance literal (services/capability_truth.py).

From services/capability_truth.py (Phase 7):
- `Provenance = Literal["confirmed","inferred","missing","contradictory"]` — import and reuse for DailyFlowStep.provenance.

NOTE: services/daily_flow.py CAN import from services/task_routing, services/agentize, services/next_action, services/capability_truth, and stdlib. It MUST NOT import from bin/. The dry_run kwarg on agentize_request is the critical contract change; the SAVEPOINT wrapper goes inside services/agentize.py so any downstream writer in Phase 2 also rolls back.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend services/agentize.py with dry_run kwarg and SAVEPOINT-then-ROLLBACK wrapper; verify no persistence in dry mode</name>
  <files>services/agentize.py, tests/test_daily_flow.py</files>
  <read_first>
    - services/agentize.py — read the agentize_request function definition and its full body; identify every SQL mutation (`INSERT`, `UPDATE`, `DELETE`) the function performs directly OR indirectly via helper calls. Find via `grep -n "def agentize_request\|INSERT\|UPDATE\|DELETE\|cursor.execute\|conn.execute" services/agentize.py | head -30`.
    - .planning/phases/10-operator-surfaces-query-and-daily-flow-visibility/10-RESEARCH.md — Pitfall 3 verbatim (SAVEPOINT-then-ROLLBACK invariant; 100-call assertion).
    - tests/test_agentize.py (if exists) — paste-of-record for agentize fixture style; find via `ls tests/test_agentize*.py 2>/dev/null`.
    - sqlite3 docs for SAVEPOINT — confirm syntax: `SAVEPOINT dry_run_<n>` / `ROLLBACK TO SAVEPOINT dry_run_<n>` / `RELEASE SAVEPOINT dry_run_<n>`. Nested savepoints work; the outer ROLLBACK undoes all nested mutations.
  </read_first>
  <behavior>
    - Test 1 `test_agentize_dry_run_inserts_no_rows`: invoke `agentize_request(conn, objective="...", project_id="p1", dry_run=True)` once on a seeded DB; assert COUNT(*) FROM orchestration_runs is unchanged; assert COUNT(*) FROM briefing_packets is unchanged; assert COUNT(*) FROM orchestration_invocations is unchanged (if the table is touched by agentize). Run the same call 100 times in a tight loop; the row counts remain unchanged after the loop.
    - Test 2 `test_agentize_dry_run_returns_packet_shape`: dry_run=True still returns a dict with the same keys as a live call (packet_id, sections_loaded, sections_skipped, selection_trace) — the packet_id may be a generated UUID or a sentinel like "preview-<hash>" (document the convention). Subsequent SELECTs on briefing_packets WHERE id = packet_id return zero rows.
    - Test 3 `test_agentize_live_mode_still_inserts`: invoke with dry_run=False (the default) on the same fixture; row counts increase by one each on orchestration_runs and briefing_packets (and whatever else agentize writes). Existing behavior unchanged.
    - Test 4 `test_agentize_dry_run_savepoint_isolation_under_concurrent_writes`: simulate a "transaction during preview" scenario — open a connection, BEGIN an outer transaction (a write to orchestration_runs by an unrelated path), then call agentize_request with dry_run=True; the outer write must still be present after the dry call returns, and no dry-mode rows must appear (the SAVEPOINT scope must NOT roll back the outer transaction).
  </behavior>
  <action>
    1. **Add a `dry_run: bool = False` kwarg** to `agentize_request` (and to any nested helpers it calls that perform mutations).

    2. **Implement the SAVEPOINT-then-ROLLBACK wrapper** inside agentize_request. The recommended structure:
       ```
       def agentize_request(conn, *, objective, project_id, ..., dry_run: bool = False):
           if dry_run:
               savepoint_name = f"daily_flow_preview_{id(conn)}_{int(time.time()*1e6)}"
               conn.execute(f"SAVEPOINT {savepoint_name}")
               try:
                   result = _agentize_request_impl(conn, objective=objective, project_id=project_id, ...)
                   conn.execute(f"ROLLBACK TO SAVEPOINT {savepoint_name}")
                   conn.execute(f"RELEASE SAVEPOINT {savepoint_name}")
                   return result
               except Exception:
                   conn.execute(f"ROLLBACK TO SAVEPOINT {savepoint_name}")
                   conn.execute(f"RELEASE SAVEPOINT {savepoint_name}")
                   raise
           else:
               return _agentize_request_impl(conn, objective=objective, project_id=project_id, ...)
       ```
       Refactor the current body into `_agentize_request_impl(...)` (or whatever name fits the existing module's conventions; if a more idiomatic refactor exists — e.g., extracting a single inner function or using a context manager — adopt it as long as the SAVEPOINT-then-ROLLBACK invariant is preserved).

    3. **DO NOT change behavior in live mode (dry_run=False)** — the live path goes through _agentize_request_impl directly with no SAVEPOINT wrapping. Existing tests must continue to pass.

    4. **Create tests/test_daily_flow.py** with the four behavior tests above. Use in-memory sqlite + the existing agentize-relevant schema; if the schema bootstrap is heavy, isolate the minimum tables agentize touches.

    5. **Quality ladder per touched file:**
       - `uv run pytest tests/test_daily_flow.py tests/test_agentize.py 2>/dev/null -x -q -k "agentize_dry_run or agentize_live_mode or agentize_dry_run_savepoint"`
       - `uv run ruff check services/agentize.py tests/test_daily_flow.py`
       - `uv run ruff format --check services/agentize.py tests/test_daily_flow.py`
       - `uv run basedpyright services/agentize.py`
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_daily_flow.py -x -q -k "agentize_dry_run_inserts_no_rows or agentize_dry_run_returns_packet_shape or agentize_live_mode_still_inserts or agentize_dry_run_savepoint_isolation"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dry_run.*=.*False\|dry_run: bool" services/agentize.py` returns at least 1
    - `grep -c "SAVEPOINT\|ROLLBACK TO SAVEPOINT" services/agentize.py` returns at least 2 (savepoint + rollback)
    - `grep -c "def test_agentize_dry_run_inserts_no_rows\|def test_agentize_dry_run_returns_packet_shape\|def test_agentize_live_mode_still_inserts\|def test_agentize_dry_run_savepoint_isolation_under_concurrent_writes" tests/test_daily_flow.py | grep -v '^#'` returns at least 4
    - `uv run pytest tests/test_daily_flow.py -x -q` exits 0
    - `uv run pytest tests/test_agentize.py 2>/dev/null -x -q` exits 0 if the file exists (no regression on existing agentize behavior)
    - `uv run ruff check services/agentize.py tests/test_daily_flow.py` exits 0
    - `uv run ruff format --check services/agentize.py tests/test_daily_flow.py` exits 0
    - `uv run basedpyright services/agentize.py` exits 0
  </acceptance_criteria>
  <done>
    services/agentize.py:agentize_request accepts dry_run kwarg; in dry mode wraps the implementation in SAVEPOINT-then-ROLLBACK so no rows persist; in live mode behaves unchanged; four behavior tests including a 100-call no-leak assertion and a concurrent-outer-transaction isolation test pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create services/daily_flow.py with DailyFlowStepKind, DailyFlowStep, DailyFlowTrace dataclasses; implement preview_from_objective and replay_from_run with eight-step canonical ordering and graceful degradation</name>
  <files>services/daily_flow.py, tests/test_daily_flow.py, tests/test_architecture_enforcement.py</files>
  <read_first>
    - .planning/phases/10-operator-surfaces-query-and-daily-flow-visibility/10-RESEARCH.md — Pattern 3 (Daily-Flow Trace) verbatim; Pitfall 1 (graceful degradation); Pitfall 3 (preview is dry); Pitfall 5 (centralized drill-down).
    - services/task_routing.py — recommend_route_primitives signature + RouteResult shape (find via `grep -n "def recommend_route_primitives\|@dataclass\\.*RouteResult\|class RouteResult" services/task_routing.py | head -10`).
    - services/agentize.py (post-Task 1) — agentize_request signature with dry_run kwarg.
    - services/next_action.py (Plan 10-02) — get_next_actions signature + NextAction shape.
    - services/capability_truth.py — Provenance literal definition (find via `grep -n "Provenance = Literal" services/capability_truth.py | head -3`).
    - schema.sql — confirm column names on orchestration_runs, briefing_packets, success_criteria_findings, improvement_writebacks, standards_delta_items.
  </read_first>
  <behavior>
    - Test 5 `test_preview_returns_eight_step_trace_in_canonical_order`: invoke `preview_from_objective(conn, objective="implement login", project_id="p1")` on a seeded DB; the returned DailyFlowTrace has `is_preview=True`, `objective="implement login"`, `project_id="p1"`, and `steps` is a tuple of exactly 8 DailyFlowSteps in the order: goal, route, packet, run, evaluation, writeback, unresolved_delta, next_action.
    - Test 6 `test_preview_is_dry_zero_persistence`: starting row counts on orchestration_runs and briefing_packets; invoke preview_from_objective 100 times in a loop; final row counts unchanged.
    - Test 7 `test_replay_returns_eight_step_trace_for_existing_run`: seed a complete run (orchestration_runs + briefing_packets + a success_criteria_findings row + an improvement_writebacks row + a standards_delta_items row for the project + a workflow_learning_events row); invoke `replay_from_run(conn, run_id="r1")`; returns DailyFlowTrace with `is_preview=False`, `objective` matching the run's objective, eight steps in canonical order; each step's evidence_ref points to the seeded row's table+id; each step's drill_down_path is non-null and starts with "/".
    - Test 8 `test_replay_is_pure_read`: snapshot all source table row counts before replay; invoke replay_from_run; counts unchanged.
    - Test 9 `test_every_step_has_evidence_ref_and_drill_down_and_provenance`: for both preview and replay outputs, every DailyFlowStep has a dict evidence_ref with at least the keys "table" and "id" (when applicable; for the goal step, evidence_ref may be `{"kind": "objective", "value": objective}`); every step has drill_down_path non-null; every step has provenance in {"confirmed", "inferred", "missing", "contradictory"}.
    - Test 10 `test_replay_degrades_when_evaluation_table_missing`: seed orchestration_runs + briefing_packets + improvement_writebacks but OMIT success_criteria_findings (Phase 6 incomplete scenario); invoke replay_from_run; the evaluation step exists (canonical 8-step ordering preserved) with provenance='missing' and summary="source data unavailable — Phase 6 success_criteria_findings table not present" (or equivalent documented message). The other steps populate normally.
    - Test 11 `test_replay_degrades_when_writeback_table_missing`: same shape — omit improvement_writebacks; the writeback step has provenance='missing' and the rest populate.
    - Test 12 `test_replay_degrades_when_delta_table_missing`: omit standards_delta_items; unresolved_delta step has provenance='missing'.
    - Test 13 `test_replay_missing_run_returns_trace_with_missing_steps`: invoke replay_from_run with a run_id that doesn't exist; returns a DailyFlowTrace with steps populated as provenance='missing' for every step that depends on the run (route, packet, run, evaluation, writeback); summary on the run step explains "run_id not found"; no exception raised.
    - Test 14 `test_preview_step_drill_downs_point_to_existing_routes`: preview goal step drill_down_path == f"/search?query={url-encoded objective}"; preview route step drill_down_path == "/search" (no route_id available in preview); preview packet step drill_down_path == "/control"; preview run step drill_down_path == "/"; preview next_action step drill_down_path matches the top NextAction's drill_down_path or "/" when no actions available.
    - Test 15 (architecture) `test_daily_flow_does_not_import_bin`.
  </behavior>
  <action>
    1. **Create services/daily_flow.py** with the following structure:

       Module top:
       - imports: stdlib (annotations, json, logging, sqlite3, urllib.parse, from dataclasses, from datetime, from typing).
       - imports from services: `from services.task_routing import recommend_route_primitives, RouteResult` (or whatever names match Phase 1); `from services.agentize import agentize_request`; `from services.next_action import get_next_actions, NextAction`; `from services.capability_truth import Provenance` (if available — otherwise define a local Literal mirroring the four values).
       - logger setup.
       - `DailyFlowStepKind = Literal["goal", "route", "packet", "run", "evaluation", "writeback", "unresolved_delta", "next_action"]` — eight kinds in canonical order.
       - `CANONICAL_STEP_ORDER: Final[tuple[DailyFlowStepKind, ...]] = ("goal", "route", "packet", "run", "evaluation", "writeback", "unresolved_delta", "next_action")`.

       Dataclasses:
       - `DailyFlowStep` frozen — kind, summary, evidence_ref (dict), drill_down_path (str), provenance (Literal), freshness (str — ISO timestamp), metadata (dict).
       - `DailyFlowTrace` frozen — project_id (Optional[str]), objective (str), is_preview (bool), steps (tuple[DailyFlowStep, ...]).

       Helpers:
       - `_now_iso() -> str` returning UTC isoformat (Z-suffix optional but consistent).
       - `_safe_table_exists(conn, table_name)` mirroring the helper from services/operator_search.py and services/next_action.py (duplicate, three lines).
       - `_drill_down_for_step(kind, *, run_id=None, packet_id=None, finding_id=None, writeback_id=None, delta_id=None, project_id=None, objective=None, route_id=None) -> str` — path construction per the truths block above. For preview-mode steps (where IDs aren't real), use the documented preview paths (/search?query=..., /control, /, etc.). For replay-mode steps, use the canonical resource paths.

       `_missing_step(kind, summary) -> DailyFlowStep`:
       - Returns a DailyFlowStep with provenance='missing', evidence_ref={"missing_reason": summary}, drill_down_path="/" (or a kind-appropriate fallback like "/control"), freshness=_now_iso(), metadata={}.

       `preview_from_objective(conn, *, objective, project_id=None) -> DailyFlowTrace`:
       1. Call `route_result = recommend_route_primitives(objective=objective, project_id=project_id)` (this is read-only in Phase 1; no DB writes).
       2. Call `packet_result = agentize_request(conn, objective=objective, project_id=project_id, route_result=route_result, dry_run=True)` — Task 1 ensures no persistence.
       3. Build the eight steps. For each kind, populate evidence_ref from the in-memory route_result / packet_result; mark provenance='inferred' (these are projections, not persisted facts). Where data is unavailable (e.g., no run, no evaluation, no writeback yet — preview has none), use _missing_step(kind, "preview — no real run yet").
       4. Compute next_action: call `get_next_actions(conn, project_id=project_id, limit=1)`. If non-empty, the next_action step carries the top NextAction's summary + drill_down_path + provenance='confirmed'; else _missing_step("next_action", "no next actions available").
       5. Return DailyFlowTrace(project_id, objective, is_preview=True, steps=tuple(steps in canonical order)).

       `replay_from_run(conn, *, run_id) -> DailyFlowTrace`:
       1. SELECT id, objective, project_id, workflow_key, status, route_result_json, created_at FROM orchestration_runs WHERE id=?; if the row is absent, return a trace where every dependent step is _missing_step(...) and is_preview=False (Test 13).
       2. For each step, query the corresponding source table guarded by _safe_table_exists:
          - goal: from run.objective; evidence_ref={"kind": "objective", "value": run.objective}; drill_down_path=_drill_down_for_step("goal", objective=run.objective); provenance='confirmed' (from the run row).
          - route: from run.route_result_json; parse JSON; provenance='confirmed' if present else 'missing'.
          - packet: SELECT id, summary, selection_trace_json, created_at FROM briefing_packets WHERE run_id=?; provenance='confirmed' if row found else 'missing'.
          - run: the run row itself; status + workflow_key + timestamps.
          - evaluation: SELECT id, criterion_id, level, message, resolution_status, evidence_json, created_at FROM success_criteria_findings WHERE run_id=? ORDER BY created_at ASC LIMIT 1. Provenance='confirmed' if row found; 'missing' if table absent or no rows; 'contradictory' if findings include both 'blocker' resolution_status='open' AND the run completed (edge case worth flagging — document in the helper).
          - writeback: SELECT id, layer_type, layer_key, status, requires_approval, created_at FROM improvement_writebacks WHERE run_id=? ORDER BY created_at DESC LIMIT 1.
          - unresolved_delta: SELECT id, domain, priority_bucket, summary FROM standards_delta_items WHERE project_id=? AND status IN ('open', 'pending') ORDER BY priority_bucket ASC LIMIT 1 (project-scoped).
          - next_action: get_next_actions(conn, project_id=run.project_id, limit=1)[0] if any.
       3. Return DailyFlowTrace(project_id=run.project_id, objective=run.objective, is_preview=False, steps=tuple(...)).

    2. **Append behavior Tests 5-15 to tests/test_daily_flow.py** (Task 1 already created the file with tests 1-4). The file now contains 15 tests total. Tests 13-14 verify graceful degradation; Test 15 verifies architecture.

    3. **Extend tests/test_architecture_enforcement.py** with `test_daily_flow_does_not_import_bin` mirroring the existing pattern.

    4. **Quality ladder per touched file:**
       - `uv run pytest tests/test_daily_flow.py tests/test_architecture_enforcement.py -x -q`
       - `uv run ruff check services/daily_flow.py tests/test_daily_flow.py tests/test_architecture_enforcement.py`
       - `uv run ruff format --check services/daily_flow.py tests/test_daily_flow.py tests/test_architecture_enforcement.py`
       - `uv run basedpyright services/daily_flow.py`
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_daily_flow.py tests/test_architecture_enforcement.py -x -q -k "preview_returns_eight_step or preview_is_dry_zero_persistence or replay_returns_eight_step or replay_is_pure_read or every_step_has_evidence_ref or replay_degrades_when_evaluation or replay_degrades_when_writeback or replay_degrades_when_delta or replay_missing_run or preview_step_drill_downs or daily_flow_does_not_import_bin"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f services/daily_flow.py && echo OK` prints "OK"
    - `grep -c "DailyFlowStepKind = Literal\|@dataclass" services/daily_flow.py` returns at least 2
    - `grep -c "CANONICAL_STEP_ORDER" services/daily_flow.py` returns at least 1
    - `grep -c "def preview_from_objective\|def replay_from_run" services/daily_flow.py` returns at least 2
    - `grep -c "from services.task_routing\|from services.agentize\|from services.next_action" services/daily_flow.py` returns at least 3
    - `grep -c "from bin\|import bin" services/daily_flow.py` returns 0
    - `grep -c "_safe_table_exists\|_drill_down_for_step\|_missing_step" services/daily_flow.py` returns at least 3
    - `grep -c "def test_preview_returns_eight_step_trace_in_canonical_order\|def test_preview_is_dry_zero_persistence\|def test_replay_returns_eight_step_trace_for_existing_run\|def test_replay_is_pure_read\|def test_every_step_has_evidence_ref_and_drill_down_and_provenance\|def test_replay_degrades_when_evaluation_table_missing\|def test_replay_degrades_when_writeback_table_missing\|def test_replay_degrades_when_delta_table_missing\|def test_replay_missing_run_returns_trace_with_missing_steps\|def test_preview_step_drill_downs_point_to_existing_routes" tests/test_daily_flow.py | grep -v '^#'` returns at least 10
    - `grep -c "def test_daily_flow_does_not_import_bin" tests/test_architecture_enforcement.py | grep -v '^#'` returns at least 1
    - `uv run pytest tests/test_daily_flow.py -x -q` exits 0
    - `uv run pytest tests/test_architecture_enforcement.py -x -q` exits 0
    - `uv run pytest tests/test_next_action.py tests/test_operator_search.py -x -q` exits 0 (regression on prior plans)
    - `uv run ruff check services/daily_flow.py tests/test_daily_flow.py tests/test_architecture_enforcement.py` exits 0
    - `uv run ruff format --check services/daily_flow.py tests/test_daily_flow.py tests/test_architecture_enforcement.py` exits 0
    - `uv run basedpyright services/daily_flow.py` exits 0
  </acceptance_criteria>
  <done>
    services/daily_flow.py defines DailyFlowStepKind, DailyFlowStep, DailyFlowTrace, preview_from_objective (uses agentize dry_run + recommend_route_primitives + get_next_actions), replay_from_run (pure-read joins across seven tables + next_action), CANONICAL_STEP_ORDER, _missing_step graceful degradation, _drill_down_for_step centralization; 10 behavior tests + 1 architecture test pass; no bin/ imports.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire aios daily-flow CLI subcommand with mutually-exclusive --objective vs --run-id; extend contracts-audit with DailyFlow row</name>
  <files>services/aios_cli.py, tests/test_aios_cli.py</files>
  <read_first>
    - services/aios_cli.py — argparse subparser registration block; mutually-exclusive group pattern if any subcommand already uses it (find via `grep -n "add_mutually_exclusive_group\|mutually_exclusive" services/aios_cli.py | head -10`; if no existing pattern, use argparse's standard add_mutually_exclusive_group(required=True)).
    - services/daily_flow.py (Task 2) — confirm preview_from_objective and replay_from_run signatures.
    - tests/test_aios_cli.py — JSON-output CLI test pattern.
    - .planning/phases/10-operator-surfaces-query-and-daily-flow-visibility/10-RESEARCH.md — Standard Stack (services/aios_cli.py extension); Validation Architecture (test commands).
  </read_first>
  <behavior>
    - Test 1 `test_daily_flow_preview_cli`: invoke `aios daily-flow --objective "implement login" --project p1 --dry-run --json` on a seeded DB; returns JSON with keys `trace` (DailyFlowTrace dict), `is_preview: true`. The trace.steps array has 8 entries in canonical order; every step dict has drill_down_path non-null. After the call, COUNT(*) on orchestration_runs is unchanged (preview is dry).
    - Test 2 `test_daily_flow_replay_cli`: seed a run with id r1; invoke `aios daily-flow --run-id r1 --json`; returns JSON with `is_preview: false` and 8 steps; every step references the seeded data.
    - Test 3 `test_daily_flow_cli_requires_one_mode`: invoke `aios daily-flow --json` (neither --objective nor --run-id); argparse rejects with non-zero exit and a remediation message that --objective or --run-id is required.
    - Test 4 `test_daily_flow_cli_rejects_both_modes`: invoke `aios daily-flow --objective "x" --run-id r1`; argparse rejects (mutually exclusive group).
    - Test 5 `test_contracts_audit_includes_daily_flow_row`: invoke `aios contracts-audit --json`; the contracts list contains an entry with name="DailyFlow", status="partial", source_of_truth containing "services/daily_flow.py".
  </behavior>
  <action>
    1. **Add `from services.daily_flow import preview_from_objective, replay_from_run, DailyFlowTrace`** to services/aios_cli.py imports.

    2. **Register the daily-flow subparser** with a mutually-exclusive group:
       ```
       p = subparsers.add_parser("daily-flow", help="...")
       mode = p.add_mutually_exclusive_group(required=True)
       mode.add_argument("--objective", type=str)
       mode.add_argument("--run-id", type=str, dest="run_id")
       p.add_argument("--project", type=str, default=None)
       p.add_argument("--dry-run", action="store_true")  # informational; preview is always dry by design
       p.add_argument("--json", action="store_true", default=True)
       ```
       Map dispatcher: `args.command == "daily-flow" -> cmd_daily_flow(args)`.

    3. **Add `cmd_daily_flow(args) -> dict[str, Any]`:**
       - Open DB via existing helper.
       - If args.objective: call `trace = preview_from_objective(conn, objective=args.objective, project_id=args.project)`.
       - Else if args.run_id: call `trace = replay_from_run(conn, run_id=args.run_id)`.
       - Return `{"trace": dataclasses.asdict(trace), "is_preview": trace.is_preview, "objective": trace.objective, "project_id": trace.project_id, "step_count": len(trace.steps)}`.

    4. **Extend `_contracts_audit_payload()`** with one new row:
       - name: "DailyFlow"
       - status: "partial" if not Path("aios-ui/server/aios/daily-flow.ts").exists() else "implemented".
       - source_of_truth: ["services/daily_flow.py", "services/agentize.py:agentize_request(dry_run=True)", "services/aios_cli.py:cmd_daily_flow"]
       - notes: "Daily-flow trace backend (Phase 10, Plan 03). Preview is dry via SAVEPOINT; replay is pure-read. UI mirror lands in Plan 04; tRPC router in Plan 05; DailyFlowTrace component on /runs/[id] and Command Center in Plan 06."

    5. **Add the five behavior tests** to tests/test_aios_cli.py.

    6. **Quality ladder per touched file:**
       - `uv run pytest tests/test_aios_cli.py -x -q -k "daily_flow or contracts_audit_includes_daily_flow"`
       - `uv run ruff check services/aios_cli.py tests/test_aios_cli.py`
       - `uv run ruff format --check services/aios_cli.py tests/test_aios_cli.py`
       - `uv run basedpyright services/aios_cli.py`
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_aios_cli.py -x -q -k "daily_flow_preview_cli or daily_flow_replay_cli or daily_flow_cli_requires_one_mode or daily_flow_cli_rejects_both_modes or contracts_audit_includes_daily_flow_row"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\"daily-flow\"\|'daily-flow'" services/aios_cli.py` returns at least 2
    - `grep -c "def cmd_daily_flow" services/aios_cli.py` returns at least 1
    - `grep -c "from services.daily_flow import\|import services.daily_flow" services/aios_cli.py` returns at least 1
    - `grep -c "add_mutually_exclusive_group" services/aios_cli.py` returns at least 1
    - `grep -c "\"DailyFlow\"\|'DailyFlow'" services/aios_cli.py` returns at least 1
    - `grep -c "def test_daily_flow_preview_cli\|def test_daily_flow_replay_cli\|def test_daily_flow_cli_requires_one_mode\|def test_daily_flow_cli_rejects_both_modes\|def test_contracts_audit_includes_daily_flow_row" tests/test_aios_cli.py | grep -v '^#'` returns at least 5
    - `uv run pytest tests/test_aios_cli.py -x -q` exits 0
    - `uv run pytest tests/test_daily_flow.py tests/test_next_action.py tests/test_operator_search.py tests/test_architecture_enforcement.py -x -q` exits 0 (regression)
    - `uv run ruff check services/aios_cli.py tests/test_aios_cli.py` exits 0
    - `uv run ruff format --check services/aios_cli.py tests/test_aios_cli.py` exits 0
    - `uv run basedpyright services/aios_cli.py` exits 0
  </acceptance_criteria>
  <done>
    aios daily-flow CLI subcommand registered with mutually-exclusive --objective vs --run-id; cmd_daily_flow dispatches to preview_from_objective or replay_from_run; contracts-audit lists DailyFlow; five behavior tests pass; full regression green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aios CLI -> services/daily_flow.preview_from_objective or replay_from_run | Operator-controlled input; argparse mutually-exclusive group prevents both modes; SAVEPOINT-then-ROLLBACK guarantees preview persists nothing. |
| services/daily_flow.py -> SQLite tables | Read-only joins across orchestration_runs, briefing_packets, success_criteria_findings, improvement_writebacks, standards_delta_items, workflow_learning_events; replay is pure-read; preview only mutates within a rolled-back savepoint. |
| services/agentize.py SAVEPOINT wrapper | Encloses every mutation agentize_request performs in dry mode; tests assert no row count change after 100-call loop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-12 | Tampering | Daily-flow preview persists side effects (Pitfall 3) | mitigate | Task 1 implements SAVEPOINT-then-ROLLBACK in services/agentize.py around the entire dry-mode path; Task 1 Test 1 asserts zero row count change after a 100-call loop; Task 1 Test 4 asserts savepoint isolation under concurrent outer transactions. |
| T-10-13 | Information Disclosure | Daily-flow step rationale exposes evidence body text (proposed_change_json, evidence_json) | mitigate | Step summaries project only the table's `summary`/`title`/`message` column, never JSON body content. Step evidence_ref carries `{"table": "...", "id": "..."}` only — the UI drills to the actual record via drill_down_path. |
| T-10-14 | Repudiation | Daily-flow step ordering inconsistent between preview and replay | mitigate | CANONICAL_STEP_ORDER constant enforces eight-step ordering; tests assert exact order for both modes. |
| T-10-15 | Information Disclosure | Drill-down path embeds user-specific tokens (Security Domain row 4) | mitigate | _drill_down_for_step() takes only entity IDs; no auth tokens. |
| T-10-16 | Denial of Service | Replay across a run with thousands of findings/writebacks scans the whole table | mitigate | Each step's source SELECT uses LIMIT 1 (single most-recent row per step) and ORDER BY indexed columns (created_at DESC). Long-running queries documented in Pitfall 5 (Detector Performance) — Phase 10 inherits the same boundedness discipline. |
| T-10-17 | Tampering | Pitfall 1 — daily-flow renders empty steps when upstream phases incomplete and operator confuses "no work" with "phase incomplete" | mitigate | _missing_step() emits provenance='missing' with an explicit `missing_reason` field; UI rendering in Plan 06 labels missing steps as "phase N not complete" rather than blank. |
</threat_model>

<verification>
- `uv run pytest tests/test_daily_flow.py tests/test_aios_cli.py tests/test_next_action.py tests/test_operator_search.py tests/test_architecture_enforcement.py -x -q` exits 0
- `uv run ruff check services/daily_flow.py services/agentize.py services/aios_cli.py tests/test_daily_flow.py tests/test_aios_cli.py tests/test_architecture_enforcement.py` exits 0
- `uv run ruff format --check services/daily_flow.py services/agentize.py services/aios_cli.py tests/test_daily_flow.py tests/test_aios_cli.py tests/test_architecture_enforcement.py` exits 0
- `uv run basedpyright services/daily_flow.py services/agentize.py services/aios_cli.py` exits 0
- `python -c "import services.aios_cli as m; p=m.create_parser(); p.parse_args(['daily-flow', '--objective', 'x'])"` exits 0
- `python -c "import services.aios_cli as m; p=m.create_parser(); p.parse_args(['daily-flow', '--run-id', 'r1'])"` exits 0
- Quality ladder per CLAUDE.md: hard gates green.
</verification>

<success_criteria>
- OPER-04 (partial — backend layer): services/daily_flow.py exports preview_from_objective and replay_from_run returning the eight-step canonical trace; CLI parity via `aios daily-flow`.
- OPER-03 (partial — drill-down path discipline on daily-flow steps): every DailyFlowStep carries non-null drill_down_path + provenance + evidence_ref.
- Pitfall 3 closed: agentize_request(dry_run=True) wrapped in SAVEPOINT-then-ROLLBACK; 100-call no-leak assertion passes; concurrent-outer-transaction isolation verified.
- Pitfall 1 partial close: graceful degradation with provenance='missing' for incomplete upstream phases; UI labeling completes the mitigation in Plan 06.
- Architecture invariant: services/daily_flow.py does not import bin/; services/agentize.py extension does not introduce new bin imports.
- contracts-audit lists DailyFlow (status partial in this plan).
</success_criteria>

<output>
After completion, create `.planning/phases/10-operator-surfaces-query-and-daily-flow-visibility/10-03-SUMMARY.md` per the template, including:
- The DailyFlowStepKind Literal (eight kinds) and CANONICAL_STEP_ORDER tuple.
- The DailyFlowStep + DailyFlowTrace dataclass shapes (paste-of-record so Plan 04's aios-ui/server/aios/daily-flow.ts can mirror exactly).
- The agentize_request dry_run contract: SAVEPOINT-then-ROLLBACK wrapper; 100-call no-leak invariant; concurrent-outer-transaction isolation; live mode unchanged.
- The eight-step ordering and the per-step source table mapping (goal -> objective, route -> orchestration_runs.route_result_json, packet -> briefing_packets, run -> orchestration_runs, evaluation -> success_criteria_findings, writeback -> improvement_writebacks, unresolved_delta -> standards_delta_items, next_action -> services.next_action.get_next_actions).
- The provenance derivation rules per step (confirmed = source row present; inferred = computed projection; missing = source table absent OR no matching row; contradictory = open blocker on a completed run).
- The aios daily-flow CLI argparse block with mutually-exclusive --objective vs --run-id.
- The DailyFlow contracts-audit row shape.
- Note that Plan 04 will create aios-ui/server/aios/daily-flow.ts mirroring this shape; Plan 05 will create the dailyFlow tRPC router (preview + replay procedures); Plan 06 will render DailyFlowTrace component on /runs/[id] and embed dailyFlowSummary on Command Center.
- Note that this plan does NOT introduce any launch path; previewed traces remain inspection-only until an operator clicks "Launch" through Plan 05's controlPlane.plan + controlPlane.invoke two-step flow.
</output>
