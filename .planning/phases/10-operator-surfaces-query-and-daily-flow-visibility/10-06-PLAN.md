---
phase: 10-operator-surfaces-query-and-daily-flow-visibility
plan: "06"
type: execute
wave: 4
autonomous: true
depends_on:
  - "10-05"
files_modified:
  - aios-ui/lib/constants.ts
  - config/operator-surfaces/search-policy.json
  - aios-ui/components/layout/TopBar.tsx
  - aios-ui/components/search/GlobalSearchInput.tsx
  - aios-ui/components/search/SearchResults.tsx
  - aios-ui/components/next-action/NextActionPanel.tsx
  - aios-ui/components/daily-flow/DailyFlowTrace.tsx
  - aios-ui/components/command-center/PhaseStatusBanner.tsx
  - aios-ui/components/command-center/SeedDataBanner.tsx
  - aios-ui/app/page.tsx
  - aios-ui/app/search/page.tsx
  - aios-ui/app/projects/[id]/page.tsx
  - aios-ui/app/runs/[id]/page.tsx
  - aios-ui/app/query/page.tsx
  - aios-ui/app/writebacks/page.tsx
  - aios-ui/app/automations/page.tsx
requirements:
  - OPER-01
  - OPER-02
  - OPER-03
  - OPER-04
must_haves:
  truths:
    - "aios-ui/lib/constants.ts:PRIMARY_NAV gains a /search entry (per RESEARCH A12). Existing nav entries preserved in their existing order."
    - "config/operator-surfaces/search-policy.json (NEW, per RESEARCH A11) records the small thresholds (max_query_length=200, default_limit=50, per_kind_cap=20, recency_boost_half_life_days=14, exact_key_boost=0.3, default_debounce_ms=200) so they are reviewable in git; aios-ui/server/aios/operator-search.ts module constants stay as-is in this plan, but a follow-up phase may switch them to read this file."
    - "aios-ui/components/layout/TopBar.tsx (extension) hosts a GlobalSearchInput (NEW client component) that debounces (200ms) input → calls operatorSearch.search via tRPC → renders a top-5 suggestion dropdown with drill-down links → 'See all results' link sends the operator to /search?query=<encoded> for the full SearchResults render."
    - "aios-ui/components/search/SearchResults.tsx (NEW) renders the OperatorSearchHit[] grouped by EntityKind in a faceted sidebar layout: each kind shows its hit count, a filter chip; the main pane lists hits with title + summary + score + last_updated_at + a 'Drill in' link to drill_down_path. Empty-query state shows recent activity per RESEARCH Pitfall 9."
    - "aios-ui/components/next-action/NextActionPanel.tsx (NEW) renders NextAction[] grouped by priority_bucket; each action shows kind + title + rationale + drill_down_path link + (when kind=='launch_remediation_workflow' AND recommended_workflow_key is non-null) a 'Launch remediation workflow' button that triggers projects.launchRemediation; mutations call utils.invalidate() on the relevant queries so the panel refreshes after a click (Pitfall 10 closure)."
    - "aios-ui/components/daily-flow/DailyFlowTrace.tsx (NEW) renders DailyFlowTrace as a card stack of eight steps in canonical order (matches CANONICAL_STEP_ORDER from Plan 10-03/05); each card shows kind icon + summary + provenance badge (confirmed/inferred/missing/contradictory with distinct color) + drill-in link via drill_down_path; missing-step cards (provenance='missing') render with a muted style and an explicit label like 'Phase 6 not complete' or 'preview — no real run yet'."
    - "aios-ui/components/command-center/PhaseStatusBanner.tsx (NEW, per Pitfall 1 + 8) reads phaseStatus from getControlPlaneOverview and renders ONLY when at least one phase is partial or missing. Banner copy is constructively framed per Pitfall 8: 'Some operator surfaces are still being populated (Phase {N} of 10). Metrics will fill in as those phases ship.' with a [Learn more] link to a phase-status panel (the panel itself can be the existing /control page or a new /phase-status route — Plan 06 inlines the explanation in the Command Center side panel to avoid scope creep)."
    - "aios-ui/components/command-center/SeedDataBanner.tsx (NEW, per Pitfall 7) reads any isSeedData=true rows from getControlPlaneOverview (catalog rows) and renders a 'Demo data shown — registry parse failed for some rows' banner with a [Refresh] action that re-fetches the overview."
    - "aios-ui/app/page.tsx (Command Center extension) renders: PhaseStatusBanner (top), SeedDataBanner (top), NextActionPanel (right side or main column — depending on existing layout), DailyFlowTrace for the topProjectId (if available), the existing attention alerts + timeline + health grid + automations (preserved), and the existing controlPlane.overview consumer flow. The page is a server component (per CLAUDE.md Next.js convention) and calls the extended getControlPlaneOverview which now includes nextActions, dailyFlowSummary, learningImpactRollup, phaseStatus, and catalog rows with isSeedData."
    - "aios-ui/app/search/page.tsx (NEW) is the global search landing page. Server component that calls operatorSearch.search with the query from searchParams; renders SearchResults; supports kinds/projectId filter chips via URL params."
    - "aios-ui/app/projects/[id]/page.tsx (extension) renders NextActionPanel scoped to projectId=params.id, plus drill-down deltas with Launch Remediation buttons next to high-priority bucket items. Existing project surface preserved."
    - "aios-ui/app/runs/[id]/page.tsx (extension) renders DailyFlowTrace via dailyFlow.replay(runId=params.id) at the top of the page (or in a prominent section), alongside the existing run detail content. Operators can see the seven-step trace for any run from its detail page."
    - "aios-ui/app/query/page.tsx (extension via GroundedQueryStudio component or this page itself) renders a 'Launch workflow' button when the grounded-query answer carries recommendedWorkflow; clicking it triggers automations.triggerWorkflow with the workflow_key + objective from the answer; utils.invalidate() fires on success so the answer card refreshes to show the launched run."
    - "aios-ui/app/writebacks/page.tsx (migration per RESEARCH State of the Art) switches from `caller.divergent.writebacks()` to `caller.writebacks.list({...})` (the new router from Plan 10-05). Filter chips for status/policyClass/source/projectId surface in the UI; default view shows pending_approval rows."
    - "aios-ui/app/automations/page.tsx (extension) renders a 'Trigger workflow' button on each automation row that calls automations.triggerWorkflow; existing list rendering preserved."
    - "No operator-visible surface shows a recommendation, score, or row without either a drill_down_path link OR an explicit 'phase not complete' label (OPER-03 closure at the rendering layer)."
  artifacts:
    - path: "aios-ui/lib/constants.ts"
      provides: "PRIMARY_NAV extended with /search entry; existing entries preserved."
    - path: "config/operator-surfaces/search-policy.json"
      provides: "Reviewable search policy thresholds in git."
    - path: "aios-ui/components/layout/TopBar.tsx"
      provides: "Extended to host GlobalSearchInput; existing top-bar elements preserved."
    - path: "aios-ui/components/search/GlobalSearchInput.tsx"
      provides: "Client component with 200ms debounce, tRPC suggestion fetch, top-5 dropdown, 'See all results' link."
    - path: "aios-ui/components/search/SearchResults.tsx"
      provides: "Faceted sidebar + main results pane; empty-query recent-activity behavior."
    - path: "aios-ui/components/next-action/NextActionPanel.tsx"
      provides: "Grouped-by-priority-bucket rendering; 'Launch remediation workflow' button calling projects.launchRemediation; utils.invalidate() on success."
    - path: "aios-ui/components/daily-flow/DailyFlowTrace.tsx"
      provides: "Card-stack rendering of eight canonical steps with provenance badges; missing-step explicit labels."
    - path: "aios-ui/components/command-center/PhaseStatusBanner.tsx"
      provides: "Constructively-framed banner that renders when any phase is partial/missing."
    - path: "aios-ui/components/command-center/SeedDataBanner.tsx"
      provides: "Demo-data warning banner when any catalog row carries isSeedData=true."
    - path: "aios-ui/app/page.tsx"
      provides: "Extended Command Center server component that consumes getControlPlaneOverview (with new fields) and renders all new components alongside existing alerts/timeline/health/automations."
    - path: "aios-ui/app/search/page.tsx"
      provides: "Global search landing server component consuming operatorSearch.search."
    - path: "aios-ui/app/projects/[id]/page.tsx"
      provides: "Project detail extension with NextActionPanel + Launch Remediation buttons."
    - path: "aios-ui/app/runs/[id]/page.tsx"
      provides: "Run detail extension with DailyFlowTrace via dailyFlow.replay."
    - path: "aios-ui/app/query/page.tsx"
      provides: "Grounded query extension with Launch Workflow button on recommendation."
    - path: "aios-ui/app/writebacks/page.tsx"
      provides: "Writebacks page migrated to caller.writebacks.list with filter chips."
    - path: "aios-ui/app/automations/page.tsx"
      provides: "Automations page extension with Trigger Workflow button per row."
  key_links:
    - from: "aios-ui/components/layout/TopBar.tsx"
      to: "aios-ui/components/search/GlobalSearchInput.tsx -> operatorSearch.search tRPC"
      via: "client component embedded in TopBar; useDebouncedValue (or equivalent) -> trpc.operatorSearch.search.useQuery with 200ms debounce"
      pattern: "GlobalSearchInput|operatorSearch.search"
    - from: "aios-ui/components/next-action/NextActionPanel.tsx"
      to: "projects.launchRemediation tRPC mutation"
      via: "Button onClick triggers mutation with deltaId + projectId; utils.invalidate() called on success to refresh next-action panel + project detail + control-plane overview"
      pattern: "launchRemediation|utils.invalidate"
    - from: "aios-ui/components/daily-flow/DailyFlowTrace.tsx"
      to: "DailyFlowTrace prop / dailyFlow.preview|replay tRPC procedures"
      via: "renders the passed-in trace; the consumer page (Command Center for dailyFlowSummary; /runs/[id] for replay) fetches and passes the trace via props"
      pattern: "DailyFlowTrace"
    - from: "aios-ui/app/page.tsx"
      to: "controlPlane.overview tRPC (now extended with nextActions/dailyFlowSummary/learningImpactRollup/phaseStatus)"
      via: "server component await caller.controlPlane.overview() pulls extended payload"
      pattern: "controlPlane.overview"
    - from: "aios-ui/app/writebacks/page.tsx"
      to: "writebacks.list tRPC (NEW from Plan 10-05) — replaces divergent.writebacks"
      via: "server component await caller.writebacks.list({...})"
      pattern: "writebacks.list"
    - from: "aios-ui/app/automations/page.tsx"
      to: "automations.triggerWorkflow tRPC mutation"
      via: "Button onClick triggers mutation with automationId + workflowKey + objective + projectId; utils.invalidate() on success"
      pattern: "automations.triggerWorkflow|utils.invalidate"
---

<objective>
Ship the rendered React surfaces that complete OPER-01/02/03/04 visibility: a global search input + /search page, a NextActionPanel on Command Center + project detail, a DailyFlowTrace component on Command Center + /runs/[id], a PhaseStatusBanner + SeedDataBanner on Command Center, a Launch Workflow button on grounded-query answers, a Launch Remediation button on project detail delta rows, a Trigger Workflow button on automations rows, and a migrated /writebacks page consuming the new writebacks router. Close Pitfall 1 (phase status visible), Pitfall 7 (seed data labeled), Pitfall 8 (banner copy constructive), Pitfall 9 (search faceted with limit), Pitfall 10 (utils.invalidate after mutations).

Purpose: All backend + projection + router work from Plans 10-01..05 is invisible until rendered. This plan closes the loop so operators can search, see what to do next, inspect daily-flow traces, drill into evidence, and launch workflows through governed two-step flows.

Output: Eight new components, eight extended pages, one new config file, one extended constants file. No new backend code; no new Python; no new tRPC procedures (all consumed from Plan 10-05). Quality gates are pnpm lint + pnpm tsc --noEmit + pnpm lint:architecture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-operator-surfaces-query-and-daily-flow-visibility/10-RESEARCH.md
@.planning/phases/10-operator-surfaces-query-and-daily-flow-visibility/10-04-PLAN.md
@.planning/phases/10-operator-surfaces-query-and-daily-flow-visibility/10-05-PLAN.md

<interfaces>
<!-- Symbols and contracts this plan composes against. Verify during planning via grep + reads. -->

From Plan 10-04 (Wave 2):
- aios-ui/lib/drill-down.ts (18 path builders) — every new component imports the relevant builders for href construction.
- aios-ui/lib/control-plane.ts types: OperatorSearchHit, NextAction, NextActionKind, PriorityBucket, DailyFlowStep, DailyFlowStepKind, DailyFlowTrace, PhaseId, PhaseStatus, PhaseStatusReport.

From Plan 10-05 (Wave 3):
- tRPC procedures: operatorSearch.search; nextAction.getForProject + topAcrossProjects; dailyFlow.preview + replay; writebacks.list + detail; automations.triggerWorkflow; projects.launchRemediation; query.ask (extended response).
- aios-ui/server/aios/control-plane.ts:getControlPlaneOverview now returns nextActions, dailyFlowSummary, learningImpactRollup, phaseStatus, catalog rows with isSeedData.
- aios-ui/server/aios/control-plane.ts:getGovernanceOverview returns policyClassDrillDowns.

From aios-ui/app/page.tsx (existing — 206 lines):
- Server component; calls `const caller = await getCaller(); const overview = await caller.controlPlane.overview();`. Existing buildAttentionAlerts + buildCommandCenterTimeline consumers; existing HealthGrid; existing automations.list and experiments.list consumers.

From aios-ui/components/layout/TopBar.tsx (existing):
- Read full file (probably <100 lines); identify slot for inserting the search input.

From aios-ui/components/layout/Sidebar.tsx (existing):
- Consumes PRIMARY_NAV from lib/constants.ts. Adding /search to PRIMARY_NAV makes it appear automatically.

From aios-ui/app/writebacks/page.tsx (existing — 51 lines):
- Currently `const proposals = await caller.divergent.writebacks();` — Plan 10-06 replaces with `caller.writebacks.list({ status: 'pending_approval', limit: 100 })`.

From aios-ui/app/automations/page.tsx (existing — 61 lines):
- Currently lists automations with health columns. Plan 10-06 adds a "Trigger workflow" button column.

From aios-ui/app/query/page.tsx (existing — 17 lines):
- Renders GroundedQueryStudio component. The studio component is likely client-side and handles the ask mutation. Plan 10-06 extends the studio (or this page) to render the Launch Workflow button when answer.recommendedWorkflow is present.

From aios-ui/app/projects/[id]/page.tsx (existing):
- Project detail page; consumes projects.detail. Plan 10-06 adds NextActionPanel (scoped to projectId) and Launch Remediation buttons next to high-priority deltas.

From aios-ui/app/runs/[id]/page.tsx (existing):
- Run detail page; Plan 10-06 adds DailyFlowTrace via dailyFlow.replay.

From aios-ui/app/search/page.tsx — does not exist yet (NEW).

From aios-ui/components/query/GroundedQueryStudio.tsx (existing per RESEARCH Recommended Project Structure):
- Find via `ls aios-ui/components/query/ 2>/dev/null`. If GroundedQueryStudio is a client component handling the ask mutation, extend it; if it's a server component delegating to a client child, extend the child.

From aios-ui/components/command-center/HealthGrid.tsx (existing):
- Already on the Command Center; Plan 06 keeps it. The new LearningImpactRollup row is rendered as a sibling (not within HealthGrid) to avoid component coupling.

From tRPC client wiring in aios-ui:
- Find the tRPC client hook + utils pattern (find via `grep -rn "trpc.useUtils\|createTRPCReact\|useQuery" aios-ui/app aios-ui/components | head -10`). The conventional pattern is `const utils = trpc.useUtils();` and `utils.controlPlane.overview.invalidate()` after mutations.

NOTE: All client components in this plan use `"use client"` directive; all server pages use server components (per CLAUDE.md). When a page needs to render a client component that consumes a tRPC mutation, the client component is a child of the server page and receives initial data via props.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the four new components (GlobalSearchInput, SearchResults, NextActionPanel, DailyFlowTrace) plus the two banner components (PhaseStatusBanner, SeedDataBanner); add /search to PRIMARY_NAV; create config/operator-surfaces/search-policy.json; extend TopBar to host GlobalSearchInput</name>
  <files>aios-ui/lib/constants.ts, config/operator-surfaces/search-policy.json, aios-ui/components/layout/TopBar.tsx, aios-ui/components/search/GlobalSearchInput.tsx, aios-ui/components/search/SearchResults.tsx, aios-ui/components/next-action/NextActionPanel.tsx, aios-ui/components/daily-flow/DailyFlowTrace.tsx, aios-ui/components/command-center/PhaseStatusBanner.tsx, aios-ui/components/command-center/SeedDataBanner.tsx</files>
  <read_first>
    - aios-ui/lib/constants.ts — read full file; confirm PRIMARY_NAV type (NavItem[]) and existing entries; identify the natural slot for /search (likely between /knowledge and /projects, or as last entry — adopt the existing alphabetical or workflow order).
    - aios-ui/components/layout/TopBar.tsx — read full file; identify the JSX slot for the search input (between logo and right-side icons, or wherever the existing space accommodates).
    - aios-ui/components/layout/Sidebar.tsx — confirm PRIMARY_NAV consumption pattern; no change needed but verify the nav re-renders when constants.ts updates.
    - aios-ui/lib/trusted-signals.ts — if available, reuse trustedSignal() for any metric the new components render (per RESEARCH Architectural Responsibility Map row "Operator-visible compounding").
    - aios-ui/components/command-center/HealthGrid.tsx OR another existing client component — read the trpc.useUtils + .useQuery pattern (find via `grep -rn "trpc\\..*\\.useQuery\|trpc.useUtils" aios-ui/components | head -10`).
    - .planning/phases/10-04-PLAN.md (truths block) — confirm types from lib/control-plane.ts.
    - .planning/phases/10-05-PLAN.md (truths block) — confirm tRPC procedure names and input shapes.
    - .planning/phases/10-operator-surfaces-query-and-daily-flow-visibility/10-RESEARCH.md — Pitfall 8 (banner copy framing); Pitfall 9 (faceted search); Pitfall 10 (invalidate after mutations); A14 (top-5 dropdown).
    - aios-ui/package.json — confirm any debounce helper available (e.g., use-debounce, lodash, or write a small custom hook); recharts available for any future chart needs.
  </read_first>
  <behavior>
    - Test 1 (type-level): `cd aios-ui && pnpm tsc --noEmit` exits 0 after all new components.
    - Test 2 (lint): `cd aios-ui && pnpm lint` exits 0.
    - Test 3 (architecture lint): `cd aios-ui && pnpm lint:architecture` exits 0 — client components don't import from server/, server components don't violate the "use client" / "use server" boundaries.
    - Test 4 (PRIMARY_NAV): aios-ui/lib/constants.ts contains `/search` entry in PRIMARY_NAV; verified via grep.
    - Test 5 (search policy config): config/operator-surfaces/search-policy.json exists and parses as JSON; contains the six documented keys (max_query_length, default_limit, per_kind_cap, recency_boost_half_life_days, exact_key_boost, default_debounce_ms).
    - Test 6 (GlobalSearchInput uses tRPC + debounce): source contains `"use client"`, `useState`, debounce (custom or library), `trpc.operatorSearch.search.useQuery`, and a 'See all results' link to /search. Verified via grep.
    - Test 7 (NextActionPanel uses launchRemediation + utils.invalidate): source contains `trpc.projects.launchRemediation.useMutation` and `utils.invalidate()` (or `.utils.nextAction.getForProject.invalidate`). Verified via grep — Pitfall 10 closure.
    - Test 8 (DailyFlowTrace renders eight steps in canonical order): source iterates over the steps array and renders one card per step; the canonical order is enforced by the DailyFlowTrace prop's steps tuple (Plan 10-03/04/05 guarantees the order; component does not re-sort).
    - Test 9 (PhaseStatusBanner constructive framing): banner text contains a phrase like "being populated" or "rolling out" or "fill in as those phases ship" rather than "broken" or "incomplete" alone (Pitfall 8). Verified via grep on the JSX literal strings.
    - Test 10 (SeedDataBanner): banner text contains "Demo data" and surfaces a [Refresh] action.
  </behavior>
  <action>
    1. **Extend aios-ui/lib/constants.ts**:
       - Add `{ href: "/search", label: "Search", short: "Search" }` to PRIMARY_NAV. Preserve existing entries.

    2. **Create config/operator-surfaces/search-policy.json**:
       ```json
       {
         "version": "2026-05-22",
         "max_query_length": 200,
         "default_limit": 50,
         "per_kind_cap": 20,
         "recency_boost_half_life_days": 14,
         "exact_key_boost": 0.3,
         "default_debounce_ms": 200,
         "notes": "Reviewable thresholds for operatorSearch. Module constants in services/operator_search.py + aios-ui/server/aios/operator-search.ts currently hardcode these values; a follow-up phase may switch them to read this file. Centralized here so operators can audit and tune before that work lands."
       }
       ```

    3. **Create aios-ui/components/search/GlobalSearchInput.tsx** (client component):
       - `"use client"` directive at top.
       - Imports: `import { useState } from "react"; import Link from "next/link"; import { trpc } from "@/lib/trpc";` (confirm the trpc client path during read_first).
       - State: search query (string).
       - Debounce: a small custom hook `useDebouncedValue(value, 200)` (define inline or in a small utils file if multiple components need it; for this plan, inline is fine per "three similar lines is better than premature abstraction").
       - tRPC query: `const { data: hits, isLoading } = trpc.operatorSearch.search.useQuery({ query: debouncedQuery, limit: 5 }, { enabled: debouncedQuery.length >= 2 });`.
       - JSX: input element + dropdown panel (when hits available) listing top-5 hits as Link components to drill_down_path; "See all results" link to `/search?query=${encodeURIComponent(query)}`.
       - Style: minimal Tailwind / existing utility class conventions; match TopBar's existing height + spacing.

    4. **Create aios-ui/components/search/SearchResults.tsx** (server component OR client component depending on filter interaction model — recommend client component for filter chip toggles):
       - Accept props: `hits: OperatorSearchHit[]`, `query: string`, `selectedKinds?: EntityKind[]`, `projectId?: string`.
       - Render a faceted sidebar grouping hits by kind with counts; main pane lists hits with title + summary + score badge + last_updated_at + drill-in Link to drill_down_path.
       - Empty-query behavior: when query.length === 0, the hits are the recent-activity hits from the server's `searchEntities(db, { query: "", limit: 50 })` call; the component renders "Recent activity" heading.
       - Filter chips: clicking a chip updates the URL (`?kinds=run&kinds=writeback`) and re-fetches via the page's server component refresh (or client-side state — pick the simpler one; client-side state with `useState` + `useRouter` is acceptable for a v1).

    5. **Create aios-ui/components/next-action/NextActionPanel.tsx** (client component):
       - `"use client"` directive.
       - Accept props: `actions: NextAction[]`, `projectId: string | null`.
       - Render actions grouped by priority_bucket (foundational, high_leverage, quick_wins, blocked, waived_deferred). For each action:
         - Title + rationale + drill-in Link to drill_down_path.
         - When `action.kind === "launch_remediation_workflow"` AND `action.recommendedWorkflowKey != null` AND `action.projectId != null`: render a "Launch remediation workflow" button.
         - Button onClick: call `trpc.projects.launchRemediation.useMutation()` with `{ deltaId: <derived from evidenceIds[0]>, projectId: action.projectId }`. On success: render a confirmation toast (or inline message) and call `utils.invalidate()` on nextAction.getForProject + projects.detail + controlPlane.overview queries (Pitfall 10).
       - When `action.kind === "approve_pending_writeback"` OR `"review_learning_proposal"`: render a drill-in link to /writebacks#<id> (no inline approve button — approvals happen on the writebacks page).
       - When `action.kind === "promote_candidate_asset"`: drill-in link to /workflows?promotion=<id>.

    6. **Create aios-ui/components/daily-flow/DailyFlowTrace.tsx** (server component is fine — pure render):
       - Accept props: `trace: DailyFlowTrace`.
       - Render the eight-step card stack in order (the trace.steps tuple is already canonical). Each card:
         - kind icon (use existing icon set if available — Heroicons / Lucide / etc. — verify during read_first; otherwise text-based labels).
         - summary (multi-line truncated to 3 lines).
         - provenance badge: green for "confirmed", blue for "inferred", muted gray for "missing", red for "contradictory".
         - drill-in Link to drill_down_path.
         - When provenance === "missing": render with reduced opacity and the metadata.missing_reason as a tooltip or inline label.
       - Add a small header with "Goal: <objective>" and "Preview" / "Replay" badge based on trace.isPreview.

    7. **Create aios-ui/components/command-center/PhaseStatusBanner.tsx** (server component):
       - Accept props: `phaseStatus: PhaseStatusReport[]`.
       - Filter to phases where status !== "complete".
       - If filtered list is empty, render nothing (no banner).
       - Else, render a Tailwind alert/banner with copy:
         ```
         <p>
           Some operator surfaces are still being populated.
           Phase {N} of 10 ({phaseName}) is still rolling out;
           metrics depending on it will fill in as it ships.
         </p>
         ```
         (List up to 3 incomplete phases; if more, append "and N more.")
       - Banner style: informational, not alarming (Pitfall 8 framing).
       - Optional: a [Learn more] link pointing to an internal phase-status panel (deferred for follow-up phase; for v1, link to /control which already shows control plane health).

    8. **Create aios-ui/components/command-center/SeedDataBanner.tsx** (client component for the Refresh action):
       - `"use client"`.
       - Accept props: `seedDataCount: number` (count of catalog rows with isSeedData=true).
       - When seedDataCount > 0, render a warning banner: "Demo data shown for {N} catalog rows — registry parse failed or fallback active. [Refresh]"
       - [Refresh] button: calls `utils.controlPlane.overview.invalidate()` to re-fetch.

    9. **Extend aios-ui/components/layout/TopBar.tsx** to embed GlobalSearchInput:
       - Insert the GlobalSearchInput component in the existing top-bar JSX, ideally between logo and right-side icons.
       - The TopBar may need to become a client component (or stay server + wrap the GlobalSearchInput as a client child); decide based on the existing file's shape during read_first. Recommend: keep TopBar as-is (server component if currently server) and import the client GlobalSearchInput as a child.

    10. **Quality ladder:** `cd aios-ui && pnpm lint && pnpm tsc --noEmit && pnpm lint:architecture`.
  </action>
  <verify>
    <automated>cd aios-ui && pnpm lint && pnpm tsc --noEmit && pnpm lint:architecture</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/search" aios-ui/lib/constants.ts` returns at least 1
    - `test -f config/operator-surfaces/search-policy.json && python -c "import json; json.load(open('config/operator-surfaces/search-policy.json'))"` exits 0 (valid JSON)
    - `grep -c "max_query_length\|default_limit\|per_kind_cap\|recency_boost_half_life_days\|exact_key_boost\|default_debounce_ms" config/operator-surfaces/search-policy.json` returns at least 6
    - `test -f aios-ui/components/search/GlobalSearchInput.tsx && test -f aios-ui/components/search/SearchResults.tsx && test -f aios-ui/components/next-action/NextActionPanel.tsx && test -f aios-ui/components/daily-flow/DailyFlowTrace.tsx && test -f aios-ui/components/command-center/PhaseStatusBanner.tsx && test -f aios-ui/components/command-center/SeedDataBanner.tsx && echo OK` prints "OK"
    - `grep -l "\"use client\"\|'use client'" aios-ui/components/search/GlobalSearchInput.tsx aios-ui/components/next-action/NextActionPanel.tsx aios-ui/components/command-center/SeedDataBanner.tsx | wc -l | tr -d ' '` returns at least 3
    - `grep -c "operatorSearch.search" aios-ui/components/search/GlobalSearchInput.tsx` returns at least 1
    - `grep -c "launchRemediation" aios-ui/components/next-action/NextActionPanel.tsx` returns at least 1
    - `grep -c "utils.invalidate\|invalidate(" aios-ui/components/next-action/NextActionPanel.tsx` returns at least 1
    - `grep -c "Phase" aios-ui/components/command-center/PhaseStatusBanner.tsx` returns at least 1
    - `grep -c "Demo data" aios-ui/components/command-center/SeedDataBanner.tsx` returns at least 1
    - `grep -c "GlobalSearchInput" aios-ui/components/layout/TopBar.tsx` returns at least 1
    - `cd aios-ui && pnpm lint` exits 0
    - `cd aios-ui && pnpm tsc --noEmit` exits 0
    - `cd aios-ui && pnpm lint:architecture` exits 0
  </acceptance_criteria>
  <done>
    All six new components created (GlobalSearchInput, SearchResults, NextActionPanel, DailyFlowTrace, PhaseStatusBanner, SeedDataBanner); /search added to PRIMARY_NAV; config/operator-surfaces/search-policy.json created with six documented thresholds; TopBar.tsx embeds GlobalSearchInput; all lint + tsc + lint:architecture gates green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend Command Center (app/page.tsx) with phase status banner + seed data banner + NextActionPanel + DailyFlowTrace + LearningImpactRollup row; create app/search/page.tsx; extend project detail (app/projects/[id]/page.tsx) and run detail (app/runs/[id]/page.tsx) with respective new components; migrate app/writebacks/page.tsx to writebacks router; extend app/automations/page.tsx with Trigger button; extend app/query/page.tsx with Launch Workflow button</name>
  <files>aios-ui/app/page.tsx, aios-ui/app/search/page.tsx, aios-ui/app/projects/[id]/page.tsx, aios-ui/app/runs/[id]/page.tsx, aios-ui/app/query/page.tsx, aios-ui/app/writebacks/page.tsx, aios-ui/app/automations/page.tsx</files>
  <read_first>
    - aios-ui/app/page.tsx — read entire 206-line file; identify the layout structure (probably a grid or stack); identify where each new component slots in. Existing consumers: controlPlane.overview, changes.list, automations.list, experiments.list, buildAttentionAlerts, buildCommandCenterTimeline.
    - aios-ui/app/control/page.tsx — read full file (29 lines per RESEARCH); paste-of-record for `await getCaller()` + `await caller.X.Y()` pattern.
    - aios-ui/app/projects/[id]/page.tsx — read entire file; identify the layout and where NextActionPanel and Launch Remediation buttons slot in.
    - aios-ui/app/runs/[id]/page.tsx — read entire file; identify where DailyFlowTrace component slots in (recommend: a prominent section at the top of the page, before the existing run detail content).
    - aios-ui/app/query/page.tsx — 17 lines; identify GroundedQueryStudio component path and whether the studio handles the answer rendering (if so, Plan 10-06 extends the studio; if the page renders the answer directly, extend here).
    - aios-ui/app/writebacks/page.tsx — 51 lines; identify the caller.divergent.writebacks() call to replace.
    - aios-ui/app/automations/page.tsx — 61 lines; identify the row rendering structure.
    - aios-ui/components/query/GroundedQueryStudio.tsx — if it handles the ask mutation client-side, extend with the Launch Workflow button rendering.
    - Plan 10-05 truths block — confirm getControlPlaneOverview extended return shape (nextActions, dailyFlowSummary, learningImpactRollup, phaseStatus, catalog rows with isSeedData).
  </read_first>
  <behavior>
    - Test 1 (type-level): `cd aios-ui && pnpm tsc --noEmit` exits 0 after all page changes.
    - Test 2 (lint): `cd aios-ui && pnpm lint` exits 0.
    - Test 3 (architecture lint): `cd aios-ui && pnpm lint:architecture` exits 0.
    - Test 4 (Command Center consumes new fields): aios-ui/app/page.tsx source references overview.nextActions, overview.dailyFlowSummary, overview.phaseStatus and renders the matching components. Verified via grep.
    - Test 5 (search page exists and consumes operatorSearch.search): aios-ui/app/search/page.tsx imports SearchResults and calls operatorSearch.search.
    - Test 6 (project detail consumes nextAction.getForProject): aios-ui/app/projects/[id]/page.tsx source references nextAction.getForProject and renders NextActionPanel.
    - Test 7 (run detail consumes dailyFlow.replay): aios-ui/app/runs/[id]/page.tsx source references dailyFlow.replay and renders DailyFlowTrace.
    - Test 8 (writebacks migration): aios-ui/app/writebacks/page.tsx no longer references `caller.divergent.writebacks` but references `caller.writebacks.list`.
    - Test 9 (automations Trigger button): aios-ui/app/automations/page.tsx references automations.triggerWorkflow.
    - Test 10 (query Launch Workflow button): aios-ui/app/query/page.tsx OR aios-ui/components/query/GroundedQueryStudio.tsx references recommendedWorkflow + automations.triggerWorkflow (or a similar launch path).
  </behavior>
  <action>
    1. **Extend aios-ui/app/page.tsx (Command Center):**
       - Continue to `await caller.controlPlane.overview()` — the call now returns the extended payload (Plan 10-05).
       - Add at the top of the rendered JSX (above existing alerts):
         - `<PhaseStatusBanner phaseStatus={overview.phaseStatus ?? []} />` — renders only when something is incomplete.
         - `<SeedDataBanner seedDataCount={countSeedDataRows(overview)} />` where `countSeedDataRows` is a small helper that counts isSeedData=true entries across workflowTemplates, agentProfiles, invocationBackends.
       - Add a NextActionPanel section (right side or after attention alerts): `<NextActionPanel actions={overview.nextActions ?? []} projectId={null} />`.
       - Add a DailyFlowTrace section when overview.dailyFlowSummary is non-null: `<DailyFlowTrace trace={overview.dailyFlowSummary.trace} />` with a header like "Daily flow — top project {topProjectId}".
       - Add a LearningImpactRollup row showing the top 3 workflows by sample size: render workflow_key + trend badge + drill-down link. Use a small table or list (no chart in v1 — per RESEARCH alternatives "card stack is sufficient").
       - Preserve all existing renderers (buildAttentionAlerts, buildCommandCenterTimeline, HealthGrid, automations panel, experiments panel).

    2. **Create aios-ui/app/search/page.tsx** (NEW):
       ```typescript
       import { getCaller } from "@/server/getCaller";  // confirm exact path during read_first
       import { SearchResults } from "@/components/search/SearchResults";

       interface SearchPageProps {
           searchParams: { query?: string; kinds?: string | string[]; project?: string };
       }

       export default async function SearchPage({ searchParams }: SearchPageProps): Promise<React.JSX.Element> {
           const caller = await getCaller();
           const query = searchParams.query ?? "";
           const kindsArr = Array.isArray(searchParams.kinds) ? searchParams.kinds : (searchParams.kinds ? [searchParams.kinds] : undefined);
           const hits = await caller.operatorSearch.search({
               query,
               kinds: kindsArr as EntityKind[] | undefined,  // confirm runtime validation
               projectId: searchParams.project ?? undefined,
               limit: 100,
           });
           return (
               <main>
                   <h1>Search</h1>
                   <SearchResults hits={hits} query={query} selectedKinds={kindsArr as EntityKind[] | undefined} projectId={searchParams.project ?? null} />
               </main>
           );
       }
       ```

    3. **Extend aios-ui/app/projects/[id]/page.tsx:**
       - Continue to `await caller.projects.detail({ projectId: params.id })`.
       - Add `const actions = await caller.nextAction.getForProject({ projectId: params.id, limit: 12 });`.
       - Render `<NextActionPanel actions={actions} projectId={params.id} />` at a prominent location (e.g., right column or below project header).
       - For each high-priority delta in the project's standards-health data, render a "Launch remediation" button next to it (the button is part of NextActionPanel, but a duplicate may appear on the dedicated deltas list for explicit operator workflow).
       - Preserve all existing project detail content.

    4. **Extend aios-ui/app/runs/[id]/page.tsx:**
       - Add `const trace = await caller.dailyFlow.replay({ runId: params.id });`.
       - Render `<DailyFlowTrace trace={trace} />` at the top of the page (or in a prominent section).
       - Preserve all existing run detail content.

    5. **Extend aios-ui/app/query/page.tsx (and/or aios-ui/components/query/GroundedQueryStudio.tsx):**
       - Depending on whether the studio handles answer rendering: extend the answer rendering to check `if (answer.recommendedWorkflow) { ... render Launch Workflow button ... }`.
       - The button onClick: call `trpc.automations.triggerWorkflow.useMutation()` with `{ workflowKey: answer.recommendedWorkflow.workflow_key, objective: <derived from question or from the studio's input>, projectId: <from the page's project context if available> }`. On success: utils.invalidate() on controlPlane.overview + query.ask (or revalidate the answer).

    6. **Migrate aios-ui/app/writebacks/page.tsx:**
       - Replace `const proposals = await caller.divergent.writebacks();` with `const proposals = await caller.writebacks.list({ status: 'pending_approval', limit: 100 });`.
       - The render layer accepts whatever shape writebacks.list returns (Plan 10-05); add filter chips for status/policyClass/source (URL-param-driven, similar to /search).

    7. **Extend aios-ui/app/automations/page.tsx:**
       - For each automation row, add a "Trigger workflow" button that opens a small dialog (or inline form) collecting workflowKey + objective + projectId.
       - Button onClick: trpc.automations.triggerWorkflow.useMutation() with the form inputs. On success: utils.invalidate() on automations.list + controlPlane.overview.
       - The dialog/form can be a simple client component embedded in the page; preserve the existing row rendering.

    8. **Quality ladder:** `cd aios-ui && pnpm lint && pnpm tsc --noEmit && pnpm lint:architecture`.
  </action>
  <verify>
    <automated>cd aios-ui && pnpm lint && pnpm tsc --noEmit && pnpm lint:architecture</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PhaseStatusBanner\|SeedDataBanner\|NextActionPanel\|DailyFlowTrace" aios-ui/app/page.tsx` returns at least 4
    - `grep -c "overview.nextActions\|overview.dailyFlowSummary\|overview.phaseStatus\|overview.learningImpactRollup" aios-ui/app/page.tsx` returns at least 3
    - `test -f aios-ui/app/search/page.tsx && echo OK` prints "OK"
    - `grep -c "operatorSearch.search\|SearchResults" aios-ui/app/search/page.tsx` returns at least 2
    - `grep -c "nextAction.getForProject\|NextActionPanel" aios-ui/app/projects/\[id\]/page.tsx` returns at least 2
    - `grep -c "dailyFlow.replay\|DailyFlowTrace" aios-ui/app/runs/\[id\]/page.tsx` returns at least 2
    - `grep -c "writebacks.list" aios-ui/app/writebacks/page.tsx` returns at least 1
    - `grep -c "divergent.writebacks" aios-ui/app/writebacks/page.tsx` returns 0 (migration complete)
    - `grep -c "automations.triggerWorkflow" aios-ui/app/automations/page.tsx aios-ui/components/query/GroundedQueryStudio.tsx aios-ui/app/query/page.tsx 2>/dev/null` returns at least 1
    - `grep -c "recommendedWorkflow" aios-ui/app/query/page.tsx aios-ui/components/query/GroundedQueryStudio.tsx 2>/dev/null` returns at least 1
    - `cd aios-ui && pnpm lint` exits 0
    - `cd aios-ui && pnpm tsc --noEmit` exits 0
    - `cd aios-ui && pnpm lint:architecture` exits 0
  </acceptance_criteria>
  <done>
    aios-ui/app/page.tsx renders PhaseStatusBanner + SeedDataBanner + NextActionPanel + DailyFlowTrace + LearningImpactRollup alongside existing surfaces; /search page exists; project detail renders NextActionPanel; run detail renders DailyFlowTrace; /writebacks page migrated to writebacks.list; automations gets Trigger button; query gets Launch Workflow button on recommendation; all lint + tsc + lint:architecture gates green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual operator check — Phase 10 end-to-end visibility (OPER-01/02/03/04 visible to a real human)</name>
  <what-built>
    The full Phase 10 surface stack is now in place:
    - Python backends (Plans 01-03): operator_search, next_action, daily_flow + CLI subcommands.
    - UI projection layer (Plan 04): drill-down.ts + types + operator-search.ts + next-action.ts + drill_down_path on standards-health and learning rollups.
    - Router layer (Plan 05): four new tRPC routers + automations.triggerWorkflow + projects.launchRemediation + catalog migration + phaseCompletionAudit + query-as-launcher intent.
    - UI rendering (Plan 06, Task 1+2): six new components + /search page + extended Command Center, project detail, run detail, query, writebacks, automations pages.
  </what-built>
  <how-to-verify>
    Run the aios-ui dev server (`cd aios-ui && pnpm dev`) and inspect the operator surfaces as a real human would:

    1. **Sidebar nav**: confirm /search appears in the left sidebar. Click it — the /search page renders with an empty-query "Recent activity" view.

    2. **Top bar search**: type a query (e.g., "implementation") in the top-bar search input. After ~200ms, a dropdown shows top-5 mixed-kind hits. Click one — you land on the entity's drill-down page (e.g., /runs/<id>). Click "See all results" — you land on /search?query=implementation with the full faceted result list.

    3. **Command Center (/)**: the Phase 10 surfaces visible:
       - PhaseStatusBanner at the top IF any phase is incomplete; the banner copy frames the gap constructively ("being populated") not alarmingly ("broken"). If all 10 phases are complete, no banner appears.
       - SeedDataBanner IF any catalog row has isSeedData=true. If the registry parse succeeds, no banner appears.
       - NextActionPanel showing fused next actions across projects, grouped by priority bucket.
       - DailyFlowTrace card stack for the top project (when overview.dailyFlowSummary is non-null) — eight steps in canonical order with provenance badges.
       - LearningImpactRollup row showing top 3 workflows by sample size.
       - Existing alerts + timeline + health grid + automations + experiments panels preserved.

    4. **Project detail (/projects/<id>)**: the page shows NextActionPanel scoped to the project. For high-priority delta actions, the "Launch remediation workflow" button is visible. Click it (on a test project) — the mutation calls projects.launchRemediation; the proposed packet appears in the response; the panel refreshes via utils.invalidate; the next-action list updates within ~1 second.

    5. **Run detail (/runs/<id>)**: the page shows DailyFlowTrace via dailyFlow.replay at the top — eight steps in canonical order with each step's drill-in link working.

    6. **Grounded query (/query)**: ask "what should I do next?" or "is there anything to fix?". When the intent classifier returns should_i_run_workflow AND a recommendation is available, a "Launch workflow" button appears next to the answer. Clicking the button calls automations.triggerWorkflow; the planned packet appears; utils.invalidate fires.

    7. **Writebacks (/writebacks)**: confirm the page now lists writebacks from the new writebacks.list router (not divergent.writebacks). Filter chips for status/policyClass/source work via URL params. Default view shows pending_approval rows including learning_analysis-sourced proposals (with both proposal_status and current_lifecycle_state visible — Pitfall 8 from Phase 9 closed at the UI layer).

    8. **Automations (/automations)**: each automation row has a "Trigger workflow" button. Clicking opens a dialog/form; submitting calls automations.triggerWorkflow with the inputs.

    9. **Drill-down discipline (OPER-03 visual sweep)**: pick five random rows across NextActionPanel, SearchResults, DailyFlowTrace, the writebacks list, and the deltas list on /projects/<id>. Every row has a clickable drill-in link that takes you to the source evidence's canonical page.

    10. **CLI parity check**: in a terminal, run:
        - `aios operator-search --query "implementation" --json | head -50` — returns mixed-kind hits.
        - `aios next-action --project <some-project-id> --json` — returns ranked NextActions.
        - `aios daily-flow --run-id <some-real-run-id> --json | head -100` — returns the eight-step trace.
        - `aios daily-flow --objective "test preview" --dry-run --json` — returns a preview trace; afterwards `aios contracts-audit --json | grep -A2 "OperatorSurface\|NextAction\|DailyFlow"` confirms all three contract rows are now `status: "implemented"` (each was "partial" while the UI mirror was missing in Plans 10-01/02/03; the rows flip to "implemented" once Plans 10-04/05/06 are present on disk).

    11. **Workflow trigger safety (Pitfall 4 sanity check)**: in browser devtools' Network panel, click any "Launch workflow" / "Launch remediation" / "Trigger workflow" button. Confirm the request that fires is the mutation procedure (automations.triggerWorkflow OR projects.launchRemediation). Inside the server logs (or by inspecting the response shape), confirm BOTH a planTask result AND an invokeControlPlaneRun result are returned. There is no path where invokeControlPlaneRun fires without a prior planTask.

    12. **OPER-04 end-to-end daily flow**: pick a vague goal (e.g., "fix the login bug"). Submit through the grounded-query interface OR through `aios daily-flow --objective "fix the login bug" --dry-run --json`. Confirm the trace shows all eight steps with drill-down links and provenance labels. Confirm no orchestration_runs / briefing_packets rows were inserted by the preview (verify via `sqlite3 ~/AIOS/data/aios.db "SELECT COUNT(*) FROM orchestration_runs;"` before and after).

    If any of these checks fails, capture the failure (screenshot or terminal output) and describe it. Otherwise, type "approved".
  </how-to-verify>
  <resume-signal>Type "approved" or describe the failure (which check, what was expected, what was observed).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser -> tRPC procedures | All Plan 10-05 procedures already validated with zod; Plan 10-06 components only construct inputs from URL params + form state + props. |
| Plan 10-06 components -> drill-down hrefs | All hrefs come from drill_down_path fields populated by Plan 04's lib/drill-down.ts; no inline string interpolation in components. |
| Mutations (launchRemediation, triggerWorkflow) -> planTask + invoke | Plan 10-05 enforced ordering at the router level; Plan 10-06 just calls the mutation. Browser cannot bypass — the server enforces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-31 | Information Disclosure | Phase status banner copy frames the gap as "broken" — operator stops using the tool (Pitfall 8) | mitigate | Banner copy uses "being populated" / "rolling out" framing; Test 9 + acceptance criterion grep gate enforce the wording. |
| T-10-32 | Tampering | Operator clicks Launch Workflow but the mutation flow skips planTask (Pitfall 4) | accept (out of scope for Plan 10-06) | Plan 10-05 enforces planTask-first at the router level; the UI calls the mutation as-is and cannot bypass. If the router contract is violated, the bug surfaces at the API layer, not the UI. |
| T-10-33 | Information Disclosure | Next-action stale after operator acts (Pitfall 10) | mitigate | NextActionPanel calls utils.invalidate() on all relevant queries after each mutation; Test 7 acceptance criterion grep gate enforces. |
| T-10-34 | Information Disclosure | Catalog seed data appears in the Command Center without a "demo data" label (Pitfall 7) | mitigate | SeedDataBanner renders when seedDataCount > 0; Task 1 Test 10 enforces "Demo data" text. |
| T-10-35 | Repudiation | Search dropdown shows top-5 but the "See all results" link goes to the wrong query string | mitigate | GlobalSearchInput uses encodeURIComponent on the query parameter; the search page reads searchParams.query and re-runs the search with the same string. |
| T-10-36 | Denial of Service | Operator types fast → search fires on every keystroke (Pitfall 9) | mitigate | GlobalSearchInput debounces 200ms (config/operator-surfaces/search-policy.json default_debounce_ms=200); useQuery enabled gate `query.length >= 2` prevents 1-char fan-out. |
| T-10-37 | Repudiation | DailyFlowTrace misorders steps | mitigate | The trace.steps tuple is canonical (Plans 10-03/04/05 enforce CANONICAL_STEP_ORDER); the component renders in iteration order without sorting. Server-side trace shape is the contract. |
| T-10-38 | Information Disclosure | Drill-down link to a private route (auth-gated content) | accept | AIOS is single-user local-first per AGENTS.md; no auth boundary inside the UI. If multi-user is later introduced, drill-down construction in lib/drill-down.ts will need auth scoping — out of Phase 10's scope. |
</threat_model>

<verification>
- `cd aios-ui && pnpm lint` exits 0
- `cd aios-ui && pnpm tsc --noEmit` exits 0
- `cd aios-ui && pnpm lint:architecture` exits 0
- Manual operator check (Task 3) completed and approved.
- `python -c "import json; json.load(open('config/operator-surfaces/search-policy.json'))"` exits 0.
- Phase 10 contracts-audit transitions: `aios contracts-audit --json | python -c "import json,sys; d=json.load(sys.stdin); print([c for c in d['contracts'] if c['name'] in ('OperatorSurface', 'NextAction', 'DailyFlow')])"` shows all three rows with `status: "implemented"` once aios-ui/server/aios/operator-search.ts, next-action.ts, daily-flow.ts exist on disk (they do — Plans 10-04 and 10-05 placed them).
- Quality ladder per CLAUDE.md (UI lane): hard gates green.
</verification>

<success_criteria>
- OPER-01 (rendering complete): GlobalSearchInput in TopBar; /search page; SearchResults grouped by EntityKind with filter chips.
- OPER-02 (rendering complete): NextActionPanel on Command Center + project detail; Launch Workflow button on grounded-query recommendations; Launch Remediation button on high-priority deltas.
- OPER-03 (rendering complete): every operator-visible row has a working drill-down link (visual sweep in Task 3 step 9 verifies); standards-health rows + learning rollups + writeback rows + delta rows all carry drill_down_path; centralized URL construction in lib/drill-down.ts prevents drift.
- OPER-04 (rendering complete): DailyFlowTrace on Command Center (top project) + /runs/[id]; eight steps in canonical order; preview is dry; replay is read-only; provenance badges visible; missing-step labels constructive.
- Pitfall 1 closed: PhaseStatusBanner renders explicit "Phase N being populated" labels.
- Pitfall 7 closed: SeedDataBanner renders when isSeedData=true rows present.
- Pitfall 8 closed: banner copy framed constructively.
- Pitfall 9 closed: faceted search with limit + per-kind cap + filter chips + empty-query recent-activity.
- Pitfall 10 closed: utils.invalidate() after every mutation in NextActionPanel + GroundedQueryStudio + automations page.
- Catalog migration visible at the UI layer; legacy hardcoded constants no longer authoritative.
- Writebacks page now consumes the new writebacks router; divergent.writebacks reference removed.
- Phase 10 contracts-audit shows OperatorSurface, NextAction, DailyFlow as `status: implemented`.
</success_criteria>

<output>
After completion, create `.planning/phases/10-operator-surfaces-query-and-daily-flow-visibility/10-06-SUMMARY.md` per the template, including:
- The eight new components (their files + purpose + key tRPC procedures they consume).
- The eight extended pages and what each gains.
- The PRIMARY_NAV /search entry and the config/operator-surfaces/search-policy.json thresholds.
- The Pitfall closures with the specific UI-layer mitigation per pitfall (1, 7, 8, 9, 10).
- The catalog migration UI surface (SeedDataBanner; the operator can now see when fallback fires).
- The two new mutation surfaces (Launch Remediation on project detail; Launch Workflow on grounded query) and the utils.invalidate calls that close Pitfall 10.
- The writebacks page migration from divergent.writebacks to writebacks.list.
- The manual operator check evidence (which checks passed; any caveats).
- Final note: Phase 10 is complete; all OPER-01..04 requirements observable end-to-end; the daily-flow loop (vague goal -> routing -> execution -> evaluation -> writeback -> unresolved deltas -> next action) is visible from both the Command Center and the per-run page; the system is now ready for operator hand-off and (potentially) milestone 5 closeout.
- Optional follow-up: a phase-status detail page (currently a banner with [Learn more] link to /control); FTS5 search indexing if profiling shows latency >200ms; auto-invoke fast paths for trusted automations (deferred per RESEARCH A6 — no Phase-10 scope).
</output>
