---
phase: 10-operator-surfaces-query-and-daily-flow-visibility
plan: "02"
type: execute
wave: 1
autonomous: true
depends_on: []
files_modified:
  - services/next_action.py
  - services/aios_cli.py
  - tests/test_next_action.py
  - tests/test_aios_cli.py
  - tests/test_architecture_enforcement.py
requirements:
  - OPER-02
  - OPER-03
must_haves:
  truths:
    - "services/next_action.py exports get_next_actions(conn, *, project_id=None, limit=10) -> list[NextAction] that fuses recommendations from at least seven sources: standards_delta_items (Phase 7), improvement_writebacks (Phase 5 pending), success_criteria_findings (Phase 6 open blockers), terminal-run gaps (existing), workflow_learning_events / improvement_writebacks where source=learning_analysis (Phase 9 conservative proposals), standards_backfill_tasks (Phase 7), promotion_lifecycle_items in candidate state (Phase 8)."
    - "NextAction dataclass carries kind (NextActionKind Literal), title, rationale, project_id (nullable), priority_bucket (foundational|high_leverage|quick_wins|blocked|waived_deferred), recommended_workflow_key (nullable), evidence_ids (tuple of run/writeback/finding/delta ids), drill_down_path (non-null), confidence (0..1), metadata dict."
    - "NextActionKind Literal includes at least: launch_remediation_workflow, approve_pending_writeback, resolve_open_blocker, review_learning_proposal, fix_terminal_run_gap, promote_candidate_asset, investigate_regressed_metric, complete_backfill_task."
    - "Ranking sorts by (_bucket_weight(priority_bucket), -confidence) ascending; foundational bucket ranks highest; ties broken by confidence descending; limit applied after sort."
    - "Every NextAction has a non-null drill_down_path constructed via the same path conventions as services/operator_search.py (/runs/<id>, /writebacks#<id>, /projects/<id>?delta=<id>, /projects/<id>?backfill=<id>, /control?pattern=<id>); the construction is centralized in services/next_action.py:_drill_down_for so paths stay consistent within the Python layer (Plan 04 mirrors in aios-ui/lib/drill-down.ts)."
    - "get_next_actions tolerates missing source tables: when a Phase-N table is absent on the DB, the corresponding sub-fetcher is skipped silently (debug-logged); the remaining buckets still populate; the returned actions list still ranks correctly. Empty result is valid; raises no exceptions."
    - "services/next_action.py does NOT import from bin/; when it needs Phase 5 helpers (writeback_approval_policy), it imports from the services-side shim Phase 8 established or duplicates the small policy fragment — never imports bin/aios_orchestration_runtime."
    - "services/aios_cli.py exposes a new aios next-action subcommand: --project (optional), --limit (default 10, max 50), --json (default true). cmd_next_action returns dataclasses.asdict() of each NextAction; contracts-audit gains a NextAction row tracking implementation status."
  artifacts:
    - path: "services/next_action.py"
      provides: "NextActionKind Literal; NextAction dataclass; get_next_actions(); _bucket_weight(); per-source sub-fetchers (_from_health_deltas, _from_pending_writebacks, _from_open_blockers, _from_terminal_run_gaps, _from_learning_proposals, _from_backfill_tasks, _from_promotion_candidates); _drill_down_for() helper; _safe_table_exists() helper."
    - path: "services/aios_cli.py"
      provides: "cmd_next_action; argparse subparser registration; dispatcher branch; contracts-audit row for NextAction (status=partial in this plan; flips to implemented in Plan 05/06)."
    - path: "tests/test_next_action.py"
      provides: "fusion test (≥5 sources contribute); priority ranking test (foundational > high_leverage > quick_wins); confidence tiebreak test; remediation action carries recommended_workflow_key; drill_down_path non-null for every action; missing-table tolerance; per-project filter; limit truncation; cross-project default when project_id=None."
    - path: "tests/test_aios_cli.py"
      provides: "next-action CLI subcommand JSON test; limit validation test; contracts-audit NextAction row test."
    - path: "tests/test_architecture_enforcement.py"
      provides: "assertion that services/next_action.py does not import from bin/."
  key_links:
    - from: "services/aios_cli.py:cmd_next_action"
      to: "services/next_action.get_next_actions"
      via: "direct module import; argparse validates project/limit; JSON output via dataclasses.asdict"
      pattern: "from services.next_action import|get_next_actions"
    - from: "services/next_action.py:get_next_actions"
      to: "per-source sub-fetchers (_from_health_deltas, _from_pending_writebacks, _from_open_blockers, _from_terminal_run_gaps, _from_learning_proposals, _from_backfill_tasks, _from_promotion_candidates)"
      via: "sequential calls; results extended into actions list; sorted by (_bucket_weight, -confidence)"
      pattern: "_from_health_deltas|_from_pending_writebacks|_from_open_blockers|_from_terminal_run_gaps|_from_learning_proposals|_from_backfill_tasks|_from_promotion_candidates"
    - from: "services/next_action.py:_from_*"
      to: "SQLite tables (standards_delta_items, improvement_writebacks, success_criteria_findings, orchestration_runs, standards_backfill_tasks, promotion_lifecycle_items)"
      via: "bounded SELECT with project_id WHERE filter when applicable; _safe_table_exists() guards"
      pattern: "WHERE.*resolution_status\\s*=\\s*'open'|WHERE.*status\\s*=\\s*'pending_approval'|WHERE.*priority_bucket"
---

<objective>
Ship the Python next-action fusion projection that powers OPER-02: a single ranked NextAction list per project (or across projects) fusing seven existing recommendation sources from Phases 5/6/7/8/9. Produce a typed `NextAction` shape carrying `recommended_workflow_key` (so the UI can render a "Launch" button) and `drill_down_path` (OPER-03). Expose via an `aios next-action` CLI subcommand. No new SQLite tables (per RESEARCH "no new tables for next-action").

Purpose: OPER-02 requires AIOS to answer "which project needs attention, what good looks like, which workflow should run, which assets apply, and which context an agent needs" before manual prep. Each piece of evidence already exists (Phase 7 health deltas, Phase 5 pending writebacks, Phase 6 open blockers, Phase 9 learning proposals, etc.). What is missing is the fusion. This plan ships the fusion backend; the UI projection (Plan 04), tRPC router (Plan 05), and rendered surfaces (Plan 06) consume it.

Output: services/next_action.py NEW (~350 lines); services/aios_cli.py extension; two test file additions; one architecture-enforcement assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-operator-surfaces-query-and-daily-flow-visibility/10-RESEARCH.md

<interfaces>
<!-- Symbols and contracts this plan composes against. Read these in read_first before writing. -->

From services/standards_health.py (Phase 7 — read during planning to confirm shape):
- standards_delta_items columns: id, project_id, domain, priority_bucket, severity, leverage, summary, remediation_playbook_json, status, last_evaluated_at — find via `grep -n "standards_delta_items\|CREATE TABLE.*standards_delta_items" schema.sql services/standards_health.py | head -10`.
- standards_delta_items.remediation_playbook_json carries `recommended_workflow_key` (Phase 7) — that key becomes NextAction.recommended_workflow_key for launch_remediation_workflow kinds.

From improvement_writebacks (Phase 5):
- columns: id, run_id, project_id, layer_type, layer_key, title, summary, status, requires_approval, impact_scope, proposed_change_json, created_at, decided_at, decided_by — verify via `grep -n "CREATE TABLE.*improvement_writebacks" schema.sql`.
- pending approval rows have status='pending_approval' AND requires_approval=1.
- learning-analysis-sourced proposals have proposed_change_json LIKE '%"source": "learning_analysis"%' (Phase 9 metadata convention).

From success_criteria_findings (Phase 6):
- columns: id, run_id, criterion_id, level, message, resolution_status, evidence_json, created_at — verify via `grep -n "CREATE TABLE.*success_criteria_findings" schema.sql`.
- open blockers: level='blocker' AND resolution_status='open'.

From orchestration_runs (existing):
- columns: id, objective, project_id, workflow_key, status, route_result_json, terminal_at, created_at, updated_at — verify via grep.
- terminal-run gaps: existing helper `terminal_run_gaps` may live in services/aios_cli.py or services/standards_health.py; find via `grep -n "terminal_run_gap\|terminalRunGap" services/ aios-ui/server/aios/control-plane.ts | head -10`.

From standards_backfill_tasks (Phase 7):
- columns: id, project_id, summary, status, priority_bucket (or equivalent), created_at — verify via grep.

From promotion_lifecycle_items (Phase 5/8):
- columns: id, item_kind, item_key, status, project_id (if present), created_at, updated_at — verify via grep.
- candidate-state rows ready for promotion: status='candidate' (per Phase 8 five-state literal).

From services/standards_health.py recommend_workflow_from_health (Phase 7):
- signature: recommend_workflow_from_health(conn, project_id) -> list[WorkflowRecommendation] or similar; find via `grep -n "def recommend_workflow_from_health" services/standards_health.py | head -5`.
- if Phase 7 is incomplete, the helper may not exist; this plan's _from_health_deltas tolerates absence by reading standards_delta_items directly (without the recommend_workflow_from_health intermediary).

From services/learning_analysis.py + services/conservative_optimizer.py (Phase 9):
- listConservativeProposals tRPC shape (Plan 09-05) mirrors a SELECT on improvement_writebacks WHERE proposed_change_json LIKE '%"source": "learning_analysis"%'; the Python equivalent is the same query.

From services/aios_cli.py:
- argparse subparser registration block; existing cmd_* function shape; _contracts_audit_payload location; _open_connection (or equivalent) helper — verify via grep as in Plan 10-01.

From RESEARCH.md (this phase):
- Pattern 2 (Next-Action Fusion) — code sketch for NextActionKind, NextAction, get_next_actions, _bucket_weight.
- Pitfall 10 (stale recommendation after operator acts) — next-action recomputes on every read; no cache.
- Architectural Responsibility Map row for next-action recommendation — Primary Tier: Python (`services/next_action.py`); Secondary Tier: UI mirror.
- Don't Hand-Roll — recommend_workflow_from_health (Phase 7), listConservativeProposals (Phase 9), terminal_run_gaps (existing), pending writebacks (Phase 5), open blockers (Phase 6), backfill tasks (Phase 7), promotion candidates (Phase 8).

NOTE: services/next_action.py CAN import services/operator_search._drill_down_for to share path construction, OR duplicate the small path helper (no abstraction yet; "three similar lines is better than premature abstraction"). Recommended: duplicate the small helper so the modules don't gain unnecessary cross-dependencies. If Plan 04 later refactors path construction into a shared services/drill_down.py, both Python modules can adopt it then.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create services/next_action.py with NextActionKind Literal, NextAction dataclass, seven per-source sub-fetchers, get_next_actions entrypoint, ranking logic, and missing-table tolerance</name>
  <files>services/next_action.py, tests/test_next_action.py, tests/test_architecture_enforcement.py</files>
  <read_first>
    - .planning/phases/10-operator-surfaces-query-and-daily-flow-visibility/10-RESEARCH.md — Pattern 2 (Next-Action Fusion) verbatim; Pitfall 10 (stale recommendation); Don't Hand-Roll list (which recommenders to fuse).
    - services/standards_health.py — read the first 150 lines; identify the standards_delta_items shape, priority_bucket values, and recommend_workflow_from_health signature if it exists (find via `grep -n "def recommend_workflow_from_health\|priority_bucket\|remediation_playbook_json" services/standards_health.py | head -10`).
    - services/aios_cli.py — find existing terminal_run_gaps helper (`grep -n "terminal_run_gap\|def _terminal_run\|fix_terminal" services/aios_cli.py | head -10`); if found, study its query shape.
    - aios-ui/server/aios/control-plane.ts — read `terminalRunGaps` to confirm the gap-row shape (run_id, missing_evidence, project_id, last_status).
    - tests/test_standards_health.py — first 80 lines for in-memory sqlite + schema seeding pattern.
    - schema.sql — verify columns on standards_delta_items, improvement_writebacks, success_criteria_findings, orchestration_runs, standards_backfill_tasks, promotion_lifecycle_items (find via the grep commands in the interfaces block).
    - tests/test_architecture_enforcement.py — existing pattern.
  </read_first>
  <behavior>
    - Test 1 `test_next_actions_fuse_multiple_sources`: seed a DB with at least five source signals — one standards_delta_items row with priority_bucket='foundational', one pending improvement_writebacks row, one open blocker (success_criteria_findings level='blocker' resolution_status='open'), one terminal run with status='completed' but no workflow_learning_event, one standards_backfill_tasks row with status='pending'; call `get_next_actions(conn, project_id="p1")`; the returned list contains at least one NextAction per source kind (kinds include launch_remediation_workflow, approve_pending_writeback, resolve_open_blocker, fix_terminal_run_gap, complete_backfill_task at minimum).
    - Test 2 `test_next_action_ranking_respects_bucket_weight`: seed two deltas — one foundational, one high_leverage — both for project p1; the returned list has the foundational action ranked above the high_leverage action regardless of confidence values.
    - Test 3 `test_next_action_confidence_tiebreak_within_bucket`: seed two foundational deltas with confidence values 0.9 and 0.5; the 0.9-confidence action ranks above the 0.5-confidence action.
    - Test 4 `test_remediation_action_carries_workflow_key`: seed a standards_delta_items row whose remediation_playbook_json contains `{"recommended_workflow_key": "standards-backfill"}`; the resulting launch_remediation_workflow NextAction's `recommended_workflow_key == "standards-backfill"`. (For deltas without a workflow key in the playbook, recommended_workflow_key is None.)
    - Test 5 `test_review_learning_proposal_kind_from_writeback`: seed an improvement_writebacks row with `proposed_change_json` containing `"source": "learning_analysis"` and status='pending_approval'; the resulting NextAction has kind='review_learning_proposal' (NOT approve_pending_writeback — learning proposals are routed to the learning bucket).
    - Test 6 `test_all_next_actions_have_drill_down_path`: every NextAction in the result has a non-null drill_down_path starting with "/"; the path shape matches the action's kind (delta -> /projects/<id>?delta=<id>, writeback -> /writebacks#<id>, finding -> /runs/<run_id>?finding=<id>, backfill -> /projects/<id>?backfill=<id>, terminal-run gap -> /runs/<run_id>, learning proposal -> /writebacks#<id>, promotion candidate -> /workflows?promotion=<id>).
    - Test 7 `test_next_action_tolerates_missing_tables`: create a DB with ONLY orchestration_runs and improvement_writebacks (omit standards_delta_items, success_criteria_findings, standards_backfill_tasks, promotion_lifecycle_items); `get_next_actions(conn, project_id="p1")` returns NextActions from the two available sources WITHOUT raising; missing-table sub-fetchers are skipped.
    - Test 8 `test_next_action_honors_project_filter`: seed deltas and writebacks across two projects (p1 and p2); `get_next_actions(conn, project_id="p1")` returns only actions where action.project_id == "p1" or project_id IS NULL (project-agnostic rows like cross-project promotion candidates may pass through with project_id=None; document this in the function docstring).
    - Test 9 `test_next_action_cross_project_when_no_filter`: seed actions across two projects; `get_next_actions(conn, project_id=None)` returns actions from both projects, ranked together.
    - Test 10 `test_next_action_limit_truncation`: seed 20 actions across mixed priority buckets; `get_next_actions(conn, project_id="p1", limit=5)` returns at most 5 actions; the top-5 are the highest-priority by ranking.
    - Test 11 `test_next_action_empty_db_returns_empty_list`: an empty DB (just CREATE TABLE statements, no rows) -> get_next_actions returns []; no exception.
    - Test 12 (architecture) `test_next_action_does_not_import_bin`.
  </behavior>
  <action>
    1. **Create services/next_action.py** with the following structure (per RESEARCH Pattern 2):

       Module top:
       - imports: `from __future__ import annotations`, `import json`, `import logging`, `import sqlite3`, `from dataclasses import dataclass, field`, `from typing import Any, Final, Literal`.
       - `logger = logging.getLogger(__name__)`.
       - `NextActionKind = Literal["launch_remediation_workflow", "approve_pending_writeback", "resolve_open_blocker", "review_learning_proposal", "fix_terminal_run_gap", "promote_candidate_asset", "investigate_regressed_metric", "complete_backfill_task"]`.
       - `PriorityBucket = Literal["foundational", "high_leverage", "quick_wins", "blocked", "waived_deferred"]`.
       - Constants: `DEFAULT_LIMIT: Final[int] = 10`, `MAX_LIMIT: Final[int] = 50`, `BUCKET_WEIGHTS: Final[dict[PriorityBucket, int]] = {"foundational": 0, "high_leverage": 1, "quick_wins": 2, "blocked": 3, "waived_deferred": 4}` (lower weight = higher rank).

       `NextAction` frozen dataclass:
       - fields: kind (NextActionKind), title (str), rationale (str), project_id (Optional[str]), priority_bucket (PriorityBucket), recommended_workflow_key (Optional[str]), evidence_ids (tuple[str, ...]), drill_down_path (str), confidence (float), metadata (dict[str, Any]) via `field(default_factory=dict)`.

       `_bucket_weight(bucket: PriorityBucket) -> int`: returns BUCKET_WEIGHTS[bucket] with a fallback of 99 for unknown values (defensive coding for legacy data).

       `_safe_table_exists(conn, table_name)`: mirrors the helper from services/operator_search.py (duplicate, do not import — small, three-line function).

       `_drill_down_for(kind, *, id_, project_id=None, run_id=None) -> str`: same path constructions as services/operator_search.py for the seven NextAction-relevant kinds. Duplicate here (small, deterministic; consolidation can happen later if both modules grow further).

       Per-source sub-fetchers (each returns `list[NextAction]`):

       - `_from_health_deltas(conn, *, project_id) -> list[NextAction]`:
         - skip if not _safe_table_exists(conn, "standards_delta_items").
         - SELECT id, project_id, domain, priority_bucket, severity, leverage, summary, remediation_playbook_json, status, last_evaluated_at FROM standards_delta_items WHERE status IN ('open', 'pending') and apply project_id filter; ORDER BY priority_bucket ASC, severity DESC; LIMIT 20.
         - For each row, parse remediation_playbook_json (JSON); extract recommended_workflow_key; build NextAction(kind="launch_remediation_workflow", title=f"Remediate {domain}", rationale=summary, priority_bucket=row['priority_bucket'] or 'quick_wins', recommended_workflow_key=playbook.get('recommended_workflow_key'), drill_down_path=_drill_down_for("delta_item", id_=row['id'], project_id=row['project_id']), confidence=min(severity/10.0, 1.0) if severity else 0.5, evidence_ids=(row['id'],)).

       - `_from_pending_writebacks(conn, *, project_id) -> list[NextAction]`:
         - skip if not _safe_table_exists(conn, "improvement_writebacks").
         - SELECT id, run_id, project_id, layer_type, layer_key, title, summary, status, requires_approval, proposed_change_json, created_at FROM improvement_writebacks WHERE status='pending_approval' AND requires_approval=1 and apply project_id filter; ORDER BY created_at DESC; LIMIT 20.
         - For each row: if proposed_change_json contains `"source": "learning_analysis"` then kind="review_learning_proposal" else kind="approve_pending_writeback". priority_bucket=high_leverage for learning proposals, foundational for non-learning approvals. drill_down_path=_drill_down_for("writeback", id_=row['id']). evidence_ids=(row['id'], row.get('run_id') or "").

       - `_from_open_blockers(conn, *, project_id) -> list[NextAction]`:
         - skip if not _safe_table_exists(conn, "success_criteria_findings").
         - SELECT f.id, f.run_id, f.criterion_id, f.level, f.message, f.resolution_status, f.evidence_json, f.created_at, r.project_id AS project_id FROM success_criteria_findings f LEFT JOIN orchestration_runs r ON r.id = f.run_id WHERE f.level='blocker' AND f.resolution_status='open' and apply project_id filter via the joined column; ORDER BY f.created_at DESC; LIMIT 20.
         - For each row: kind="resolve_open_blocker", priority_bucket=foundational, drill_down_path=_drill_down_for("finding", id_=row['id'], run_id=row['run_id']), confidence=0.9 (open blockers are high-signal), evidence_ids=(row['id'], row['run_id']).

       - `_from_terminal_run_gaps(conn, *, project_id) -> list[NextAction]`:
         - Detect terminal runs (status IN ('completed', 'failed', 'partial', 'follow_up_needed')) with NO corresponding workflow_learning_events row. SELECT r.id, r.project_id, r.objective, r.status FROM orchestration_runs r LEFT JOIN workflow_learning_events e ON e.run_id = r.id WHERE r.status IN (...) AND e.id IS NULL and apply project_id filter; LIMIT 20.
         - If workflow_learning_events table is missing, skip the LEFT JOIN guard via _safe_table_exists; otherwise list all terminal runs (graceful degradation when Phase 9 isn't fully populated).
         - kind="fix_terminal_run_gap", priority_bucket=high_leverage, drill_down_path=_drill_down_for("run", id_=row['id']), evidence_ids=(row['id'],), confidence=0.7.

       - `_from_learning_proposals(conn, *, project_id) -> list[NextAction]`:
         - NOTE: learning proposals are already routed via _from_pending_writebacks (kind="review_learning_proposal" path) — keep _from_learning_proposals as a stub that returns [] OR remove from the dispatch list. Decision: REMOVE this sub-fetcher; learning proposals are surfaced through the writebacks fetcher with kind discrimination. Document this in the get_next_actions docstring so the seven-source claim still holds (counting "learning proposals" as a discriminated kind within pending writebacks).

       - `_from_backfill_tasks(conn, *, project_id) -> list[NextAction]`:
         - skip if not _safe_table_exists(conn, "standards_backfill_tasks").
         - SELECT id, project_id, summary, status, priority_bucket, created_at FROM standards_backfill_tasks WHERE status IN ('pending', 'in_progress') and apply project_id filter; ORDER BY priority_bucket ASC, created_at ASC; LIMIT 20.
         - kind="complete_backfill_task", priority_bucket=row['priority_bucket'] or 'quick_wins', drill_down_path=_drill_down_for("backfill_task", id_=row['id'], project_id=row['project_id']), confidence=0.6, evidence_ids=(row['id'],).

       - `_from_promotion_candidates(conn, *, project_id) -> list[NextAction]`:
         - skip if not _safe_table_exists(conn, "promotion_lifecycle_items").
         - SELECT id, item_kind, item_key, status, created_at FROM promotion_lifecycle_items WHERE status='candidate'; LIMIT 20. (Note: promotion_lifecycle_items may not carry project_id; the project filter doesn't apply here, and these rows surface across projects.)
         - kind="promote_candidate_asset", priority_bucket=quick_wins, drill_down_path=_drill_down_for("promotion_lifecycle_item", id_=row['id']), confidence=0.65, evidence_ids=(row['id'],), project_id=None.

       `get_next_actions(conn, *, project_id=None, limit=DEFAULT_LIMIT) -> list[NextAction]`:
       - validate: `if limit < 1 or limit > MAX_LIMIT: raise ValueError(...)`.
       - aggregate from the six active sub-fetchers (the _from_learning_proposals stub returns []; documented).
       - sort by (`_bucket_weight(action.priority_bucket)`, `-action.confidence`).
       - return `actions[:limit]`.

    2. **Create tests/test_next_action.py** with the 11 behavior tests. Use in-memory sqlite + per-test schema seeding (no bin/ schema bootstrap). Each test asserts on the returned NextAction list shape and ranking.

    3. **Extend tests/test_architecture_enforcement.py** with `test_next_action_does_not_import_bin`.

    4. **Quality ladder per touched file:**
       - `uv run pytest tests/test_next_action.py tests/test_architecture_enforcement.py -x -q`
       - `uv run ruff check services/next_action.py tests/test_next_action.py tests/test_architecture_enforcement.py`
       - `uv run ruff format --check services/next_action.py tests/test_next_action.py tests/test_architecture_enforcement.py`
       - `uv run basedpyright services/next_action.py`
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_next_action.py tests/test_architecture_enforcement.py -x -q -k "next_actions_fuse or next_action_ranking or next_action_confidence_tiebreak or remediation_action_carries or review_learning_proposal_kind or all_next_actions_have_drill or next_action_tolerates_missing or next_action_honors_project or next_action_cross_project or next_action_limit_truncation or next_action_empty_db or next_action_does_not_import_bin"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f services/next_action.py && echo OK` prints "OK"
    - `grep -c "NextActionKind = Literal\|@dataclass" services/next_action.py` returns at least 2
    - `grep -c "_from_health_deltas\|_from_pending_writebacks\|_from_open_blockers\|_from_terminal_run_gaps\|_from_backfill_tasks\|_from_promotion_candidates" services/next_action.py` returns at least 6
    - `grep -c "from bin\|import bin" services/next_action.py` returns 0
    - `grep -c "get_next_actions\|_bucket_weight\|_drill_down_for\|_safe_table_exists" services/next_action.py` returns at least 4
    - `grep -c "launch_remediation_workflow\|approve_pending_writeback\|resolve_open_blocker\|review_learning_proposal\|fix_terminal_run_gap\|promote_candidate_asset\|complete_backfill_task" services/next_action.py` returns at least 7
    - `grep -c "def test_next_actions_fuse_multiple_sources\|def test_next_action_ranking_respects_bucket_weight\|def test_next_action_confidence_tiebreak_within_bucket\|def test_remediation_action_carries_workflow_key\|def test_review_learning_proposal_kind_from_writeback\|def test_all_next_actions_have_drill_down_path\|def test_next_action_tolerates_missing_tables\|def test_next_action_honors_project_filter\|def test_next_action_cross_project_when_no_filter\|def test_next_action_limit_truncation\|def test_next_action_empty_db_returns_empty_list" tests/test_next_action.py | grep -v '^#'` returns at least 11
    - `grep -c "def test_next_action_does_not_import_bin" tests/test_architecture_enforcement.py | grep -v '^#'` returns at least 1
    - `uv run pytest tests/test_next_action.py -x -q` exits 0
    - `uv run pytest tests/test_architecture_enforcement.py -x -q` exits 0
    - `uv run ruff check services/next_action.py tests/test_next_action.py tests/test_architecture_enforcement.py` exits 0
    - `uv run ruff format --check services/next_action.py tests/test_next_action.py tests/test_architecture_enforcement.py` exits 0
    - `uv run basedpyright services/next_action.py` exits 0
  </acceptance_criteria>
  <done>
    services/next_action.py defines NextActionKind, NextAction, six active per-source sub-fetchers, get_next_actions, bucket ranking, drill-down path construction, and missing-table tolerance; eleven behavior tests + one architecture test pass; no bin/ imports.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire aios next-action CLI subcommand; extend contracts-audit with NextAction row</name>
  <files>services/aios_cli.py, tests/test_aios_cli.py</files>
  <read_first>
    - services/aios_cli.py — argparse subparser registration block; existing cmd_* function shape as paste-of-record; _contracts_audit_payload location (find via the same greps as Plan 10-01 Task 2).
    - services/next_action.py (Task 1) — confirm get_next_actions signature and NextAction fields.
    - tests/test_aios_cli.py — JSON-output CLI test pattern.
    - .planning/phases/10-operator-surfaces-query-and-daily-flow-visibility/10-RESEARCH.md — Standard Stack (services/aios_cli.py extension); Validation Architecture (test commands).
  </read_first>
  <behavior>
    - Test 1 `test_next_action_cli_json`: invoke `aios next-action --project p1 --json` against a seeded DB; returns JSON with keys `actions` (list of dicts matching NextAction shape), `total_actions` (int), `project_id` (echoed), `limit` (int). Every action dict has `drill_down_path` non-null and `priority_bucket` in the documented literal set.
    - Test 2 `test_next_action_cli_cross_project`: invoke `aios next-action --json` (no --project); the response includes actions from multiple projects.
    - Test 3 `test_next_action_cli_limit_validation`: invoke `aios next-action --project p1 --limit 100`; argparse rejects (MAX_LIMIT=50) with a structured error message OR cmd_next_action surfaces the ValueError as a JSON error with exit 1 (mirror Plan 10-01 Task 2 convention).
    - Test 4 `test_contracts_audit_includes_next_action_row`: invoke `aios contracts-audit --json`; the contracts list contains an entry with name="NextAction", status="partial" (UI mirror lands in Plan 04), source_of_truth containing "services/next_action.py".
  </behavior>
  <action>
    1. **Add `from services.next_action import get_next_actions, NextAction, MAX_LIMIT as NEXT_ACTION_MAX_LIMIT`** (or equivalent module-level import) to services/aios_cli.py imports block.

    2. **Register the next-action subparser:**
       - subcommand: `next-action`
       - `--project` (optional str)
       - `--limit` (int, default 10, range 1-50; validated via type=int + argparse choices or via cmd_next_action's ValueError surfacing — mirror Plan 10-01 Task 2 convention).
       - `--json` (flag, default True).

    3. **Add `cmd_next_action(args) -> dict[str, Any]`:**
       - Open DB via existing helper.
       - Call `get_next_actions(conn, project_id=args.project, limit=args.limit)`.
       - Catch ValueError and return structured error JSON.
       - On success, return `{"actions": [dataclasses.asdict(a) for a in actions], "total_actions": len(actions), "project_id": args.project, "limit": args.limit}`.

    4. **Extend `_contracts_audit_payload()`** with one new row:
       - name: "NextAction"
       - status: "partial" if not Path("aios-ui/server/aios/next-action.ts").exists() else "implemented" (flips when Plan 04 ships).
       - source_of_truth: ["services/next_action.py", "services/aios_cli.py:cmd_next_action"]
       - notes: "Next-action fusion backend (Phase 10, Plan 02). Fuses health deltas, pending writebacks, open blockers, terminal-run gaps, backfill tasks, promotion candidates, and learning proposals. UI mirror lands in Plan 04; tRPC router in Plan 05; rendered Command Center + project detail surfaces in Plan 06."

    5. **Add the four behavior tests** to tests/test_aios_cli.py.

    6. **Quality ladder per touched file:**
       - `uv run pytest tests/test_aios_cli.py -x -q -k "next_action or contracts_audit_includes_next_action"`
       - `uv run ruff check services/aios_cli.py tests/test_aios_cli.py`
       - `uv run ruff format --check services/aios_cli.py tests/test_aios_cli.py`
       - `uv run basedpyright services/aios_cli.py`
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_aios_cli.py -x -q -k "next_action_cli_json or next_action_cli_cross_project or next_action_cli_limit_validation or contracts_audit_includes_next_action_row"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\"next-action\"\|'next-action'" services/aios_cli.py` returns at least 2 (subparser + dispatcher branch)
    - `grep -c "def cmd_next_action" services/aios_cli.py` returns at least 1
    - `grep -c "from services.next_action import\|import services.next_action" services/aios_cli.py` returns at least 1
    - `grep -c "\"NextAction\"\|'NextAction'" services/aios_cli.py` returns at least 1 (contracts row)
    - `grep -c "def test_next_action_cli_json\|def test_next_action_cli_cross_project\|def test_next_action_cli_limit_validation\|def test_contracts_audit_includes_next_action_row" tests/test_aios_cli.py | grep -v '^#'` returns at least 4
    - `uv run pytest tests/test_aios_cli.py -x -q` exits 0
    - `uv run pytest tests/test_next_action.py tests/test_architecture_enforcement.py -x -q` exits 0 (regression)
    - `uv run ruff check services/aios_cli.py tests/test_aios_cli.py` exits 0
    - `uv run ruff format --check services/aios_cli.py tests/test_aios_cli.py` exits 0
    - `uv run basedpyright services/aios_cli.py` exits 0
  </acceptance_criteria>
  <done>
    aios next-action CLI subcommand registered, dispatcher wired, and tested; contracts-audit lists NextAction; four behavior tests pass; no regression in next_action or architecture tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aios CLI -> services/next_action.get_next_actions | Operator-controlled input; argparse validates --project string + --limit range; sub-fetchers tolerate missing tables. |
| services/next_action.py -> SQLite tables | Read-only bounded SELECTs with parameterized project filters; no mutation; _safe_table_exists guards every sub-fetcher. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Information Disclosure | NextAction rationale field leaks sensitive evidence (writeback proposed_change body, finding evidence_json body) | mitigate | Each sub-fetcher projects ONLY safe columns into title/rationale: summary, title, message — never proposed_change_json body or evidence_json body. Test asserts on shape. |
| T-10-08 | Tampering | next-action recommends a workflow that bypasses approval gates (Pitfall 4 from RESEARCH) | mitigate | NextAction.recommended_workflow_key is informational only — it is rendered as a suggestion the operator can click. The actual launch happens via Plan 05's controlPlane.plan + invokeControlPlaneRun two-step flow; this plan does not introduce any launch path. Test verifies that get_next_actions never mutates the DB. |
| T-10-09 | Information Disclosure | Drill-down path embeds user-specific tokens (Security Domain row 4) | mitigate | _drill_down_for() takes only entity IDs and project IDs; no auth tokens. |
| T-10-10 | Repudiation | next-action recommendation stale after operator acts (Pitfall 10) | accept (out of scope for backend) | The backend recomputes on every read; staleness is a UI invalidation concern handled by Plan 06 via tRPC `utils.invalidate()` after mutations. This plan ensures no caching in the Python layer. |
| T-10-11 | Denial of Service | Unbounded fan-out across 6+ sources causes slow queries | mitigate | Per-source LIMIT 20; default limit 10; MAX_LIMIT=50. |
</threat_model>

<verification>
- `uv run pytest tests/test_next_action.py tests/test_aios_cli.py tests/test_architecture_enforcement.py -x -q` exits 0
- `uv run ruff check services/next_action.py services/aios_cli.py tests/test_next_action.py tests/test_aios_cli.py tests/test_architecture_enforcement.py` exits 0
- `uv run ruff format --check services/next_action.py services/aios_cli.py tests/test_next_action.py tests/test_aios_cli.py tests/test_architecture_enforcement.py` exits 0
- `uv run basedpyright services/next_action.py services/aios_cli.py` exits 0
- `python -c "import services.aios_cli as m; p=m.create_parser(); p.parse_args(['next-action', '--project', 'p1'])"` exits 0
- Quality ladder per CLAUDE.md: hard gates green.
</verification>

<success_criteria>
- OPER-02 (partial — backend layer): services/next_action.py exports get_next_actions fusing 6+ existing recommendation sources with bucket-weight ranking and confidence tiebreak; CLI parity via `aios next-action`.
- OPER-03 (partial — drill-down path discipline on next-action surfaces): every NextAction carries non-null drill_down_path.
- Backend never mutates: get_next_actions is read-only; launch decisions remain a two-step UI flow in Plans 05/06.
- Missing-table tolerance: get_next_actions degrades gracefully when Phases 5/6/7/8/9 tables are absent.
- Architecture invariant: services/next_action.py does not import bin/.
- contracts-audit lists NextAction (status partial in this plan).
</success_criteria>

<output>
After completion, create `.planning/phases/10-operator-surfaces-query-and-daily-flow-visibility/10-02-SUMMARY.md` per the template, including:
- The NextActionKind Literal values (eight kinds) and the NextAction dataclass shape.
- The BUCKET_WEIGHTS map and the ranking formula (lower bucket weight first; higher confidence as tiebreak).
- The six active per-source sub-fetchers and what each contributes (paste-of-record so Plan 04's aios-ui/server/aios/next-action.ts can mirror exactly).
- The note that _from_learning_proposals is a stub — learning proposals are discriminated within _from_pending_writebacks via the proposed_change_json "source": "learning_analysis" marker.
- The aios next-action CLI argparse block.
- The NextAction contracts-audit row shape.
- Note that Plan 04 will create aios-ui/server/aios/next-action.ts that mirrors this shape using better-sqlite3 prepared statements; Plan 05 will create the nextAction tRPC router; Plan 06 will render the NextActionPanel component on Command Center and project detail pages.
- Note that this plan does NOT introduce any workflow launch path — those mutations land in Plan 05's automations.triggerWorkflow + projects.launchRemediation procedures.
</output>
