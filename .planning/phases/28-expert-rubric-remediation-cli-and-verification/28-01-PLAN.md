---
phase: 28-expert-rubric-remediation-cli-and-verification
plan: 01
type: execute
wave: 1
depends_on:
  - 27-01
  - 27-02
  - 27-03
files_modified:
  - services/aios_cli.py
  - tests/test_aios_cli.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "The `aios tmcp review-plan` command runs `expert_rubric_remediation_v1` without mutating target project files."
    - "The command accepts objective, project path, output directory, evidence JSON, and selected slice id inputs."
    - "The command JSON output exposes workflow key, run id, artifact paths, validations, and remediation slices."
  artifacts:
    - path: services/aios_cli.py
      provides: "TMCP review-plan parser and run_cli execution branch"
    - path: tests/test_aios_cli.py
      provides: "CLI regression for artifact-writing review-plan behavior"
  key_links:
    - from: tmcp review-plan objective
      to: compile_tmcp_packet
      via: "existing TMCP compiler invocation"
    - from: --evidence-json
      to: WorkflowExecutionContext.evidence_items
      via: "parsed list or single evidence item"
    - from: execute_workflow report
      to: CLI JSON output
      via: "workflow report artifacts and validations"
---

<objective>
Expose the expert rubric remediation workflow through the existing AIOS TMCP CLI.

Purpose: Operators need a first-class read-only command that turns TMCP expertise plus concrete evidence into rubric, audit, remediation, and handoff artifacts.
Output: `aios tmcp review-plan` parser, execution branch, JSON output, and CLI tests.
</objective>

<execution_context>
@/Users/jakyeamos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakyeamos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-expert-rubric-remediation-cli-and-verification/28-RESEARCH.md
@.planning/phases/28-expert-rubric-remediation-cli-and-verification/28-VALIDATION.md
@.planning/phases/27-expert-rubric-remediation-workflow-runtime/27-01-SUMMARY.md
@.planning/phases/27-expert-rubric-remediation-workflow-runtime/27-02-SUMMARY.md
@.planning/phases/27-expert-rubric-remediation-workflow-runtime/27-03-SUMMARY.md
@docs/superpowers/specs/2026-06-24-expert-rubric-remediation-design.md

<interfaces>
From `services/aios_cli.py`:
- TMCP parser group begins at `tmcp_parser = subparsers.add_parser("tmcp", ...)`.
- TMCP command branches live under `elif args.command == "tmcp" and args.tmcp_command == ...`.
- JSON mode is controlled by existing `--json` parsing and `run_cli` output behavior.

From Phase 27:
- `execute_workflow(..., context=WorkflowExecutionContext(...))` executes `expert_rubric_remediation_v1`.
- `WorkflowExecutionContext` carries `tmcp_packet`, `evidence_items`, selected slice id, and output-root data needed by expert dispatch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing CLI regression for review-plan artifacts</name>
  <files>tests/test_aios_cli.py</files>
  <read_first>
    - tests/test_aios_cli.py
    - services/aios_cli.py
    - tests/test_workflow_orchestration.py
  </read_first>
  <action>
    Add `test_tmcp_review_plan_writes_expert_review_artifacts` that calls `run_cli` with `--json tmcp review-plan`, a temporary project path, a temporary output directory, one evidence item, and a selected slice id. Assert the command returns `EXIT_OK`, prints JSON with `workflow_key == "expert_rubric_remediation_v1"`, reports passing validations, and writes artifact files under the requested output directory.
  </action>
  <verify>
    <automated>
      `uv run pytest tests/test_aios_cli.py::test_tmcp_review_plan_writes_expert_review_artifacts -q`
    </automated>
  </verify>
  <done>
    The test fails for the expected missing `review-plan` command before implementation.
  </done>
  <acceptance_criteria>
    - Test uses existing `run_cli` style rather than shelling out.
    - Test covers artifact paths and JSON output shape.
    - Test evidence payload includes a concrete source path and recommended fix.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement tmcp review-plan parser and execution branch</name>
  <files>services/aios_cli.py, tests/test_aios_cli.py</files>
  <read_first>
    - services/aios_cli.py
    - services/tmcp_runtime.py
    - services/workflow_orchestration.py
    - services/expert_rubric_remediation.py
  </read_first>
  <action>
    Add `tmcp review-plan` under the existing TMCP subparser with objective, `--project-path`, `--output-dir`, `--evidence-json`, `--selected-slice-id`, and `--json`. Compile a TMCP packet from the objective and project path, parse evidence JSON into the context format expected by Phase 27, execute `expert_rubric_remediation_v1`, and return structured JSON containing workflow metadata, validations, artifact paths, and remediation-slice summaries.
  </action>
  <verify>
    <automated>
      `uv run pytest tests/test_aios_cli.py::test_tmcp_review_plan_writes_expert_review_artifacts -q`
    </automated>
  </verify>
  <done>
    The CLI test passes and the command does not mutate the target project path.
  </done>
  <acceptance_criteria>
    - Command belongs to the existing `tmcp` parser group.
    - Malformed `--evidence-json` produces a clear non-success CLI result.
    - Output directory is created when missing and artifact paths are reported.
    - Command invokes `compile_tmcp_packet` and `execute_workflow`; it does not call Phase 26 builders directly.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> workflow context | Operator-provided evidence and paths enter runtime execution. |
| Workflow report -> CLI JSON | Generated artifact paths and validation results become operator-facing output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | `--evidence-json` | mitigate | Parse structured JSON and let Phase 26 evidence validation reject unsupported findings. |
| T-28-02 | Information Disclosure | CLI output | accept | Output includes only local artifact paths and caller-provided evidence summaries. |
| T-28-03 | Elevation of Privilege | target project path | mitigate | Keep command read-only against target project and write artifacts only under the requested output directory. |
</threat_model>

<verification>
Run:
- `uv run pytest tests/test_aios_cli.py::test_tmcp_review_plan_writes_expert_review_artifacts -q`
- `uv run ruff check services/aios_cli.py tests/test_aios_cli.py`
</verification>

<success_criteria>
- `aios tmcp review-plan` exists and is documented by test behavior.
- Command writes review artifacts under the requested output directory.
- JSON output is sufficient for downstream smoke verification and handoff.
- Target project files are not modified.
</success_criteria>

<output>
After completion, create `.planning/phases/28-expert-rubric-remediation-cli-and-verification/28-01-SUMMARY.md`.
</output>
