---
phase: 29-linked-repo-aios-adoption-readiness-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/jakyeamos/projects/portfolio
  - /Users/jakyeamos/Projects/R-Project
  - /Users/jakyeamos/projects/csds391-s26-6
  - docs/audits/linked-repo-adoption-readiness-audit.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "portfolio, R-Project, and csds391-s26-6 have no missing required gates in the AIOS readiness report."
    - "Each repo has passing local CI proof recorded after its non-ci gates pass."
  artifacts:
    - path: "quality_pipeline_runs"
      provides: "fresh pass evidence for portfolio, R-Project, and csds391-s26-6"
    - path: "docs/audits/linked-repo-adoption-readiness-audit.md"
      provides: "repo readiness evidence notes"
  key_links:
    - from: "repo-local fixes"
      to: "quality_pipeline_runs"
      via: "scripts/linked-repo-quality-runner.py"
---

<objective>
Clear the smallest required gate sets for `portfolio`, `R-Project`, and `csds391-s26-6`.

Purpose: Establish the repo-by-repo remediation loop on low-cardinality blockers before larger repos.
Output: Three repos with passing non-`ci` and local `ci` evidence.
</objective>

<execution_context>
@/Users/jakyeamos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakyeamos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-linked-repo-aios-adoption-readiness-remediation/29-RESEARCH.md
@.planning/phases/29-linked-repo-aios-adoption-readiness-remediation/29-VALIDATION.md
@.planning/phases/29-linked-repo-aios-adoption-readiness-remediation/29-IMPLEMENTATION.md
@config/quality-pipeline.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remediate portfolio pre-cr and local ci</name>
  <read_first>config/quality-pipeline.json, docs/audits/linked-repo-adoption-readiness-audit.md, /Users/jakyeamos/projects/portfolio/README.md, /Users/jakyeamos/projects/portfolio/package.json</read_first>
  <files>/Users/jakyeamos/projects/portfolio, docs/audits/linked-repo-adoption-readiness-audit.md</files>
  <action>For `portfolio`, run the configured `pre_cr` gate through `python3 scripts/linked-repo-quality-runner.py --project portfolio --gate pre_cr`. Fix the repo-local cause of any failure without removing the required gate. After `pre_cr` passes, run `python3 scripts/linked-repo-quality-runner.py --project portfolio --gate ci` and fix local CI proof failures until the `ci` gate records pass evidence. Keep executable command definitions in AIOS unless the command points at the wrong canonical repo tool.</action>
  <acceptance_criteria>
    - `python3 scripts/linked-repo-quality-runner.py --report` shows `portfolio` with `missing_gate_keys: []`.
    - `portfolio` has `adoption_status: adoption_ready`.
    - `portfolio` has `aios_wired`, `quality_standard_compliant`, and `release_ready` certification stages passing.
    - `quality_pipeline_runs` has latest `portfolio` `pre_cr` and `ci` records with status `pass`.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/linked-repo-quality-runner.py --project portfolio --gate pre_cr && python3 scripts/linked-repo-quality-runner.py --project portfolio --gate ci</automated>
  </verify>
  <done>`portfolio` is adoption-ready in the readiness report.</done>
</task>

<task type="auto">
  <name>Task 2: Remediate R-Project and csds391-s26-6 class gates</name>
  <read_first>config/quality-pipeline.json, /Users/jakyeamos/Projects/R-Project/README.md, /Users/jakyeamos/projects/csds391-s26-6/README.md</read_first>
  <files>/Users/jakyeamos/Projects/R-Project, /Users/jakyeamos/projects/csds391-s26-6, docs/audits/linked-repo-adoption-readiness-audit.md</files>
  <action>For `R-Project`, make the configured `lint` and `pre_cr` gates pass, then run local `ci` proof. For `csds391-s26-6`, make the configured `lint` and `pre_cr` gates pass, then run local `ci` proof. Preserve the class-specific course/research gate shape; do not replace these gates with app-style build gates.</action>
  <acceptance_criteria>
    - `R-Project` has `missing_gate_keys: []`.
    - `csds391-s26-6` has `missing_gate_keys: []`.
    - `R-Project` has `adoption_status: adoption_ready`.
    - `csds391-s26-6` has `adoption_status: adoption_ready`.
    - Both repos have `aios_wired`, `quality_standard_compliant`, and `release_ready` certification stages passing.
    - Both repos have latest passing `ci` records.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/linked-repo-quality-runner.py --project R-Project --gate lint && python3 scripts/linked-repo-quality-runner.py --project R-Project --gate pre_cr && python3 scripts/linked-repo-quality-runner.py --project R-Project --gate ci && python3 scripts/linked-repo-quality-runner.py --project csds391-s26-6 --gate lint && python3 scripts/linked-repo-quality-runner.py --project csds391-s26-6 --gate pre_cr && python3 scripts/linked-repo-quality-runner.py --project csds391-s26-6 --gate ci</automated>
  </verify>
  <done>`R-Project` and `csds391-s26-6` are adoption-ready in the readiness report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| AIOS runner to external repos | AIOS executes configured commands in linked repo workspaces. |
| Repo evidence to readiness report | Passing evidence changes portfolio readiness status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-29-01-01 | Tampering | required gates | mitigate | Do not remove or downgrade `pre_cr`, `lint`, or `ci` gates to clear readiness. |
| T-29-01-02 | Repudiation | evidence rows | mitigate | Record every gate through `linked-repo-quality-runner.py`, not manual notes. |
</threat_model>

<verification>
Run `python3 scripts/linked-repo-quality-runner.py --report` and confirm the three target repos have empty `missing_gate_keys`, `adoption_status: adoption_ready`, and passing `aios_wired`, `quality_standard_compliant`, and `release_ready` certification stages.
</verification>

<success_criteria>
`portfolio`, `R-Project`, and `csds391-s26-6` report ready while excluded repos remain excluded.
</success_criteria>

<output>
After completion, create `.planning/phases/29-linked-repo-aios-adoption-readiness-remediation/29-01-SUMMARY.md`
</output>
