---
phase: 29-linked-repo-aios-adoption-readiness-remediation
plan: 05
type: execute
wave: 5
depends_on: ["29-04"]
files_modified:
  - /Users/jakyeamos/projects/eslint-plugin-anti-slop
  - /Users/jakyeamos/projects/Terrace
  - /Users/jakyeamos/AIOS
  - docs/audits/linked-repo-adoption-readiness-audit.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "eslint-plugin-anti-slop, Terrace, and AIOS have passing package/tool/platform gates and local CI proof."
    - "AIOS self-readiness does not weaken its own architecture or context validation standards."
  artifacts:
    - path: "quality_pipeline_runs"
      provides: "fresh pass evidence for tool/package and platform repos"
  key_links:
    - from: "tool/platform quality commands"
      to: "adoption readiness"
      via: "AIOS-owned quality pipeline gate adapters"
---

<objective>
Clear developer-tool and platform blockers for `eslint-plugin-anti-slop`, `Terrace`, and `AIOS`.

Purpose: Prove that the repos that define or enforce agent quality can meet the same adoption-readiness bar.
Output: Three tool/platform repos with passing local CI proof.
</objective>

<execution_context>
@/Users/jakyeamos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakyeamos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-linked-repo-aios-adoption-readiness-remediation/29-RESEARCH.md
@config/quality-pipeline.json
@config/architecture-rules.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remediate eslint-plugin-anti-slop package gates</name>
  <read_first>config/quality-pipeline.json, /Users/jakyeamos/projects/eslint-plugin-anti-slop/package.json, /Users/jakyeamos/projects/eslint-plugin-anti-slop/README.md</read_first>
  <files>/Users/jakyeamos/projects/eslint-plugin-anti-slop, docs/audits/linked-repo-adoption-readiness-audit.md</files>
  <action>Make `eslint-plugin-anti-slop` lint, typecheck, architecture, and local `ci` proof pass. Preserve rule behavior and package-consumer expectations while clearing the configured gates.</action>
  <acceptance_criteria>
    - `eslint-plugin-anti-slop` has latest pass records for `lint`, `typecheck`, `architecture`, and `ci`.
    - `eslint-plugin-anti-slop` has `missing_gate_keys: []`.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/linked-repo-quality-runner.py --project eslint-plugin-anti-slop --gate lint && python3 scripts/linked-repo-quality-runner.py --project eslint-plugin-anti-slop --gate typecheck && python3 scripts/linked-repo-quality-runner.py --project eslint-plugin-anti-slop --gate architecture && python3 scripts/linked-repo-quality-runner.py --project eslint-plugin-anti-slop --gate ci</automated>
  </verify>
  <done>`eslint-plugin-anti-slop` is adoption-ready in the readiness report.</done>
</task>

<task type="auto">
  <name>Task 2: Remediate Terrace package gates</name>
  <read_first>config/quality-pipeline.json, /Users/jakyeamos/projects/Terrace/package.json, /Users/jakyeamos/projects/Terrace/README.md</read_first>
  <files>/Users/jakyeamos/projects/Terrace, docs/audits/linked-repo-adoption-readiness-audit.md</files>
  <action>Make `Terrace` test, architecture, dependency security, `pre_cr`, and local `ci` proof pass. Keep Terrace's GSD-compatible workflow contracts intact while correcting failures.</action>
  <acceptance_criteria>
    - `Terrace` has latest pass records for `test`, `architecture`, `dependency_security`, `pre_cr`, and `ci`.
    - `Terrace` has `missing_gate_keys: []`.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/linked-repo-quality-runner.py --project Terrace --gate test && python3 scripts/linked-repo-quality-runner.py --project Terrace --gate architecture && python3 scripts/linked-repo-quality-runner.py --project Terrace --gate dependency_security && python3 scripts/linked-repo-quality-runner.py --project Terrace --gate pre_cr && python3 scripts/linked-repo-quality-runner.py --project Terrace --gate ci</automated>
  </verify>
  <done>`Terrace` is adoption-ready in the readiness report.</done>
</task>

<task type="auto">
  <name>Task 3: Remediate AIOS platform gates</name>
  <read_first>config/quality-pipeline.json, PROJECT.md, AGENTS.md, package.json, pyproject.toml</read_first>
  <files>/Users/jakyeamos/AIOS, docs/audits/linked-repo-adoption-readiness-audit.md</files>
  <action>Make `AIOS` install, test, architecture, `pre_cr`, and local `ci` proof pass without lowering root Python, UI, context, architecture, or quality-pipeline standards. Treat failures in AIOS itself as control-plane issues that need durable fixes or accurately recorded accepted risk.</action>
  <acceptance_criteria>
    - `AIOS` has latest pass records for `install`, `test`, `architecture`, `pre_cr`, and `ci`.
    - `AIOS` has `missing_gate_keys: []`.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/linked-repo-quality-runner.py --project AIOS --gate install && python3 scripts/linked-repo-quality-runner.py --project AIOS --gate test && python3 scripts/linked-repo-quality-runner.py --project AIOS --gate architecture && python3 scripts/linked-repo-quality-runner.py --project AIOS --gate pre_cr && python3 scripts/linked-repo-quality-runner.py --project AIOS --gate ci</automated>
  </verify>
  <done>`AIOS` is adoption-ready in the readiness report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Quality-tool repos | Tool/package repos define checks that other projects rely on. |
| AIOS self-governance | AIOS readiness evidence is both product state and control-plane authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-29-05-01 | Tampering | quality tools | mitigate | Fix package/tool failures without weakening exported quality rules. |
| T-29-05-02 | Repudiation | AIOS self-proof | mitigate | Record AIOS gate output through the same runner as linked repos. |
</threat_model>

<verification>
Run `python3 scripts/linked-repo-quality-runner.py --report` and confirm `eslint-plugin-anti-slop`, `Terrace`, and `AIOS` have empty `missing_gate_keys`.
</verification>

<success_criteria>
The three tool/platform repos report ready with passing local CI proof.
</success_criteria>

<output>
After completion, create `.planning/phases/29-linked-repo-aios-adoption-readiness-remediation/29-05-SUMMARY.md`
</output>
