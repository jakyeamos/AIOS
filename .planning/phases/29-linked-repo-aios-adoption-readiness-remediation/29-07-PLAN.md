---
phase: 29-linked-repo-aios-adoption-readiness-remediation
plan: 07
type: execute
wave: 7
depends_on: ["29-06"]
files_modified:
  - /Users/jakyeamos/projects/BidCamp
  - /Users/jakyeamos/LayerC0de/tenure
  - /Users/jakyeamos/EliHealth
  - /Users/jakyeamos/projects/amos-saas
  - /Users/jakyeamos/projects/soundscape-app
  - /Users/jakyeamos/projects/remodelvision
  - docs/audits/linked-repo-adoption-readiness-audit.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "BidCamp, tenure, EliHealth, amos-saas, soundscape-app, and remodelvision have their applicable adoption gates passing."
    - "Secret scanning, dependency scanning, build, smoke/e2e, and local CI proof remain blocking where configured."
  artifacts:
    - path: "quality_pipeline_runs"
      provides: "fresh pass evidence for high-complexity production web app and mobile-app repos"
  key_links:
    - from: "production web app gates"
      to: "portfolio readiness"
      via: "AIOS readiness report"
---

<objective>
Clear the highest-complexity app blockers for `BidCamp`, `tenure`, `EliHealth`, `amos-saas`, `soundscape-app`, and `remodelvision`.

Purpose: Finish the large app remediation wave after smaller repos have proven the loop and evidence path.
Output: Six app repos with passing local CI proof and required security, smoke, and mobile release evidence where applicable.
</objective>

<execution_context>
@/Users/jakyeamos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakyeamos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-linked-repo-aios-adoption-readiness-remediation/29-RESEARCH.md
@.planning/phases/29-linked-repo-aios-adoption-readiness-remediation/29-VALIDATION.md
@config/quality-pipeline.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remediate newly added app adoption targets</name>
  <read_first>config/quality-pipeline.json, /Users/jakyeamos/projects/BidCamp/package.json, /Users/jakyeamos/LayerC0de/tenure/package.json, /Users/jakyeamos/EliHealth/package.json</read_first>
  <files>/Users/jakyeamos/projects/BidCamp, /Users/jakyeamos/LayerC0de/tenure, /Users/jakyeamos/EliHealth, docs/audits/linked-repo-adoption-readiness-audit.md</files>
  <action>Make `BidCamp`, `tenure`, and `EliHealth` pass their selected gate profiles and record first pass evidence for every required gate, including repo truth, Pre-CR, anti-slop, local CI proof, and EliHealth mobile release proof.</action>
  <acceptance_criteria>
    - `BidCamp`, `tenure`, and `EliHealth` have latest pass records for all configured required gates.
    - `BidCamp`, `tenure`, and `EliHealth` have `missing_gate_keys: []`.
    - Their `quality_certification.stage_statuses` values are all `pass`.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/linked-repo-quality-runner.py --report</automated>
  </verify>
  <done>`BidCamp`, `tenure`, and `EliHealth` are adoption-ready in the readiness report.</done>
</task>

<task type="auto">
  <name>Task 2: Remediate amos-saas production gates</name>
  <read_first>config/quality-pipeline.json, /Users/jakyeamos/projects/amos-saas/package.json, /Users/jakyeamos/projects/amos-saas/README.md</read_first>
  <files>/Users/jakyeamos/projects/amos-saas, docs/audits/linked-repo-adoption-readiness-audit.md</files>
  <action>Make `amos-saas` lint, typecheck, build, architecture, dependency security, `pre_cr`, and local `ci` proof pass. Preserve app behavior and package-manager authority while clearing the configured gates.</action>
  <acceptance_criteria>
    - `amos-saas` has latest pass records for `lint`, `typecheck`, `build`, `architecture`, `dependency_security`, `pre_cr`, and `ci`.
    - `amos-saas` has `missing_gate_keys: []`.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/linked-repo-quality-runner.py --project amos-saas --gate lint && python3 scripts/linked-repo-quality-runner.py --project amos-saas --gate typecheck && python3 scripts/linked-repo-quality-runner.py --project amos-saas --gate build && python3 scripts/linked-repo-quality-runner.py --project amos-saas --gate architecture && python3 scripts/linked-repo-quality-runner.py --project amos-saas --gate dependency_security && python3 scripts/linked-repo-quality-runner.py --project amos-saas --gate pre_cr && python3 scripts/linked-repo-quality-runner.py --project amos-saas --gate ci</automated>
  </verify>
  <done>`amos-saas` is adoption-ready in the readiness report.</done>
</task>

<task type="auto">
  <name>Task 3: Remediate soundscape-app production gates</name>
  <read_first>config/quality-pipeline.json, /Users/jakyeamos/projects/soundscape-app/package.json, /Users/jakyeamos/projects/soundscape-app/README.md</read_first>
  <files>/Users/jakyeamos/projects/soundscape-app, docs/audits/linked-repo-adoption-readiness-audit.md</files>
  <action>Make `soundscape-app` lint, typecheck, test, secret scan, dependency security, e2e smoke, `pre_cr`, and local `ci` proof pass. Keep the smoke/e2e evidence tied to the maintained product experience.</action>
  <acceptance_criteria>
    - `soundscape-app` has latest pass records for all configured required gates.
    - `soundscape-app` has `missing_gate_keys: []`.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/linked-repo-quality-runner.py --project soundscape-app --gate lint && python3 scripts/linked-repo-quality-runner.py --project soundscape-app --gate typecheck && python3 scripts/linked-repo-quality-runner.py --project soundscape-app --gate test && python3 scripts/linked-repo-quality-runner.py --project soundscape-app --gate secret_scan && python3 scripts/linked-repo-quality-runner.py --project soundscape-app --gate dependency_security && python3 scripts/linked-repo-quality-runner.py --project soundscape-app --gate e2e_smoke && python3 scripts/linked-repo-quality-runner.py --project soundscape-app --gate pre_cr && python3 scripts/linked-repo-quality-runner.py --project soundscape-app --gate ci</automated>
  </verify>
  <done>`soundscape-app` is adoption-ready in the readiness report.</done>
</task>

<task type="auto">
  <name>Task 4: Remediate remodelvision production gates</name>
  <read_first>config/quality-pipeline.json, /Users/jakyeamos/projects/remodelvision/package.json, /Users/jakyeamos/projects/remodelvision/README.md</read_first>
  <files>/Users/jakyeamos/projects/remodelvision, docs/audits/linked-repo-adoption-readiness-audit.md</files>
  <action>Make `remodelvision` lint, typecheck, test, build, architecture, secret scan, dependency security, e2e smoke, `pre_cr`, and local `ci` proof pass. Keep all security and smoke gates required until they produce pass evidence.</action>
  <acceptance_criteria>
    - `remodelvision` has latest pass records for all configured required gates.
    - `remodelvision` has `missing_gate_keys: []`.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/linked-repo-quality-runner.py --project remodelvision --gate lint && python3 scripts/linked-repo-quality-runner.py --project remodelvision --gate typecheck && python3 scripts/linked-repo-quality-runner.py --project remodelvision --gate test && python3 scripts/linked-repo-quality-runner.py --project remodelvision --gate build && python3 scripts/linked-repo-quality-runner.py --project remodelvision --gate architecture && python3 scripts/linked-repo-quality-runner.py --project remodelvision --gate secret_scan && python3 scripts/linked-repo-quality-runner.py --project remodelvision --gate dependency_security && python3 scripts/linked-repo-quality-runner.py --project remodelvision --gate e2e_smoke && python3 scripts/linked-repo-quality-runner.py --project remodelvision --gate pre_cr && python3 scripts/linked-repo-quality-runner.py --project remodelvision --gate ci</automated>
  </verify>
  <done>`remodelvision` is adoption-ready in the readiness report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Production app security proof | Secret and dependency scans affect public release readiness. |
| E2E/smoke evidence | Browser or smoke checks prove maintained app paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-29-07-01 | Information disclosure | secret scans | mitigate | Preserve secret scans as required and remediate findings without committing sensitive values. |
| T-29-07-02 | Spoofing | smoke evidence | mitigate | Run smoke/e2e through configured commands that exercise maintained product routes. |
</threat_model>

<verification>
Run `python3 scripts/linked-repo-quality-runner.py --report` and confirm the six target repos have empty `missing_gate_keys`.
</verification>

<success_criteria>
`BidCamp`, `tenure`, `EliHealth`, `amos-saas`, `soundscape-app`, and `remodelvision` report ready with security, smoke, mobile release, and local CI evidence where applicable.
</success_criteria>

<output>
After completion, create `.planning/phases/29-linked-repo-aios-adoption-readiness-remediation/29-07-SUMMARY.md`
</output>
