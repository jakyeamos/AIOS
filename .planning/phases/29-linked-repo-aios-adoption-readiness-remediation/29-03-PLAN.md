---
phase: 29-linked-repo-aios-adoption-readiness-remediation
plan: 03
type: execute
wave: 3
depends_on: ["29-02"]
files_modified:
  - /Users/jakyeamos/projects/BBDSE
  - /Users/jakyeamos/Vaults
  - docs/audits/linked-repo-adoption-readiness-audit.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "BBDSE and Vaults have class-specific validation and security proof, not app-style surrogate gates."
    - "BBDSE remains marked as a container repo unless aggregate or delegated subproject gates prove stronger readiness."
  artifacts:
    - path: "quality_pipeline_runs"
      provides: "fresh validation, pre_cr or architecture, security, and ci pass evidence"
  key_links:
    - from: "content/container validation"
      to: "adoption readiness"
      via: "AIOS required gates and readiness report"
---

<objective>
Clear the content and container repo blockers for `BBDSE` and `Vaults`.

Purpose: Prove adoption readiness without fabricating application gates for repositories whose value is content, vault material, or delegated subprojects.
Output: Two content/container repos with passing class-specific validation and local CI proof.
</objective>

<execution_context>
@/Users/jakyeamos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakyeamos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-linked-repo-aios-adoption-readiness-remediation/29-RESEARCH.md
@.planning/phases/29-linked-repo-aios-adoption-readiness-remediation/29-VALIDATION.md
@config/quality-pipeline.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remediate BBDSE container gates</name>
  <read_first>config/quality-pipeline.json, /Users/jakyeamos/projects/BBDSE/README.md, /Users/jakyeamos/projects/BBDSE/.aios-quality-gate.json</read_first>
  <files>/Users/jakyeamos/projects/BBDSE, docs/audits/linked-repo-adoption-readiness-audit.md</files>
  <action>Make `BBDSE` validation, `pre_cr`, and local `ci` proof pass using container-appropriate aggregate or delegated subproject checks. If the repo still lacks mature aggregate coverage, keep that maturity limitation recorded in AIOS truth while making the required configured gates honest and executable.</action>
  <acceptance_criteria>
    - `BBDSE` has latest pass records for `validation`, `pre_cr`, and `ci`.
    - `BBDSE` readiness notes distinguish mature aggregate proof from delegated or still-maturing container proof.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/linked-repo-quality-runner.py --project BBDSE --gate validation && python3 scripts/linked-repo-quality-runner.py --project BBDSE --gate pre_cr && python3 scripts/linked-repo-quality-runner.py --project BBDSE --gate ci</automated>
  </verify>
  <done>`BBDSE` has no missing required gates in the readiness report, with maturity notes preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Remediate Vaults architecture, secret, validation, and local ci</name>
  <read_first>config/quality-pipeline.json, /Users/jakyeamos/Vaults/README.md, /Users/jakyeamos/Vaults/.aios-quality-gate.json</read_first>
  <files>/Users/jakyeamos/Vaults, docs/audits/linked-repo-adoption-readiness-audit.md</files>
  <action>Make `Vaults` architecture, secret scan, `pre_cr`, validation, and local `ci` proof pass using content/vault checks that protect private material and repo structure. Preserve privacy boundaries while recording evidence through AIOS.</action>
  <acceptance_criteria>
    - `Vaults` has latest pass records for `architecture`, `secret_scan`, `pre_cr`, `validation`, and `ci`.
    - `Vaults` has `missing_gate_keys: []` while `secret_scan` remains required.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/linked-repo-quality-runner.py --project Vaults --gate architecture && python3 scripts/linked-repo-quality-runner.py --project Vaults --gate secret_scan && python3 scripts/linked-repo-quality-runner.py --project Vaults --gate pre_cr && python3 scripts/linked-repo-quality-runner.py --project Vaults --gate validation && python3 scripts/linked-repo-quality-runner.py --project Vaults --gate ci</automated>
  </verify>
  <done>`Vaults` is adoption-ready in the readiness report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Private content to AIOS evidence | Vault/content checks must avoid leaking private material into artifacts. |
| Container repo to delegated checks | BBDSE may rely on subproject or aggregate commands rather than a single application build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-29-03-01 | Information disclosure | Vault evidence | mitigate | Record pass/fail metadata and command output summaries without copying private vault content into planning docs. |
| T-29-03-02 | Tampering | container maturity | mitigate | Do not mark BBDSE mature unless its aggregate or delegated checks actually exercise the maintained surfaces. |
</threat_model>

<verification>
Run `python3 scripts/linked-repo-quality-runner.py --report` and confirm `BBDSE` and `Vaults` have empty `missing_gate_keys`.
</verification>

<success_criteria>
`BBDSE` and `Vaults` report ready or, for BBDSE, ready with an explicit maturity note that reflects the actual container proof.
</success_criteria>

<output>
After completion, create `.planning/phases/29-linked-repo-aios-adoption-readiness-remediation/29-03-SUMMARY.md`
</output>
