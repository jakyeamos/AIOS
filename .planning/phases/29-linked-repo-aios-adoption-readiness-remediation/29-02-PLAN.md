---
phase: 29-linked-repo-aios-adoption-readiness-remediation
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - /Users/jakyeamos/career-ops
  - /Users/jakyeamos/projects/claude-improvement-lab
  - /Users/jakyeamos/projects/pre-cr-suite-lsp
  - docs/audits/linked-repo-adoption-readiness-audit.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "career-ops, claude-improvement-lab, and pre-cr-suite-lsp have no missing required gates."
    - "Dependency security evidence is passing for repos that currently fail it."
  artifacts:
    - path: "quality_pipeline_runs"
      provides: "fresh pass evidence for the three target repos"
  key_links:
    - from: "dependency/security fixes"
      to: "local ci proof"
      via: "required non-ci gates must pass before ci"
---

<objective>
Clear the next smallest blocker group: `career-ops`, `claude-improvement-lab`, and `pre-cr-suite-lsp`.

Purpose: Remove validation, Pre-CR, architecture, and dependency-security blockers from repos with compact gate sets.
Output: Three additional adoption-ready repos.
</objective>

<execution_context>
@/Users/jakyeamos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakyeamos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-linked-repo-aios-adoption-readiness-remediation/29-RESEARCH.md
@config/quality-pipeline.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remediate career-ops and claude-improvement-lab gates</name>
  <read_first>config/quality-pipeline.json, /Users/jakyeamos/career-ops/README.md, /Users/jakyeamos/projects/claude-improvement-lab/README.md, /Users/jakyeamos/projects/claude-improvement-lab/pyproject.toml</read_first>
  <files>/Users/jakyeamos/career-ops, /Users/jakyeamos/projects/claude-improvement-lab</files>
  <action>For `career-ops`, make `validation` and `pre_cr` pass, then run local `ci` proof. For `claude-improvement-lab`, make `dependency_security` and `pre_cr` pass, then run local `ci` proof. Keep benchmark/course validation meaningful; do not satisfy validation by only checking file existence when the configured command expects executable validation.</action>
  <acceptance_criteria>
    - `career-ops` has `missing_gate_keys: []`.
    - `claude-improvement-lab` has `missing_gate_keys: []`.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/linked-repo-quality-runner.py --project career-ops --gate validation && python3 scripts/linked-repo-quality-runner.py --project career-ops --gate pre_cr && python3 scripts/linked-repo-quality-runner.py --project career-ops --gate ci && python3 scripts/linked-repo-quality-runner.py --project claude-improvement-lab --gate dependency_security && python3 scripts/linked-repo-quality-runner.py --project claude-improvement-lab --gate pre_cr && python3 scripts/linked-repo-quality-runner.py --project claude-improvement-lab --gate ci</automated>
  </verify>
  <done>`career-ops` and `claude-improvement-lab` are adoption-ready in the readiness report.</done>
</task>

<task type="auto">
  <name>Task 2: Remediate pre-cr-suite-lsp architecture and dependency security</name>
  <read_first>config/quality-pipeline.json, /Users/jakyeamos/projects/pre-cr-suite-lsp/package.json, /Users/jakyeamos/projects/pre-cr-suite-lsp/README.md</read_first>
  <files>/Users/jakyeamos/projects/pre-cr-suite-lsp</files>
  <action>Make the configured `architecture` and `dependency_security` gates pass for `pre-cr-suite-lsp`, then run local `ci` proof. Architecture evidence must remain a substantive package/tool boundary check rather than a file-presence-only assertion.</action>
  <acceptance_criteria>
    - `pre-cr-suite-lsp` has latest pass records for `architecture`, `dependency_security`, and `ci`.
    - `pre-cr-suite-lsp` has `missing_gate_keys: []`.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/linked-repo-quality-runner.py --project pre-cr-suite-lsp --gate architecture && python3 scripts/linked-repo-quality-runner.py --project pre-cr-suite-lsp --gate dependency_security && python3 scripts/linked-repo-quality-runner.py --project pre-cr-suite-lsp --gate ci</automated>
  </verify>
  <done>`pre-cr-suite-lsp` is adoption-ready in the readiness report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Package dependency metadata | Dependency commands inspect package state and may expose vulnerabilities. |
| Architecture proof | Tool/package boundary evidence affects readiness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-29-02-01 | Tampering | dependency gates | mitigate | Keep dependency security gates blocking until a pass is recorded. |
| T-29-02-02 | Repudiation | architecture proof | mitigate | Record architecture gate output through the AIOS runner. |
</threat_model>

<verification>
Run `python3 scripts/linked-repo-quality-runner.py --report` and confirm the three target repos have empty `missing_gate_keys`.
</verification>

<success_criteria>
`career-ops`, `claude-improvement-lab`, and `pre-cr-suite-lsp` report ready.
</success_criteria>

<output>
After completion, create `.planning/phases/29-linked-repo-aios-adoption-readiness-remediation/29-02-SUMMARY.md`
</output>
