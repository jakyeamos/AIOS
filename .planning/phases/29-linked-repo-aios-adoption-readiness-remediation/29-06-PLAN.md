---
phase: 29-linked-repo-aios-adoption-readiness-remediation
plan: 06
type: execute
wave: 6
depends_on: ["29-05"]
files_modified:
  - /Users/jakyeamos/projects/tm
  - /Users/jakyeamos/projects/Bballedu
  - /Users/jakyeamos/projects/dispatches-from-cyberspace
  - docs/audits/linked-repo-adoption-readiness-audit.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "tm, Bballedu, and dispatches-from-cyberspace have production/public app gates passing."
    - "E2E or smoke coverage remains meaningful for Bballedu."
  artifacts:
    - path: "quality_pipeline_runs"
      provides: "fresh pass evidence for the three app repos"
  key_links:
    - from: "web-app build and architecture checks"
      to: "local CI proof"
      via: "AIOS readiness report"
---

<objective>
Clear medium-complexity production/public web app blockers for `tm`, `Bballedu`, and `dispatches-from-cyberspace`.

Purpose: Move web app repos with architecture, dependency, typecheck, smoke, and Pre-CR failures into passing local adoption evidence.
Output: Three production/public app repos with passing local CI proof.
</objective>

<execution_context>
@/Users/jakyeamos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakyeamos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-linked-repo-aios-adoption-readiness-remediation/29-RESEARCH.md
@config/quality-pipeline.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remediate tm architecture, dependency, pre-cr, and local ci</name>
  <read_first>config/quality-pipeline.json, /Users/jakyeamos/projects/tm/package.json, /Users/jakyeamos/projects/tm/README.md</read_first>
  <files>/Users/jakyeamos/projects/tm, docs/audits/linked-repo-adoption-readiness-audit.md</files>
  <action>Make `tm` architecture, dependency security, `pre_cr`, and local `ci` proof pass. Preserve the repo's canonical frontend or package-manager workflow while correcting gate failures.</action>
  <acceptance_criteria>
    - `tm` has latest pass records for `architecture`, `dependency_security`, `pre_cr`, and `ci`.
    - `tm` has `missing_gate_keys: []`.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/linked-repo-quality-runner.py --project tm --gate architecture && python3 scripts/linked-repo-quality-runner.py --project tm --gate dependency_security && python3 scripts/linked-repo-quality-runner.py --project tm --gate pre_cr && python3 scripts/linked-repo-quality-runner.py --project tm --gate ci</automated>
  </verify>
  <done>`tm` is adoption-ready in the readiness report.</done>
</task>

<task type="auto">
  <name>Task 2: Remediate Bballedu architecture, security, smoke, and local ci</name>
  <read_first>config/quality-pipeline.json, /Users/jakyeamos/projects/Bballedu/package.json, /Users/jakyeamos/projects/Bballedu/README.md</read_first>
  <files>/Users/jakyeamos/projects/Bballedu, docs/audits/linked-repo-adoption-readiness-audit.md</files>
  <action>Make `Bballedu` architecture, dependency security, e2e smoke, `pre_cr`, and local `ci` proof pass. Keep the smoke gate tied to a user-relevant app path or equivalent maintained route.</action>
  <acceptance_criteria>
    - `Bballedu` has latest pass records for `architecture`, `dependency_security`, `e2e_smoke`, `pre_cr`, and `ci`.
    - `Bballedu` has `missing_gate_keys: []`.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/linked-repo-quality-runner.py --project Bballedu --gate architecture && python3 scripts/linked-repo-quality-runner.py --project Bballedu --gate dependency_security && python3 scripts/linked-repo-quality-runner.py --project Bballedu --gate e2e_smoke && python3 scripts/linked-repo-quality-runner.py --project Bballedu --gate pre_cr && python3 scripts/linked-repo-quality-runner.py --project Bballedu --gate ci</automated>
  </verify>
  <done>`Bballedu` is adoption-ready in the readiness report.</done>
</task>

<task type="auto">
  <name>Task 3: Remediate dispatches-from-cyberspace typecheck, architecture, security, and local ci</name>
  <read_first>config/quality-pipeline.json, /Users/jakyeamos/projects/dispatches-from-cyberspace/package.json, /Users/jakyeamos/projects/dispatches-from-cyberspace/README.md</read_first>
  <files>/Users/jakyeamos/projects/dispatches-from-cyberspace, docs/audits/linked-repo-adoption-readiness-audit.md</files>
  <action>Make `dispatches-from-cyberspace` typecheck, architecture, dependency security, `pre_cr`, and local `ci` proof pass. Preserve its public web app build/runtime contract while correcting failures.</action>
  <acceptance_criteria>
    - `dispatches-from-cyberspace` has latest pass records for `typecheck`, `architecture`, `dependency_security`, `pre_cr`, and `ci`.
    - `dispatches-from-cyberspace` has `missing_gate_keys: []`.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/linked-repo-quality-runner.py --project dispatches-from-cyberspace --gate typecheck && python3 scripts/linked-repo-quality-runner.py --project dispatches-from-cyberspace --gate architecture && python3 scripts/linked-repo-quality-runner.py --project dispatches-from-cyberspace --gate dependency_security && python3 scripts/linked-repo-quality-runner.py --project dispatches-from-cyberspace --gate pre_cr && python3 scripts/linked-repo-quality-runner.py --project dispatches-from-cyberspace --gate ci</automated>
  </verify>
  <done>`dispatches-from-cyberspace` is adoption-ready in the readiness report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Public web app proof | Build, dependency, and smoke evidence affect user-facing app readiness. |
| AIOS runner | AIOS executes repo commands across separate package ecosystems. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-29-06-01 | Tampering | smoke gates | mitigate | Keep smoke/e2e gates tied to maintained user paths. |
| T-29-06-02 | Denial of service | local CI | mitigate | Use repo-canonical commands and avoid long-running unrelated suites unless the gate requires them. |
</threat_model>

<verification>
Run `python3 scripts/linked-repo-quality-runner.py --report` and confirm the three target repos have empty `missing_gate_keys`.
</verification>

<success_criteria>
`tm`, `Bballedu`, and `dispatches-from-cyberspace` report ready.
</success_criteria>

<output>
After completion, create `.planning/phases/29-linked-repo-aios-adoption-readiness-remediation/29-06-SUMMARY.md`
</output>
