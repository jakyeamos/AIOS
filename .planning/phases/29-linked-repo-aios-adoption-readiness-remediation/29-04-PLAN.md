---
phase: 29-linked-repo-aios-adoption-readiness-remediation
plan: 04
type: execute
wave: 4
depends_on: ["29-03"]
files_modified:
  - /Users/jakyeamos/Projects/LIS
  - /Users/jakyeamos/projects/Dsci-proj
  - /Users/jakyeamos/projects/Fantasy
  - docs/audits/linked-repo-adoption-readiness-audit.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "LIS, Dsci-proj, and Fantasy use Python/data/research/course-appropriate lint, test, dependency, validation, and CI proof."
    - "Validation work proves maintained project behavior or data integrity."
  artifacts:
    - path: "quality_pipeline_runs"
      provides: "fresh pass evidence for all configured gates on the three target repos"
  key_links:
    - from: "Python/data checks"
      to: "local CI proof"
      via: "required non-ci gates must pass before readiness"
---

<objective>
Clear Python, data, research, and course repo blockers for `LIS`, `Dsci-proj`, and `Fantasy`.

Purpose: Convert research/course repos from configured gate coverage to passing adoption evidence without changing them into application projects.
Output: Three additional adoption-ready repos with class-appropriate proof.
</objective>

<execution_context>
@/Users/jakyeamos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakyeamos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-linked-repo-aios-adoption-readiness-remediation/29-RESEARCH.md
@config/quality-pipeline.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remediate LIS lint, test, dependency security, and local ci</name>
  <read_first>config/quality-pipeline.json, /Users/jakyeamos/Projects/LIS/README.md, /Users/jakyeamos/Projects/LIS/pyproject.toml</read_first>
  <files>/Users/jakyeamos/Projects/LIS, docs/audits/linked-repo-adoption-readiness-audit.md</files>
  <action>Make `LIS` lint, test, dependency security, and local `ci` proof pass using the repo's canonical Python tooling. Keep security gates authoritative and avoid removing dependencies or checks solely to clear a report.</action>
  <acceptance_criteria>
    - `LIS` has latest pass records for `lint`, `test`, `dependency_security`, and `ci`.
    - `LIS` has `missing_gate_keys: []`.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/linked-repo-quality-runner.py --project LIS --gate lint && python3 scripts/linked-repo-quality-runner.py --project LIS --gate test && python3 scripts/linked-repo-quality-runner.py --project LIS --gate dependency_security && python3 scripts/linked-repo-quality-runner.py --project LIS --gate ci</automated>
  </verify>
  <done>`LIS` is adoption-ready in the readiness report.</done>
</task>

<task type="auto">
  <name>Task 2: Remediate Dsci-proj and Fantasy data validation gates</name>
  <read_first>config/quality-pipeline.json, /Users/jakyeamos/projects/Dsci-proj/README.md, /Users/jakyeamos/projects/Fantasy/README.md</read_first>
  <files>/Users/jakyeamos/projects/Dsci-proj, /Users/jakyeamos/projects/Fantasy, docs/audits/linked-repo-adoption-readiness-audit.md</files>
  <action>For `Dsci-proj`, make lint, test, dependency security, `pre_cr`, and local `ci` proof pass. For `Fantasy`, make lint, test, dependency security, validation, and local `ci` proof pass. Validation must exercise the maintained data or research workflow encoded in each repo.</action>
  <acceptance_criteria>
    - `Dsci-proj` has `missing_gate_keys: []`.
    - `Fantasy` has `missing_gate_keys: []`.
    - Both repos have latest passing local `ci` records.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/linked-repo-quality-runner.py --project Dsci-proj --gate lint && python3 scripts/linked-repo-quality-runner.py --project Dsci-proj --gate test && python3 scripts/linked-repo-quality-runner.py --project Dsci-proj --gate dependency_security && python3 scripts/linked-repo-quality-runner.py --project Dsci-proj --gate pre_cr && python3 scripts/linked-repo-quality-runner.py --project Dsci-proj --gate ci && python3 scripts/linked-repo-quality-runner.py --project Fantasy --gate lint && python3 scripts/linked-repo-quality-runner.py --project Fantasy --gate test && python3 scripts/linked-repo-quality-runner.py --project Fantasy --gate dependency_security && python3 scripts/linked-repo-quality-runner.py --project Fantasy --gate validation && python3 scripts/linked-repo-quality-runner.py --project Fantasy --gate ci</automated>
  </verify>
  <done>`Dsci-proj` and `Fantasy` are adoption-ready in the readiness report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Research/data validation | Commands may inspect notebooks, scripts, data manifests, and dependency metadata. |
| AIOS evidence | Passing records become readiness authority for repos with varied maturity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-29-04-01 | Tampering | validation commands | mitigate | Keep validation tied to repo behavior or data integrity, not only file presence. |
| T-29-04-02 | Elevation of privilege | dependency tooling | mitigate | Use canonical local package managers and scanners without introducing privileged installs into gate commands. |
</threat_model>

<verification>
Run `python3 scripts/linked-repo-quality-runner.py --report` and confirm the three target repos have empty `missing_gate_keys`.
</verification>

<success_criteria>
`LIS`, `Dsci-proj`, and `Fantasy` report ready with passing local CI proof.
</success_criteria>

<output>
After completion, create `.planning/phases/29-linked-repo-aios-adoption-readiness-remediation/29-04-SUMMARY.md`
</output>
