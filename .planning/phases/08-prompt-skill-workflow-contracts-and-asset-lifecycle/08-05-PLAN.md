---
phase: 08-prompt-skill-workflow-contracts-and-asset-lifecycle
plan: "05"
type: execute
wave: 3
autonomous: true
depends_on:
  - "08-01"
  - "08-02"
files_modified:
  - config/workflows/registry.json
  - config/workflows/skills.json
  - bin/validate-prompts.py
  - tests/test_planned_workflows.py
  - tests/test_validate_prompts.py
requirements:
  - WFLO-01
  - WFLO-02
must_haves:
  truths:
    - "config/workflows/registry.json now carries four new planned workflow contracts — audit-only, audit-and-implement, standards-backfill, security-review — each as lifecycle_state='candidate' (NOT active, per A1+Pitfall 1) with stage-rich vNext bindings (required_inputs, required_outputs, validations, approval_gates, expected_artifacts, writeback_behavior, learning_signals, prompt_bindings, standards_bindings); each new workflow lists trigger_hints aligned with WORKFLOW_MATRIX.md."
    - "config/workflows/skills.json carries one stub skill per new workflow (audit_only_executor, audit_and_implement_executor, standards_backfill_executor, security_review_executor) each with lifecycle_state='draft', execution_mode delegating to _execute_learned_workflow_skill (per Pitfall 6), so validate_workflow_bindings passes."
    - "load_workflow_registry + validate_workflow_bindings now succeed on a registry containing all ten workflows (6 existing + 4 new) without manual edits; the new candidate workflows are recommended by recommend_workflow_from_health (Phase 7) with available_in_registry: true."
    - "bin/validate-prompts.py REQUIRED_FIELDS now includes lifecycle_state, applicability, and last_evaluated_at; this gate is flipped only because Plan 01 backfilled the fields into every existing prompt frontmatter (Pitfall 7 ordering); running validate-prompts.py against the canonical prompts directory continues to exit 0."
    - "validate-prompts.py rejects any new prompt that omits lifecycle_state or sets it to a value outside the five-state literal; the validator reports a structured error with the offending file, missing/invalid field, and a remediation hint."
  artifacts:
    - path: "config/workflows/registry.json"
      provides: "audit-only, audit-and-implement, standards-backfill, security-review workflow definitions as lifecycle_state='candidate'"
    - path: "config/workflows/skills.json"
      provides: "four stub skill specs as lifecycle_state='draft' with execution_mode pointing at the learned-workflow seam"
    - path: "bin/validate-prompts.py"
      provides: "REQUIRED_FIELDS extended with lifecycle_state, applicability, last_evaluated_at; lifecycle_state value validated against the five-state literal; structured rejection messages"
    - path: "tests/test_planned_workflows.py"
      provides: "each new workflow loads and validates; their stage bindings parse to non-empty tuples; they are candidate not active; their stub skills resolve"
    - path: "tests/test_validate_prompts.py"
      provides: "lifecycle_state required; invalid lifecycle_state rejected; canonical prompts pass after Plan 01 backfill"
  key_links:
    - from: "config/workflows/registry.json (new entries)"
      to: "config/workflows/skills.json (new stubs)"
      via: "stages[].required_skills references to <workflow>_executor stub keys"
      pattern: "audit_only_executor|audit_and_implement_executor|standards_backfill_executor|security_review_executor"
    - from: "bin/validate-prompts.py:REQUIRED_FIELDS"
      to: "prompts/*.md frontmatter"
      via: "string match against tuple"
      pattern: "REQUIRED_FIELDS"
    - from: "services/workflow_orchestration.py:validate_workflow_bindings"
      to: "config/workflows/registry.json (new candidates)"
      via: "Pitfall 1 enforcement allows candidate-state workflows with zero validations on some stages, but every new contract still ships with at least one validation on its core stage"
      pattern: "lifecycle_state.*candidate"
---

<objective>
Ship the four planned workflow contracts (audit-only, audit-and-implement, standards-backfill, security-review) as governed lifecycle_state="candidate" entries in config/workflows/registry.json using the vNext schema from Plan 02. Provide one stub skill per workflow in config/workflows/skills.json so validate_workflow_bindings passes (Pitfall 6). Flip bin/validate-prompts.py REQUIRED_FIELDS to require lifecycle_state, applicability, last_evaluated_at now that Plan 01 has backfilled every existing prompt frontmatter (Pitfall 7 ordering). Add validate-prompts tests that prove the gate works for both passing and failing cases.

Purpose: WFLO-01 and WFLO-02 require that each governed workflow is a stage-rich contract; Plan 05 makes that real for the four high-leverage planned workflows so Phase 7's recommend_workflow_from_health stops recommending workflows that "exist only in WORKFLOW_MATRIX.md prose" (RESEARCH State of the Art). The validate-prompts gate completes the lifecycle metadata coverage so future prompts cannot enter the registry without lifecycle state.

Output: four new workflow entries + four stub skills (config-only — no Python module changes), validate-prompts.py extension, and two test files (one new, one extended).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/WORKFLOW_MATRIX.md
@.planning/phases/08-prompt-skill-workflow-contracts-and-asset-lifecycle/08-RESEARCH.md
@.planning/phases/08-prompt-skill-workflow-contracts-and-asset-lifecycle/08-01-PLAN.md
@.planning/phases/08-prompt-skill-workflow-contracts-and-asset-lifecycle/08-02-PLAN.md

<interfaces>
<!-- Symbols and contracts this plan composes against. -->

From services/workflow_orchestration.py (post-Plan-02):
- StageSpec carries eight binding tuples; _stage_from_row parses each; validate_workflow_bindings cross-resolves criterion_ids, prompt_bindings, and standards_bindings.
- Pitfall 1 rule: workflows with lifecycle_state in {approved, active} must declare at least one validation across all stages. Plan 05 ships the four new workflows as "candidate" — but per RESEARCH "every new contract still ships with at least one validation on its core stage" so the workflows are ready to graduate.

From services/asset_lifecycle.py (Plan 01):
- AssetLifecycleState literal — values used in registry JSON.

From bin/validate-prompts.py (current):
- VALID_CLASSIFICATIONS set, REQUIRED_FIELDS tuple, parse_frontmatter, _parse_mapping helpers. Plan 05 extends REQUIRED_FIELDS and adds VALID_LIFECYCLE_STATES set with strict value check.

From WORKFLOW_MATRIX.md:
- Per-workflow contract: required_inputs, required_outputs, validations, approvals, artifacts, writebacks, learning signals. Plan 05 implements the four contracts from this table.

From config/workflows/skills.json (post-Plan-01):
- 23 existing skills. Plan 05 appends four new stub specs. The execution_mode "learned_workflow" (or whatever the existing learned-workflow seam expects — confirm via read_first) maps to services/workflow_orchestration._execute_learned_workflow_skill at runtime.

From config/success-criteria/registry.json (Phase 6):
- Set of valid criterion_ids that new workflow stages can bind to.

From prompts/registry.json (post-Plan-01):
- Five existing templates. Plan 05 does NOT add new templates; it only references existing ones in prompt_bindings.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register four planned workflow contracts (audit-only, audit-and-implement, standards-backfill, security-review) as lifecycle_state="candidate" with vNext stage bindings; register four stub executor skills</name>
  <files>config/workflows/registry.json, config/workflows/skills.json, tests/test_planned_workflows.py</files>
  <read_first>
    - .planning/WORKFLOW_MATRIX.md — full file; identify rows for audit-only, audit-and-implement, standards-backfill, security-review. Each row should give: stage list, prompt family, required skills/tools, validations, approval gates, artifacts, writebacks, learning signals.
    - config/workflows/registry.json — full file (post-Plan-02); identify where new workflow entries should be appended; reuse the existing JSON shape exactly.
    - config/workflows/skills.json — full file (post-Plan-01); identify the existing skill stub or learned-workflow-skill pattern; confirm the execution_mode field name and the values used by existing learned-workflow skills (likely "learned_workflow" or similar — confirm via grep).
    - services/workflow_orchestration.py — find via `grep -n "_execute_learned_workflow_skill\|execution_mode\|learned_workflow" services/workflow_orchestration.py | head -20` to confirm which execution_mode value routes to the learned-workflow seam.
    - config/success-criteria/registry.json — find via `cat config/success-criteria/registry.json | head -60` to identify real criterion_ids these new workflows can bind to (audit-only likely binds to scope-integrity, audit-and-implement to verification-completeness, standards-backfill to standards-coverage, security-review to security-baseline — confirm exact ids).
    - prompts/registry.json — confirm which prompt template_ids (e.g., "research", "reasoning", "coding_debug") match the prompt-family expectations for each new workflow per WORKFLOW_MATRIX.md.
    - .planning/phases/08-prompt-skill-workflow-contracts-and-asset-lifecycle/08-RESEARCH.md — Pitfall 6 (skill stubs delegate to learned-workflow seam), Anti-Patterns (do not ship all ten — A1 caps at four).
  </read_first>
  <behavior>
    - Test 1 `test_audit_only_workflow_loads_and_validates`: load_workflow_registry()["audit-only"] returns a WorkflowSpec with lifecycle_state="candidate", non-empty applicability, at least three stages, at least one stage with non-empty validations[], at least one stage with prompt_bindings referencing an existing template_id.
    - Test 2 `test_audit_and_implement_workflow_loads_and_validates`: same shape assertions for audit-and-implement (additionally: has an approval_gate on the implementation stage with impact_scope="workflow-default", condition="on_failure").
    - Test 3 `test_standards_backfill_workflow_loads_and_validates`: same shape assertions for standards-backfill (additionally: has standards_bindings populated on at least one stage; verify those bindings resolve when validate_workflow_bindings runs).
    - Test 4 `test_security_review_workflow_loads_and_validates`: same shape assertions for security-review (additionally: at least one validation references a real criterion_id from the success-criteria registry).
    - Test 5 `test_all_four_executor_skills_resolve`: load_skill_registry returns audit_only_executor, audit_and_implement_executor, standards_backfill_executor, security_review_executor; each has lifecycle_state="draft".
    - Test 6 `test_validate_workflow_bindings_passes_for_full_registry_post_plan_05`: load_workflow_registry + load_skill_registry + validate_workflow_bindings returns no errors. (Plan 02 Test 6 enforced this on the 6-workflow set; Plan 05 enforces it on the 10-workflow set.)
    - Test 7 `test_new_workflows_are_candidate_not_active`: each of the four new workflows has lifecycle_state="candidate" (Pitfall 1 — they should NOT be active until their stub skills are replaced with real implementations in a later phase).
  </behavior>
  <action>
    1. **Append four new workflow entries to config/workflows/registry.json.** For each entry, use the vNext shape from Plan 02 Pattern 2 and source the per-stage bindings from WORKFLOW_MATRIX.md. Each workflow needs:
       - `key` (e.g., "audit-only")
       - `name` (human-readable)
       - `workflow_family` (e.g., "audit_only", "audit_and_implement", "standards_backfill", "security_review")
       - `lifecycle_state: "candidate"` (NOT active — per Pitfall 1)
       - `applicability` (workflow families and task families it serves)
       - `purpose` and `purpose_long`
       - `trigger_hints` (keywords from WORKFLOW_MATRIX.md)
       - `output_contract` (top-level output shape — match the existing workflows' style)
       - `required_validations` (top-level — match existing style)
       - `stages[]` — at least three stages per workflow. For each stage, populate:
         - `key`, `kind`, `required_skills` (reference the stub executor skill from Task 2 + any existing skills like `agentize_intent_compiler` that apply)
         - `required_inputs[]` (objective from packet for early stages; prior_stage refs for later stages)
         - `required_outputs[]` (report-targeted for analysis stages; writeback-targeted for closeout)
         - `validations[]` — at least one validation per workflow's core stage referencing a real criterion_id from config/success-criteria/registry.json (use the ids identified in read_first)
         - `approval_gates[]` — at minimum on the implementation/remediation stages: impact_scope="workflow-default", condition="on_failure" or "on_blocker", rationale_template (workflow-specific)
         - `expected_artifacts[]` — patch artifacts for implement stages, report artifacts for audit stages, evidence artifacts for security-review
         - `writeback_behavior` — on_failure includes "workflow_learning_event" and "follow_up_item"; on_success includes "workflow_learning_event"
         - `learning_signals[]` — rework_rate + validation_pass on every workflow; additionally writeback_usefulness on standards-backfill
         - `prompt_bindings[]` — bind to existing template_ids (e.g., audit-only → "research" + "reasoning"; audit-and-implement → "coding_debug" + "research"; standards-backfill → "reasoning"; security-review → "research" + "reasoning"). Roles: "primary" for the core stage prompt, "fallback" for the secondary.
         - `standards_bindings[]` — standards-backfill in particular should bind to one or two real standards_definitions ids from config/standards/registry.json (read_first identifies them).

    2. **Append four new stub skill specs to config/workflows/skills.json.** Each skill:
       - `key` (e.g., "audit_only_executor")
       - `purpose` (short)
       - `purpose_long`
       - `allowed_stages` (list the kinds the stub can run in)
       - `input_schema` and `output_schema` (minimal — `{type: "object"}` or copy the closest existing learned-workflow skill's shape)
       - `invariants`, `failure_conditions`, `side_effects` (each a string list — minimal but truthful)
       - `execution_mode` — set to the value that routes to `_execute_learned_workflow_skill` (confirmed via read_first; likely "learned_workflow")
       - `lifecycle_state: "draft"` (per Pitfall 6: stub is draft, workflow is candidate)
       - `applicability` (workflow_family tags)
       - `source_path: null`, `installed_name: null` (these are pure registry stubs — no SKILL.md file required for Phase 8 per RESEARCH)
       - Bump skills.json `version` to "2026-05-21" if not already.

    3. **Create tests/test_planned_workflows.py** with the seven behavior tests above. Use load_workflow_registry + load_skill_registry + validate_workflow_bindings as the primary harness; no extra fixtures needed beyond what already exists in tests/test_workflow_orchestration.py.

    4. **JSON well-formedness verification:** `python -c "import json; json.load(open('config/workflows/registry.json')); json.load(open('config/workflows/skills.json'))"` exits 0.

    5. **Quality ladder:** `uv run ruff check tests/test_planned_workflows.py && uv run ruff format --check tests/test_planned_workflows.py`. JSON files validated by test pass.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_planned_workflows.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import json; r=json.load(open('config/workflows/registry.json')); keys={w['key'] for w in r['workflows']}; missing={'audit-only','audit-and-implement','standards-backfill','security-review'}-keys; assert not missing, f'missing workflows: {missing}'"` exits 0
    - `python -c "import json; r=json.load(open('config/workflows/registry.json')); planned=[w for w in r['workflows'] if w['key'] in ('audit-only','audit-and-implement','standards-backfill','security-review')]; assert all(w['lifecycle_state']=='candidate' for w in planned), 'one of the planned workflows is not candidate'"` exits 0
    - `python -c "import json; r=json.load(open('config/workflows/skills.json')); keys={s['key'] for s in r['skills']}; missing={'audit_only_executor','audit_and_implement_executor','standards_backfill_executor','security_review_executor'}-keys; assert not missing, f'missing skills: {missing}'"` exits 0
    - `python -c "import json; r=json.load(open('config/workflows/skills.json')); stubs=[s for s in r['skills'] if s['key'].endswith('_executor')]; assert all(s['lifecycle_state']=='draft' for s in stubs), 'one stub is not draft'"` exits 0
    - `grep -c "def test_audit_only_workflow_loads_and_validates\|def test_audit_and_implement_workflow_loads_and_validates\|def test_standards_backfill_workflow_loads_and_validates\|def test_security_review_workflow_loads_and_validates\|def test_all_four_executor_skills_resolve\|def test_validate_workflow_bindings_passes_for_full_registry_post_plan_05\|def test_new_workflows_are_candidate_not_active" tests/test_planned_workflows.py` returns at least 7
    - `uv run pytest tests/test_planned_workflows.py -x -q` exits 0
    - `uv run pytest tests/test_workflow_orchestration.py -x -q` exits 0 (regression check on existing six workflows)
    - `python -c "import json; json.load(open('config/workflows/registry.json')); json.load(open('config/workflows/skills.json'))"` exits 0
    - `uv run ruff check tests/test_planned_workflows.py` exits 0
    - `uv run ruff format --check tests/test_planned_workflows.py` exits 0
  </acceptance_criteria>
  <done>
    Four new planned workflow contracts are registered as lifecycle_state="candidate" with stage-rich vNext bindings; four stub executor skills are registered as lifecycle_state="draft" delegating to the learned-workflow seam; validate_workflow_bindings passes on the 10-workflow registry; the seven behavior tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend bin/validate-prompts.py REQUIRED_FIELDS to require lifecycle_state, applicability, last_evaluated_at; enforce five-state literal on lifecycle_state; add structured rejection messages</name>
  <files>bin/validate-prompts.py, tests/test_validate_prompts.py</files>
  <read_first>
    - bin/validate-prompts.py — full file (small). Locate REQUIRED_FIELDS, VALID_CLASSIFICATIONS, parse_frontmatter, the main validation loop, and the error-emission shape.
    - prompts/research.md and one other prompts/*.md — confirm Plan 01 backfilled lifecycle_state, applicability, last_evaluated_at. Verify by `grep -l "^lifecycle_state:" prompts/*.md | wc -l` should return 5.
    - prompts/registry.json — confirm the same fields are present on every template entry post-Plan-01.
    - tests/test_validate_prompts.py — find via `ls tests/test_validate_prompts.py 2>/dev/null && head -60 tests/test_validate_prompts.py` to confirm the existing test fixture pattern. If the file does not exist, create it.
    - .planning/phases/08-prompt-skill-workflow-contracts-and-asset-lifecycle/08-RESEARCH.md — Pitfall 7 (ordering: backfill first, gate second).
  </read_first>
  <behavior>
    - Test 1 `test_validate_prompts_requires_lifecycle_state`: a synthetic prompt frontmatter dict missing `lifecycle_state` causes the validator to emit an error mentioning "lifecycle_state" and "required".
    - Test 2 `test_validate_prompts_rejects_invalid_lifecycle_state_value`: a synthetic prompt frontmatter with `lifecycle_state: "promoted"` (legacy value not in the five-state literal) is rejected with an error referencing "promoted" and listing the allowed set.
    - Test 3 `test_validate_prompts_accepts_canonical_prompts_post_plan_01`: invoking validate-prompts on every file in prompts/*.md (using the validator's main entry point) returns no errors. This confirms Plan 01's backfill is complete and the gate flip is safe.
    - Test 4 `test_validate_prompts_requires_applicability`: missing `applicability` is rejected.
    - Test 5 `test_validate_prompts_requires_last_evaluated_at`: missing `last_evaluated_at` is rejected.
    - Test 6 `test_validate_prompts_accepts_route_status_alias_during_transition`: a prompt that carries both `lifecycle_state: "active"` and the legacy `route_status: "approved"` validates (Plan 01 one-cycle alias support); a prompt that carries only `route_status` and no `lifecycle_state` is rejected.
  </behavior>
  <action>
    1. **Extend bin/validate-prompts.py:**
       - Add a module-level constant `VALID_LIFECYCLE_STATES = frozenset({"draft", "candidate", "approved", "active", "deprecated"})`.
       - Extend `REQUIRED_FIELDS` tuple by appending `"lifecycle_state"`, `"applicability"`, `"last_evaluated_at"`.
       - In the main validation loop, after the existing classification check, add a lifecycle_state value check: if `fm.get("lifecycle_state")` is not in VALID_LIFECYCLE_STATES, emit an error string like `f"{path}: lifecycle_state {value!r} not in {sorted(VALID_LIFECYCLE_STATES)}; valid values are draft|candidate|approved|active|deprecated"`.
       - Keep the existing structured-error shape; do not change error code conventions.
       - The validator's main() exit code: non-zero when any error emitted (existing behavior). Confirm via read_first.

    2. **Extend tests/test_validate_prompts.py (or create it if absent)** with the six behavior tests. For Tests 1, 2, 4, 5, 6, build synthetic frontmatter dicts and call the validation function directly (do not write temp files). For Test 3, invoke the validator's main entry point (subprocess or in-process) on the canonical prompts/ directory.

    3. **Quality ladder per touched file:** `uv run ruff check bin/validate-prompts.py tests/test_validate_prompts.py && uv run ruff format --check bin/validate-prompts.py tests/test_validate_prompts.py && uv run basedpyright bin/validate-prompts.py`.

    4. **Smoke test:** `uv run python bin/validate-prompts.py` (or equivalent main invocation, confirmed via read_first) against the canonical prompts/ directory; exit code MUST be 0. If non-zero, Plan 01's backfill is incomplete — DO NOT proceed; report the gap.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_validate_prompts.py -x -q && uv run python bin/validate-prompts.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "VALID_LIFECYCLE_STATES" bin/validate-prompts.py | wc -l | tr -d ' '` returns at least 2 (definition + usage)
    - `grep -n "lifecycle_state\|applicability\|last_evaluated_at" bin/validate-prompts.py | grep -i "REQUIRED_FIELDS\|required" | head -5` shows the new fields are in REQUIRED_FIELDS
    - `grep -c "def test_validate_prompts_requires_lifecycle_state\|def test_validate_prompts_rejects_invalid_lifecycle_state_value\|def test_validate_prompts_accepts_canonical_prompts_post_plan_01\|def test_validate_prompts_requires_applicability\|def test_validate_prompts_requires_last_evaluated_at\|def test_validate_prompts_accepts_route_status_alias_during_transition" tests/test_validate_prompts.py` returns at least 6
    - `uv run pytest tests/test_validate_prompts.py -x -q` exits 0
    - `uv run python bin/validate-prompts.py` exits 0 (canonical prompts/ directory still passes the gate after the flip — confirms Plan 01 backfill is complete)
    - `uv run ruff check bin/validate-prompts.py tests/test_validate_prompts.py` exits 0
    - `uv run ruff format --check bin/validate-prompts.py tests/test_validate_prompts.py` exits 0
    - `uv run basedpyright bin/validate-prompts.py` exits 0
  </acceptance_criteria>
  <done>
    bin/validate-prompts.py now requires lifecycle_state, applicability, last_evaluated_at and rejects lifecycle_state values outside the five-state literal; canonical prompts/*.md continue to pass; six behavior tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| New workflow contracts -> registry | New contracts enter via reviewable git edits; lifecycle_state="candidate" prevents accidental activation per Pitfall 1. |
| Stub executor skills -> learned-workflow seam | Stubs delegate to existing `_execute_learned_workflow_skill`; no new code path; lifecycle_state="draft" signals the gap. |
| validate-prompts gate -> prompt frontmatter | Gate flip is safe only because Plan 01 backfilled all existing prompts; Task 2 smoke test enforces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-23 | Tampering | New workflow shipped with lifecycle_state="active" but no validations | mitigate | Plan 02 validate_workflow_bindings rejects active+zero-validations; Plan 05 ships all four as "candidate"; Test 7 in Task 1 enforces. |
| T-08-24 | Integrity | Stub skill silently used in production | mitigate | Stubs are lifecycle_state="draft"; future Phase 9 will not promote a workflow whose required_skills include a draft skill (will need to be enforced by Plan 04's promotion path — note as a follow-up). |
| T-08-25 | Tampering | New prompt enters registry without lifecycle_state | mitigate | Task 2 extends REQUIRED_FIELDS; validator rejects with structured error. |
| T-08-26 | Repudiation | Legacy route_status used without lifecycle_state | mitigate | Task 2 Test 6 enforces: route_status alone is rejected; lifecycle_state required. |
| T-08-27 | Integrity | New workflow stage references unresolved standard | mitigate | Plan 02 validate_workflow_bindings resolves standards_bindings cross-registry; Test 3 in Plan 05 Task 1 enforces for standards-backfill. |
</threat_model>

<verification>
- `uv run pytest tests/test_planned_workflows.py tests/test_validate_prompts.py tests/test_workflow_orchestration.py tests/test_asset_lifecycle.py tests/test_architecture_enforcement.py -x -q` exits 0
- `uv run python bin/validate-prompts.py` exits 0 (canonical prompts/ pass the post-flip gate)
- `uv run ruff check bin/validate-prompts.py tests/test_planned_workflows.py tests/test_validate_prompts.py` exits 0
- `uv run ruff format --check bin/validate-prompts.py tests/test_planned_workflows.py tests/test_validate_prompts.py` exits 0
- `uv run basedpyright bin/validate-prompts.py` exits 0
- `python -c "import json; r=json.load(open('config/workflows/registry.json')); assert len([w for w in r['workflows'] if w['key'] in ('audit-only','audit-and-implement','standards-backfill','security-review')]) == 4"` exits 0
- Quality ladder per CLAUDE.md: hard gates green.
</verification>

<success_criteria>
- WFLO-01 completion: four new planned workflow contracts ship as stage-rich vNext entries in config/workflows/registry.json with lifecycle_state="candidate"; load_workflow_registry + validate_workflow_bindings succeed on the full 10-workflow registry.
- WFLO-02 completion: the new contracts bind prompts (via prompt_bindings), skills (via required_skills + new stub executor skills), tools (via existing executor seam), standards (via standards_bindings on standards-backfill), approval gates (impact_scope=workflow-default on implementation/remediation stages), writeback behavior (workflow_learning_event + follow_up_item on failure), and learning signals (rework_rate + validation_pass everywhere; writeback_usefulness on standards-backfill).
- Pitfall 6 (stubs delegate to learned-workflow seam) honored; Pitfall 1 (no active workflows with zero validations) honored; Pitfall 7 (validate-prompts gate ordering) honored.
- A1 scope (ship four, defer six) respected — the remaining six planned workflow families stay deferred to Phase 9 or a follow-up.
</success_criteria>

<output>
After completion, create `.planning/phases/08-prompt-skill-workflow-contracts-and-asset-lifecycle/08-05-SUMMARY.md` per the template, including:
- The exact JSON shape of the four new workflow entries (stage list per workflow, per-stage binding counts)
- The four new stub executor skills with their lifecycle_state and applicability
- Confirmation that validate_workflow_bindings passes on the 10-workflow registry
- The Pitfall 7 sequencing proof: validate-prompts.py REQUIRED_FIELDS was flipped only after Plan 01 confirmed every prompt frontmatter carries lifecycle_state, applicability, last_evaluated_at
- Note that the remaining six planned workflow families (repo-cleanup, UI-polish, PRD, test-first-implementation, research-to-plan, project-truth-update, prompt-experiment, agent-handoff-generation, codebase-architecture-review) stay deferred per A1; Phase 9 or a Phase 8 follow-up wave owns them
- Note that the stub executor skills are lifecycle_state="draft" and that promoting any of the four new workflows to "approved" or "active" will require a follow-up phase to replace the stubs with real implementations
- Note that the validate_prompts gate flip closes Pitfall 7; subsequent prompt additions must include the new required fields
</output>
