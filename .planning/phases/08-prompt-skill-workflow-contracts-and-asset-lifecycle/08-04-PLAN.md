---
phase: 08-prompt-skill-workflow-contracts-and-asset-lifecycle
plan: "04"
type: execute
wave: 3
autonomous: true
depends_on:
  - "08-01"
  - "08-02"
  - "08-03"
files_modified:
  - services/workflow_promotion.py
  - services/workflow_orchestration.py
  - services/aios_cli.py
  - bin/hook-stop.py
  - tests/test_workflow_promotion.py
  - tests/test_workflow_orchestration.py
  - tests/test_aios_cli.py
requirements:
  - WFLO-03
  - WFLO-04
must_haves:
  truths:
    - "execute_workflow appends a stage_evaluations[] block to workflow_execution_reports.report_json with one entry per stage, each containing stage_key, passed_validations, total_validations, blocker_count, warning_count, stage_finding_ids, outcome (one of completed | failed | blocked); the block is computed via _build_stage_evaluation_summary using stage validations + success_criteria_stage_findings joined by run_id + stage_key."
    - "compare_workflow_effectiveness returns a WorkflowEffectiveness record with run_count, completed_count, failed_count, rework_rate, validation_pass_rate, mean_blocker_count, mean_blockers_per_stage (Pitfall 5 normalization), writeback_usefulness, and a per-stage breakdown; the function reads only durable evidence (workflow_execution_reports + success_criteria_stage_findings + improvement_writebacks)."
    - "propose_workflow_promotion writes one improvement_writebacks row (via the same shim Plan 01 introduced, since services/ cannot import bin/) with layer_type='workflow' and impact_scope='workflow-default' before flipping the lifecycle; when policy.requires_approval is True (which Phase 5 always returns for workflow-default), the corresponding promotion_lifecycle_items row is recorded with status='proposed' (NOT the target state) and is updated to the target state only after the writeback is approved."
    - "bin/hook-stop.py emits an asset_promotion_candidate writeback at governed closeout when the run's evidence (high outcome score, low rework, validations all passed) crosses the threshold; the writeback is advisory (status='proposed'); the actual promotion still requires explicit operator action via aios CLI or UI."
    - "services/aios_cli.py exposes aios workflow-compare, aios promote-asset, and aios asset-lifecycle subcommands with --json output; aios contracts-audit adds an AssetLifecycle contract row and a WorkflowComparison contract row."
  artifacts:
    - path: "services/workflow_promotion.py"
      provides: "WorkflowEffectiveness dataclass; compare_workflow_effectiveness(conn, *, workflow_key, since) -> WorkflowEffectiveness; propose_workflow_promotion(conn, *, workflow_key, to_state, evidence, actor, rationale) -> dict; finalize_workflow_promotion(conn, *, lifecycle_id, approval_writeback_id, actor) -> dict"
    - path: "services/workflow_orchestration.py"
      provides: "_build_stage_evaluation_summary(...) helper invoked inside execute_workflow; report_json now carries stage_evaluations[]"
    - path: "services/aios_cli.py"
      provides: "workflow-compare, promote-asset, asset-lifecycle subcommands; contracts-audit extended with AssetLifecycle and WorkflowComparison rows"
    - path: "bin/hook-stop.py"
      provides: "asset_promotion_candidate writeback emission at governed closeout when evidence threshold crossed"
    - path: "tests/test_workflow_promotion.py"
      provides: "compare metrics; propose-then-approve flow; promotion requires approval; per-stage normalization; finalize after approval"
  key_links:
    - from: "services/workflow_orchestration.py:execute_workflow"
      to: "services/workflow_orchestration.py:_build_stage_evaluation_summary"
      via: "per-stage call after stage skills + validations + stage_findings are collected; output appended to report.report_json.stage_evaluations[]"
      pattern: "_build_stage_evaluation_summary"
    - from: "services/workflow_promotion.py:propose_workflow_promotion"
      to: "improvement_writebacks + promotion_lifecycle_items"
      via: "writeback_approval_policy_shim from Plan 01 then INSERT into both tables"
      pattern: "improvement_writebacks|promotion_lifecycle_items"
    - from: "services/aios_cli.py:cmd_workflow_compare"
      to: "services/workflow_promotion.compare_workflow_effectiveness"
      via: "CLI subcommand returns JSON of the dataclass"
      pattern: "compare_workflow_effectiveness"
    - from: "bin/hook-stop.py:closeout"
      to: "improvement_writebacks (asset_promotion_candidate row)"
      via: "threshold check against last run's evidence; advisory writeback insert"
      pattern: "asset_promotion_candidate"
---

<objective>
Make workflow success measurable at the stage level and the run level from durable evidence; expose workflow effectiveness comparison; route every workflow promotion through Phase 5 writeback approval; and seed asset promotion candidates at governed closeout. Plan 04 ships services/workflow_promotion.py with three functions, extends execute_workflow to record per-stage evaluation rollups, extends aios CLI with three new subcommands, and extends bin/hook-stop.py to emit advisory promotion candidates when a run's evidence crosses the threshold.

Purpose: WFLO-03 requires stage- and run-level evaluation from durable evidence. WFLO-04 requires comparison over time + governed promote/revise/deprecate. Plan 04 turns the recommender's evidence into actionable promotion proposals while keeping every transition reviewable through Phase 5 rails.

Output: services/workflow_promotion.py (new), execute_workflow stage_evaluations[] rollup, three new aios CLI subcommands, hook-stop closeout enrichment, and three test files (one new, two extended).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-prompt-skill-workflow-contracts-and-asset-lifecycle/08-RESEARCH.md
@.planning/phases/08-prompt-skill-workflow-contracts-and-asset-lifecycle/08-01-PLAN.md
@.planning/phases/08-prompt-skill-workflow-contracts-and-asset-lifecycle/08-02-PLAN.md
@.planning/phases/08-prompt-skill-workflow-contracts-and-asset-lifecycle/08-03-PLAN.md

<interfaces>
<!-- Symbols this plan composes against. -->

From services/asset_lifecycle.py (Plan 01):
- AssetLifecycleState literal, LIFECYCLE_TRANSITIONS map
- promote_asset(conn, *, asset_kind, asset_key, from_state, to_state, actor, rationale, evidence_ids=(), proposed_change=None) -> LifecycleTransition
- writeback_approval_policy_shim(*, layer_type, impact_scope, proposed_change) -> dict (the duplicated Phase 5 classifier — Plan 04 reuses this exclusively; never imports from bin/)
- ensure_asset_lifecycle_schema(conn)

From services/workflow_orchestration.py (post-Plan-02):
- StageSpec carries validations, approval_gates, expected_artifacts, writeback_behavior, learning_signals
- execute_workflow(conn, ..., *, workflows, skills, route_id, packet_id, ...) — current shape iterates stages, dispatches skills via _execute_skill, collects validations into a list. Plan 04 adds _build_stage_evaluation_summary per stage and appends to report.report_json.stage_evaluations[].
- validate_workflow_bindings — extended in Plan 02.

From services/asset_recommendation.py (Plan 03):
- build_asset_usage_evidence(conn, *, asset_kind, asset_key, since=None) -> AssetUsageEvidence — Plan 04 reuses for prompt/skill candidate scoring at closeout.

From schema.sql:
- workflow_execution_reports: id, run_id, invocation_id, workflow_key, status, report_json, artifact_path, created_at.
- improvement_writebacks (Phase 5, ensured via bin/aios_orchestration_runtime.ensure_writeback_columns): id, layer_type, impact_scope, policy_class, status, proposed_change_json, rationale, actor, decision_note, decision_actor, decision_at, created_at, updated_at. Plan 04 INSERTs through the Plan 01 shim's helper (which already mirrors the column set).
- promotion_lifecycle_items: id, item_kind, item_key, source_run_id, status, evidence_json, status_reason, created_at, updated_at, metadata_json.
- success_criteria_stage_findings (Phase 6): id, run_id, stage_key, criterion_id, level (info|warning|blocker), summary, evidence_json, created_at, resolution_status, resolution_actor, resolution_rationale, resolution_evidence_json, resolved_at.

From services/aios_cli.py:
- argparse-based subparsers — current subcommands include contracts-audit, governance-audit, prove-project-health, capability-audit. Pattern for adding new subcommand: subparsers.add_parser("name", help=...), plus a cmd_<name>(args) -> dict function, plus a branch in the main dispatcher. _contracts_audit_payload lists current canonical contracts — extend with AssetLifecycle and WorkflowComparison.

From bin/hook-stop.py:
- Closeout invokes services.success_criteria.evaluate, persists workflow_learning_events, processes governed closeout. Plan 04 adds a small block after the existing writeback/follow-up emission that checks the most recent agentize_evaluations.recommendation_source + workflow_execution_reports.status + count of blocker findings; if the rolling per-asset evidence crosses the threshold, insert an asset_promotion_candidate writeback row (advisory).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend execute_workflow with _build_stage_evaluation_summary; append stage_evaluations[] to report_json</name>
  <files>services/workflow_orchestration.py, tests/test_workflow_orchestration.py</files>
  <read_first>
    - services/workflow_orchestration.py — find via `grep -n "def execute_workflow\|def _execute_skill\|def _execute_learned_workflow_skill\|stages\.append\|report\[.stages.\]" services/workflow_orchestration.py` and read execute_workflow in full plus the stage loop body
    - services/success_criteria.py — find via `grep -n "success_criteria_stage_findings\|stage_finding\|def write_stage_finding\|def evaluate_stage" services/success_criteria.py` and read the writer + reader pattern
    - schema.sql — re-confirm success_criteria_stage_findings column set
    - .planning/phases/06-standards-resolution-and-evidence-based-evaluation/06-RESEARCH.md — find via `grep -n "success_criteria_stage_findings\|stage_finding\|stage_evaluation" .planning/phases/06-standards-resolution-and-evidence-based-evaluation/06-RESEARCH.md | head -10` to confirm Phase 6 wrote the table
    - .planning/phases/08-prompt-skill-workflow-contracts-and-asset-lifecycle/08-RESEARCH.md — Pattern 5 (_build_stage_evaluation_summary) paste-of-record
  </read_first>
  <behavior>
    - Test 1 `test_stage_evaluation_summary_in_report`: after execute_workflow finishes on a seeded workflow (use implementation-delivery from the canonical registry or a small synthetic 2-stage workflow), the resulting workflow_execution_reports row's report_json contains a top-level "stage_evaluations" key with a list whose length equals the number of stages executed.
    - Test 2 `test_stage_evaluation_summary_outcome_completed_when_no_blockers`: a stage with all validations.passed=true and zero blocker-level stage_findings has stage_evaluations entry with outcome="completed".
    - Test 3 `test_stage_evaluation_summary_outcome_failed_when_validation_fails`: a stage with at least one validation.passed=false has outcome="failed".
    - Test 4 `test_stage_evaluation_summary_outcome_blocked_when_blocker_finding`: a stage that has a success_criteria_stage_findings row with level="blocker" matched by run_id+stage_key has outcome="blocked".
    - Test 5 `test_stage_evaluation_summary_includes_stage_finding_ids`: stage_finding_ids list contains the ids of the matched stage findings.
    - Test 6 `test_stage_evaluation_summary_counts_warnings_separately`: warning_count reflects stage findings at level="warning" but does not affect outcome.
  </behavior>
  <action>
    1. **Add `_build_stage_evaluation_summary` to services/workflow_orchestration.py** per RESEARCH Pattern 5. Signature: `_build_stage_evaluation_summary(*, stage_key: str, skill_reports: list[dict[str, Any]], validations: list[dict[str, Any]], stage_issues: list[str], stage_findings: list[dict[str, Any]] | None = None) -> dict[str, Any]`. Returns a dict with stage_key, passed_validations, total_validations, blocker_count, warning_count, stage_finding_ids, outcome.
       - outcome rules:
         - If any stage_finding has level="blocker" → outcome="blocked"
         - Else if any validation in stage_validations has passed=False → outcome="failed"
         - Else outcome="completed"

    2. **Extend execute_workflow** to:
       - After each stage finishes (right before appending the stage dict to `stages[]` or right after, depending on the existing structure), query success_criteria_stage_findings for the current run_id and stage_key: `SELECT id, level, summary FROM success_criteria_stage_findings WHERE run_id = ? AND stage_key = ?`. Convert rows to dicts.
       - Call _build_stage_evaluation_summary with the stage's skill_reports, validations, issues, and the loaded stage_findings.
       - Append the resulting dict to a local `stage_evaluations_list`.
       - When constructing the final `report` dict, set `report["stage_evaluations"] = stage_evaluations_list`. Persist with the existing INSERT into workflow_execution_reports (the report_json column already serializes the full dict).
       - Wrap the SELECT in try/except sqlite3.OperationalError so tests that don't seed success_criteria_stage_findings still pass (treat as empty stage_findings list).

    3. **Add the six behavior tests** to tests/test_workflow_orchestration.py. For Tests 4–5 the test must CREATE TABLE success_criteria_stage_findings inline before execute_workflow runs and INSERT a seed row keyed by the run_id that execute_workflow will use.

    4. **Quality ladder per touched file:** `uv run ruff check services/workflow_orchestration.py tests/test_workflow_orchestration.py && uv run ruff format --check services/workflow_orchestration.py tests/test_workflow_orchestration.py && uv run basedpyright services/workflow_orchestration.py`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_workflow_orchestration.py -x -q -k "stage_evaluation_summary"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def _build_stage_evaluation_summary" services/workflow_orchestration.py | wc -l | tr -d ' '` returns 1
    - `grep -n "stage_evaluations\|_build_stage_evaluation_summary" services/workflow_orchestration.py | wc -l | tr -d ' '` returns at least 3 (helper def + execute_workflow call + report_json assignment)
    - `grep -c "def test_stage_evaluation_summary_in_report\|def test_stage_evaluation_summary_outcome_completed_when_no_blockers\|def test_stage_evaluation_summary_outcome_failed_when_validation_fails\|def test_stage_evaluation_summary_outcome_blocked_when_blocker_finding\|def test_stage_evaluation_summary_includes_stage_finding_ids\|def test_stage_evaluation_summary_counts_warnings_separately" tests/test_workflow_orchestration.py` returns at least 6
    - `uv run pytest tests/test_workflow_orchestration.py -x -q` exits 0
    - `uv run ruff check services/workflow_orchestration.py tests/test_workflow_orchestration.py` exits 0
    - `uv run ruff format --check services/workflow_orchestration.py tests/test_workflow_orchestration.py` exits 0
    - `uv run basedpyright services/workflow_orchestration.py` exits 0
  </acceptance_criteria>
  <done>
    execute_workflow now records per-stage evaluation rollups into workflow_execution_reports.report_json.stage_evaluations[]; outcome semantics distinguish completed/failed/blocked; six behavior tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create services/workflow_promotion.py with compare_workflow_effectiveness, propose_workflow_promotion, finalize_workflow_promotion; route all promotions through Plan 01 writeback shim</name>
  <files>services/workflow_promotion.py, tests/test_workflow_promotion.py</files>
  <read_first>
    - services/asset_lifecycle.py (Plan 01) — promote_asset, writeback_approval_policy_shim signatures
    - services/workflow_orchestration.py — execute_workflow output shape post-Task-1 (report_json.stage_evaluations[]) for compare_workflow_effectiveness inputs
    - schema.sql — workflow_execution_reports, improvement_writebacks (including the additional columns ensured by bin/aios_orchestration_runtime.py), promotion_lifecycle_items
    - .planning/phases/08-prompt-skill-workflow-contracts-and-asset-lifecycle/08-RESEARCH.md — Pattern 4 (compare + propose), Pitfall 4 (writeback bypass), Pitfall 5 (per-stage normalization)
    - services/divergent_strategy.py — find via `grep -n "_writeback_id\|insert_writeback\|improvement_writebacks" services/divergent_strategy.py | head -10` to confirm whether services/ already has a sibling helper that INSERTs into improvement_writebacks; if it does, reuse; if it does not, copy the Phase 5 INSERT column list into a private helper inside services/workflow_promotion.py
  </read_first>
  <behavior>
    - Test 1 `test_compare_workflow_returns_metrics`: seed three workflow_execution_reports rows for workflow_key "implementation-delivery" with mixed status (2 completed, 1 failed), plus matching success_criteria_stage_findings (1 blocker on the failed run, 0 on the completed runs). compare_workflow_effectiveness(conn, workflow_key="implementation-delivery", since="2026-04-01T00:00:00Z") returns run_count=3, completed_count=2, failed_count=1, validation_pass_rate=2/3, mean_blocker_count=1/3.
    - Test 2 `test_compare_normalizes_per_stage_blockers` (Pitfall 5): seed two workflows — implementation-delivery (4 stages, 1 blocker total) and academic_paper_v1 (7 stages, 2 blockers total). Both compare runs return mean_blockers_per_stage = 0.25 and 0.286 respectively; mean_blocker_count returns raw 1 and 2. The dataclass exposes both views.
    - Test 3 `test_compare_includes_writeback_usefulness`: seed improvement_writebacks rows tied to workflow_execution_reports.run_id (via metadata_json or a join) where 1 of 2 was approved. writeback_usefulness = 0.5.
    - Test 4 `test_propose_workflow_promotion_writes_writeback_with_workflow_default_scope` (Pitfall 4): propose_workflow_promotion(conn, workflow_key="implementation-delivery", to_state="active", evidence=..., actor="op", rationale="x") inserts an improvement_writebacks row with layer_type="workflow", impact_scope="workflow-default", policy_class="workflow-default_change", status="proposed". Verify with SELECT.
    - Test 5 `test_propose_workflow_promotion_records_proposed_not_target_state` (Pitfall 4): the corresponding promotion_lifecycle_items row has status="proposed" (NOT "active") until finalize_workflow_promotion runs.
    - Test 6 `test_finalize_workflow_promotion_requires_approved_writeback`: calling finalize_workflow_promotion with a writeback whose status is still "proposed" raises ValueError; with status="approved", it updates the promotion_lifecycle_items row to the target state and returns {lifecycle_id, previous_status, new_status, finalized_at}.
    - Test 7 `test_promote_active_requires_writeback`: trying to bypass propose by calling promote_asset(conn, asset_kind="workflow", from_state="approved", to_state="active") directly without proposed_change/writeback insert still hits writeback_approval_policy_shim (which returns requires_approval=True); promote_asset (Plan 01) inserts the writeback row before the lifecycle row — assert both rows exist after the call.
  </behavior>
  <action>
    1. **Create services/workflow_promotion.py** with:

       - Frozen dataclass `WorkflowEffectiveness`:
         - workflow_key: str
         - since: str
         - run_count: int
         - completed_count: int
         - failed_count: int
         - rework_rate: float (count of runs that share a packet_id with a later run, divided by run_count)
         - validation_pass_rate: float (completed_count / run_count)
         - mean_blocker_count: float (total blockers / run_count, raw)
         - mean_blockers_per_stage: float (total blockers / total stage count across all runs) (Pitfall 5)
         - writeback_usefulness: float (approved improvement_writebacks / total improvement_writebacks tied to these runs)
         - stage_evaluations: dict[str, dict[str, float]] (per-stage averages: passed_validation_rate, blocker_rate, warning_rate keyed by stage_key)

       - Function `compare_workflow_effectiveness(conn, *, workflow_key: str, since: str) -> WorkflowEffectiveness`:
         - SELECT id, run_id, status, report_json, created_at FROM workflow_execution_reports WHERE workflow_key=? AND created_at >= ?
         - For each row, json.loads(report_json) and accumulate stage_evaluations counts.
         - SELECT count from success_criteria_stage_findings JOIN on run_id (level='blocker' / 'warning' aggregations).
         - SELECT improvement_writebacks WHERE proposed_change_json LIKE '%"workflow_key":"<workflow_key>"%' (or metadata link — choose whichever join the writeback row already supports based on read_first).
         - Compute rework_rate: for each run, check if there is a later run within `since` with the same packet_id (via orchestration_runs.briefing_packet_id if accessible — if not accessible cheaply, set rework_rate to 0.0 and note in WorkflowEffectiveness via a metadata field rework_rate_unavailable: bool).
         - Build and return the dataclass.

       - Function `propose_workflow_promotion(conn, *, workflow_key: str, to_state: AssetLifecycleState, evidence: WorkflowEffectiveness, actor: str, rationale: str) -> dict[str, Any]`:
         - Call writeback_approval_policy_shim(layer_type="workflow", impact_scope="workflow-default", proposed_change={"workflow_key": workflow_key, "to_state": to_state, "evidence_summary": dataclasses.asdict(evidence)}).
         - INSERT into improvement_writebacks via a private `_insert_workflow_promotion_writeback(conn, ...)` helper that mirrors the Phase 5 column set. Returns the writeback_id.
         - INSERT into promotion_lifecycle_items with status="proposed" (NOT to_state), source_run_id=None, item_kind="workflow", item_key=workflow_key, evidence_json=json.dumps(dataclasses.asdict(evidence)), status_reason=rationale, metadata_json=json.dumps({"actor": actor, "target_state": to_state, "approval_writeback_id": writeback_id, "from_state": <current lifecycle_state read from registry>}).
         - Return {writeback_id, lifecycle_id, requires_approval: policy["requires_approval"], rationale: policy["reason"]}.

       - Function `finalize_workflow_promotion(conn, *, lifecycle_id: str, approval_writeback_id: str, actor: str) -> dict[str, Any]`:
         - SELECT status FROM improvement_writebacks WHERE id=approval_writeback_id; if status != "approved", raise ValueError("Cannot finalize promotion: writeback {id} is {status!r}, must be 'approved'").
         - SELECT current promotion_lifecycle_items row by lifecycle_id; parse metadata_json.target_state.
         - UPDATE promotion_lifecycle_items SET status=target_state, updated_at=now, status_reason=status_reason || " | finalized by " || actor.
         - Return {lifecycle_id, previous_status: "proposed", new_status: target_state, finalized_at: now}.

    2. **Create tests/test_workflow_promotion.py** with the seven behavior tests above. Use the seeding helpers from Plan 03 Task 1 plus the Plan 01 _seed_writeback_table helper.

    3. **Quality ladder per touched file:** `uv run ruff check services/workflow_promotion.py tests/test_workflow_promotion.py && uv run ruff format --check services/workflow_promotion.py tests/test_workflow_promotion.py && uv run basedpyright services/workflow_promotion.py`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_workflow_promotion.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class WorkflowEffectiveness\|def compare_workflow_effectiveness\|def propose_workflow_promotion\|def finalize_workflow_promotion" services/workflow_promotion.py | wc -l | tr -d ' '` returns at least 4
    - `grep -n "mean_blockers_per_stage" services/workflow_promotion.py | wc -l | tr -d ' '` returns at least 1 (Pitfall 5 normalization shipped)
    - `grep -n "from services.asset_lifecycle import\|writeback_approval_policy_shim" services/workflow_promotion.py | wc -l | tr -d ' '` returns at least 2
    - `grep -c "def test_compare_workflow_returns_metrics\|def test_compare_normalizes_per_stage_blockers\|def test_compare_includes_writeback_usefulness\|def test_propose_workflow_promotion_writes_writeback_with_workflow_default_scope\|def test_propose_workflow_promotion_records_proposed_not_target_state\|def test_finalize_workflow_promotion_requires_approved_writeback\|def test_promote_active_requires_writeback" tests/test_workflow_promotion.py` returns at least 7
    - `uv run pytest tests/test_workflow_promotion.py -x -q` exits 0
    - `uv run pytest tests/test_architecture_enforcement.py -x -q` exits 0 (no services -> bin import introduced)
    - `uv run ruff check services/workflow_promotion.py tests/test_workflow_promotion.py` exits 0
    - `uv run ruff format --check services/workflow_promotion.py tests/test_workflow_promotion.py` exits 0
    - `uv run basedpyright services/workflow_promotion.py` exits 0
  </acceptance_criteria>
  <done>
    services/workflow_promotion.py exposes compare_workflow_effectiveness, propose_workflow_promotion, finalize_workflow_promotion; promotion proposals route through the Plan 01 writeback shim and record status="proposed" until approval; Pitfall 4 (writeback bypass) is enforced; Pitfall 5 (per-stage normalization) is shipped; seven behavior tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add aios asset-lifecycle / workflow-compare / promote-asset CLI subcommands; extend contracts-audit with AssetLifecycle + WorkflowComparison rows; emit asset_promotion_candidate writeback at governed closeout in bin/hook-stop.py</name>
  <files>services/aios_cli.py, bin/hook-stop.py, tests/test_aios_cli.py</files>
  <read_first>
    - services/aios_cli.py — find via `grep -n "def create_parser\|subparsers\|def cmd_\|_contracts_audit_payload\|EVALUATION_FINDING_LIFECYCLE_STATES" services/aios_cli.py | head -40` and read the existing subcommand registration pattern. Specifically read:
        - the create_parser block to see how subcommands are added
        - one full cmd_<name> function (e.g., cmd_governance_audit or cmd_contracts_audit) as a paste-of-record
        - _contracts_audit_payload to identify where AssetLifecycle and WorkflowComparison rows should be appended
    - bin/hook-stop.py — find via `grep -n "writeback\|workflow_learning_event\|closeout\|no_learning\|insert_writeback" bin/hook-stop.py | head -30` and read the closeout block where writebacks are emitted today
    - services/asset_lifecycle.py + services/workflow_promotion.py — confirm the public signatures Task 3 invokes from CLI
    - tests/test_aios_cli.py — find via `head -80 tests/test_aios_cli.py` to confirm the existing CLI fixture pattern (likely subprocess.run on a small db fixture)
  </read_first>
  <behavior>
    - Test 1 `test_asset_lifecycle_list_subcommand`: invoking `aios asset-lifecycle list --kind workflow --json` on a seeded db returns valid JSON with a list of records, each carrying {kind, key, lifecycle_state, last_updated_at}.
    - Test 2 `test_asset_lifecycle_promote_subcommand`: invoking `aios asset-lifecycle promote --kind prompt --key research --to candidate --actor op --rationale "test"` writes a promotion_lifecycle_items row and returns JSON with {lifecycle_id, from_state, to_state, requires_approval}.
    - Test 3 `test_workflow_compare_cli`: invoking `aios workflow-compare --workflow-key implementation-delivery --since 30d --json` returns JSON keyed by WorkflowEffectiveness fields (workflow_key, run_count, completed_count, mean_blocker_count, mean_blockers_per_stage, writeback_usefulness).
    - Test 4 `test_promote_asset_cli_for_workflow_uses_workflow_promotion`: invoking `aios promote-asset --kind workflow --key implementation-delivery --to approved --actor op --rationale "evidence"` delegates to propose_workflow_promotion; returns JSON with {writeback_id, lifecycle_id, requires_approval: true}.
    - Test 5 `test_contracts_audit_includes_asset_lifecycle_and_workflow_comparison`: invoking `aios contracts-audit --json` returns a payload whose contracts list contains entries with names "AssetLifecycle" and "WorkflowComparison" each with status="implemented" and at least one source-of-truth file reference.
    - Test 6 `test_hook_stop_emits_asset_promotion_candidate_when_threshold_crossed`: simulate hook-stop closeout for a run whose workflow_execution_reports.status="completed", stage_evaluations all "completed", and recommendation_source on the matching agentize_evaluations row is "recommender". After closeout, an improvement_writebacks row with policy_class containing "asset_promotion_candidate" exists. When status="failed", no such row is emitted.
  </behavior>
  <action>
    1. **Extend services/aios_cli.py.**

       - In create_parser, register three new subparsers:
         - `asset-lifecycle` with a sub-subparser for `list` and `promote`:
             - `list` accepts `--kind {prompt,skill,workflow}` (optional), `--state {draft,candidate,approved,active,deprecated}` (optional), `--json` (flag default true; respected by the existing JSON-first convention).
             - `promote` accepts `--kind`, `--key`, `--to`, `--actor`, `--rationale` (all required), `--evidence-id` (repeatable).
         - `workflow-compare` accepts `--workflow-key` (required), `--since` (string like "30d" or ISO timestamp; helper to resolve "Nd" → now - N days ISO), `--json`.
         - `promote-asset` accepts `--kind`, `--key`, `--to`, `--actor`, `--rationale`. When --kind == "workflow", delegate to propose_workflow_promotion; else delegate to promote_asset directly.

       - Add cmd_asset_lifecycle_list(args) -> dict that calls list_assets(conn, kind=args.kind, state=args.state).
       - Add cmd_asset_lifecycle_promote(args) -> dict that calls promote_asset (Plan 01).
       - Add cmd_workflow_compare(args) -> dict that resolves --since via a small helper _resolve_since_argument(arg: str) -> str, calls compare_workflow_effectiveness, and returns dataclasses.asdict(result).
       - Add cmd_promote_asset(args) -> dict that branches on args.kind: workflow → propose_workflow_promotion; prompt/skill → promote_asset.
       - Wire them into the existing main dispatcher.

       - Extend `_contracts_audit_payload()` to append two new contract rows:
         - {"name": "AssetLifecycle", "status": "implemented", "source_of_truth": ["services/asset_lifecycle.py", "config/workflows/registry.json", "prompts/registry.json", "config/workflows/skills.json", "promotion_lifecycle_items"], "notes": "Five-state lifecycle across prompts/skills/workflows (Phase 8)."}
         - {"name": "WorkflowComparison", "status": "implemented", "source_of_truth": ["services/workflow_promotion.py", "workflow_execution_reports", "success_criteria_stage_findings", "improvement_writebacks"], "notes": "Stage- and run-level effectiveness comparison (Phase 8 WFLO-04)."}

    2. **Extend bin/hook-stop.py** with a small block after the existing closeout writeback emission:
       - After the run's writeback / follow-up / no-learning evidence has been decided, query the most recent workflow_execution_reports row for the current run_id. If status=="completed" AND report_json.stage_evaluations[] is non-empty AND none have outcome="blocked":
         - For each of the run's prompts_used.classification entries (or its agentize_evaluations.selected_skills_json entries), check if recommendation_source=="recommender" on the agentize_evaluations row.
         - If yes, INSERT one advisory improvement_writebacks row with layer_type="workflow", impact_scope="workflow-default", policy_class="asset_promotion_candidate", status="proposed", proposed_change_json=json.dumps({"workflow_key": <workflow_key>, "candidate_to_state": "approved", "evidence_summary": <small summary built from the report>}), rationale=f"Run {run_id} completed with all stages clean using recommender-driven assets — candidate for promotion review.", actor="hook-stop". Use the Phase 5 INSERT pattern via the bin-side helper (this is bin/, so it CAN import from bin/aios_orchestration_runtime.insert_writeback).
       - If status != "completed" OR there are blocker stage findings, do not emit the advisory.
       - Wrap the entire block in try/except so closeout never fails due to advisory emission errors; log to stderr.

    3. **Extend tests/test_aios_cli.py** with the five CLI-flavored tests (Tests 1–5). Test 6 belongs to a test file for hook-stop (`tests/test_hook_stop.py` likely already exists — extend it). Confirm via `ls tests/test_hook_stop.py 2>/dev/null` during read_first; if it does not exist, add the hook-stop test to tests/test_aios_cli.py with a clear separator comment.

    4. **Quality ladder per touched file:** `uv run ruff check services/aios_cli.py bin/hook-stop.py tests/test_aios_cli.py && uv run ruff format --check services/aios_cli.py bin/hook-stop.py tests/test_aios_cli.py && uv run basedpyright services/aios_cli.py bin/hook-stop.py`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_aios_cli.py -x -q -k "asset_lifecycle or workflow_compare or promote_asset or contracts_audit_includes_asset_lifecycle or hook_stop_emits_asset_promotion_candidate"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "asset-lifecycle\|workflow-compare\|promote-asset" services/aios_cli.py | wc -l | tr -d ' '` returns at least 6 (parser registration + dispatcher branches for the three subcommands)
    - `grep -n "def cmd_asset_lifecycle_list\|def cmd_asset_lifecycle_promote\|def cmd_workflow_compare\|def cmd_promote_asset" services/aios_cli.py | wc -l | tr -d ' '` returns at least 4
    - `grep -n "AssetLifecycle\|WorkflowComparison" services/aios_cli.py | wc -l | tr -d ' '` returns at least 2
    - `grep -n "asset_promotion_candidate" bin/hook-stop.py | wc -l | tr -d ' '` returns at least 1
    - `grep -c "def test_asset_lifecycle_list_subcommand\|def test_asset_lifecycle_promote_subcommand\|def test_workflow_compare_cli\|def test_promote_asset_cli_for_workflow_uses_workflow_promotion\|def test_contracts_audit_includes_asset_lifecycle_and_workflow_comparison\|def test_hook_stop_emits_asset_promotion_candidate_when_threshold_crossed" tests/test_aios_cli.py tests/test_hook_stop.py 2>/dev/null | awk -F: '{s+=$2} END {print s}'` returns at least 6
    - `uv run pytest tests/test_aios_cli.py -x -q` exits 0
    - `uv run pytest tests/test_workflow_promotion.py tests/test_asset_lifecycle.py tests/test_architecture_enforcement.py -x -q` exits 0
    - `uv run ruff check services/aios_cli.py bin/hook-stop.py tests/test_aios_cli.py` exits 0
    - `uv run ruff format --check services/aios_cli.py bin/hook-stop.py tests/test_aios_cli.py` exits 0
    - `uv run basedpyright services/aios_cli.py bin/hook-stop.py` exits 0
  </acceptance_criteria>
  <done>
    aios CLI exposes asset-lifecycle, workflow-compare, promote-asset subcommands with JSON output; contracts-audit now lists AssetLifecycle and WorkflowComparison contracts; bin/hook-stop.py emits asset_promotion_candidate advisory writebacks at clean closeout when recommender-driven; six behavior tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compare_workflow_effectiveness -> evidence tables | Read-only; no recommendation poisoning vector beyond what Plan 03 already covers. |
| propose_workflow_promotion -> writeback approval | Promotion proposals MUST hit writeback_approval_policy_shim before any lifecycle write. |
| hook-stop closeout -> advisory writebacks | hook-stop is bin/-side and CAN use bin/aios_orchestration_runtime.insert_writeback directly; the advisory writeback is always status="proposed", never auto-approved. |
| CLI promote-asset -> propose_workflow_promotion vs promote_asset | Branching on kind preserves the Phase 5 governance discipline for workflows (proposal-then-approval) while prompts/skills can promote through the simpler promote_asset path (writeback shim still fires for to_state="active"). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-17 | Tampering | Workflow promotion bypassing approval (Pitfall 4) | mitigate | propose_workflow_promotion writes writeback BEFORE lifecycle row; finalize_workflow_promotion rejects unless writeback.status="approved"; Tests 4–7 in Task 2 enforce. |
| T-08-18 | Integrity | Workflow comparison skewed by stage count (Pitfall 5) | mitigate | WorkflowEffectiveness exposes both raw mean_blocker_count and mean_blockers_per_stage; Test 2 in Task 2 enforces. |
| T-08-19 | Repudiation | CLI promotion without rationale | mitigate | --rationale is a required argparse argument for both asset-lifecycle promote and promote-asset; argparse error blocks the call. |
| T-08-20 | Tampering | hook-stop advisory writeback accidentally writing approved status | mitigate | Advisory writebacks are hard-coded status="proposed"; Test 6 in Task 3 verifies. |
| T-08-21 | Information Disclosure | workflow_compare output exposing run-level run_ids to non-operator surfaces | accept | CLI is operator-only by design; no external network surface. |
| T-08-22 | Integrity | finalize without approval check | mitigate | finalize_workflow_promotion reads writeback.status and raises ValueError when not "approved"; Test 6 in Task 2 enforces. |
</threat_model>

<verification>
- `uv run pytest tests/test_workflow_orchestration.py tests/test_workflow_promotion.py tests/test_aios_cli.py tests/test_asset_lifecycle.py tests/test_asset_recommendation.py tests/test_agentize.py tests/test_task_routing.py tests/test_architecture_enforcement.py -x -q` exits 0
- `uv run ruff check services/workflow_promotion.py services/workflow_orchestration.py services/aios_cli.py bin/hook-stop.py tests/test_workflow_promotion.py tests/test_workflow_orchestration.py tests/test_aios_cli.py` exits 0
- `uv run ruff format --check services/workflow_promotion.py services/workflow_orchestration.py services/aios_cli.py bin/hook-stop.py tests/test_workflow_promotion.py tests/test_workflow_orchestration.py tests/test_aios_cli.py` exits 0
- `uv run basedpyright services/workflow_promotion.py services/workflow_orchestration.py services/aios_cli.py bin/hook-stop.py` exits 0
- `python -c "from services.workflow_promotion import compare_workflow_effectiveness, propose_workflow_promotion, finalize_workflow_promotion, WorkflowEffectiveness"` exits 0
- Quality ladder per CLAUDE.md: hard gates green.
</verification>

<success_criteria>
- WFLO-03: stage_evaluations[] in workflow_execution_reports.report_json provides durable per-stage rollup; the report's existing top-level status provides the run-level outcome.
- WFLO-04: compare_workflow_effectiveness returns the metrics needed for promotion decisions; propose_workflow_promotion + finalize_workflow_promotion route every workflow lifecycle change through Phase 5 writeback approval with status="proposed" until approved; the Pitfall 5 per-stage normalization is shipped.
- CLI parity: aios asset-lifecycle, aios workflow-compare, aios promote-asset, aios contracts-audit all support the headless operator path.
- Governed closeout enrichment: bin/hook-stop.py advises promotion when a recommender-driven run completed cleanly, without ever auto-promoting.
</success_criteria>

<output>
After completion, create `.planning/phases/08-prompt-skill-workflow-contracts-and-asset-lifecycle/08-04-SUMMARY.md` per the template, including:
- Files created and modified with line counts
- The exact WorkflowEffectiveness field set and how each is computed
- The Pitfall 4 enforcement chain (propose → status="proposed" → approval → finalize → status=target)
- The Pitfall 5 normalization (mean_blocker_count + mean_blockers_per_stage both exposed)
- The exact JSON shape of `aios workflow-compare --json` output and `aios contracts-audit --json` extension
- The asset_promotion_candidate writeback shape emitted by hook-stop and the conditions under which it fires
- Note that Plan 05 will register the four planned workflow contracts (audit-only / audit-and-implement / standards-backfill / security-review) as lifecycle_state="candidate" entries and flip bin/validate-prompts.py REQUIRED_FIELDS to require lifecycle_state once every existing prompt has been backfilled (Pitfall 7 ordering)
</output>
