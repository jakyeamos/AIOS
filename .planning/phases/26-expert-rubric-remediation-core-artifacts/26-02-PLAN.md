---
phase: 26-expert-rubric-remediation-core-artifacts
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - services/expert_rubric_remediation.py
  - tests/test_expert_rubric_remediation.py
  - tests/fixtures/expert-rubric-remediation/soundscape-visual-polish-evidence.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "Soundscape-inspired evidence items become evidence-backed audit findings."
    - "Audit findings become ordered remediation slices with source finding IDs and verification expectations."
    - "A selected remediation slice becomes an implementation handoff that requires user approval."
  artifacts:
    - path: tests/fixtures/expert-rubric-remediation/soundscape-visual-polish-evidence.json
      provides: "Deterministic evidence fixture for visual-polish audit behavior"
    - path: services/expert_rubric_remediation.py
      provides: "build_audit_report and build_implementation_handoff public functions"
    - path: tests/test_expert_rubric_remediation.py
      provides: "Fixture-to-audit-to-remediation-to-handoff regression coverage"
  key_links:
    - from: tests/fixtures/expert-rubric-remediation/soundscape-visual-polish-evidence.json
      to: build_audit_report
      via: "fixture JSON loaded as evidence_items"
    - from: build_audit_report
      to: build_remediation_plan
      via: "audit_report findings with evidence and recommended_fix"
    - from: build_remediation_plan
      to: build_implementation_handoff
      via: "selected_slice_id references generated remediation slice id"
---

<objective>
Add evidence fixture builders and approval-gated implementation handoff construction.

Purpose: Phase 27 and Phase 28 need audit/remediation/handoff artifacts that can be generated from concrete evidence without runtime ambiguity.
Output: Soundscape visual-polish fixture plus service functions for audit reports and handoffs.
</objective>

<execution_context>
@/Users/jakyeamos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakyeamos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-expert-rubric-remediation-core-artifacts/26-RESEARCH.md
@.planning/phases/26-expert-rubric-remediation-core-artifacts/26-VALIDATION.md
@.planning/phases/26-expert-rubric-remediation-core-artifacts/26-01-SUMMARY.md
@docs/superpowers/specs/2026-06-24-expert-rubric-remediation-design.md
@docs/superpowers/plans/2026-06-24-expert-rubric-remediation.md

<interfaces>
Extend `services/expert_rubric_remediation.py` with:
- `build_audit_report(*, rubric: dict[str, Any], evidence_items: list[dict[str, Any]], run_id: str) -> dict[str, Any]`
- `build_implementation_handoff(*, remediation_plan: dict[str, Any], run_id: str, selected_slice_id: str | None) -> dict[str, Any]`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Soundscape evidence fixture and failing builder test</name>
  <files>tests/fixtures/expert-rubric-remediation/soundscape-visual-polish-evidence.json, tests/test_expert_rubric_remediation.py</files>
  <read_first>
    - tests/test_expert_rubric_remediation.py
    - docs/superpowers/plans/2026-06-24-expert-rubric-remediation.md
    - docs/superpowers/specs/2026-06-24-expert-rubric-remediation-design.md
  </read_first>
  <behavior>
    - The fixture contains four evidence items for `data_realism`, `product_evidence`, `surface_hierarchy`, and `interaction_architecture`.
    - The test loads the fixture, builds an audit, builds remediation, builds a handoff for `slice-1`, and asserts evidence/verification/approval fields.
  </behavior>
  <action>Create `tests/fixtures/expert-rubric-remediation/soundscape-visual-polish-evidence.json` with the four evidence rows from the approved implementation plan Task 2. Extend `tests/test_expert_rubric_remediation.py` to import `build_audit_report` and `build_implementation_handoff`, add `ROOT = Path(__file__).resolve().parents[1]`, and add `test_soundscape_fixture_builds_evidence_backed_audit_and_plan`. Run only that new test before implementing the functions and confirm it fails because the builder functions are missing.</action>
  <verify>
    <automated>uv run pytest tests/test_expert_rubric_remediation.py::test_soundscape_fixture_builds_evidence_backed_audit_and_plan -q</automated>
  </verify>
  <done>The fixture and failing test exist, and the failure identifies missing builder imports/functions rather than fixture path errors.</done>
  <acceptance_criteria>
    - Fixture JSON includes evidence reference `packages/web/src/components/feed/FeedItem.tsx:427`.
    - The test asserts `audit["findings"][0]["evidence"] == ["packages/web/src/components/feed/FeedItem.tsx:427"]`.
    - The test asserts `handoff["requires_user_approval"] is True`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement audit report and handoff builders</name>
  <files>services/expert_rubric_remediation.py, tests/test_expert_rubric_remediation.py, tests/fixtures/expert-rubric-remediation/soundscape-visual-polish-evidence.json</files>
  <read_first>
    - services/expert_rubric_remediation.py
    - tests/test_expert_rubric_remediation.py
    - tests/fixtures/expert-rubric-remediation/soundscape-visual-polish-evidence.json
  </read_first>
  <behavior>
    - `build_audit_report` maps evidence items to rubric dimensions, preserves evidence references, and assigns lower scores to blockers than warnings.
    - `build_remediation_plan` output includes verification and source finding IDs for every generated slice.
    - `build_implementation_handoff` returns schema `aios-expert-implementation-handoff-v0.1`, `requires_user_approval: True`, `follow_up_workflow: implementation-delivery`, and the selected slice when found.
  </behavior>
  <action>Add `build_audit_report` and `build_implementation_handoff` to `services/expert_rubric_remediation.py`. `build_audit_report` must accept explicit `evidence_items`, preserve evidence strings, coerce unknown dimension IDs to an existing rubric dimension, create `scores` for every rubric dimension, and add low-confidence gaps for dimensions without evidence. Update `build_remediation_plan` if needed so each slice includes `source_findings`. `build_implementation_handoff` must only produce a handoff artifact and must not call implementation workflows or mutate code.</action>
  <verify>
    <automated>uv run pytest tests/test_expert_rubric_remediation.py -q</automated>
    <automated>uv run ruff check services/expert_rubric_remediation.py tests/test_expert_rubric_remediation.py</automated>
    <automated>uv run ruff format --check services/expert_rubric_remediation.py tests/test_expert_rubric_remediation.py</automated>
  </verify>
  <done>All expert rubric remediation service tests pass and the fixture drives an evidence-backed audit, remediation plan, and approval-gated handoff.</done>
  <acceptance_criteria>
    - `validate_audit_report(build_audit_report(...))["passed"] is True` for the Soundscape fixture.
    - `validate_remediation_plan(build_remediation_plan(...))["passed"] is True`.
    - The handoff has `selected_slice_id == "slice-1"` and `artifact_inputs` listing `expertise-packet.json`, `rubric.json`, `audit-report.json`, and `remediation-plan.json`.
    - No workflow registry, workflow runtime, or CLI files are changed in this plan.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture/evidence JSON -> audit report | Evidence item shape is user-controllable and must not create unsupported findings. |
| remediation plan -> implementation handoff | Handoff crosses from review/planning into potential implementation workflow selection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Tampering | `build_audit_report` | mitigate | Preserve evidence references exactly as strings and rely on `validate_audit_report` to reject empty evidence findings. |
| T-26-05 | Elevation of Privilege | `build_implementation_handoff` | mitigate | Always set `requires_user_approval` to true and do not execute `implementation-delivery`. |
| T-26-06 | Repudiation | remediation slices | mitigate | Include `source_findings` on each slice so follow-up implementation can trace work back to audit findings. |
</threat_model>

<verification>
Run:
- `uv run pytest tests/test_expert_rubric_remediation.py -q`
- `uv run ruff check services/expert_rubric_remediation.py tests/test_expert_rubric_remediation.py`
- `uv run ruff format --check services/expert_rubric_remediation.py tests/test_expert_rubric_remediation.py`
</verification>

<success_criteria>
- Soundscape fixture evidence becomes an audit report with evidence-backed findings.
- Audit findings become remediation slices with verification and source finding traceability.
- Implementation handoff is produced as an approval-gated artifact only.
- Phase 26 remains independent from workflow runtime and CLI integration.
</success_criteria>

<output>
After completion, create `.planning/phases/26-expert-rubric-remediation-core-artifacts/26-02-SUMMARY.md`.
</output>
