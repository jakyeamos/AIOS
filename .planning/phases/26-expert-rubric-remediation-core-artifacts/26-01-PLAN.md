---
phase: 26-expert-rubric-remediation-core-artifacts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/expert_rubric_remediation.py
  - tests/test_expert_rubric_remediation.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "A TMCP-shaped packet can be converted into a rubric with 5 or more evidence-oriented dimensions."
    - "Invalid audit findings without evidence and invalid remediation slices without verification fail validation."
    - "Review artifacts are written as stable JSON and markdown filenames under the requested output directory."
  artifacts:
    - path: services/expert_rubric_remediation.py
      provides: "Schema constants, rubric synthesis, validation functions, renderers, and artifact writer"
    - path: tests/test_expert_rubric_remediation.py
      provides: "Focused pytest coverage for service contracts and artifact writing"
  key_links:
    - from: tests/test_expert_rubric_remediation.py
      to: services/expert_rubric_remediation.py
      via: "direct imports of RUBRIC_SCHEMA, AUDIT_REPORT_SCHEMA, REMEDIATION_PLAN_SCHEMA, synthesize_rubric, validators, build_remediation_plan, and write_review_artifacts"
    - from: write_review_artifacts
      to: target output directory
      via: "stable filenames expertise-packet.json, rubric.json, rubric.md, audit-report.json, audit-report.md, remediation-plan.json, remediation-plan.md"
---

<objective>
Create the core expert rubric remediation service contracts and artifact writer.

Purpose: Phase 27 needs a deterministic service surface before the workflow runtime can dispatch expert review skills.
Output: `services/expert_rubric_remediation.py` plus focused service tests.
</objective>

<execution_context>
@/Users/jakyeamos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jakyeamos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-expert-rubric-remediation-core-artifacts/26-RESEARCH.md
@.planning/phases/26-expert-rubric-remediation-core-artifacts/26-VALIDATION.md
@docs/superpowers/specs/2026-06-24-expert-rubric-remediation-design.md
@docs/superpowers/plans/2026-06-24-expert-rubric-remediation.md

<interfaces>
Create these public constants and functions in `services/expert_rubric_remediation.py`:
- `RUBRIC_SCHEMA = "aios-expert-rubric-v0.1"`
- `AUDIT_REPORT_SCHEMA = "aios-expert-audit-report-v0.1"`
- `REMEDIATION_PLAN_SCHEMA = "aios-expert-remediation-plan-v0.1"`
- `IMPLEMENTATION_HANDOFF_SCHEMA = "aios-expert-implementation-handoff-v0.1"`
- `synthesize_rubric(*, packet: dict[str, Any], run_id: str, objective: str) -> dict[str, Any]`
- `validate_rubric(rubric: dict[str, Any]) -> dict[str, Any]`
- `validate_audit_report(report: dict[str, Any]) -> dict[str, Any]`
- `build_remediation_plan(*, audit_report: dict[str, Any], run_id: str) -> dict[str, Any]`
- `validate_remediation_plan(plan: dict[str, Any]) -> dict[str, Any]`
- `write_review_artifacts(...) -> dict[str, Path]`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock service validation and writer contracts with failing tests</name>
  <files>tests/test_expert_rubric_remediation.py</files>
  <read_first>
    - docs/superpowers/plans/2026-06-24-expert-rubric-remediation.md
    - docs/superpowers/specs/2026-06-24-expert-rubric-remediation-design.md
    - tests/test_workflow_orchestration.py
  </read_first>
  <behavior>
    - `synthesize_rubric` returns schema `aios-expert-rubric-v0.1`, preserves `run_id`, creates at least 5 dimensions, and includes source nodes.
    - `validate_audit_report` rejects a finding whose `evidence` list is empty.
    - `validate_remediation_plan` rejects a slice whose `verification` list is empty.
    - `write_review_artifacts` creates the expected JSON and markdown files under `tmp_path`.
  </behavior>
  <action>Create `tests/test_expert_rubric_remediation.py` from the approved implementation plan Task 1. Include `_visual_packet()`, import the schema constants and service functions listed in this plan, and assert the exact schema strings and stable artifact filenames. Run the test before creating the service module and confirm it fails with `ModuleNotFoundError: No module named 'services.expert_rubric_remediation'`.</action>
  <verify>
    <automated>uv run pytest tests/test_expert_rubric_remediation.py -q</automated>
  </verify>
  <done>The test file exists, imports the planned service API, and fails before service creation for the missing module only.</done>
  <acceptance_criteria>
    - `tests/test_expert_rubric_remediation.py` contains `_visual_packet`.
    - The failing test output names `services.expert_rubric_remediation` as missing.
    - The tests assert `rubric_json`, `rubric_markdown`, `audit_report_json`, and `remediation_plan_markdown` artifact paths.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic rubric, validation, rendering, and artifact writing</name>
  <files>services/expert_rubric_remediation.py, tests/test_expert_rubric_remediation.py</files>
  <read_first>
    - tests/test_expert_rubric_remediation.py
    - docs/superpowers/specs/2026-06-24-expert-rubric-remediation-design.md
    - .planning/codebase/CONVENTIONS.md
  </read_first>
  <behavior>
    - Visual-polish packets produce `surface_hierarchy`, `interaction_architecture`, `data_realism`, `product_evidence`, and `design_system_fit` dimensions.
    - Validation functions return dictionaries with `validation_key`, `passed`, and `issues`.
    - Artifact writer serializes JSON with stable indentation and writes markdown renderings.
  </behavior>
  <action>Create `services/expert_rubric_remediation.py` with `from __future__ import annotations`, stdlib-only imports `json`, `Path`, and `Any`, the schema constants listed above, profile helpers for `visual_polish`, `security_privacy`, `developer_experience`, and `general_review`, validators for rubric/audit/remediation contracts, markdown renderers, and `write_review_artifacts`. Keep all functions deterministic: derive dimensions from packet `task_id` and `selected_nodes`, write only inside the supplied `output_dir`, and do not call TMCP, subprocesses, SQLite, or network APIs.</action>
  <verify>
    <automated>uv run pytest tests/test_expert_rubric_remediation.py -q</automated>
    <automated>uv run ruff check services/expert_rubric_remediation.py tests/test_expert_rubric_remediation.py</automated>
    <automated>uv run ruff format --check services/expert_rubric_remediation.py tests/test_expert_rubric_remediation.py</automated>
  </verify>
  <done>The service tests pass, the new service module contains all public functions, and targeted Ruff checks pass for the touched files.</done>
  <acceptance_criteria>
    - `services/expert_rubric_remediation.py` exports all constants and functions named in `<interfaces>`.
    - `validate_audit_report` returns `passed: false` and issue text containing `finding-1 is missing evidence` for empty finding evidence.
    - `validate_remediation_plan` returns `passed: false` and issue text containing `slice-1 is missing verification` for empty slice verification.
    - `write_review_artifacts` writes `expertise-packet.json`, `rubric.json`, `rubric.md`, `audit-report.json`, `audit-report.md`, `remediation-plan.json`, and `remediation-plan.md`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence input -> validation functions | User-provided evidence data becomes audit/remediation artifacts. |
| output_dir -> filesystem | Caller-selected output directory receives JSON and markdown artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | `validate_audit_report` | mitigate | Reject findings with empty `evidence` so unsupported claims cannot pass as expert audit output. |
| T-26-02 | Information Disclosure | `write_review_artifacts` | accept | Phase 26 writes only caller-provided packet/evidence data to caller-provided local paths; no secrets are read or discovered by this service. |
| T-26-03 | Repudiation | validation result dictionaries | mitigate | Return explicit `validation_key`, `passed`, and `issues` fields for all validators so Phase 27 can preserve stage validation evidence. |
</threat_model>

<verification>
Run:
- `uv run pytest tests/test_expert_rubric_remediation.py -q`
- `uv run ruff check services/expert_rubric_remediation.py tests/test_expert_rubric_remediation.py`
- `uv run ruff format --check services/expert_rubric_remediation.py tests/test_expert_rubric_remediation.py`
</verification>

<success_criteria>
- The expert review service exists and is deterministic.
- Rubric, audit, and remediation validation failure cases are covered by tests.
- Artifact writer creates stable local review artifacts.
- Phase 26 has not changed workflow registry, workflow runtime, or CLI behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/26-expert-rubric-remediation-core-artifacts/26-01-SUMMARY.md`.
</output>
