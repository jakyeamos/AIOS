---
phase: 24-rectify-linked-repo-aios-readiness-blockers-except-agent-router
plan: "02"
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - config/quality-pipeline.json
  - docs/audits/linked-repo-adoption-readiness-audit.md
  - PROJECT.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "soundscape-app has fresh recorded local evidence for every configured strict gate."
    - "AIOS has fresh recorded local evidence for every platform-control-plane strict gate."
    - "Neither repo is marked ready without CI/default-branch proof."
  artifacts:
    - path: quality_pipeline_runs
      provides: "Fresh pass/fail evidence rows for soundscape-app and AIOS"
    - path: docs/audits/linked-repo-adoption-readiness-audit.md
      provides: "Evidence IDs and any remaining CI/default proof blockers"
  key_links:
    - from: scripts/linked-repo-quality-runner.py
      to: soundscape-app gates
      via: "runs configured commands from `config/quality-pipeline.json`"
    - from: scripts/linked-repo-quality-runner.py
      to: AIOS gates
      via: "runs configured platform-control-plane commands"
---

<objective>
Close the evidence-only blockers for `soundscape-app` and AIOS without mixing in broader repo maturation.

Purpose: These two repos already have substantial gate configuration and should prove the evidence path early.
Output: Fresh local evidence rows, CI/default proof status, and updated audit notes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-rectify-linked-repo-aios-readiness-blockers-except-agent-router/24-RESEARCH.md
@.planning/phases/24-rectify-linked-repo-aios-readiness-blockers-except-agent-router/24-VALIDATION.md
@config/quality-pipeline.json
@docs/audits/linked-repo-adoption-readiness-audit.md
@/Users/jakyeamos/projects/soundscape-app/package.json
@/Users/jakyeamos/AIOS/pyproject.toml
@/Users/jakyeamos/AIOS/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record soundscape-app strict gate evidence</name>
  <files>quality_pipeline_runs, docs/audits/linked-repo-adoption-readiness-audit.md</files>
  <read_first>config/quality-pipeline.json, /Users/jakyeamos/projects/soundscape-app/package.json, /Users/jakyeamos/projects/soundscape-app/.github/workflows/pr-checks.yml</read_first>
  <action>Run `scripts/linked-repo-quality-runner.py` for every configured `soundscape-app` gate that has a local command. Record pass/fail evidence with `source=phase24-local`. For workflow-only gates such as `.github/workflows/...`, record evidence only after inspecting the workflow file and default-branch status source; if default-branch status cannot be verified locally, keep the repo at `evidence_required` and record the missing proof in the audit.</action>
  <verify>
    <automated>python3 scripts/linked-repo-quality-runner.py --project soundscape-app --report</automated>
  </verify>
  <acceptance_criteria>
    - Report lists latest evidence IDs for local gates that were executed.
    - Any failed gate remains a blocker with command and failure summary.
    - No ready verdict is emitted without CI/default proof.
  </acceptance_criteria>
  <done>soundscape-app has fresh local evidence and an explicit remaining proof state.</done>
</task>

<task type="auto">
  <name>Task 2: Record AIOS platform gate evidence</name>
  <files>quality_pipeline_runs, PROJECT.md, docs/audits/linked-repo-adoption-readiness-audit.md</files>
  <read_first>config/quality-pipeline.json, pyproject.toml, package.json, aios-ui/package.json, PROJECT.md</read_first>
  <action>Run the configured AIOS platform-control-plane gates through the Phase 24 evidence runner where possible. Required commands include `uv run pytest -q`, `uv run pytest -q tests/test_commit_quality_ladder.py`, `pnpm context:validate`, and the configured architecture/pre-PR gates where available. Record failed or blocked commands as evidence instead of replacing them with prose.</action>
  <verify>
    <automated>uv run pytest -q tests/test_quality_gates.py tests/test_commit_quality_ladder.py tests/test_quality_pipeline.py && pnpm context:validate</automated>
  </verify>
  <acceptance_criteria>
    - AIOS evidence rows include command, source, completed_at, and metadata.
    - PROJECT.md records only factual readiness state.
    - Dirty tree notes remain separate from readiness verdict.
  </acceptance_criteria>
  <done>AIOS has fresh local evidence and a factual remaining proof state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm evidence proof status</name>
  <files>docs/audits/linked-repo-adoption-readiness-audit.md, config/quality-pipeline.json</files>
  <action>Pause after local evidence is recorded so the user can confirm whether remote/default-branch CI proof is available for `soundscape-app` and AIOS, or whether both repos should remain `evidence_required` until Plan 24-09.</action>
  <verify>Human response is required before continuing.</verify>
  <done>User either approves the recorded state or supplies missing CI/default proof source details.</done>
  <what-built>Local evidence for soundscape-app and AIOS has been recorded or failures have been captured.</what-built>
  <how-to-verify>
    1. Review the Phase 24 readiness report for `soundscape-app` and AIOS.
    2. Confirm whether remote/default-branch CI proof is available for either repo.
    3. If CI proof is unavailable, approve keeping the repo at `evidence_required` until Plan 24-09.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the missing CI/default proof source.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Local command output -> AIOS DB | Command output is untrusted until exit code and evidence summary are recorded. |
| Remote CI proof -> readiness verdict | Remote status may be unavailable or stale. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-24-03 | Repudiation | Evidence rows | mitigate | Record pass/fail rows for every executed gate. |
| T-24-04 | Spoofing | CI/default proof | mitigate | Do not mark ready from workflow-file presence alone; require status evidence or Plan 24-09 exception. |
</threat_model>

<verification>
- `python3 scripts/linked-repo-quality-runner.py --project soundscape-app --report`
- `python3 scripts/linked-repo-quality-runner.py --project aios --report`
- `uv run pytest -q tests/test_quality_gates.py tests/test_commit_quality_ladder.py tests/test_quality_pipeline.py`
- `pnpm context:validate`
</verification>

<success_criteria>
- `soundscape-app` and AIOS have fresh local evidence rows.
- Any remaining blocker is CI/default proof, a failed command, or dirty-tree closeout hygiene recorded separately.
- Neither repo is marked ready without proof.
</success_criteria>

<output>
After completion, create `.planning/phases/24-rectify-linked-repo-aios-readiness-blockers-except-agent-router/24-02-SUMMARY.md`
</output>
