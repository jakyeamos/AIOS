---
phase: 24-rectify-linked-repo-aios-readiness-blockers-except-agent-router
plan: "09"
type: execute
wave: 9
depends_on: ["24-08"]
files_modified:
  - config/quality-pipeline.json
  - docs/audits/linked-repo-adoption-readiness-audit.md
  - PROJECT.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "Every Phase 24 in-scope repo has CI/default-branch proof or an explicit non-remote exception."
    - "Exceptions include owner, reason, review date, local proof command, and replacement path."
    - "Readiness verdicts are derived from evidence rows plus CI/exception proof."
  artifacts:
    - path: config/quality-pipeline.json
      provides: "CI/default proof or non-remote exceptions"
    - path: docs/audits/linked-repo-adoption-readiness-audit.md
      provides: "Portfolio CI/default proof index"
  key_links:
    - from: CI/default proof
      to: readiness verdict
      via: "ready verdict requires remote pass or non_remote_ci_exception"
    - from: non_remote_ci_exception
      to: local proof command
      via: "exception names the replacement command and review date"
---

<objective>
Close the portfolio-wide CI/default-branch proof and non-remote exception blocker after repo-local gates have been remediated.

Purpose: Local gate evidence is insufficient without remote/default proof or a documented exception.
Output: CI proof records, explicit exceptions, and updated readiness verdicts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-rectify-linked-repo-aios-readiness-blockers-except-agent-router/24-RESEARCH.md
@docs/quality/linked-repo-strict-release-readiness.md
@config/quality-pipeline.json
@docs/audits/linked-repo-adoption-readiness-audit.md
@.planning/phases/23-mature-linked-repositories-to-aios-strict-release-readiness/23-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collect CI/default proof for repos with workflows</name>
  <files>config/quality-pipeline.json, docs/audits/linked-repo-adoption-readiness-audit.md</files>
  <read_first>config/quality-pipeline.json, docs/audits/linked-repo-adoption-readiness-audit.md, docs/quality/linked-repo-strict-release-readiness.md</read_first>
  <action>For each Phase 24 in-scope repo with `.github/workflows`, record default-branch CI proof status in AIOS truth/config/audit using workflow path, status source, observed date, and evidence URL or local artifact path. If remote status cannot be queried, leave the repo not ready and record `ci_default_proof_missing` as the blocker.</action>
  <verify>
    <automated>python3 scripts/linked-repo-quality-runner.py --report</automated>
  </verify>
  <acceptance_criteria>
    - Workflow-file presence alone is not treated as default-branch pass proof.
    - Each CI proof entry has workflow path, source, observed date, and status.
    - Missing CI/default status remains a blocker.
  </acceptance_criteria>
  <done>Repos with workflows have CI/default proof status recorded.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Decide non-remote CI exceptions</name>
  <files>config/quality-pipeline.json, docs/audits/linked-repo-adoption-readiness-audit.md</files>
  <action>Pause for a user decision on whether any repos without remote CI should receive strict non-remote exceptions. Require exact repo list and exception fields before Task 3 records anything.</action>
  <verify>Human response is required before continuing.</verify>
  <done>User selects `approve-specific` with complete exception fields or `no-exceptions`.</done>
  <decision>Approve non-remote CI exceptions for repos where remote CI is inappropriate or unavailable.</decision>
  <context>Strict readiness requires remote CI/default proof unless an exception is recorded with owner, reason, review date, local proof command, and replacement path.</context>
  <options>
    <option id="approve-specific">
      <name>Approve specific exceptions</name>
      <pros>Allows local/content/course repos to become ready with durable local proof.</pros>
      <cons>Requires explicit owner/review dates and future replacement path.</cons>
    </option>
    <option id="no-exceptions">
      <name>No exceptions</name>
      <pros>Keeps readiness strict and uniform.</pros>
      <cons>Repos without remote CI remain blocked.</cons>
    </option>
  </options>
  <resume-signal>Select `approve-specific` with repo list and exception fields, or `no-exceptions`.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record exceptions and recompute readiness</name>
  <files>config/quality-pipeline.json, docs/audits/linked-repo-adoption-readiness-audit.md, PROJECT.md</files>
  <read_first>config/quality-pipeline.json, docs/quality/linked-repo-strict-release-readiness.md, docs/audits/linked-repo-adoption-readiness-audit.md, PROJECT.md</read_first>
  <action>For approved exceptions, add `non_remote_ci_exception` objects with `owner`, `reason`, `review_date`, `local_proof_command`, and `replacement_path`. Recompute Phase 24 readiness using the service from Plan 24-01. Set `strict_readiness_status` to `ready` only for repos with complete gate evidence and CI/default proof or approved exception; otherwise keep `blocked` or `evidence_required` with exact blockers.</action>
  <verify>
    <automated>uv run pytest -q tests/test_linked_repo_readiness.py tests/test_quality_pipeline.py && python3 scripts/linked-repo-quality-runner.py --report</automated>
  </verify>
  <acceptance_criteria>
    - Every exception has all required fields.
    - No repo is ready with missing local gate evidence.
    - `agent-router` remains excluded from Phase 24 readiness counts.
  </acceptance_criteria>
  <done>CI/default proof and exceptions are reflected in readiness verdicts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Remote CI source -> AIOS truth | Remote status can be stale, missing, or unauthenticated. |
| Human exception -> readiness | Exception approval can weaken release standards if under-specified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-24-16 | Spoofing | CI proof | mitigate | Require source, observed date, workflow path, and status rather than workflow presence. |
| T-24-17 | Elevation of privilege | non-remote exception | mitigate | Require owner, review date, local proof command, and replacement path. |
</threat_model>

<verification>
- `python3 scripts/linked-repo-quality-runner.py --report`
- `uv run pytest -q tests/test_linked_repo_readiness.py tests/test_quality_pipeline.py`
- `python3 -c 'import json; json.load(open("config/quality-pipeline.json")); print("json ok")'`
</verification>

<success_criteria>
- Every in-scope repo has CI/default proof, exception, or explicit blocker.
- Readiness verdicts are recomputed from evidence.
- `agent-router` remains excluded.
</success_criteria>

<output>
After completion, create `.planning/phases/24-rectify-linked-repo-aios-readiness-blockers-except-agent-router/24-09-SUMMARY.md`
</output>
