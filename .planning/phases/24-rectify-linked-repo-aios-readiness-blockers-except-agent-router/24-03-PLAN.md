---
phase: 24-rectify-linked-repo-aios-readiness-blockers-except-agent-router
plan: "03"
type: execute
wave: 3
depends_on: ["24-02"]
files_modified:
  - /Users/jakyeamos/projects/portfolio/package.json
  - /Users/jakyeamos/projects/portfolio/.github/workflows/aios-quality.yml
  - /Users/jakyeamos/projects/dispatches-from-cyberspace/package.json
  - /Users/jakyeamos/projects/dispatches-from-cyberspace/.github/workflows/release.yml
  - /Users/jakyeamos/projects/Bballedu/package.json
  - /Users/jakyeamos/projects/tm/package.json
  - config/quality-pipeline.json
  - config/quality-gates.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "portfolio, dispatches-from-cyberspace, Bballedu, and tm have real production-web gates for test/typecheck/security/env/dependency/smoke where applicable."
    - "AIOS quality-pipeline entries match commands that actually exist in each repo."
    - "No production web app in this plan is marked ready without fresh evidence."
  artifacts:
    - path: /Users/jakyeamos/projects/portfolio/package.json
      provides: "Portfolio missing script surfaces"
    - path: /Users/jakyeamos/projects/dispatches-from-cyberspace/package.json
      provides: "Dispatches missing script surfaces"
    - path: /Users/jakyeamos/projects/Bballedu/package.json
      provides: "Bballedu missing script surfaces"
    - path: /Users/jakyeamos/projects/tm/package.json
      provides: "tm missing script surfaces"
    - path: config/quality-pipeline.json
      provides: "AIOS-owned command registry"
  key_links:
    - from: config/quality-pipeline.json
      to: repo package scripts
      via: "each configured command names an existing package script or workflow file"
    - from: repo package scripts
      to: quality_pipeline_runs
      via: "Plan 24-01 runner records evidence for each gate"
---

<objective>
Rectify the standard production/public web app gate gaps for `portfolio`, `dispatches-from-cyberspace`, `Bballedu`, and `tm`.

Purpose: These repos need real app-class gates, not only install/build/pre-cr coverage.
Output: Existing or added repo-native scripts/workflows plus AIOS config updates and local evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-rectify-linked-repo-aios-readiness-blockers-except-agent-router/24-RESEARCH.md
@docs/quality/linked-repo-strict-release-readiness.md
@config/quality-pipeline.json
@config/quality-gates.json
@/Users/jakyeamos/projects/portfolio/package.json
@/Users/jakyeamos/projects/dispatches-from-cyberspace/package.json
@/Users/jakyeamos/projects/Bballedu/package.json
@/Users/jakyeamos/projects/tm/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize missing web scripts per repo</name>
  <files>/Users/jakyeamos/projects/portfolio/package.json, /Users/jakyeamos/projects/dispatches-from-cyberspace/package.json, /Users/jakyeamos/projects/Bballedu/package.json, /Users/jakyeamos/projects/tm/package.json</files>
  <read_first>/Users/jakyeamos/projects/portfolio/package.json, /Users/jakyeamos/projects/dispatches-from-cyberspace/package.json, /Users/jakyeamos/projects/Bballedu/package.json, /Users/jakyeamos/projects/tm/package.json, docs/quality/linked-repo-strict-release-readiness.md</read_first>
  <action>For each repo, inspect existing scripts and add only commands backed by installed tooling or repo-local scripts. Required target gate keys are `test`, `typecheck`, `env_validation`, `secret_scan`, `dependency_security`, and `e2e_smoke` where applicable. Use `pnpm` for repos with `pnpm-lock.yaml`. If a gate cannot be implemented from existing tooling, add a repo-local script file with a concrete validation command and document the blocker in AIOS config instead of using a no-op command.</action>
  <verify>
    <automated>python3 -c 'import json, pathlib; [json.load(open(p)) for p in ["/Users/jakyeamos/projects/portfolio/package.json","/Users/jakyeamos/projects/dispatches-from-cyberspace/package.json","/Users/jakyeamos/projects/Bballedu/package.json","/Users/jakyeamos/projects/tm/package.json"]]; print("package json ok")'</automated>
  </verify>
  <acceptance_criteria>
    - Every added package script invokes a real command or checked-in script.
    - No script uses `echo ok`, `true`, or equivalent pass-through behavior.
    - Missing gates remain blockers in AIOS config if the repo has no honest command surface.
  </acceptance_criteria>
  <done>Each repo has honest package-script coverage or a recorded blocker for every required web gate.</done>
</task>

<task type="auto">
  <name>Task 2: Update AIOS web-app gate registry</name>
  <files>config/quality-pipeline.json, config/quality-gates.json</files>
  <read_first>config/quality-pipeline.json, config/quality-gates.json, docs/quality/linked-repo-strict-release-readiness.md</read_first>
  <action>Update the four project rows so configured gate commands exactly match the repo scripts/workflows from Task 1. Keep `strict_readiness_status` as `blocked` until every class-required gate has fresh passing evidence and CI/default proof. Remove resolved blocker text only when the replacement gate exists and has been executed through the Phase 24 runner.</action>
  <verify>
    <automated>python3 -c 'import json; [json.load(open(p)) for p in ["config/quality-pipeline.json","config/quality-gates.json"]]; print("json ok")'</automated>
  </verify>
  <acceptance_criteria>
    - AIOS config contains no command for these repos that is absent from the repo.
    - `maturation_blockers` match only unresolved blockers after the script updates.
    - Repo-local `.aios-quality-gate.json` files still declare gate IDs only.
  </acceptance_criteria>
  <done>AIOS gate registry reflects real web-app commands for the four repos.</done>
</task>

<task type="auto">
  <name>Task 3: Run and record web-app local evidence</name>
  <files>quality_pipeline_runs, docs/audits/linked-repo-adoption-readiness-audit.md</files>
  <read_first>scripts/linked-repo-quality-runner.py, config/quality-pipeline.json, docs/audits/linked-repo-adoption-readiness-audit.md</read_first>
  <action>Run the Phase 24 evidence runner for the updated gates in `portfolio`, `dispatches-from-cyberspace`, `Bballedu`, and `tm`. Record pass/fail rows for every attempted gate. Update the audit with evidence IDs, failed commands, and remaining CI/default proof status.</action>
  <verify>
    <automated>uv run pytest -q tests/test_linked_repo_readiness.py tests/test_quality_pipeline.py</automated>
  </verify>
  <acceptance_criteria>
    - Audit lists evidence IDs or failure IDs for each in-scope repo.
    - Failed local gates remain blockers.
    - No repo in this plan is marked ready before Plan 24-09 CI/default proof.
  </acceptance_criteria>
  <done>Four production web apps have honest local evidence records.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Repo package scripts -> AIOS evidence | Package scripts are repo-controlled and must be reviewed before AIOS records them as evidence. |
| Web app environment validation -> secrets | Env checks must not print secret values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-24-04 | Tampering | package scripts | mitigate | Review scripts before adding them to AIOS-owned config. |
| T-24-05 | Information disclosure | env/secret gates | mitigate | Secret/env scripts report missing/present status only, never values. |
</threat_model>

<verification>
- Package JSON parse command from Task 1
- `python3 -c 'import json; ...'` for AIOS config
- `uv run pytest -q tests/test_linked_repo_readiness.py tests/test_quality_pipeline.py`
</verification>

<success_criteria>
- `portfolio`, `dispatches-from-cyberspace`, `Bballedu`, and `tm` have real configured web gates or explicit unresolved blockers.
- Local evidence is recorded for every runnable updated gate.
- CI/default proof remains for Plan 24-09.
</success_criteria>

<output>
After completion, create `.planning/phases/24-rectify-linked-repo-aios-readiness-blockers-except-agent-router/24-03-SUMMARY.md`
</output>
