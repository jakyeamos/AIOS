---
phase: 24-rectify-linked-repo-aios-readiness-blockers-except-agent-router
plan: "08"
type: execute
wave: 8
depends_on: ["24-07"]
files_modified:
  - /Users/jakyeamos/projects/Vaults/README.md
  - /Users/jakyeamos/projects/Vaults/.tracker/PROJECT_TRUTH.md
  - /Users/jakyeamos/projects/BBDSE/docs/project-truth.md
  - /Users/jakyeamos/projects/BBDSE/docs/child-project-ownership.md
  - config/quality-pipeline.json
  - config/quality-gates.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "Vaults has a mature content/vault validation gate beyond git diff hygiene."
    - "BBDSE has aggregate delegated subproject evidence and child-project ownership documentation."
    - "Neither content/container repo is marked ready without CI/default proof or an explicit non-remote exception."
  artifacts:
    - path: /Users/jakyeamos/projects/Vaults/.tracker/PROJECT_TRUTH.md
      provides: "Vault validation and protected-path truth"
    - path: /Users/jakyeamos/projects/BBDSE/docs/child-project-ownership.md
      provides: "BBDSE delegated child-project ownership map"
    - path: config/quality-pipeline.json
      provides: "Content/container gate commands and blockers"
  key_links:
    - from: BBDSE child-project ownership map
      to: delegated pre-cr gates
      via: "each child project names owner, path, gate command, and evidence status"
    - from: Vaults validation command
      to: quality_pipeline_runs
      via: "Phase 24 runner records content validation evidence"
---

<objective>
Rectify content/vault/container blockers for `Vaults` and `BBDSE`.

Purpose: These repos need content/container validation, not fake app gates or diff-check-only proof.
Output: Vault content validator, BBDSE delegated ownership/evidence, and AIOS readiness records.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-rectify-linked-repo-aios-readiness-blockers-except-agent-router/24-RESEARCH.md
@docs/quality/linked-repo-strict-release-readiness.md
@config/quality-pipeline.json
@/Users/jakyeamos/projects/Vaults/README.md
@/Users/jakyeamos/projects/Vaults/.tracker/PROJECT_TRUTH.md
@/Users/jakyeamos/projects/BBDSE/AGENTS.md
@/Users/jakyeamos/projects/BBDSE/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Vaults content validation</name>
  <files>/Users/jakyeamos/projects/Vaults/README.md, /Users/jakyeamos/projects/Vaults/.tracker/PROJECT_TRUTH.md, config/quality-pipeline.json</files>
  <read_first>/Users/jakyeamos/projects/Vaults/README.md, /Users/jakyeamos/projects/Vaults/.tracker/PROJECT_TRUTH.md, docs/quality/linked-repo-strict-release-readiness.md, config/quality-pipeline.json</read_first>
  <action>Define a mature Vaults validation command that checks internal links, frontmatter conventions where present, protected paths, and secret-free content. Use existing vault tooling if present; otherwise add a repo-local validation script with bounded file traversal and documented rules. Update AIOS config so `validation` points to that command instead of relying on `git diff --check` as the mature gate.</action>
  <verify>
    <automated>python3 scripts/linked-repo-quality-runner.py --project Vaults --gate validation --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - Vaults validation checks links, protected paths, and secret-free content.
    - `git diff --check` may remain a floor gate but is not the mature validation gate.
    - Validation command is recorded in AIOS-owned config.
  </acceptance_criteria>
  <done>Vaults has an honest content validation gate ready for evidence recording.</done>
</task>

<task type="auto">
  <name>Task 2: Add BBDSE aggregate ownership and delegated evidence</name>
  <files>/Users/jakyeamos/projects/BBDSE/docs/project-truth.md, /Users/jakyeamos/projects/BBDSE/docs/child-project-ownership.md, config/quality-pipeline.json, config/quality-gates.json</files>
  <read_first>/Users/jakyeamos/projects/BBDSE/AGENTS.md, /Users/jakyeamos/projects/BBDSE/README.md, config/quality-gates.json, config/quality-pipeline.json, docs/quality/linked-repo-strict-release-readiness.md</read_first>
  <action>Create or update BBDSE project truth and `docs/child-project-ownership.md` with each delegated child project path, owner/context, gate command, evidence status, and whether the child is independently active in AIOS. Run delegated pre-cr gates for the configured BBDSE subprojects and record aggregate pass/fail evidence. Keep LIS independent from BBDSE readiness because it is already its own active source repo.</action>
  <verify>
    <automated>python3 scripts/linked-repo-quality-runner.py --project BBDSE --gate validation --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - BBDSE child ownership doc names every delegated subproject configured in `config/quality-gates.json`.
    - BBDSE readiness is blocked if any delegated child gate is missing or failing.
    - LIS remains represented as its own active repo and is not double-counted as BBDSE proof.
  </acceptance_criteria>
  <done>BBDSE has aggregate delegated validation and ownership proof.</done>
</task>

<task type="auto">
  <name>Task 3: Record content/container evidence</name>
  <files>quality_pipeline_runs, docs/audits/linked-repo-adoption-readiness-audit.md</files>
  <read_first>scripts/linked-repo-quality-runner.py, config/quality-pipeline.json, docs/audits/linked-repo-adoption-readiness-audit.md</read_first>
  <action>Run the Phase 24 evidence runner for Vaults and BBDSE content/container gates. Record pass/fail evidence and update the audit with evidence IDs, unresolved delegated child failures, and required CI/non-remote exception status.</action>
  <verify>
    <automated>uv run pytest -q tests/test_linked_repo_readiness.py tests/test_quality_pipeline.py</automated>
  </verify>
  <acceptance_criteria>
    - Vaults has validation evidence stronger than diff hygiene.
    - BBDSE has aggregate delegated evidence.
    - CI/default proof or exception remains for Plan 24-09.
  </acceptance_criteria>
  <done>Content/container repos have evidence-backed blocker state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Vault content -> validator | Markdown and vault content can contain links, frontmatter, or sensitive strings. |
| BBDSE container -> child repos | Container proof depends on delegated child project results. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-24-14 | Information disclosure | Vault validation | mitigate | Secret-free checks report file paths and rule IDs without printing secret values. |
| T-24-15 | Repudiation | BBDSE delegated evidence | mitigate | Child-project ownership map records command, path, and evidence status for each delegated gate. |
</threat_model>

<verification>
- `python3 scripts/linked-repo-quality-runner.py --project Vaults --gate validation --dry-run`
- `python3 scripts/linked-repo-quality-runner.py --project BBDSE --gate validation --dry-run`
- `uv run pytest -q tests/test_linked_repo_readiness.py tests/test_quality_pipeline.py`
</verification>

<success_criteria>
- Vaults has a mature content validation gate.
- BBDSE has delegated aggregate evidence and child ownership.
- Neither repo is marked ready before CI/default proof or exception.
</success_criteria>

<output>
After completion, create `.planning/phases/24-rectify-linked-repo-aios-readiness-blockers-except-agent-router/24-08-SUMMARY.md`
</output>
