---
phase: 24-rectify-linked-repo-aios-readiness-blockers-except-agent-router
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - services/linked_repo_readiness.py
  - scripts/linked-repo-quality-runner.py
  - tests/test_linked_repo_readiness.py
  - config/quality-pipeline.json
  - docs/audits/linked-repo-adoption-readiness-audit.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "AIOS can compute a strict readiness verdict for every Phase 24 in-scope repo while excluding agent-router."
    - "AIOS can record local gate evidence without executing commands from repo-local .aios-quality-gate.json files."
    - "A repo cannot be marked ready unless required gate evidence and CI/default proof or an exception are present."
  artifacts:
    - path: services/linked_repo_readiness.py
      provides: "Portfolio readiness aggregation and agent-router exclusion"
    - path: scripts/linked-repo-quality-runner.py
      provides: "Command runner that records quality_pipeline_runs evidence"
    - path: tests/test_linked_repo_readiness.py
      provides: "Regression coverage for readiness verdicts and evidence requirements"
  key_links:
    - from: scripts/linked-repo-quality-runner.py
      to: services.quality_pipeline.record_quality_pipeline_run
      via: "imports and calls the existing recorder with project_id, gate_key, command, status, source, evidence, and metadata"
    - from: services/linked_repo_readiness.py
      to: config/quality-pipeline.json
      via: "loads repo_class, strict_readiness_status, gates, maturation_blockers, and non_remote_ci_exception"
    - from: services/linked_repo_readiness.py
      to: quality_pipeline_runs
      via: "uses get_project_quality_pipeline summaries and latest_run_id fields"
---

<objective>
Create the AIOS-owned evidence and readiness reporting foundation Phase 24 needs before repo-specific remediation starts.

Purpose: Prevent repo work from ending in prose-only readiness claims.
Output: A tested service/CLI path that records gate evidence, computes readiness verdicts, and excludes `agent-router` from the Phase 24 target set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-rectify-linked-repo-aios-readiness-blockers-except-agent-router/24-RESEARCH.md
@.planning/phases/24-rectify-linked-repo-aios-readiness-blockers-except-agent-router/24-VALIDATION.md
@.planning/phases/23-mature-linked-repositories-to-aios-strict-release-readiness/23-VERIFICATION.md
@docs/quality/linked-repo-strict-release-readiness.md
@config/quality-pipeline.json
@services/quality_pipeline.py
@services/project_health_proof.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define readiness aggregation behavior</name>
  <files>tests/test_linked_repo_readiness.py, services/linked_repo_readiness.py</files>
  <read_first>services/quality_pipeline.py, services/project_health_proof.py, config/quality-pipeline.json, .planning/phases/23-mature-linked-repositories-to-aios-strict-release-readiness/23-VERIFICATION.md</read_first>
  <behavior>
    - Given a quality-pipeline config containing `agent-router`, the Phase 24 target list excludes `agent-router` and includes the other 22 active repos.
    - Given required gates with missing latest pass evidence, the readiness verdict is not `ready`.
    - Given a missing CI gate and no `non_remote_ci_exception`, the readiness verdict records `ci_default_proof_missing`.
  </behavior>
  <action>Create `services.linked_repo_readiness` with pure functions for loading Phase 24 target projects, computing per-repo readiness from `get_project_quality_pipeline`, and summarizing counts for `ready`, `evidence_required`, `blocked`, and `excluded`. Use `agent-router` as an explicit default exclusion constant with a source comment referencing Phase 24 scope. Do not execute shell commands in this service.</action>
  <verify>
    <automated>uv run pytest -q tests/test_linked_repo_readiness.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_linked_repo_readiness.py` asserts `agent-router` is excluded.
    - `services/linked_repo_readiness.py` exposes a typed function returning per-project verdict, missing gate keys, latest evidence IDs, blockers, and exclusion reason.
    - No test requires live linked repos or network access.
  </acceptance_criteria>
  <done>Readiness aggregation is covered by tests and can be consumed by dependent Phase 24 plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add local gate evidence runner</name>
  <files>scripts/linked-repo-quality-runner.py, tests/test_linked_repo_readiness.py</files>
  <read_first>services/quality_pipeline.py, config/quality-pipeline.json, bin/aios.py, services/aios_cli.py</read_first>
  <behavior>
    - The runner reads AIOS-owned commands from `config/quality-pipeline.json`.
    - The runner refuses to run `agent-router` because Phase 24 excludes that repo from the target set.
    - The runner records pass/fail evidence with command, source, completed_at, and metadata.
  </behavior>
  <action>Create `scripts/linked-repo-quality-runner.py` with arguments `--project`, `--gate`, `--dry-run`, `--record-only`, and `--db`. Resolve commands only from `config/quality-pipeline.json`. For `--dry-run`, print the resolved command and working directory without executing. For executed or record-only runs, call `record_quality_pipeline_run` with `source` set to `phase24-local` and metadata containing `phase: 24`, `repo_class`, and `working_directory`.</action>
  <verify>
    <automated>uv run pytest -q tests/test_linked_repo_readiness.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Runner rejects unknown project IDs and `agent-router`.
    - Runner never reads commands from `.aios-quality-gate.json`.
    - Dry-run output includes project id, gate key, command, and resolved working directory.
  </acceptance_criteria>
  <done>Executors can run or dry-run AIOS-owned gate commands and record durable evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Expose readiness report command or documented invocation</name>
  <files>services/linked_repo_readiness.py, scripts/linked-repo-quality-runner.py, docs/audits/linked-repo-adoption-readiness-audit.md</files>
  <read_first>services/aios_cli.py, docs/audits/linked-repo-adoption-readiness-audit.md, .planning/phases/23-mature-linked-repositories-to-aios-strict-release-readiness/23-VERIFICATION.md</read_first>
  <action>Add a report mode that prints the Phase 24 readiness summary as JSON with fields `target_count`, `excluded_count`, `ready_count`, `blocked_count`, `evidence_required_count`, and `projects`. If wiring into `bin/aios.py` is not consistent with existing CLI command patterns, document the script invocation in `docs/audits/linked-repo-adoption-readiness-audit.md` under a Phase 24 section instead of adding a new CLI command.</action>
  <verify>
    <automated>uv run pytest -q tests/test_linked_repo_readiness.py tests/test_quality_pipeline.py</automated>
  </verify>
  <acceptance_criteria>
    - Report output contains exactly 22 in-scope projects and one excluded project named `agent-router`.
    - Report output includes latest evidence IDs from `quality_pipeline_runs` when present.
    - Audit doc names the command used to regenerate the report.
  </acceptance_criteria>
  <done>Phase 24 has a repeatable readiness report surface before repo fixes begin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| AIOS config -> runner | Config commands are trusted only because they are AIOS-owned and reviewed. |
| Linked repo -> evidence store | Command output and repo state are untrusted evidence inputs. |
| Runner -> SQLite | Runner writes durable quality evidence into `quality_pipeline_runs`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-24-01 | Elevation of privilege | `scripts/linked-repo-quality-runner.py` | mitigate | Resolve commands only from AIOS-owned `config/quality-pipeline.json`; reject repo-local command declarations. |
| T-24-02 | Repudiation | `quality_pipeline_runs` | mitigate | Record command, source, evidence, completed_at, and metadata for every run. |
| T-24-03 | Tampering | Phase 24 target list | mitigate | Hard-exclude `agent-router` and test the exclusion. |
</threat_model>

<verification>
- `uv run pytest -q tests/test_linked_repo_readiness.py tests/test_quality_pipeline.py`
- `python3 scripts/linked-repo-quality-runner.py --project soundscape-app --gate lint --dry-run`
- `pnpm context:validate`
</verification>

<success_criteria>
- Phase 24 has a tested readiness aggregation service.
- Phase 24 has a tested evidence runner/report path.
- `agent-router` is excluded from Phase 24 target reporting.
</success_criteria>

<output>
After completion, create `.planning/phases/24-rectify-linked-repo-aios-readiness-blockers-except-agent-router/24-01-SUMMARY.md`
</output>
