---
phase: 16-harness-determinism-evidence-and-independent-verification
plan: "03"
type: execute
wave: 2
autonomous: true
depends_on:
  - "16-01"
  - "16-02"
files_modified:
  - schema.sql
  - services/verifier_artifacts.py
  - services/workflow_orchestration.py
  - services/commit_quality_ladder.py
  - bin/hook-stop.py
  - services/aios_cli.py
  - tests/test_verifier_artifacts.py
  - tests/test_workflow_orchestration.py
requirements:
  - HARN-04
must_haves:
  truths:
    - "AIOS defines a verifier artifact schema with task_id, run_id, verifier_agent, model, inputs_reviewed_json, checks_performed_json, result, blocking_issues_json, non_blocking_issues_json, recommended_next_phase, evidence_refs_json, created_at."
    - "Implementation-bearing governed workflows cannot close as complete unless a fresh verifier artifact exists or the workflow is explicitly configured as verification-exempt with a recorded reason."
    - "Verifier inputs are task spec, diff/changed files, and evidence artifacts. The verifier contract does not rely only on the implementer narrative."
    - "Verifier result values are structured: pass, fail, needs_work. fail and needs_work deterministically route to implementation_retry or human_review rather than closeout."
    - "Verifier findings must cite concrete files, commands, evidence IDs, or output paths when reporting blocking issues."
    - "AIOS standards ladder checks cannot become fail-closed for AIOS-managed repositories unless either the check is purely staged-file deterministic or the verifier artifact records the reviewed inputs, evidence refs, and pass/fail rationale."
  artifacts:
    - path: "services/verifier_artifacts.py"
      provides: "Verifier artifact schema, persistence, validation, and closeout gate helpers."
    - path: "schema.sql"
      provides: "Additive verifier_artifacts table or compatible extension to existing verifier evidence storage."
    - path: "tests/test_verifier_artifacts.py"
      provides: "Coverage for pass/fail/needs_work, missing verifier closeout block, stale verifier rejection, and required evidence refs."
    - path: "services/commit_quality_ladder.py"
      provides: "Fail-closed eligibility guard that rejects verifier-dependent standards checks unless a fresh verifier artifact exists."
  key_links:
    - from: "services/verifier_artifacts.py:validate_closeout_verification"
      to: "bin/hook-stop.py"
      via: "Closeout blocks completion when verifier artifact is absent or failing for implementation-bearing governed workflows."
      pattern: "verifier artifact"
---

# Plan 16-03: Independent Verifier Gate

## Objective

Make verification structurally independent from implementation. AIOS should require a verifier artifact for implementation-bearing governed workflows, and that verifier must inspect task spec, changed files or diff, and evidence artifacts rather than trusting the implementer summary.

For commit-ladder readiness, this plan defines the point where AIOS standards may become blocking in AIOS-managed repositories: deterministic staged-file checks may block directly, but evidence-dependent standards require a fresh verifier artifact before they can fail closed.

## Scope

Add the minimal verifier artifact and closeout gate needed by the current workflow system.

Implementation should prefer:

- additive persistence in `schema.sql`
- `services/verifier_artifacts.py` for schema and validation helpers
- closeout integration through the seam identified in Plan 16-01
- workflow registry/config flags only where needed to identify implementation-bearing workflows and exemptions
- commit-ladder eligibility metadata for checks that require independent verification before blocking
- CLI inspection through `services/aios_cli.py`

## Verifier Artifact Shape

Required fields:

- `task_id`
- `run_id`
- `verifier_agent`
- `model`
- `inputs_reviewed_json`
- `checks_performed_json`
- `result`: `pass | fail | needs_work`
- `blocking_issues_json`
- `non_blocking_issues_json`
- `recommended_next_phase`: `closeout | implementation_retry | human_review`
- `evidence_refs_json`

## Standards

- Do not spawn a verifier automatically unless the existing harness already has an appropriate mechanism. This plan can add the artifact contract and closeout gate first.
- Verification-exempt workflows must be explicit and auditable.
- A verifier artifact without evidence references is invalid for implementation-bearing work.
- Closeout must surface why verification blocked completion.
- Ladder blockers must cite either deterministic staged-file evidence or a verifier artifact; never block on an ungrounded standards assertion.

## Verification

- Tests prove missing verifier artifact blocks completion where required.
- Tests prove `fail` and `needs_work` do not route to closeout.
- Tests prove verifier artifacts must cite evidence refs or concrete files for blocking issues.
- Tests prove verifier-dependent ladder checks remain warn-only without a fresh verifier artifact.
