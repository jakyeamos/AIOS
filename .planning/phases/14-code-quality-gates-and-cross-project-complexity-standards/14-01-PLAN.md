---
phase: 14-code-quality-gates-and-cross-project-complexity-standards
plan: "01"
type: execute
wave: 1
autonomous: true
depends_on: []
files_modified:
  - config/success-criteria/registry.json
  - spec/success-criteria/
  - config/agent-rules.md
  - AGENTS.md
  - ~/.claude/CLAUDE.md
  - docs/quality/aios-standards-ladder-contract.md
requirements:
  - QUAL-01
  - QUAL-07
must_haves:
  truths:
    - "Baseline already landed on 2026-06-23: `config/success-criteria/registry.json` and `spec/success-criteria/` now define the AIOS quality-gate vocabulary, including `complexity-budget`, `architecture-boundary`, `simplicity`, `test-quality`, `agent-claim-verification`, and related UI/data/security/supply-chain gates. This plan must build on those criteria instead of creating a parallel gate taxonomy."
    - "config/agent-rules.md gains Rule 10: Complexity + Simplification Gate. The rule defines: (1) the mandatory trigger condition — any large piece of work defined as changes touching 5+ files, 300+ lines added/removed, new feature implementation, cross-package or cross-layer change, DB/schema/query change, data pipeline/analytics/scoring/model logic change, UI surface with meaningful state/rendering logic, agent/workflow/orchestration change, performance-sensitive code path, or code likely reused as infrastructure; (2) Gate Part A — Complexity/Performance Review covering nested loops over growing collections, repeated array scans inside loops, repeated object construction in hot paths, sort-inside-loop patterns, avoidable O(n²) joins that should use maps/sets/indexes, N+1 database/API calls, render recomputation/missing memoization/unstable props, unbounded loops/repeated parsing/serialization/deep cloning, and data structures that should be Map/Set instead of arrays; (3) Gate Part B — Simplification/Maintainability Review covering reduce nesting and branch complexity, remove duplicated logic, extract helpers only when clarity improves, improve names where intent is unclear, consolidate scattered logic, delete dead code and unnecessary abstractions, prefer direct code over clever code, preserve behavior, preserve public APIs unless explicitly approved, follow local conventions, add/update tests when simplifying logic; (4) Gate Part C — Verification — run relevant tests/typecheck/lint/build, or document why commands cannot run; (5) the rule that EVERY hotspot must be recorded with file, category, severity, confidence, suggested remediation, behavior risk, tests needed, and agent-safe classification before any fix is attempted; (6) the rule that hotspot recording comes before fixing — agents must not silently fix during the gate pass unless the fix is fewer than 5 lines and carries no behavior risk."
    - "AGENTS.md gains a section titled 'Complexity + Simplification Gate' after the existing quality and standards sections. The section references Rule 10 in config/agent-rules.md, lists the trigger conditions for large work, summarizes Gate Parts A/B/C, and states: report before fixing — record all hotspots in a finding comment or backfill doc before touching any code."
    - "~/.claude/CLAUDE.md Quality Ladder table gains a new step after step 4 (Tests): Step 5 is 'Complexity + Simplification Gate' with gate status Hard after large work (see trigger list in config/agent-rules.md Rule 10). The description field reads: run both complexity/performance review (nested loops, N+1, O(n²), memoization gaps) and simplification/maintainability review (nesting, duplication, naming, dead code) — record findings before fixing."
    - "docs/quality/aios-standards-ladder-contract.md (NEW) defines the portable commit-ladder contract for AIOS standards: which checks are eligible for global hook enforcement, which must stay AIOS-local, which begin warn-only, what waiver format is accepted, what evidence is required, and which Phase 16/22 gates must pass before fail-closed rollout."
  artifacts:
    - path: "config/success-criteria/registry.json"
      provides: "Existing baseline registry for AIOS quality gates. Phase 14 docs and agent-rule text must reference these criterion ids rather than inventing separate names."
    - path: "config/agent-rules.md"
      provides: "Rule 10 defining the mandatory Complexity+Simplification Gate with trigger conditions, Gate A (complexity), Gate B (simplification), Gate C (verification), record-before-fix policy, and hotspot field requirements."
    - path: "AGENTS.md"
      provides: "Complexity+Simplification Gate section cross-referencing Rule 10, summarizing trigger conditions and gate structure."
    - path: "~/.claude/CLAUDE.md"
      provides: "Quality Ladder extended with Step 5 Complexity+Simplification Gate as a Hard gate after large work."
    - path: "docs/quality/aios-standards-ladder-contract.md"
      provides: "Portable standards-ladder contract used to add AIOS quality checks to the user-level commit ladder in warn-only mode first."
  key_links:
    - from: "config/agent-rules.md:Rule 10"
      to: "docs/quality/complexity-simplification-gate.md"
      via: "Rule 10 references the gate doc for full specification; gate doc is created in Plan 14-03"
      pattern: "complexity-simplification-gate.md"
    - from: "AGENTS.md:Complexity+Simplification Gate"
      to: "config/agent-rules.md:Rule 10"
      via: "AGENTS.md section cross-references Rule 10 for authoritative definition"
      pattern: "Rule 10"
    - from: "docs/quality/aios-standards-ladder-contract.md"
      to: ".githooks-user/pre-commit"
      via: "The contract defines which AIOS checks may be surfaced by the portable user-level hook before Phase 22 fail-closed promotion."
      pattern: "warn-only"
---

# Plan 14-01: Agent Rule and Workflow Gate Addition

## 2026-06-23 Baseline Update

AIOS now has first-class success criteria for the broader quality-gate stack in `config/success-criteria/registry.json` and `spec/success-criteria/`. Phase 14 should no longer define quality gates only as prose. Its remaining job is to connect the already-landed criteria to agent instructions, the quality docs, and the standards ladder contract.

When writing Rule 10 and the ladder contract, use these existing criterion ids as the canonical names where relevant:

- `complexity-budget`
- `architecture-boundary`
- `simplicity`
- `test-quality`
- `agent-claim-verification`
- `performance-budget`
- `thin-display`
- `data-integrity`
- `api-contract`
- `supply-chain-review`

## Objective

Add the Complexity + Simplification Gate as a mandatory agent rule across the three root agent instruction surfaces: `config/agent-rules.md`, `AGENTS.md`, and `~/.claude/CLAUDE.md`. This is the enforcement layer — without it the gate is optional; with it the gate is a non-negotiable step after large work.

This plan also creates the first AIOS standards ladder contract. Phase 14 is responsible for making the checks concrete enough to enter the commit ladder in warn-only/reporting mode, not for making them fail-closed across every repository.

## Scope

### 1. config/agent-rules.md — Rule 10

Add Rule 10 after the existing highest-numbered rule. The rule must define:

**Trigger:** Large piece of work defined as any of:
- Changes touching 5+ files
- Changes adding/removing 300+ lines
- New feature implementation
- Cross-package or cross-layer change
- Database/schema/query change
- Data pipeline, analytics, scoring, or model logic change
- UI surface with meaningful state/rendering logic
- Agent/workflow/orchestration change
- Performance-sensitive code path
- Code that will likely be reused as infrastructure

**Gate Part A — Complexity/Performance Review:**
- Nested loops over growing collections
- Repeated array scans/find/filter/map inside loops
- Repeated object construction in hot paths
- Sort-inside-loop patterns
- Avoidable O(n²) joins that should use maps/sets/indexes
- N+1 database or API calls
- Render recomputation, missing memoization, unstable props, expensive client-side derived data
- Unbounded loops, repeated parsing/serialization, unnecessary deep cloning
- Data structures that should be Map/Set instead of arrays

**Gate Part B — Simplification/Maintainability Review:**
- Reduce nesting and branch complexity
- Remove duplicated logic
- Extract helpers only when clarity improves
- Improve names where intent is unclear
- Consolidate scattered logic into appropriate modules
- Delete dead code and unnecessary abstractions
- Prefer direct code over clever code
- Preserve behavior; preserve public APIs unless explicitly approved
- Follow local project conventions
- Add or update tests when simplifying logic

**Gate Part C — Verification:**
- Run relevant tests, typecheck, lint, and build
- If commands cannot run, document why

**Record-before-fix policy:** Every finding must be recorded with file, category, severity, confidence, suggested remediation, behavior risk, tests/benchmarks needed, and agent-safe classification before any fix attempt. Fixes are permitted only for findings under 5 lines with no behavior risk — all others must be recorded and deferred.

### 2. AGENTS.md — new gate section

Add a section after existing quality/standards sections:

```
## Complexity + Simplification Gate

After any large piece of work (see Rule 10 in config/agent-rules.md for trigger list), run:
- Gate A: complexity/performance review
- Gate B: simplification/maintainability review
- Gate C: verification (tests, lint, typecheck, build — or document why unavailable)

Report before fixing. Record every hotspot in a structured finding before touching any code.
See docs/quality/complexity-simplification-gate.md for the full checklist.
```

### 3. ~/.claude/CLAUDE.md — Quality Ladder extension

Add a row to the Quality Ladder table:

| Step | What | Gate |
| 5 | Complexity + Simplification Gate | **Hard** after large work (see trigger list: 5+ files, 300+ lines, new feature, cross-package, DB, pipeline, agent/workflow, perf-sensitive, infra code). Run complexity/performance review (nested loops, N+1, O(n²), memoization gaps) and simplification review (nesting, duplication, naming, dead code). Record findings before fixing. |

### 4. docs/quality/aios-standards-ladder-contract.md — warn-only ladder contract

Create the initial contract for adding AIOS standards to the commit quality ladder:

- Include a coverage section for the already-landed success-criteria quality gates from `config/success-criteria/registry.json`.
- Mark the success-criteria gates as AIOS-local/runtime criteria unless a separate portable staged-file check exists.
- Eligible now: cheap syntactic and staged-file checks that do not require AIOS runtime state.
- Warn-only after Phase 14: AIOS code-quality and simplification standards that can produce deterministic findings but do not yet have Phase 16 evidence binding.
- AIOS-local only: checks that require SQLite state, context receipts, success-criteria evaluation records, or project truth writebacks.
- Fail-closed prerequisites: Phase 16 evidence/verifier gates and Phase 22 progressive governance rollout.
- Waiver contract: waivers must include a named rule, concrete runtime reason, owner, and expiration or backfill reference.
- Output contract: ladder findings must identify file, rule id, severity, blocking mode (`off | warn | fail`), evidence source, and remediation.

## Standards

- Read the full current content of each file before editing.
- Add rules/sections — do not remove or restructure existing content.
- The rule text must be specific enough for an agent to apply without reading a secondary doc.
- The ladder contract must distinguish "eligible for warning now" from "eligible for fail-closed enforcement later."
