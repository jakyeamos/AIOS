---
phase: 14-code-quality-gates-and-cross-project-complexity-standards
plan: "07"
type: execute
wave: 3
autonomous: true
depends_on:
  - "14-03"
  - "14-04"
files_modified:
  - ~/projects/amos-saas/docs/complexity-simplification-backfill.md
  - ~/projects/GitNexus/docs/complexity-simplification-backfill.md
  - ~/projects/tm/docs/complexity-simplification-backfill.md
requirements:
  - QUAL-06
must_haves:
  truths:
    - "~/projects/amos-saas/docs/complexity-simplification-backfill.md (NEW) is the amos-saas backfill inventory. amos-saas is a SaaS platform (TypeScript/Next.js, likely with Prisma and tRPC based on CLAUDE.md). High-risk areas are: database query patterns (N+1, unbounded queries, missing index coverage), tRPC router logic (repeated data access patterns, over-fetching), server component / client component render performance, and any analytics or scoring logic. The agent must inspect the actual source tree before recording findings."
    - "~/projects/GitNexus/docs/complexity-simplification-backfill.md (NEW) is the GitNexus backfill inventory. GitNexus is a framework (TypeScript, likely complex orchestration/graph logic). High-risk areas are: orchestration loops, graph traversal patterns, any O(n²) relationship lookups, and repeated object construction in framework hot paths. The agent must inspect the actual source tree before recording findings."
    - "~/projects/tm/docs/complexity-simplification-backfill.md (NEW) is the tm (Taski) backfill inventory. tm is a local-first project tracker. High-risk areas are: task list operations on growing collections, filter/sort/search patterns, sync logic if present. The agent must inspect the actual source tree before recording findings."
    - "All three backfill docs include a 'Commands Attempted' section listing which quality commands were run (pnpm lint, pnpm tsc --noEmit, pnpm test, pnpm knip if wired) and their outcomes."
  artifacts:
    - path: "~/projects/amos-saas/docs/complexity-simplification-backfill.md"
      provides: "amos-saas complexity+simplification backfill with focus on DB query patterns, tRPC router complexity, render performance, and analytics logic."
    - path: "~/projects/GitNexus/docs/complexity-simplification-backfill.md"
      provides: "GitNexus complexity+simplification backfill with focus on orchestration loops, graph traversal patterns, and framework hot paths."
    - path: "~/projects/tm/docs/complexity-simplification-backfill.md"
      provides: "tm complexity+simplification backfill with focus on task list operations, filter/sort patterns, and sync logic."
  key_links:
    - from: "~/projects/amos-saas/docs/complexity-simplification-backfill.md"
      to: "docs/quality/complexity-simplification-gate.md"
      via: "hotspot template and gate spec are authoritative for all project backfill docs"
      pattern: "complexity-simplification-gate.md"
    - from: "~/projects/GitNexus/docs/complexity-simplification-backfill.md"
      to: "docs/quality/complexity-simplification-gate.md"
      via: "hotspot template and gate spec are authoritative for all project backfill docs"
      pattern: "complexity-simplification-gate.md"
    - from: "~/projects/tm/docs/complexity-simplification-backfill.md"
      to: "docs/quality/complexity-simplification-gate.md"
      via: "hotspot template and gate spec are authoritative for all project backfill docs"
      pattern: "complexity-simplification-gate.md"
---

# Plan 14-07: amos-saas, GitNexus, and tm Backfill Docs

## Objective

Create complexity+simplification backfill inventories for `amos-saas`, `GitNexus`, and `tm`. These are medium-risk projects with non-trivial business logic. Each gets a backfill doc at `<project>/docs/complexity-simplification-backfill.md`.

## Scope

### amos-saas (`~/projects/amos-saas`)

**Before writing:** Inspect source directories. Run:
- `pnpm lint`
- `pnpm tsc --noEmit`
- `pnpm knip` if wired

**High-risk areas:**
- **tRPC routers** — check for N+1 DB access patterns (calling `prisma.findUnique()` inside a loop over a list)
- **Prisma queries** — unbounded queries, missing `take`/pagination, queries on unindexed columns
- **Server Components** — data fetching patterns, expensive derived data
- **Client Components** — unstable props, missing memo, expensive render-time computation
- **Any scoring or analytics logic** — repeated aggregations, in-memory group-by patterns

### GitNexus (`~/projects/GitNexus`)

**Before writing:** Inspect source directories. Run available lint/typecheck commands.

**High-risk areas:**
- **Framework orchestration** — loops over nodes/edges, repeated graph relationship lookups
- **Object construction** — repeated dict/object construction in framework processing hot paths
- **Type resolution** — any O(n²) matching logic for resolving workflow or schema relationships
- **Plugin/hook dispatch** — repeated scans over registered handlers

### tm (`~/projects/tm`)

**Before writing:** Inspect source directories. Run:
- `pnpm lint`
- `pnpm tsc --noEmit`

**High-risk areas:**
- **Task list operations** — filter/sort/search on in-memory task collections that could grow
- **Sync logic** — if CRDT or similar — repeated object comparison patterns
- **View derivation** — computed views recalculated on every state change without memoization

### For each project, document

Same structure as Plan 14-05/06:
- Project name and date
- Scope reviewed (actual files/dirs inspected)
- Commands attempted and outcomes
- Complexity hotspots (Hotspot template)
- Simplification hotspots (Hotspot template)
- Test gaps blocking safe cleanup
- Remediation order (P0/P1/P2)
- Risk level per hotspot
- Definition of Done for quality standard

## Standards

- If a project is large, sample the highest-risk areas first (data access, orchestration, recently changed code) and note the sampling in the scope section.
- Every hotspot references a real file path and function/line.
- "No major hotspots found in this area" is a valid observation if honest.
- Do not fix anything — record and classify only.
