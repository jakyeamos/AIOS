---
phase: 11-testing-benchmark-evaluation-and-shadow-workflows
plan: "07"
type: execute
wave: 5
autonomous: true
depends_on:
  - "11-04"
  - "11-06"
files_modified:
  - services/portable_context_packet_generator.py
  - services/external_benchmark_adapter.py
  - config/context-packets/.gitkeep
  - aios-ui/server/aios/eval-data.ts
  - aios-ui/components/eval/EvalSummaryPanel.tsx
  - aios-ui/components/eval/ShadowCandidateQueue.tsx
  - aios-ui/app/runs/[id]/page.tsx
  - aios-ui/app/projects/[id]/page.tsx
  - aios-ui/app/page.tsx
  - services/aios_cli.py
  - tests/test_portable_context_packet_generator.py
  - tests/test_external_benchmark_adapter.py
  - tests/test_aios_cli.py
requirements:
  - EVAL-07
  - EVAL-08
must_haves:
  truths:
    - "services/portable_context_packet_generator.py (NEW) exports: generate_packet(conn, *, task_description, repo_path, packet_id=None) -> dict (returns the packet as a dict and writes it to config/context-packets/{packet_id}.json). The generator: (1) lists relevant repo files using git log --follow and file extension heuristics (py, ts, tsx, json, md up to 20 files); (2) detects framework conventions from package.json and pyproject.toml; (3) extracts test commands from package.json scripts and pyproject.toml [tool.pytest.ini_options]; (4) extracts success criteria file references from config/success-criteria/ directory listing; (5) applies a privacy filter that rejects any file path containing .env, secrets, credentials, or personal/ prefixes and raises PrivacyFilterError if the task_description contains known personal vault paths. The packet JSON structure matches the portable-context-packet.md template fields exactly."
    - "services/external_benchmark_adapter.py (NEW) exports: to_swe_bench_format(eval_task_dict) -> dict with keys: repo, instance_id, problem_statement, base_commit, FAIL_TO_PASS (list), PASS_TO_PASS (list); to_terminal_bench_format(eval_task_dict) -> dict with keys: task_id, command, expected_exit_code, setup_commands (list), timeout_seconds; normalize_external_result(external_result, *, eval_task_id, harness, model) -> dict (returns an EvalRun-compatible dict with mode='external', context_profile='external_clean_room', final_status derived from external result). to_swe_bench_format maps: eval_task.start_sha → base_commit, eval_task.prompt_summary → problem_statement, eval_task.repo_id → repo, eval_task.task_id → instance_id. normalize_external_result sets context_profile='external_clean_room' unconditionally — external runs can never be labeled as personalized."
    - "config/context-packets/.gitkeep (NEW) — empty file to ensure the directory is tracked in git. Generated packet JSON files in this directory are gitignored (or explicitly committed per packet — agent decides at generation time based on sensitivity)."
    - "aios-ui/server/aios/eval-data.ts (NEW) is a server-side module exporting: getEvalSummaryForProject(projectId) -> Promise<EvalSummary | null>; getEvalRunsForRun(runId) -> Promise<EvalRun[]>; getShadowCandidateQueue() -> Promise<ShadowCandidate[]>. These read from the SQLite eval_* tables via the existing db connection pattern. All functions return null or empty arrays when the eval_* tables are absent (graceful degradation matching Phase 10 pattern)."
    - "aios-ui/components/eval/EvalSummaryPanel.tsx (NEW) is a collapsible client component displaying: AIOS Effectiveness Score (overall), score breakdown (task success / quality / speed / cost / context / autonomy / user trust / portability), context profile label, Second Brain Lift if available, Portability Gap if available, and a link to the full eval run detail. It renders only when evalData prop is non-null; parent pages pass null when no eval data exists for the project/run."
    - "aios-ui/components/eval/ShadowCandidateQueue.tsx (NEW) is a collapsible client component displaying the shadow candidate queue: candidate ID, score, recommendation tier (color-coded), reasons list, blockers list, current automation_state, and an 'Approve' button that calls a tRPC mutation to trigger approve_candidate. The 'Approve' button is gated behind a confirmation modal matching the governance constraint from Phase 10."
    - "aios-ui/app/runs/[id]/page.tsx (EXTENSION) renders an EvalSummaryPanel below the existing run detail sections when eval data exists for the run. The panel is collapsible and defaults to collapsed to preserve density. It does not appear when no eval data exists."
    - "aios-ui/app/projects/[id]/page.tsx (EXTENSION) renders an EvalSummaryPanel showing project-level aggregate eval scores, and a ShadowCandidateQueue panel showing pending candidates for the project. Both panels are collapsible and default to collapsed."
    - "aios-ui/app/page.tsx (EXTENSION — Command Center) renders a ShadowCandidateQueue panel in the Command Center showing all pending candidates across all projects, below the existing governance overview section. The panel defaults to collapsed and renders nothing when the queue is empty."
    - "services/aios_cli.py gains: 'aios packet generate --task <description> --repo-path <path> --packet-id <optional-id> --json' (calls generate_packet, returns packet dict); 'aios benchmark to-swe-bench --task-id <id> --json' (calls to_swe_bench_format); 'aios benchmark to-terminal-bench --task-id <id> --json' (calls to_terminal_bench_format); 'aios benchmark normalize-result --task-id <id> --harness <name> --model <name> --result-file <path> --json' (reads result JSON file and calls normalize_external_result)."
    - "tests/test_portable_context_packet_generator.py covers: generate_packet returns dict with all template fields; privacy filter raises PrivacyFilterError on .env file path; privacy filter raises PrivacyFilterError on personal/ path in task description; packet JSON is written to config/context-packets/{packet_id}.json; test commands are extracted from package.json scripts. tests/test_external_benchmark_adapter.py covers: to_swe_bench_format maps all required fields; to_terminal_bench_format maps all required fields; normalize_external_result always sets context_profile='external_clean_room'; normalize_external_result derives final_status from external result."
  artifacts:
    - path: "services/portable_context_packet_generator.py"
      provides: "Packet generation; file relevance heuristics; convention detection; privacy filter; packet JSON output to config/context-packets/."
    - path: "services/external_benchmark_adapter.py"
      provides: "SWE-bench format translation; Terminal-Bench format translation; external result normalization; external_clean_room context profile enforcement."
    - path: "aios-ui/server/aios/eval-data.ts"
      provides: "getEvalSummaryForProject; getEvalRunsForRun; getShadowCandidateQueue with graceful degradation."
    - path: "aios-ui/components/eval/EvalSummaryPanel.tsx"
      provides: "Collapsible eval score panel; Second Brain Lift and Portability Gap display; context profile label."
    - path: "aios-ui/components/eval/ShadowCandidateQueue.tsx"
      provides: "Collapsible candidate queue; score and state display; approval button with confirmation modal."
    - path: "aios-ui/app/runs/[id]/page.tsx"
      provides: "EvalSummaryPanel extension (collapsible, defaults collapsed, absent when no eval data)."
    - path: "aios-ui/app/projects/[id]/page.tsx"
      provides: "EvalSummaryPanel + ShadowCandidateQueue extensions."
    - path: "aios-ui/app/page.tsx"
      provides: "ShadowCandidateQueue extension in Command Center."
    - path: "services/aios_cli.py"
      provides: "aios packet generate; aios benchmark to-swe-bench; aios benchmark to-terminal-bench; aios benchmark normalize-result subcommands."
    - path: "tests/test_portable_context_packet_generator.py"
      provides: "Packet generation; privacy filter; file writing; convention detection."
    - path: "tests/test_external_benchmark_adapter.py"
      provides: "Format translation; external_clean_room enforcement; result normalization."
  key_links:
    - from: "services/portable_context_packet_generator.py:generate_packet"
      to: "config/context-packets/"
      via: "writes packet JSON to this directory"
      pattern: "context-packets|generate_packet"
    - from: "services/external_benchmark_adapter.py:normalize_external_result"
      to: "services/eval_run_service.py:create_eval_run"
      via: "normalized result dict is passed to create_eval_run with mode='external' and context_profile='external_clean_room'"
      pattern: "create_eval_run|external_clean_room"
    - from: "aios-ui/components/eval/ShadowCandidateQueue.tsx"
      to: "services/shadow_automation.py:approve_candidate"
      via: "Approve button triggers tRPC mutation → approve_candidate via aios-ui/server/aios/eval-data.ts"
      pattern: "approve_candidate|ShadowCandidateQueue"
    - from: "aios-ui/server/aios/eval-data.ts"
      to: "eval_tasks, eval_runs, eval_scores, shadow_candidates tables"
      via: "SQLite reads via existing db connection pattern; graceful degradation when tables absent"
      pattern: "eval_tasks|eval_runs|eval_scores|shadow_candidates"
---

<objective>
Complete Phase 11 by delivering portable context packet generation, external harness adapters, and eval UI panels on existing operator surfaces. This closes the full Testing & Benchmark Evaluation system — every evaluation mode from the design spec now has infrastructure.

Purpose: Portable context packets answer the question "how much of AIOS lift is portable to peers without my second brain?" External harness adapters answer the question "how does AIOS compare to public benchmarks?" The UI panels make eval data visible in the operator surfaces built in Phase 10 without adding new pages.

Output: 2 new Python services; aios-ui server module; 2 new React components; 3 page extensions; services/aios_cli.py extension (4 subcommands); 2 new test files.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/11-testing-benchmark-evaluation-and-shadow-workflows/11-RESEARCH.md
@aios-ui/server/aios/
@aios-ui/app/page.tsx
@aios-ui/app/runs/[id]/page.tsx
@aios-ui/app/projects/[id]/page.tsx
@services/shadow_automation.py
@services/eval_run_service.py
@docs/evals/templates/portable-context-packet.md
</context>
