---
phase: 02-context-query-and-briefing-compilation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - aios-ui/server/aios/schema.ts
  - aios-ui/server/aios/control-plane.ts
  - aios-ui/server/aios/packet-assembly.ts
  - tests/test_aios_cli.py
  - tests/test_orchestration_runtime.py
autonomous: true
requirements:
  - CONT-01
  - CONT-04
must_haves:
  truths:
    - "AIOS has one route-aware packet storage contract readable from both the CLI and UI/control-plane paths."
    - "Packet records persist enough route and provenance metadata for later packet/query/writeback phases without recomputation."
  artifacts:
    - path: "aios-ui/server/aios/schema.ts"
      provides: "route-aware packet schema upkeep in the UI/runtime path"
    - path: "aios-ui/server/aios/control-plane.ts"
      provides: "packet readers/writers that understand route metadata and packet provenance"
    - path: "aios-ui/server/aios/packet-assembly.ts"
      provides: "packet assembly outputs that match the shared persisted packet shape"
  key_links:
    - from: "services/aios_cli.py"
      to: "aios-ui/server/aios/control-plane.ts"
      via: "shared briefing_packets route-aware storage shape"
      pattern: "route_id|route_result_json|selection_trace_json"
---

<objective>
Define the shared packet persistence contract for Phase 2 before query/compiler integration adds more logic on top.

Purpose: remove storage/schema drift between the CLI route-aware packet path and the UI/control-plane packet path.
Output: route-aware packet schema support in UI/runtime readers plus tests that prove packet provenance survives persistence.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-context-query-and-briefing-compilation/02-CONTEXT.md
@.planning/phases/02-context-query-and-briefing-compilation/02-RESEARCH.md
@services/aios_cli.py
@aios-ui/server/aios/schema.ts
@aios-ui/server/aios/control-plane.ts
@aios-ui/server/aios/packet-assembly.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend UI/control-plane schema upkeep for route-aware briefing packets</name>
  <files>aios-ui/server/aios/schema.ts</files>
  <action>Backfill the route-aware packet/run columns added in the Phase 1 CLI path so the UI/runtime schema keeper does not drift from `schema.sql` and `services/aios_cli.py`.</action>
  <verify>pnpm test -- --runInBand</verify>
  <done>UI schema upkeep recognizes route-aware packet columns and no longer assumes the older packet shape.</done>
</task>

<task type="auto">
  <name>Task 2: Make packet readers and assembly outputs aware of route metadata</name>
  <files>aios-ui/server/aios/control-plane.ts, aios-ui/server/aios/packet-assembly.ts</files>
  <action>Update packet row types and persistence logic so route identity and route result payload can be loaded, surfaced, and carried through packet assembly without recomputing routing.</action>
  <verify>uv run pytest tests/test_aios_cli.py tests/test_orchestration_runtime.py -q</verify>
  <done>Packet readers/writers and assembly outputs expose the route-aware persisted packet contract.</done>
</task>

<task type="auto">
  <name>Task 3: Lock packet persistence contract with focused regression coverage</name>
  <files>tests/test_aios_cli.py, tests/test_orchestration_runtime.py</files>
  <action>Add or extend tests so packet storage and retrieval prove route-aware packet metadata is durable and readable from the shared orchestration spine.</action>
  <verify>uv run pytest tests/test_aios_cli.py tests/test_orchestration_runtime.py -q</verify>
  <done>Phase 2 packet persistence contract is protected against schema/reader drift.</done>
</task>

</tasks>
