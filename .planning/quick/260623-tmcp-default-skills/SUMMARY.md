# Quick Task 260623: TMCP Default Skills Summary

AIOS now documents and tests that the default local TMCP graph is `skills-library/skills.tmcp`.

The portable dev-process graph remains registered as a namespace overlay for portable behavior, not as the canonical local skill graph. Operating-language objectives now select `@module:operating_language`, which points to `skills/operating-language/SKILL.md` and `OPERATING_LANGUAGE.md` in the local canonical graph.
