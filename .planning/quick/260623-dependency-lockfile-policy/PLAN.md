# Quick Task 260623: Dependency And Lockfile Policy

## Task

Add the requested dependency and lockfile policy as an AIOS agent rule.

## Scope

- Add an always-loaded rule to `config/agent-rules.md`.
- Mirror the execution rule in `AGENTS.md`.
- Update `PROJECT.md` truth to record the new durable state.

## Verification

- Run `pnpm context:compile --task "Add dependency and lockfile policy as AIOS and agent rule"` before editing.
- Run `pnpm context:validate` after editing.
