# Quick Task 260516: Agent Rules Runtime Wiring - Plan

## Goal

Make `config/agent-rules.md` affect real AIOS runtime behavior across session-start context, context compiler selection, and workflow execution reports without duplicating the rule content.

## Tasks

1. Add a shared Python loader for `config/agent-rules.md` and wire it into `bin/hook-session-start.py` and `services/workflow_orchestration.py`.
   - Verify: unit tests assert session packet and workflow reports include parsed agent rules.

2. Extend `tools/context-compile.mjs` to include `config/agent-rules.md` as a synthetic authoritative context source when present.
   - Verify: context compiler test asserts the selected context and receipt include the config path.

3. Preserve installed skill sync metadata so workflow execution can trace synced skills back to installed `SKILL.md` sources.
   - Verify: sync script test covers source fields and workflow execution keeps agent rules visible alongside skill reports.

4. Run focused tests plus the live session-start path, then update `PROJECT.md` only if the runtime contract changed.
