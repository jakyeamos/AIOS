# Quick Task Plan

Task: Implement the TMCP multi-project benchmark foundation scaffold from the attached master prompt.

Scope:
- Add a non-mutating service that creates the required `tmcp-benchmark/` directory structure.
- Discover Git repositories read-only and write core benchmark manifests.
- Encode experimental conditions, shortcut states, run schema defaults, and report stubs.
- Avoid branch synchronization, agent execution, or benchmark claims in this first foundation slice.

Verification:
- Focused Python tests for inventory and scaffold generation.
- Ruff check for the new service and tests.
- Context validation after truth-file updates.
