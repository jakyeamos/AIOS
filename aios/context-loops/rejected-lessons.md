# Rejected Context Loop Lessons

Rejected lessons are tracked so the outer loop does not keep proposing the same weak generalization.

Each entry should include the proposed lesson, rejection reason, related examples, and date.
