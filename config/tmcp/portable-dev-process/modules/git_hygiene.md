# Module: Git Hygiene

Module ID: `@module:git_hygiene`

Inspect `git status --short --branch` before staging, committing, rebasing, or pushing. Keep commits atomic and exclude unrelated user changes.

Default branch names should be short, task-specific, and follow the host repo convention when one exists.

Never run destructive history or workspace commands without explicit approval.

