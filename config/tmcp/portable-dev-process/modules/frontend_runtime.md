# Module: Frontend Runtime

Module ID: `@module:frontend_runtime`

For UI work, verify rendered behavior rather than relying only on static inspection. Check changed routes and relevant responsive sizes.

Look for blank renders, broken assets, text overlap, unusable controls, hydration/runtime errors, and visual states that contradict the task.

Use existing component and styling conventions before adding new design systems.

