# Branch: Network Required

Branch ID: `@branch:network_required`

Use this branch when the task needs package registry access, remote CI logs, web documentation, dependency downloads, or push/fetch/pull operations.

State why network is required, prefer official or primary sources for technical facts, and request runtime approval when network is restricted.

