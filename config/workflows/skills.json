{
  "version": "2026-04-23",
  "skills": [
    {
      "key": "prompt_library_normalizer",
      "purpose": "Map loose request wording to a validated prompt template and normalized intent contract.",
      "allowed_stages": [
        "normalize_prompt"
      ],
      "input_schema": {
        "objective": "string",
        "workflow_key": "string"
      },
      "output_schema": {
        "template_id": "string|null",
        "normalized_prompt": "string"
      },
      "invariants": [
        "Preserve user intent.",
        "Do not introduce new task objectives."
      ],
      "failure_conditions": [
        "No template match and no fallback prompt."
      ],
      "side_effects": [],
      "execution_mode": "deterministic"
    },
    {
      "key": "obsidian_corpus_retriever",
      "purpose": "Retrieve bounded style profile context from the personal corpus for writing tasks.",
      "allowed_stages": [
        "enrich_context"
      ],
      "input_schema": {
        "objective": "string",
        "vault_root": "string"
      },
      "output_schema": {
        "style_profile": "object",
        "evidence_notes": "string[]"
      },
      "invariants": [
        "Retrieve bounded excerpts only.",
        "Do not dump full notes into prompts."
      ],
      "failure_conditions": [
        "Vault unavailable and no fallback style profile."
      ],
      "side_effects": [],
      "execution_mode": "deterministic"
    },
    {
      "key": "academic_draft_generator",
      "purpose": "Generate structured draft sections from normalized objective and context profile.",
      "allowed_stages": [
        "generate"
      ],
      "input_schema": {
        "normalized_prompt": "string",
        "style_profile": "object"
      },
      "output_schema": {
        "draft_text": "string"
      },
      "invariants": [
        "Preserve argument intent and section structure."
      ],
      "failure_conditions": [
        "Missing sectioned draft output."
      ],
      "side_effects": [],
      "execution_mode": "heuristic"
    },
    {
      "key": "personal_corpus_humanizer",
      "purpose": "Improve prose rhythm/readability while preserving factual meaning, citations, and specificity.",
      "allowed_stages": [
        "transform"
      ],
      "input_schema": {
        "draft_text": "string",
        "style_profile": "object"
      },
      "output_schema": {
        "humanized_text": "string"
      },
      "invariants": [
        "Must preserve factual meaning.",
        "Must preserve argument structure.",
        "Must preserve citation references."
      ],
      "failure_conditions": [
        "Meaning drift beyond configured threshold."
      ],
      "side_effects": [],
      "execution_mode": "heuristic"
    },
    {
      "key": "structure_checker",
      "purpose": "Verify required paper structure is present.",
      "allowed_stages": [
        "validate"
      ],
      "input_schema": {
        "humanized_text": "string"
      },
      "output_schema": {
        "passed": "boolean",
        "issues": "string[]"
      },
      "invariants": [
        "Validation output must be explicit pass/fail."
      ],
      "failure_conditions": [
        "Missing required section headings."
      ],
      "side_effects": [],
      "execution_mode": "deterministic"
    },
    {
      "key": "citation_checker",
      "purpose": "Verify citation coverage is present and non-empty.",
      "allowed_stages": [
        "validate"
      ],
      "input_schema": {
        "humanized_text": "string"
      },
      "output_schema": {
        "passed": "boolean",
        "issues": "string[]"
      },
      "invariants": [
        "No fake citation claims when no citation marker exists."
      ],
      "failure_conditions": [
        "No citation markers in output."
      ],
      "side_effects": [],
      "execution_mode": "deterministic"
    },
    {
      "key": "meaning_preservation_checker",
      "purpose": "Check transformed prose for major keyword/intent drift from draft.",
      "allowed_stages": [
        "validate"
      ],
      "input_schema": {
        "draft_text": "string",
        "humanized_text": "string"
      },
      "output_schema": {
        "passed": "boolean",
        "issues": "string[]",
        "overlap_ratio": "number"
      },
      "invariants": [
        "Meaning-preservation evaluation must be reproducible."
      ],
      "failure_conditions": [
        "Keyword overlap below threshold."
      ],
      "side_effects": [],
      "execution_mode": "deterministic"
    },
    {
      "key": "scope_check",
      "purpose": "Ensure output remains scoped to objective and contract.",
      "allowed_stages": [
        "validate"
      ],
      "input_schema": {
        "objective": "string",
        "output_text": "string"
      },
      "output_schema": {
        "passed": "boolean",
        "issues": "string[]"
      },
      "invariants": [
        "Detect obvious out-of-scope expansion."
      ],
      "failure_conditions": [
        "Output does not reference objective terms."
      ],
      "side_effects": [],
      "execution_mode": "deterministic"
    },
    {
      "key": "divergent_task_classifier",
      "purpose": "Classify whether a task benefits from divergent strategy exploration and select a mode.",
      "allowed_stages": [
        "parse_request"
      ],
      "input_schema": {
        "objective": "string"
      },
      "output_schema": {
        "worthwhile": "boolean",
        "recommended_mode": "string",
        "signals": "string[]"
      },
      "invariants": [
        "Simple deterministic tasks should not be escalated to broad exploration.",
        "Mode selection must be explainable."
      ],
      "failure_conditions": [
        "No classification reason is available."
      ],
      "side_effects": [],
      "execution_mode": "deterministic"
    },
    {
      "key": "divergent_candidate_generator",
      "purpose": "Generate role-based candidate strategies from the divergent candidate registry.",
      "allowed_stages": [
        "generate"
      ],
      "input_schema": {
        "objective": "string",
        "mode": "string"
      },
      "output_schema": {
        "candidates": "object[]"
      },
      "invariants": [
        "Candidates must include risks and why they might fail.",
        "Candidates must represent distinct strategic angles."
      ],
      "failure_conditions": [
        "All candidates converge to the same formulation."
      ],
      "side_effects": [],
      "execution_mode": "heuristic"
    },
    {
      "key": "divergent_judge_panel",
      "purpose": "Evaluate divergent candidates through reusable judge profiles with structured rationale.",
      "allowed_stages": [
        "validate"
      ],
      "input_schema": {
        "candidates": "object[]",
        "judges": "object[]"
      },
      "output_schema": {
        "judgments": "object[]"
      },
      "invariants": [
        "Every judgment must include a score, verdict, critique, and recommended action.",
        "Judge results are evidence, not ground truth."
      ],
      "failure_conditions": [
        "A judge emits a score without rationale."
      ],
      "side_effects": [],
      "execution_mode": "heuristic"
    },
    {
      "key": "divergent_portfolio_selector",
      "purpose": "Select a portfolio of useful candidates instead of collapsing to one winner.",
      "allowed_stages": [
        "finalize"
      ],
      "input_schema": {
        "candidates": "object[]",
        "judgments": "object[]"
      },
      "output_schema": {
        "portfolio": "object"
      },
      "invariants": [
        "Portfolio must include best overall, safest implementation, highest upside, and useful failure slots."
      ],
      "failure_conditions": [
        "Workflow completes without portfolio output."
      ],
      "side_effects": [],
      "execution_mode": "deterministic"
    },
    {
      "key": "memory_writeback_proposer",
      "purpose": "Create approval-gated HOW, WHAT, FAILURE, and ENTROPY memory proposals.",
      "allowed_stages": [
        "finalize"
      ],
      "input_schema": {
        "portfolio": "object",
        "entropy": "object"
      },
      "output_schema": {
        "writebacks": "object[]"
      },
      "invariants": [
        "Do not silently mutate global memory.",
        "Writeback proposals must include rationale and evidence."
      ],
      "failure_conditions": [
        "Memory is promoted without approval evidence."
      ],
      "side_effects": [
        "Writes proposed memory records only."
      ],
      "execution_mode": "deterministic"
    },
    {
      "key": "find_skills",
      "purpose": "Helps users discover and install agent skills when they ask questions like \"how do I do X\", \"find a skill for X\", \"is there a skill that can...\", or express interest in extending capabilities. This skill should be used when the user is looking for functionality that might exist as an installable ski",
      "allowed_stages": [
        "generate"
      ],
      "input_schema": {},
      "output_schema": {},
      "invariants": [],
      "failure_conditions": [],
      "side_effects": [],
      "execution_mode": "heuristic"
    },
    {
      "key": "humanizer",
      "purpose": "|",
      "allowed_stages": [
        "transform"
      ],
      "input_schema": {},
      "output_schema": {},
      "invariants": [],
      "failure_conditions": [],
      "side_effects": [],
      "execution_mode": "heuristic"
    },
    {
      "key": "react_components",
      "purpose": "Converts Stitch designs into modular Vite and React components using system-level networking and AST-based validation.",
      "allowed_stages": [
        "validate"
      ],
      "input_schema": {},
      "output_schema": {},
      "invariants": [],
      "failure_conditions": [],
      "side_effects": [],
      "execution_mode": "heuristic"
    },
    {
      "key": "personalized_humanizer_classifier",
      "purpose": "Classify writing tasks into personalized voice modes before corpus retrieval or rewriting.",
      "allowed_stages": [
        "parse_request"
      ],
      "input_schema": {
        "text": "string",
        "requested_mode": "string|null"
      },
      "output_schema": {
        "mode": "string"
      },
      "invariants": [
        "Mode selection must be explainable.",
        "Creative and professional contexts must remain separated unless explicitly allowed."
      ],
      "failure_conditions": [
        "Unknown requested mode."
      ],
      "side_effects": [],
      "execution_mode": "deterministic"
    },
    {
      "key": "personalized_voice_retriever",
      "purpose": "Retrieve bounded, mode-compatible personal voice references for the personalized humanizer.",
      "allowed_stages": [
        "enrich_context"
      ],
      "input_schema": {
        "mode": "string",
        "corpus_refs": "object[]"
      },
      "output_schema": {
        "evidence_refs": "object[]"
      },
      "invariants": [
        "Prefer approved user-written examples.",
        "Do not store sensitive excerpt text by default.",
        "Do not use creative corpus for professional outreach unless explicitly allowed."
      ],
      "failure_conditions": [
        "Retrieved examples violate mode boundaries."
      ],
      "side_effects": [],
      "execution_mode": "deterministic"
    },
    {
      "key": "personalized_voice_packet_builder",
      "purpose": "Build a compact task-specific voice packet from the versioned personal voice profile and evidence refs.",
      "allowed_stages": [
        "enrich_context"
      ],
      "input_schema": {
        "mode": "string",
        "profile": "object",
        "evidence_refs": "object[]"
      },
      "output_schema": {
        "voice_packet": "object"
      },
      "invariants": [
        "Voice packet must include style rules, anti-style rules, confidence, and provenance.",
        "Voice packet must be compact enough for agent use."
      ],
      "failure_conditions": [
        "Requested mode is missing from the profile."
      ],
      "side_effects": [],
      "execution_mode": "deterministic"
    },
    {
      "key": "personalized_humanizer_transformer",
      "purpose": "Rewrite text toward the selected personal voice mode while preserving meaning, structure, and factual claims.",
      "allowed_stages": [
        "transform"
      ],
      "input_schema": {
        "text": "string",
        "voice_packet": "object"
      },
      "output_schema": {
        "humanized_text": "string"
      },
      "invariants": [
        "Preserve factual meaning and original intent.",
        "Do not add unsupported personal details.",
        "Do not over-humanize technical prompts."
      ],
      "failure_conditions": [
        "Meaning drift or unsupported claim introduction."
      ],
      "side_effects": [],
      "execution_mode": "heuristic"
    },
    {
      "key": "personalized_humanizer_quality_checker",
      "purpose": "Score personalized humanizer output for meaning preservation, voice match, context fit, specificity, and risk.",
      "allowed_stages": [
        "validate"
      ],
      "input_schema": {
        "input_text": "string",
        "output_text": "string",
        "voice_packet": "object"
      },
      "output_schema": {
        "scorecard": "object",
        "risks": "string[]"
      },
      "invariants": [
        "Quality checks must include hallucination and over-personalization risk.",
        "Debug output must explain selected profile and applied rules."
      ],
      "failure_conditions": [
        "Scorecard is missing required dimensions."
      ],
      "side_effects": [],
      "execution_mode": "deterministic"
    },
    {
      "key": "personalized_humanizer_feedback_collector",
      "purpose": "Record approved, edited, or rejected outputs as evidence for candidate profile updates.",
      "allowed_stages": [
        "finalize"
      ],
      "input_schema": {
        "run_id": "string",
        "verdict": "string",
        "notes": "string"
      },
      "output_schema": {
        "feedback_id": "string",
        "candidate_update": "object|null"
      },
      "invariants": [
        "Do not silently mutate canonical voice profiles.",
        "Profile update proposals require evidence before approval."
      ],
      "failure_conditions": [
        "Feedback directly changes active profile rules."
      ],
      "side_effects": [
        "Writes feedback and candidate profile update records."
      ],
      "execution_mode": "deterministic"
    },
    {
      "key": "remotion_best_practices",
      "purpose": "Best practices for Remotion - Video creation in React",
      "allowed_stages": [
        "generate"
      ],
      "input_schema": {},
      "output_schema": {},
      "invariants": [],
      "failure_conditions": [],
      "side_effects": [],
      "execution_mode": "heuristic"
    },
    {
      "key": "smart_search",
      "purpose": "基于 opencli 命令的智能搜索路由器。当用户想要搜索、查询、查找或研究信息时，尤其是涉及指定网站、社交媒体、技术资料、新闻、购物、旅游、求职、金融或中文内容时，务必使用此 skill",
      "allowed_stages": [
        "enrich_context"
      ],
      "input_schema": {},
      "output_schema": {},
      "invariants": [],
      "failure_conditions": [],
      "side_effects": [],
      "execution_mode": "heuristic"
    }
  ]
}
