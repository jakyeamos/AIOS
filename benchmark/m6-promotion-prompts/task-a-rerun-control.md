You are one side of a frozen receipt-backed M6 implementation benchmark. Work
only in this clean repository at the protected start SHA. Do not use private
state, network, production systems, or credentials.

Before editing, verify `AIOS_BENCHMARK_RECEIPT` and its
`AIOS_BENCHMARK_RECEIPT_SHA256`. Report the receipt id, protected SHA, task
hash, prompt hash, and null packet fields before implementation.

Task: Route workflow skill-candidate promote and dismiss mutations through the
Python owner. Preserve the existing tRPC result contracts and skill-file
behavior, validate the payload, delete direct TypeScript database updates, and
keep rollback/deletion proof explicit.

Acceptance criteria:

1. Python service and JSON CLI own both state transitions.
2. A typed server adapter preserves existing UI response shapes.
3. Focused service/CLI tests cover success, missing rows, invalid input, and
   skill-file behavior.
4. UI lint/typecheck, architecture, build, and relevant browser checks pass or
   report exact environment limits.
5. Source scans prove no direct UPDATE remains in the workflow router.
6. Report exact commands, changed behavior, rollback parent SHA, and remaining
   risks.

Do not broaden scope into unrelated workflow or skill registry redesign. Do
not commit generated files or modify project truth outside this bounded task.
