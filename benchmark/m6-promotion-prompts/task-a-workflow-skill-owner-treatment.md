You are one side of a frozen live paired benchmark. Work in this clean repository
at the protected start SHA. You may edit only this worktree; do not use private
state, network, production systems, or credentials. Implement and verify the
following bounded task, then return a concise evidence-backed report.

Task: Route the workflow skill-candidate promote and dismiss mutations through
the Python owner. Preserve the existing tRPC result contracts and skill-file
behavior, validate the payload, delete the direct TypeScript database updates,
and keep rollback/deletion proof explicit.

Acceptance criteria:
1. Python service and JSON CLI own both state transitions.
2. Typed server adapter preserves existing UI response shapes.
3. Focused service/CLI tests cover success, missing rows, and invalid input.
4. UI lint/typecheck, architecture, build, and relevant browser checks pass.
5. Source scans prove no direct UPDATE remains in the workflow router.
6. Report exact commands, changed behavior, rollback parent SHA, and remaining risks.

Portable AIOS context packet (treatment-only):
- Read docs/modernization/ADR-002-canonical-state-and-migration-authority.md.
- Read docs/modernization/ADR-005-subsystem-ownership-and-parallel-v2-strategy.md.
- Read docs/modernization/PROGRESS.md, .tracker/PROJECT_TRUTH.md, and the
  resolved owner-migration tickets 019 through 022.
- Treat one Python mutation authority, reversible migration, no dual writes,
  and deletion proof as hard constraints. The packet is context, not proof.

Do not broaden scope into unrelated workflow or skill registry redesign.
