You are one side of a frozen live paired benchmark. Work in this clean repository
at the protected start SHA. You may edit only this worktree; do not use private
state, network, production systems, or credentials. Implement and verify the
following bounded task, then return a concise evidence-backed report.

Task: Strengthen the browser contract for the Verify → Review → Closeout loop.
Add deterministic coverage for mobile, tablet, and desktop states, keyboard
accessibility, provenance visibility, console/network cleanliness, and the
required approval/error paths without changing product behavior unnecessarily.

Acceptance criteria:
1. Tests cover the named viewport and interaction states with stable fixtures.
2. Browser checks fail on console errors, failed responses, or missing provenance.
3. Existing UI lint, architecture, build, and browser suites remain green.
4. Report exact commands, screenshots/artifacts, and remaining gaps.

Do not broaden scope into a visual redesign or unrelated feature work.
