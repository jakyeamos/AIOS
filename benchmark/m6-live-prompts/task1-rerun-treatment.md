You are one side of a frozen live paired benchmark. Work in this clean repository
at the protected start SHA. Do not edit files, use private state, access the
network, or touch production systems. Return a concise evidence-backed report.

Task: Assess whether M6 can proceed based on the repository's paired-
effectiveness evidence. This is a gate-readiness audit, not an implementation.

Acceptance criteria:
1. State the exact M6 blocker and whether satellite migration may proceed.
2. Distinguish deterministic fixture evidence from clean live-model evidence.
3. State the required parity, contamination, scoring, durable-ID, and
   independent-review gates.
4. Inspect the eval-pair report-path contract and say whether its evidence is
   sufficient to promote M6.
5. Avoid unsupported productivity, safety, or promotion claims; record missing
   telemetry and limitations.

Portable AIOS context packet (the only extra context available to treatment):
- Read `docs/evals/M5A_EFFECTIVENESS_REPORT.md`,
  `docs/evals/benchmark-eval-architecture.md`,
  `docs/evals/context-profiles.md`, and Wayfinder tickets 012, 013, and 014.
- Read `docs/modernization/PROGRESS.md`, `.tracker/PROJECT_TRUTH.md`, and the
  `eval_pairs` service/CLI implementation.
- Treat clean protected SHA, identical model/effort/tools/budget/task,
  contamination proof, persisted run/score/pair IDs, fixed scoring, and
  independent review as mandatory gates. Do not treat the packet as proof that
  any gate passed.

Use only repository files and ordinary local read-only commands. End with a
clearly labeled decision and exact next action.
